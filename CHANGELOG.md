# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project uses [maintenance-based versioning](TOKENSAVE-VERSIONING.md), not SemVer.

## [Unreleased]

### Added
- **Terraform/OpenTofu dependency lockfiles are indexed as artifacts.** `.terraform.lock.hcl` (the basename both tools share) was dropped twice over: the hidden-entry filter skips dot-prefixed names, and no extractor or artifact extension owns `.hcl`, so sync reported it as an unsupported file and neither `tokensave_files` nor literal search could reach the provider source addresses, selected versions, and checksums it records. Classification is by exact basename, not extension: claiming `.hcl` outright would mislabel every other HCL document (Packer, Consul, Vault, Nomad) as Terraform source. The lockfile rides the existing artifact pipeline (#323): a `files` row with hash, size, and mtime, never parsed, so checksum strings cannot surface as symbols, and findable by path and by literal content search for both `registry.terraform.io/...` and `registry.opentofu.org/...` provider addresses. Other HCL files still count in the unsupported-extension summary, and other hidden files stay excluded.

### Changed
- **The Rust toolchain is pinned to 1.98.1 and the locked justfile recipes now define the local build lifecycle.** Development uses debug builds, installation uses release output only, and cleanup is scoped so `reclaim` keeps release artifacts while `clean` removes generated Cargo output.
- **Test and dev profiles now use `debug = "line-tables-only"`.** Across tokensave's 162+ integration-test crates, default debuginfo (`debug = 2`) generated up to ~395 MiB per test binary, driving `target/` peak disk usage past 25–29 GB during multi-threaded test links. Tuning `debug = "line-tables-only"` slashes test binary sizes by ~45% (down to ~216 MiB per test binary) and reduces peak disk footprint during test builds while preserving full file:line numbers in backtraces, panic messages, and test assertion locations.

### Fixed
- **HTTPS no longer stops working on a host with no system CA store (follow-up to #526).** #526 switched every HTTPS call to the OS trust store so a corporate TLS-intercepting proxy would stop breaking them. `RootCerts` is an enum, so that **replaced** ureq's bundled Mozilla roots rather than adding to them — and on Linux/BSD `rustls-platform-verifier` returns `No CA certificates were loaded from the system` when the store yields nothing, with no fallback of its own. A distroless or `scratch` container without `ca-certificates`, or a minimal CI image, therefore went from working on the bundled roots to having no HTTPS at all: `upgrade`, the update check and the counter upload would all fail on a host where nothing was wrong before. The trust source is now chosen rather than assumed. tokensave probes the store first — with `rustls-native-certs`, the same loader `rustls-platform-verifier` uses on those targets, so it answers the question the verifier is about to ask and honours `SSL_CERT_FILE`/`SSL_CERT_DIR` identically — and keeps the bundled roots when there is nothing to find, warning once per process so someone behind a proxy can see why their OS-installed CA is being ignored. The probe runs once per process and is skipped entirely on macOS and Windows, which query OS APIs with no equivalent empty-store failure. **The fallback direction is the safe one**: the bundled set is a fixed, audited list, so falling back can only narrow what is trusted, never widen it, and it is never a fallback to unverified. The proxy fix from #526 is unchanged wherever the OS has a store — which is everywhere it worked before.
- **`tokensave install` now installs the current repository's git hooks instead of claiming a global `core.hooksPath` (#506).** `core.hooksPath` is a single machine-wide slot, and taking it takes it from every other tool that installs hooks. `git lfs install --local` writes into `core.hooksPath` when one is set, finds tokensave's `pre-push` forwarder already there, and refuses with exit 2 — in **every** repository on the machine, including a brand-new empty one with no LFS hook anywhere near it. The reporter's test suite runs `git lfs install --local` in throwaway repositories; every one of them started failing hours after an unrelated `tokensave install`, with a message naming neither tokensave nor `core.hooksPath`, and it took a bisect to connect the two. git-lfs's own advice makes it worse: `git lfs update --force` overwrites the hook *in the global directory*, removing tokensave's chain-and-sync for every repository rather than the one the user was standing in. husky, pre-commit and lefthook collide the same way. **The per-repository mechanism already existed** (#455) and `init` has always used it, with reasoning that applies verbatim to `install` — so this is `install` catching up rather than a new design. `--git-hook yes` and the interactive default now install into the current repository; asking for them outside a repository says so rather than silently doing nothing. `--git-hook global` is a new explicit opt-in that keeps the old machine-wide behavior for anyone who wants one hook directory for every repo, and `githooks on` (without `--local`) still means the global hooks, as its `--local` flag implies. A machine that already has the global setup is **not** changed: touching `core.hooksPath` unprompted would stop the user's syncs with no warning, and the global choice is legitimate — so `install` prints what the slot breaks and the exact commands to migrate, and leaves the config alone. The forwarders are deliberately not narrowed: dropping the `pre-push` forwarder would dodge the git-lfs collision by silently disabling every repository's own `pre-push`, which is worse than the bug. Reported by @iKyi.
- **A bare name with a single candidate no longer binds without evidence in Python, JavaScript and TypeScript (#522, #503).** #508 taught the dotted-receiver fallback (`recv.method`) that being the only symbol of that name in the project proves nothing about whether the call site can reach it. The **bare-name** path never learned it, so a production function declaring a local variable named `exe` acquired a `uses` edge pointing at a pytest fixture named `exe` — different directory, importing nothing from the test tree — purely because the fixture was the only `exe` anywhere. The direction is not incidental: test fixtures are deliberately named after the values they supply, so they collide with exactly the ordinary names production code uses. The reporter measured 351 such `uses` edges on a 489-file project, unchanged by 7.11.1 while `calls` fell 94%. **The reported split was calls-versus-uses; the real one is dotted-versus-bare.** The gate is passed at exactly one call site — the dotted path — and the bare-name path was ungated for every edge kind *and every language*, `calls` included. That fits the per-name evidence better: everything that moved in 7.11.1 is a method name reached through a receiver (`after`, `askyesno`, `grid`), and everything that did not is a bare variable (`exe`, `cfg_path`, `node`). **Scoped by language, on measurement rather than principle.** Gating every language costs **11.4% of every Rust call edge** on a 1,824-file tree — 2,506 edges — for no reduction in impossible edges, because Rust resolves bare names through a module system the gate cannot see and never had this problem. Scoped, Rust is byte-for-byte unchanged and the phantom reduction is identical: on a 1,171-file Python project, edges from production into `tests/` fall from **1,202 to 47 for `calls` (-96%)** and 521 to 314 for `uses` (-40%), at 6.5% of total edges. Ruby is deliberately excluded despite fitting the argument — it has its own resolution paths here, and gating it drops a legitimate `Implements` edge for a module included from another file. **The `uses` figure is 40%, not 96%, and that is a known limit rather than a rounding**: the survivors all pass via the import rule, whose check confirms that a name matching the candidate's is imported without confirming the candidate *is* what was imported. Tightening that is a separate change needing its own measurement. Reported by @xaerogonzo.
- **`tokensave upgrade` no longer stages the downloaded binary at a predictable path (#525).** The download was extracted to `std::env::temp_dir().join(format!("tokensave_upgrade_{pid}"))`, and both extraction paths — `tar`'s `entry.unpack` and the zip path's `File::create` — open that destination with semantics that follow symlinks. A pid is guessable and externally observable, so on a host with a shared world-writable `/tmp` another local user could plant a symlink at that exact name ahead of the download and have the extraction follow it, writing the release binary's contents through to a file of their choosing with the upgrading user's privileges. The staging destination is now a `tempfile::TempDir`: a random name, created exclusively so it fails rather than adopting a path that already exists, which is the property that defeats the planted symlink. The directory is then narrowed to `0700`, because `tempdir()` creates with `0777 & !umask` — commonly `0755` — which would leave the staged binary readable by every other local user in the window between extraction and install. It is owned by the returned value, so it is removed when the staged binary goes out of scope whether the replacement succeeded or not, where the old code relied on an explicit `remove_file` on the success path. Neither extraction path changed. **This is the secondary point of #525 only**; verifying the download against a published `SHA256SUMS` is the substantive half and is still open. Found during a security pass on the network-facing code paths.
- **`tokensave upgrade` and every other HTTPS call now trust the OS certificate store, so corporate TLS-intercepting proxies no longer break them.** `ureq` defaulted to a bundled Mozilla root store, which never sees a corporate proxy's intercepting root CA even when that CA is legitimately installed in the OS trust store (e.g. Cato) — so the TLS handshake failed and `upgrade` reported a generic "could not reach GitHub", even though the network was fine. Every `ureq::Agent` tokensave builds now uses `RootCerts::PlatformVerifier`, which validates against the OS trust store (Windows CryptoAPI/schannel, macOS/iOS Keychain, Linux via the OS-loaded store) instead — the same model curl and git-for-windows use by default on Windows. TLS verification itself is unaffected; what changes is the set of trusted roots. Note that this **replaces** the bundled roots rather than adding to them — `RootCerts` is an enum, so the OS store is now the only source of trust anchors. On a Linux host with no system CA store at all (a distroless or `scratch` container without `ca-certificates` installed), `rustls-platform-verifier` returns `No CA certificates were loaded from the system` rather than falling back, so HTTPS there now fails where the bundled roots previously carried it; installing `ca-certificates` restores it. No new flag or environment variable is needed. Contributed by @gioxx.
- **`discover --json` now says how many turns actually carry a measured tool-result size (#523).** #474 started measuring the sizes and documented the limit that comes with it: a turn ingested before the upgrade carries 0, because the figure comes from transcript lines the database does not keep. The human output stated that in as many words; `--json` did not carry it in any form, so a consumer reading `total_addressable_input_tokens: 0` had two readings with identical bytes — measured and genuinely zero, or never measured — that lead to opposite conclusions. The reporter had quarantined these figures on *measured* evidence rather than a version check, so #474 silently moved them from quarantined to trusted while they were still unmeasured: a fix that turned a caught error into an uncaught one, purely because the JSON dropped the qualifier. **The human output had the same defect**, which is the part the issue did not claim and the code confirms: its guard was `total_replaceable_turns() > 0 && total_addressable_input_tokens() == 0`, which *infers* "unmeasured" from the total being zero. A genuinely measured zero therefore printed "unknown rather than zero", and a range straddling the upgrade — a real but partial total — printed nothing at all. So neither surface knew the answer; one of them merely guessed in prose. Both now read it: `turns_with_measured_sizes` is tracked per bucket and totalled, exported top-level and per bucket, and the human text uses it to say either "none of these were measured" or "N of M were recorded before sizes were measured, so the total is a lower bound". A count rather than a boolean because a range spanning the upgrade is partially measured and a boolean would have to round that off. Purely additive to the payload: no existing key changes name, type or meaning. A turn whose tool results were genuinely empty counts as unmeasured, which keeps the figure a lower bound rather than an overclaim. Reported by @xaerogonzo.
- **An MCP edit or read handed an absolute path outside the served project no longer indexes that file into this project's graph (#528).** The edit and read tools accept an absolute `path` on purpose, so an agent can reach a sibling worktree. The post-call freshness check then passed that path to `check_file_staleness`, which resolved it with `project_root.join(path)` — and `Path::join` discards the base when its argument is absolute. The file existed on disk, the DB had no row for it, so it was classed "new — needs indexing", and the following `sync_if_stale` wrote a `files` row under its **absolute** path. A `serve` for project A, handed one path into project B, thereafter answered `tokensave_context` and `tokensave_largest` with B's symbols — and nothing in the output said where they came from, which is the part that makes it a correctness bug rather than an untidy table. The same edit through the CLI never did this; only the server-side post-call path. `check_file_staleness` now resolves the path against the root and drops anything that lands outside it, the way it already drops a path present in neither the DB nor on disk; an absolute path that genuinely *is* inside the root is rewritten to the project-relative form the DB stores, so an in-root edit addressed absolutely still refreshes its row rather than being ignored. The comparison is on canonicalized paths, so a symlink or a `..` cannot walk past a textual prefix check. The edit itself is unchanged and keeps working. Reported by @iKyi.
- **`--path` scoping in `coupling`, `inheritance_depth` and `god_class` no longer treats the path as a SQL pattern (#524).** All three built their filter by interpolating the caller's path straight into the statement — `LIKE '{prefix}%'` — where every sibling query either binds the value or runs it through the `LIKE` escaping `get_ambiguous_calls` uses. Three consequences, two of them silent: `_` acted as a single-character wildcard, so `--path src/my_mod` also returned `src/myXmod`; `%` acted as a wildcard, so `--path 'src/%'` returned every directory in the project; and a path containing `'` closed the literal and failed the query with a bare SQLite syntax error. The wrong-results pair is the dangerous half, because a filter that quietly returns a superset reads exactly like a correct answer. **Binding a parameter would have fixed only the apostrophe** — `_` and `%` keep their meaning inside `LIKE` regardless of how the value arrives, which @vianbas demonstrated against `get_largest_nodes`, a site that already binds and still matched both directories. So the fix is the escaping, not the binding: the three now share one `push_path_prefix_filter` helper with `NodeFilter`, which quotes the prefix, escapes `\`, `%` and `_`, declares `ESCAPE '\'`, and matches the directory itself plus everything under it rather than any path merely starting with those characters. `escape_like` moved from `queries/nodes.rs` to `queries/mod.rs` so there is one copy. Four regression tests cover the control path, `_`, `%` and `'` across all three queries; three of them fail on the old code. Reported by @vianbas.
- **A non-Latin filename no longer freezes the progress line for the rest of an `init` or `sync` (#527).** The spinner keeps the last 49 **bytes** of a long message, and that cut can land inside a multi-byte character. A Korean path did exactly that — `start byte index 60 is not a char boundary; it is inside '서'` — and the panic killed the spinner thread. Indexing itself was unaffected and the command still exited 0, but the progress line stopped redrawing at the first long Hangul path, typically within half a second, so on a large project the command looked hung for its whole duration with a stale `ETA: 1s` frozen on screen. @vianbas reproduced it over 3,000 such files: the line froze immediately and the run finished about 12 seconds later. The cut now walks forward to the next character boundary, trimming at most three more bytes — the same fixup `tail` in the workflow handler already used. The logic is extracted as `spinner_tail` so it has unit tests rather than living inside a thread closure where a panic is invisible; the tests walk the cut through every offset inside a 3-byte and a 4-byte character, so a naive `+1` fixup would not pass them. Reported by @chunechew, diagnosed by @vianbas.
- **A release with no asset for your platform is no longer reported as an unreachable GitHub (#513).** `fetch_latest_stable_version` returned `Option<String>`, and three different outcomes collapsed into that one `None`: the HTTP call failed, the JSON did not parse, or GitHub answered perfectly well and the latest release simply carries no asset for the running platform. `upgrade` turned all three into `config error: failed to check for updates — could not reach GitHub`, which names the two things that are fine and not the one that is wrong. When v7.11.1 shipped without its Windows zips (#512), that sent Windows users to debug a working network — the reporter checked `curl`, PowerShell's separate TLS stack, `gh`, the rate limit, proxies, firewall rules, IPv6 and authentication, about an hour, before diffing the release asset lists. The classification compounded it: a missing asset is not the user's configuration, so `config error` was a second wrong pointer. `VersionCheckError` now distinguishes `Unreachable` — which keeps the old wording **and** appends what the HTTP layer actually said, so a 404 and a connection timeout are no longer spelled the same — from `NoAssetForPlatform`, which names the version, the platform, the asset it looked for and the assets the release does publish, and from `NoRelease` for an empty channel. The selection logic is split from the HTTP call and unit-tested against the real v7.11.1 asset list. `doctor` reports the two separately as well, since a missing asset is a passing reachability check. Exit codes are unchanged: failing to upgrade is still non-zero, only the diagnosis differs. The `Option`-returning functions remain for the passive update notice, which genuinely does treat every failure alike. Reported by @xaerogonzo.

## [7.11.1] - 2026-09-06

### Added
- **HTML and CSS extraction (#507).** Both were skipped outright — `.css: 51 file(s) skipped (no registered extractor)` — so a project's templates and stylesheets were invisible to every graph query, and the only remedy was to list them under `artifact_extensions` and get literal search but no symbols. Neither language has anything to call, so what is recorded is the names a file *defines* and the files it pulls in. HTML: an element carrying an `id` becomes a `Field`, the kind the XAML extractor already gives an `x:Name`d element; a custom element becomes a `Class`, identified by the hyphen the spec requires in the name, which is exactly what separates `<my-widget>` from every builtin tag; `<script src>` and `<link rel="stylesheet" href>` become `Use` nodes. Builtin tags are deliberately not emitted — a node per `<div>` would bury a project's real symbols under thousands of rows carrying no name. CSS: class selectors become `Class`, id selectors `Field` (the same kind as an HTML `id`, so the two sides of a page name the same thing the same way), custom properties `Const`, `@keyframes` names `Module`, and `@import` a `Use`. A selector written in three places — plainly, inside a `@media`, and beside a sibling — is one node rather than three, since a search for `.btn` should not return the same answer repeatedly. **What is deliberately not built is the cross-file edge the issue asked about.** Resolving `class="container"` to a CSS rule means matching an ordinary English word by bare name against every symbol in the project, which is precisely how #503 ended with production Python owning an edge into a test tree; linking markup to stylesheets needs a scoped resolution path rather than the general one, and that is worth its own issue. The one reference that *is* emitted is `var(--brand)`, because `--` cannot begin an identifier in any other language here, so the name cannot collide with anything. Scope is plain `.html`, `.htm` and `.css`; `.scss` and `.less` need different grammars and are not covered. Behind `lang-html` and `lang-css`, both in the `full` tier.
- **Terraform extraction** - Add full-tier support for `.tf` and `.tfvars` files, including canonical block declarations, direct attributes, traversal references, and malformed-input recovery.

### Changed
- **A sync no longer loads every unresolved reference in the graph at once (#482).** It materialised the whole `unresolved_refs` table on every sync, including a one-line edit: 189,446 records and **+74.6 MiB** on tokensave's own tree, the second largest allocation a sync makes. #409 concluded the resolution pass could not be chunked, and for the *nodes* that is right — binding a cross-file reference means looking up a name that may be defined anywhere, so a chunked name index silently loses targets and drops edges, which is a correctness regression dressed as a memory win. The references are the other input and have no such property: `resolve_batch` is a `par_iter().map(resolve_one)`, so every reference is answered against the whole index independently. The index stays global; only the input is paged, 25,000 at a time, with a keyset cursor on the table's autoincrement `id` rather than `LIMIT`/`OFFSET` so paging the table stays linear instead of quadratic in the number of pages. What still accumulates is bounded by the *answers* rather than the questions — 28,849 resolved and 11,713 ambiguity records against 189,446 inputs — and the one step that cannot be done per page, suppressing the bare-name sibling of a resolved Go selector call (#153 Bug 1), runs once over the accumulated set. **Verified equivalent rather than assumed:** indexing this repository with and without the change produces byte-identical results across all 27,897 edges and every ambiguity record, and the tests compare one-batch against many-batch at page sizes down to a single reference per page. Measured peak RSS on this repository, five runs, one added comment line, CLI `sync`: **215.8 → 175.5 MiB (median)**, and **247.0 → 175.5 MiB against the start of this line of work** — a 29% reduction, none of it from the two redesigns #409 proposed.
- **`resolve_all` no longer clones every reference that fails to resolve (#483).** It built a `Vec<UnresolvedRef>` of the failures — the name pre-filter's rejects wholesale, plus a clone for every candidate that failed or fell below the confidence floor. On tokensave's own tree that is roughly **160,000 owned records per sync**, several `String`s each, because 189,446 references go in and 28,849 resolve. They bought nothing: the caller already owns the slice it passed in, so an index is the same information, and the two uses were an iteration that can borrow (deriving the ambiguity records) and a returned field that **nothing in the product reads** — its only consumers are two assertions in `tests/resolution_test.rs`, which is why it went unnoticed. `ResolutionResult.unresolved` is now `Vec<u32>` of positions in the input slice, and the failures are carried borrowed. Measured on this repository, five runs, one added comment line, CLI `sync`: **peak RSS 238.1 → 215.8 MiB (median)**. One incidental improvement: the field is now in input order, where before it listed the pre-filter's rejects first and the resolution failures second, so its order depended on the partition rather than on the caller's slice.
- **Memory phase samples are now taken after the work they name, and the unresolved-ref load has one of its own (#409).** Every sample in the resolution block was emitted *before* the step it was named for, so each one reported the RSS of the *previous* step under the next step's label. That is not a cosmetic slip: it is why the whole-graph node load was believed to cost ~73 MiB and became the entire subject of #409's four proposed redesigns. Corrected, on this repository with one added comment line, the node load costs **16.2 MiB** — the 73 belonged to loading the 189,446 unresolved references, which had no sample at all and appears in none of that issue's options. The corrected ranking is: load unresolved refs +74.6 MiB, `resolve_all` +97.7 MiB, load the node slice +16.2 MiB, build the resolver caches +6.4 MiB. The two the issue was about are the two smallest, together 10% of the sync's growth. `sync:resolve:load_refs` is new; `load_nodes` and `build_caches` now mean what they say.
- **The build-variant pass no longer reads the whole graph, and the sync's true peak is finally visible (#481, groundwork for #409).** `propagate_variant_edges` loaded every `annotates` and `calls` edge — and did it while the resolver's node slice was still alive, so two graph-sized allocations were resident at once. On this repository that pass held the *entire sync's* peak RSS and emitted **zero** edges. It was doing a whole-graph pass to find a set that is tiny by construction: @vianbas replayed the grouping rule in SQL and found 237 `cfg`-annotated nodes, 82 callable and in a Rust file, forming 79 groups of which **3 have two members**, with exactly 4 `calls` edges pointing at any member — so 4 was the ceiling, and a measured run hit 0. The output set being provably small is the whole argument for asking SQL rather than Rust: two bounded queries now find the variant groups and then only the `calls` edges targeting a member, and the common case — no multi-member group — returns before touching the edges table at all. Both feed the same emitter the whole-graph path uses, split out so the two cannot drift, and an equivalence test runs a real indexed graph through both. That test withdraws an already-propagated edge before comparing, because the steady state after indexing has both twins' edges already and "same answer" would otherwise be two empty vectors proving nothing; it asserts the whole-graph pass found something before asserting the two agree. The Go query pre-filters on a *repeated name* rather than on directory, since SQLite has no `dirname` — a superset of the real groups, bounded by duplicate names instead of by graph size, with the exact `(directory, name)` grouping done in Rust. The `cfg` gate is kept as a correctness condition rather than an optimisation: without it two impls that merely share a `qualified_name` (`From<A>`/`From<B>`, both named `from`) would be fused, and a test asserts they are not. Measured on this repository, one added comment line, CLI `sync`: **peak RSS 247.0 → 221.4 MiB**. The full-index path is untouched — there the nodes and edges are already in memory, so it pays no extra load. Reported by @vianbas.
- **Two phase samples added, because the sync's peak was invisible to its own telemetry (#409).** `tokensave memory` records the phase of the highest *sample*, and there was no sample between `sync:resolve:done` and `sync:done` — so the peak, which lives in that window, was attributed to whichever sample came next. `tokensave memory` duly reported `sync:done`, ~27 MiB below the real high-water mark and two phases late. That blind spot is why #409 has been argued from `size_of` arithmetic rather than from RSS, and why the largest single consumer went unnoticed: `resolve_all` is **+97 MiB** on a one-line sync, more than the node load and the caches combined, and neither of that issue's proposed options touches it. `sync:resolve:refs` and `index:resolve:refs` now bracket the resolution work, and `sync:variants` the build-variant pass. The peak on this repository now reports as `sync:resolve:refs` rather than `sync:done`, which is both correct and the point.

### Fixed

- **`discover` now measures the tokens navigation actually injected, instead of a number that only looked like one (#474).** The reported `addressable_input_tokens` came out at one to five per replaceable turn, and *exactly* 2.000 per turn at short ranges — which reads as a placeholder, and was reported as one. It was a real measurement of the wrong quantity: the analyzer summed `turns.input_tokens`, and under prompt caching that column holds only the *uncached remainder* of a turn's prompt. On the reporter's data and on this machine alike it is a double-digit figure with nothing to do with what a `Read` or a `Grep` put into the conversation. The obvious correction is wrong too: `input_tokens + cache_read + cache_write` is the whole prompt, 197,359 tokens per navigation turn measured here, and almost all of it is conversation the navigation never caused and no graph query could recover. The quantity the field claims — input a tokensave query could have served more cheaply — is the size of the tool *results* those turns injected, and nothing recorded it. Now something does: the transcript parser sizes each `tool_result` block and attributes it to the turn that issued the matching `tool_use`, which needs the id-to-turn mapping to be carried across lines, since a result never travels in the same message as the call it answers. Stored in a new `tool_result_tokens` column, and that is what `discover` reads. **Measured over 3,155 real turns**, the 15 replaceable navigation turns go from **1.93 tokens each to 793** — the "hundreds to thousands per turn" the reporter said the figure should have been. Two honest limits are stated rather than papered over: turns ingested before this change carry 0, because the figure comes from transcript lines the database does not keep and re-reading every historical session would cost more than the metric is worth, so a range reaching back before the upgrade under-reports and the command says so rather than presenting a bare zero; and `RECOVERABLE_FRACTION` stays at half, but its rationale is rewritten — the old one argued that most of `input_tokens` was fixed conversational overhead, which is no longer the quantity being halved. Reported by @xaerogonzo.

- **MCP server no longer sends error responses to unhandled notifications.** Per JSON-RPC 2.0 §4.1, servers must never reply to notifications. Previously, unhandled notifications such as `notifications/roots/list_changed` or custom notifications without an `id` fell through to the wildcard handler and returned `{"id": null, "error": {"code": -32601, "message": "method not found: ..."}}`. Strict JSON-RPC clients (such as Go's MCP SDK used by Antigravity and Gemini CLI) reject responses with null IDs as `ErrInvalidRequest` and immediately terminate the connection. `handle_request` now checks `request.is_notification()` and discards unhandled notifications silently without a response.

- **MCP index-age warning no longer spams every tool result.** When the index is more than one hour old, the MCP server prepended `WARNING: Index last synced ...` to the content of every successful tool call, including zero-hit lookups (`No symbol named ...`), which agent clients could mistake for a failed or incomplete answer and retry in a loop. Freshness state is now reported as structured `_meta.freshness` metadata (`index_age_secs`, `last_sync_at`, `stale`, `warning_emitted`), the text banner is emitted at most once per hour per server session (per provenance root for selected graphs), and it is suppressed entirely on zero-hit and `tokensave_status` results so answer text stays parseable.
- **Oh My Pi lifecycle operations now keep global profile ownership consistent when the active profile changes.** Global install records each resolved OMP agent directory before updating its MCP and rules surfaces, and reinstall refreshes every still-existing recorded profile instead of only the profile active at that moment. Migration detection and doctor inspect active, recorded, and legacy default profiles, while uninstall removes Tokensave from all known profiles without creating a newly selected profile and retains failed targets for a later retry. Resolver output and the versioned profile registry are validated before mutation, registry updates are locked and atomic, and `primary_config_path` reports only the currently resolved active profile.
- **The grep guardrail no longer denies a whole command because the extra work is a substitution or a redirect (#480).** #475 established that a search is only ever a *suggestion* to ask the graph instead, so it must never veto a line it does not fully model — a denial returns the entire command unrun, and destroying a `./deploy.sh` to save a grep is the worse failure. `has_chained_command` implemented that rule for `&&`, `||`, `;`, `|` and a backgrounding `&`, but those are not the only ways a line carries work the hook cannot serve. A command substitution runs a command of its own, and ``grep -n my_function src/lib.rs `date +%s` `` or `grep -n "$(cat patterns.txt)" src/lib.rs` was denied outright, so the substitution never ran; an output redirect is a file the caller asked for, and `grep -n my_function src/lib.rs > /tmp/hits` produced no file. Both now pass through untouched, in double-quoted regions as well as bare, since `$(…)` and backticks expand inside double quotes. `2>`/`2>>` is deliberately *not* included: routing the search's own stderr away discards nothing the caller depends on, so the line still is just the search, the graph is a real substitute for it, and the suggestion stands. The same rule governs the `find`/`fd` discovery path, which shares the scanner. Reported by @dangeReis.

- **A lone bare-name candidate is no longer treated as a match when nothing can reach it (#503).** #378 taught the resolver to refuse when the dotted-receiver fallback is *ambiguous* — two candidates tied on every scoring dimension produce no edge rather than a coin-flip one — and left the case where the set has exactly one member. That is the whole of this bug: when a Python call's receiver is a type the index does not track (a stdlib object, a third-party instance, a `logging.Logger`), the fallback matches the bare method name against every indexed symbol, and a single hit was bound regardless of which module or directory it lived in. Being the only candidate is not evidence. Nothing had checked that the call site could reach it, and the reporter's contrast makes the point exactly: add a *second* same-named symbol and the resolver correctly declines and records an ambiguity — it already routes this call site into the "not confident enough" path, it just did not take it when the set had one member. The failure is quiet and directional, because test doubles are deliberately named after the API they stand in for: a fake logger defines `info`, a faithful fake of a UI toolkit defines `after`, `delete` and `grid`, and those are exactly the names production code calls on untracked receivers. Everything computed from `calls` inherits it — `circular` reports production and tests inside one strongly-connected component, `dead_code` sees a phantom caller and calls live code reachable, `impact` and `file_dependents` report production modules as depending on test files. The fallback now asks for evidence: the candidate is in the caller's own file, sits in the same directory (one package, in every language this path serves), or the caller imports its name, the class that owns it, or the module it lives in. The import test reads an entry both whole and by its last dotted segment, because the index keys each import on its last `::` segment — for Python that is the entire dotted path, so `import headroom.perf.analyzer` is one key and a naive lookup for `analyzer` finds nothing. **Measured on a 992-file Python project**, the guard removes 2,606 of 31,901 call edges, and the removals are the reported class almost end to end: `append` accounts for 1,654 of them, `debug` 346 and `execute` 172 — `list.append`, `logger.debug` and `cursor.execute`, every one a call on a receiver the index never typed. What it costs is a call through an object whose method lives in an un-imported mixin, on the order of tens of edges there; the trade is the one #378 already settled, that a missing edge degrades an answer while a fabricated one corrupts it. Reported by @xaerogonzo.
- **A COBOL file whose last line is one to five characters no longer costs a minute and gets skipped (#498).** The tree-sitter COBOL grammar does not terminate on that input. It is not slow, it does not finish: a probe left parsing a six-line program spun at full CPU for nine minutes before being killed, and in normal operation the only thing that ever stopped it was the extraction worker's 60-second watchdog, which then discarded the file and respawned the worker. `.cob`, `.cbl` and `.cpy` all reached it, and `.cpy` matters most because copybooks are fragments, so an odd final line is likeliest exactly there. Worse than the minute: on a fresh `init` the file contributed nothing, and on a `sync` over an already-indexed file the previous symbols were kept, so the graph went on serving stale data for it — and the command exited 0 either way. The boundary was measured rather than guessed: one to five characters hangs, six or more parses in about 50µs, with or without a trailing newline, and a short line *anywhere else* in the file is fine. Six is the width of the fixed-format sequence area, columns 1-6, so the final line is now padded out to it with spaces before parsing — trailing whitespace on a line COBOL ignores, appended at the end, leaving every byte offset in the real source unchanged. An in-process deadline was tried first and rejected: tree-sitter's progress callback is never polled during this spin, so the parse cannot be interrupted from inside, only avoided. The reporter's own repro now indexes in **16ms against 60,023ms**, and produces a node where it produced none. Reported by @vianbas.
- **`tokensave_status` accepts `graph_root` and `graph_branch` (#500).** Every graph query tool takes the selectors, but status was classified selector-less and answered `-32602`, so an agent that had just selected another project's snapshot had no way to ask what was in it — node and edge counts, sync metadata, the branch the database serves, fallback state. Nothing in the handler stood in the way: it reads all of that from the graph it is handed, so pointing it at a selected one needed no new plumbing, only the classification. Server statistics stay local and are attached to unselected calls only, because they describe the running process rather than the snapshot being inspected. Raised by @rNoz after watching agents attempt the call and fail.
- **`branch add` no longer indexes the working tree into the wrong database, and `branch gc` no longer deletes live branches (#501).** Two bugs with one root: both commands assumed a repository layout instead of asking git. `branch add <name>` copied the parent DB for the named branch, then synced through `TokenSave::open` — which resolves the DB for *HEAD*, not for the branch just named. Adding a branch that was not checked out therefore left its DB a bare copy of the parent while the working tree, untracked files included, was written into the *current* branch's DB: the one database the command was not asked to touch. A sync reads the working directory, so it can only ever speak for the branch that is checked out; indexing an arbitrary git tree is a feature this does not have. The command now says so — an unchecked-out branch starts as the copy of its parent, and the user is told to check it out and sync — and the sync path runs only once HEAD is confirmed to be the branch being added. `branch gc` decided a branch was gone by testing for `.git/refs/heads/<name>` on disk with a `packed-refs` fallback. Inside a linked worktree `.git` is a *file* pointing at the real git directory, so that path never exists, and a `reftable` repository keeps no loose refs at all — in both, every tracked branch looked stale and its live DB was deleted. Only the default branch survived, and only because the loop exempts it by name. Branches are now enumerated through gix, falling back to `git for-each-ref`, which is also correct for packed refs and for names containing a slash; and when the refs cannot be read at all the command fails instead of deleting, because not knowing whether a branch exists is not the same as knowing it does not. Reported by @bobbypierce42, who also identified both root causes.
- **A `tokensave_files` flat listing that matches nothing no longer kills the MCP server (#499).** `format: "flat"` joins the file list into a string, so a valid filter matching zero files produced an empty one, and `truncate_response` asserted against exactly that. The assertion is a `debug_assert`, which is the worst of both outcomes: a debug build panicked and the process exited 101 — with no JSON-RPC error, so the client saw only a closed transport and lost every tool — while a release build elided the check and returned a successful response with an empty text block, indistinguishable from a failure. The request was never invalid, so both halves are fixed rather than one: `truncate_response` returns an empty input unchanged, which makes the two build profiles agree, and the flat formatter reports `0 indexed files` when there are none, matching what the grouped format already said. Reported by @rNoz.
- **`sidecar_stem` no longer panics on a doc filename containing a non-ASCII character (#504).** It tested for the `.readme.md` suffix by calling `str::split_at` at `len - 10` and comparing the tail, but that offset is a *byte* offset into the path and a multi-byte character can straddle it: `docs/Ativação bbbb.md` is 22 bytes, the split lands at byte 12, and byte 12 is inside the two bytes of `ã`. `split_at` panics on a non-char boundary, and because the check runs for every discovered doc during `build_companion_docs`, one accented filename aborted `init`, `sync --force` and `doctor` outright and left the project with no usable index — a regression against 7.0.2, which indexed the same tree. The suffix is pure ASCII, so the comparison is now made on the byte slice: a byte-wise match also *proves* the split point is a char boundary, which is what makes the subsequent slice sound rather than merely lucky. Non-ASCII stems that really are sidecars still resolve. Reported by @mirandabsb.
- **`githooks on` no longer exits 0 after failing to install a hook (#488).** Both paths printed a red `✘ Failed to open … for writing` and then returned success, so a setup script gating on the exit code carried on with no hooks installed and no way to tell. The global path called `write_global_hook`, which returns a `bool`, in an `if` whose `else` did not exist; the local path collected `installed` and `already_present` but had nowhere to put a hook that failed. `offer_git_post_commit_hook` now returns `Result<(), String>` and `LocalHookInstall` carries a `failed` list, which `githooks on` turns into a non-zero exit naming the hooks. Failures are collected rather than raised at the first one: the command installs three hooks, and bailing early would skip the two the user also asked for. Declining the prompt, an already-present hook, and the best-effort call inside `install` all stay success — only an explicit `githooks on` that could not do what it was asked now fails.

## [7.11.0] - 2026-08-30

### Added
- **Per-repository git hooks, so keeping the index fresh no longer costs a machine-wide setting (#455).** tokensave's git hooks worked one way: claim `core.hooksPath`. That is a **single global setting**, and it overrides git's default of a separate `.git/hooks` per checkout — so accepting the hooks forced one hook directory on every repository on the machine, which is the wrong answer for anyone whose projects need different tooling. `tokensave githooks on --local` installs the same three hooks (`post-commit`, `post-checkout`, `post-merge`) into the repository's own hook directory and **writes no git config at all**, so no other repository is affected; `--local` works on `off` and the bare status form too, and `--path` targets a repository other than the working directory. `tokensave init` now offers them, defaulting to yes on a terminal and staying silent otherwise, with `--git-hook` and `--no-git-hook` to decide up front — local rather than global because `init` is per-project by definition. The offer is skipped when tokensave's global hooks are already installed, since a repository covered by both would run a sync twice per commit. Four details worth knowing. Writing is additive in the same way the global path is, so a repository already running husky, pre-commit, or a hand-written `post-commit` keeps everything it had and gains a marked tokensave section, and removal keeps any content tokensave did not write while deleting a file that is nothing but ours. There is deliberately **no chaining preamble** on local hooks: the global ones forward to `.git/hooks` because claiming `core.hooksPath` would otherwise disable it, but a local hook *is* that file and a forwarder would invoke itself. The hook directory is resolved from `--git-common-dir` rather than `--git-dir`, because linked worktrees share one hook directory with the main checkout and resolving the other way would write to a per-worktree directory git never reads. And if a `core.hooksPath` is in effect for the repository from any config scope, git resolves every hook from there with no fallback, so the hooks would be written and never run — that is reported rather than passed off as a successful install, which is the reporter's own complaint pointed the other way. The first half of the report, that `tokensave install` should not modify global hooks by default, was already the behaviour: the prompt defaults to no and a non-TTY skips it entirely. Reported by @jmagnusson.
- **`tokensave serve --idle-timeout-secs <N>`, an opt-in deadline for hosts that leak servers (#436).** Some MCP hosts start a **new** server per subagent and keep every one alive after the subagent that used it has finished. Every retained server is a direct child of the same *still-live* supervisor, so nothing ever closes its stdin and the EOF that would normally stop it never arrives — and a parent-PID watchdog cannot help, because the parent is not dead. A controlled two-subagent pilot on Windows added two servers that both persisted, with four totalling roughly 113 MB private memory, 266 MB working set and 922 handles, each holding its index open. The reporter's analysis was that this is an upstream host defect (openai/codex#38754) and that tokensave's own teardown path is correct, and both halves stand — the same supervisor retained duplicate fleets of unrelated MCP servers after tokensave was removed from its config. What tokensave can do is stop waiting. With the flag set, a server that goes `N` seconds without a request exits through the existing graceful, idempotent shutdown; omitting it preserves today's indefinite lifetime exactly, so no existing setup changes. Three properties make it safe to leave on. The deadline is evaluated **only** while the loop is parked waiting for the next request — the timer is created fresh at each park — so it can never interrupt a request, and because requests are handled serially no in-flight counter is needed. It measures *idleness rather than uptime*: each request restarts the window, so a server busier than its timeout never expires. And an expiry landing while a detached startup job is still running — the startup catch-up sync, or a version-bump reindex — is deferred and waits out another whole window, so the option cannot abandon work the next request depends on. The deferral rule is deliberately asymmetric and pinned by its truth table: catch-up is spawned unconditionally so "not done" really means "still running", while a reindex defers only while *started and not finished*, since keying on `!done` alone would make every ordinary session defer forever and the option silently do nothing. **It stays off by default** because enabling it depends on the host starting a fresh server when a tool is called after an idle exit, which varies by host and is not something tokensave can probe. It is host-integration policy rather than project state, so it lives on the command line only: there is no config-file equivalent and `tokensave install` does not write it into generated host config. Related: the reapability half of this issue — a server that ignored `SIGTERM` and needed `SIGKILL` — shipped separately in `1a9b983`, and #421's registry makes the retained servers enumerable in the first place. Reported by @jasonb194.
- **`tokensave servers` names which running server holds which index (#421).** `serve` keeps an exclusive handle on its database for the life of the process — file watching is why the process is long-lived, and the handle comes with it — so an indexed directory cannot be deleted while a client has a server up. On Windows that fails outright rather than deferring, and it bites hardest on git worktrees: a worktree per task, each one indexed, each one cleaned up afterwards, with `git worktree remove` deregistering the worktree even when the file delete fails and leaving git metadata pruned against a directory still on disk. The lock itself was never the problem — the problem was that it had no owner you could name. The reporter's machine had seven servers running and **five carried no project in argv at all**, because an MCP host supplies the project through the global database or `initialize` roots rather than `--path`, so neither `ps` nor Task Manager can answer "which of these holds this index". This is a capability regression rather than a new defect: before v6.0.0 the long-lived process was the daemon, and the daemon had `--status` to print its PID; `serve` inherited none of that, so v6.0.0 replaced a partly-broken stop path with no path at all, and the workaround in the field became correlating the SQLite `-shm` sidecar's mtime against process start times — inference from an implementation detail, which fails outright when two servers start in the same second. Each server now writes `~/.tokensave/servers/<pid>.json` once its database is open, recording `pid`, `started_at`, `project_path`, `argv_path`, `db_path` and `version`; `tokensave servers [--json]` lists them. That makes both directions answerable from files: **index → process** by matching `db_path`, which is what deleting a stuck checkout needs, and **enumerate all** by listing the directory, which is what a wrapper needs to rebuild a list UI. Four deliberate choices. The registry lives in the *global* directory rather than beside the index, because a PID file inside `.tokensave/` would sit in the one directory whose defining problem is that it cannot be deleted, and would vanish with the checkout being cleaned up. `started_at` records the **process** start time as the OS reports it rather than the write time, which turns the reporter's fragile mtime correlation into a contract: a live process at a recorded PID whose start time disagrees is a different process, so PID reuse is detected instead of guessed. `db_path` is recorded separately from `project_path` because a per-branch database is not derivable from the project root, and matching on the wrong one would silently miss. And `argv_path` is kept alongside the resolved path so a reader can reconcile the registry against what a process lister shows. Entries are removed on clean exit, and reaped both on any `serve` startup and on every read, so a hard kill that skips the clean-exit path self-heals within one listing. **There is deliberately no `servers --stop`:** MCP clients restart their servers, so "stop them all" does not converge — new PIDs appear while old ones are being killed — and terminating is safe only for a caller who knows whether the host is running and can stop it first, which tokensave cannot know. Identification is the part tokensave owes; the kill stays with whoever has that context, and `serve` now honours a `SIGTERM` on that PID since #436/#450. Related to #437, which reports the server's own resolved root — that covers the diagnosis direction; this covers the reverse lookup. Reported by @xaerogonzo.
- **Obsidian Canvas (`.canvas`) boards are now indexed (#459).** A `.canvas` file is the [JSON Canvas](https://jsoncanvas.org) format Obsidian uses for node-graph boards, and it had no extractor, so every sync reported it as an unsupported extension and its contents never reached the graph. In a vault that uses Canvas for architecture maps or planning boards that is often where the *structure* lives — the relationships between notes rather than the note bodies — so the practical symptom was that a term written only on a canvas was unfindable, and a note reachable only through canvas edges looked orphaned even though the vault links it. Because the format is plain JSON with a documented schema, this is a hand-rolled `serde_json` reader rather than a tree-sitter grammar, so it adds no grammar table to the binary. Each `type: "text"` card becomes a `Module` node — the same kind the Markdown extractor gives a heading, so a card and a note section behave alike in search — named from its first heading or first non-empty line and carrying the card's full Markdown as its docstring, which is what makes an arbitrary term on a card reachable. Each `type: "group"` with a label gets one too, since a group label is authored text like any other. Each `type: "file"` card emits a `Uses` edge to the note it points at, and entries in `edges[]` become `Uses` edges between the cards they connect, so board structure is preserved. Two deliberate choices: the `file` target is **not** filtered to code extensions the way Markdown link handling is, because a canvas card almost always points at another `.md` note and those are precisely the edges that made a linked note look orphaned; and an edge whose endpoint is a `file` card is not emitted a second time, since that card's own reference already represents it. `type: "link"` cards are skipped, having only an external URL the graph cannot resolve. A canvas that is not valid JSON still yields its `File` node plus a reported error, so a malformed board stays visible to `tokensave_files` rather than vanishing the way an unsupported extension did. Gated behind the new `lang-canvas` feature, in the `full` tier. `.base` (Obsidian Bases) is not covered — the reporter rated it lower priority and the format is newer and less widely used — so it still appears in the skipped-extension line. Reported by @jasonb194.
- **`tokensave doctor` now reports an index whose *scope* is wrong, not just one whose contents are (#450).** A home directory initialized as a project — the shape #372 could produce — leaves an index that every later `serve` inherits and keeps syncing, and nothing surfaced it: `serve`'s guardrail refuses *uninitialized* directories, and this directory is initialized, just absurd in scope. The reported case reached 29.9 GB on disk and a 38 GB `phys_footprint` once mapped, against a real project on the same machine at 420 MB, with the machine paging heavily and the server still syncing the home tree at 50–70% CPU two and three-quarter hours in. A new `Index scope` section reports two things: whether `$HOME` itself is initialized as a project, checked regardless of the working directory since the whole problem with that state is that nobody is standing in it when it does the damage; and whether the current project's index has passed 5 GB, a line set well above any plausible monorepo because a false positive costs one line of output. The size counts the WAL as well as the database, which is where an actively-syncing runaway shows up first and is part of what a running server maps. Both are warnings rather than failures — neither state is corrupt, and the remedy is a judgment call — and a directory that is both `$HOME` and the working directory is reported once. This is the third of the reporter's three suggestions; the scope cap for `serve` and a SIGTERM handler that interrupts an in-flight sync are still open on the issue. Reported by @not-antoni.
- **First-class Oh My Pi (`--agent omp`) integration for global profiles and project-local installs.** Global install, uninstall, and doctor operations use a bounded bare `omp config path` lookup, so exported `OMP_PROFILE`/`PI_PROFILE` selections and OMP-owned `PI_CONFIG_DIR`/`PI_CODING_AGENT_DIR` overrides target the profile OMP actually owns rather than a guessed Pi-compatible path; cheap detection and migration checks remain subprocess-free against the default profile. Local installs use `.omp/mcp.json` and `.omp/rules/tokensave.md` without requiring the OMP CLI. The installer preserves unrelated MCP servers and settings, refreshes an OMP-native `alwaysApply: true` advisory rule carrying an exact ownership marker, and uninstall removes only Tokensave-owned entries and rules. Doctor reports resolver failures plus missing, malformed, stale, or wrong-command surfaces. Hook enforcement is deliberately unsupported until Tokensave has a proven OMP-specific hook contract.
- **A global `post-merge` git hook now runs `tokensave sync` after `git pull`.** `tokensave install`'s git-hook offer previously wired only `post-commit` (after your own commits) and `post-checkout` (fresh clones and branch switches), so a plain `git pull` that fast-forwards or produces a merge commit fired neither — a teammate's changes landed on disk with no hook to pick them up, and the index stayed stale until your next local commit. `post-merge` is now a third owned hook, installed and removed alongside the other two under the same `--git-hook` opt-in, `tokensave githooks on`/`off`, and `tokensave uninstall`'s `--keep-git-hooks` guard; `tokensave githooks` (no action) reports it as owned like the others. It carries its own idempotency marker so an existing install migrates the next time `tokensave install` runs, and it chains to a repository's own `post-merge` hook (e.g. a Husky-managed one) the same way `post-commit`/`post-checkout` already do, so claiming a global `core.hooksPath` never silently disables a repo's own pull hook.
- **Ruby `define_method`/`define_singleton_method` with a statically-resolvable name now emit a first-class method node.** Previously these DSL definitions produced no node at all, making them invisible to `search`, `module_api`, `callers`, `rename_preview`, `dead_code` and `doc_coverage`, and leaving every call to them permanently unresolvable. The new method gets the same kind, visibility, `Contains` edge, complexity metrics and outgoing calls an equivalent `def` would.
- **Ruby `module_function` now emits both the private instance method and the public singleton copy it actually defines.** Previously the extractor emitted a single public instance method, so `ERB::Util.html_escape_once`-style constant-receiver calls into a `module_function` method could never resolve and the method showed up as public API when it wasn't. The bare mode switch, `module_function :foo, :bar`, and inline `module_function def foo; end` are all handled, with the singleton copy carrying the same docstring, calls, and complexity as the `def` it mirrors.
- **`tokensave githooks`, and git-hook removal as part of `tokensave uninstall` (#420).** Installing offered global `post-commit` and `post-checkout` hooks; nothing removed them. Since the hooks are global and outlive every agent integration, uninstalling from all agents and deleting a project's `.tokensave/` still left a hook that recreated the index on the next commit — the reporter's exact sequence. `tokensave githooks` now shows which hooks tokensave owns and where, `tokensave githooks off` removes them, `tokensave githooks on` installs them without a prompt, and a global `tokensave uninstall` removes them too unless `--keep-git-hooks` is passed. `--local` never touches them, since the hooks are global rather than project-scoped. Removal is conservative in three ways: a hook file holding anything the user wrote keeps that content and loses only tokensave's marked section; a file that is nothing but tokensave's own content is deleted; and `core.hooksPath` is unset — with the directory removed — only when it points at tokensave's own default, that directory is left empty, and the run actually removed a tokensave hook, so a `core.hooksPath` the user set themselves is never edited by a command that found nothing of ours. The section boundary handles all three shapes that have shipped, including the unfenced `post-checkout` bodies from 6.4.3 and 7.3.0 that end in a bare `fi`. Reported by @jasonb194.

### Changed
- **Installed agent rules text is now one canonical body with per-harness rendering and versioned, refreshable blocks (#441).** `tokensave install` used to copy a stale, per-agent rules paragraph into global instruction files and never refresh it in shared owner-edited files because the marker was a plain heading append. The canonical source now lives in `src/agents/rules.rs` and covers `tokensave_status`, `graph_root` for cross-project queries, branch-specific tools for cross-branch queries, scoping MCP use to code tasks, finding the active database in `.tokensave/branch-meta.json` before any SQL fallback, and guarding read-only tasks from writing the index. Claude Code still gets its mandatory "no Explore agents" overlay on the same core, Kiro keeps its delegate-tool rule as a similar overlay, and the remaining integrations (Gemini, Grok, Kimi, Qwen, Vibe) now render the same canonical body, so all thirteen installed harnesses share one rules source with doctor coverage. Shared files (`AGENTS.md`, `copilot-instructions.md`, and Pi's `~/.agents/AGENTS.md`) use HTML-comment markers with a content hash so `install` and `reinstall` refresh the tokensave block in place while preserving owner content. Dedicated rules files (`~/.claude/rules/tokensave.md`, `~/.augment/rules/tokensave.md`, `~/.config/opencode/tokensave.md`) are overwritten only when the bytes actually change. `tokensave doctor` reports, per harness, whether the rules file or block is up to date with the expected version hash, drifted, missing, or still using the legacy heading marker.
- **The worldwide-counter upload now happens at most once a day, from one shared gate.** Four code paths uploaded on four different rules: the CLI attempted a flush on every `init`, `sync` and `status`, and after a 30-second staleness check on everything else; a running MCP server flushed every 30 seconds of tool activity; and its shutdown path uploaded unconditionally. On an active machine that is a synchronous HTTP request per command, for a number that is a single global sum — an intermediate upload moves it for a few hours and is then indistinguishable from having waited. `cloud::upload_is_due` is now the one place the cadence is decided, and every path asks it: enabled, something pending, a day since the last success, and not immediately after a failed attempt. `try_flush`'s `force` parameter is gone rather than made daily, because a command that asks to upload can no longer opt out of the interval. Nothing is lost by declining: saved tokens accumulate locally either way, and the server re-derives its delta from the running total, so the periodic path deliberately does not persist an accumulated figure it did not upload — writing it while the in-process watermark stays put would count the same tokens twice. Two shorter intervals survive, doing narrower jobs: a 60-second back-off after a *failed* attempt, which is what stops a broken network from meaning a request per command (a failure leaves the daily gate open by design), and a 30-second in-process re-entry guard on the server, which only keeps a busy session from re-reading the user config on every tool call. `last_upload_at` was already tracked and is now what actually governs the schedule.
- **The two `sync` consumers of `get_all_edges()` now load only the two edge kinds they use (#418).** `propagate_variant_edges` reads `Annotates`, to find the `#[cfg]`-gated nodes, and `Calls`, to index what is called and which pairs already exist; `type_of`, `returns`, `uses`, `implements`, `extends` and `receives` were carried into both incremental-sync paths and never looked at. On this repository that is 23,167 rows instead of 25,636, and proportionally more on a corpus richer in type and return edges. The `calls` half is irreducible for this algorithm, since any call into a variant member matters. The contract is documented on the function and pinned by a test that adds an edge of every other kind, each shaped so that reading it as gating *would* change the result — the first version of that test used noise the function could not have acted on either way, and passed against a deliberately broken function. With the six sites in the issue, three handlers landed in #430 and these two here; the remaining `handle_gini` keeps its whole-graph load, which #426 confirmed it needs.
- **`tokensave_hotspots` no longer counts a recursive call as connectivity (#431).** A self-edge counted toward *both* in- and out-degree, so a recursive function scored +2 toward the exact quantity the tool ranks by, for a property of its own implementation rather than of how connected it is to anything else. `tokensave_gini`'s fan arms have always excluded self-edges on that reasoning — a recursive call is not a dependant of itself — and the two tools disagreed with nothing in either description saying so. They agree now, in `gini`'s favour. Measured before changing it: this repository holds 405 self-edges out of 25,636, all of them `calls`, and excluding them left the top twelve in identical order with one symbol's degree moving, `run` from 206 to 198. So the ranking is stable in practice and the fix is not a reshuffle. Split out of #418, where preserving the old behaviour was right for a performance change but left the inconsistency standing.
- **Documentation caught up with the resolver as it actually behaves.** `docs/INDEX-DESIGN.md` described Phase 3 as "a scoring heuristic picks the best" candidate, which stopped being true in `e8a8a8a`/`ab9ff29`: every candidate tied for the best score is returned, a lone winner resolves and a tie produces no edge and an `ambiguous_calls` record. The scoring table there was also missing three of its seven signals (directory proximity, language match, import match), the `ambiguous_calls` table was undocumented, and the stated schema version was **5** against an actual **17**. `tokensave_ambiguous_calls` is now listed in the tool tables in `README.md` and `docs/USER-GUIDE.md`, where `dead_code`'s description also notes that ambiguity candidates are excluded from it.
- **Three of `get_all_edges()`'s six consumers no longer materialise the whole edge table (#418).** `handle_hotspots` counted in- and out-degree for all 25,580 edges here, cloning both endpoints of every row, in order to emit at most `limit` rows — 10 by default, 100 at the cap. It now asks SQL for the top `limit` by combined degree. `test_reached_node_ids` and `handle_test_risk` both filtered `EdgeKind::Calls` after loading — `test_risk` on each of three separate passes — and now select that kind in SQL, which drops 6,298 of the 25,580 rows, a quarter of the load, with no change in behaviour. Peak RSS on this repository, three runs each, debug build so not comparable with the release figures in the issue: `hotspots` 40.2 → 37.2 MiB, `complexity` 68.8 → 67.8, `test_risk` 74.8 → 74.3. The aggregate is checked against the in-memory tally it replaces as an oracle, including a self-edge counting toward both degrees, so the ranking is preserved rather than quietly re-derived. **One deliberate behavioural change:** ties are now broken by node id. The tally it replaced sorted a `HashMap`'s iteration order, so `tokensave_hotspots` returned equal-degree nodes in an order that varied between runs on an unchanged index — three runs here produced three different orderings, where the new query produces one. The result set and the degree sequence are identical either way; only the order within a tie group moves, and it moves from arbitrary to fixed. The two `sync` consumers are left alone as #409's territory, and `handle_gini` keeps its whole-graph load, which #426 confirmed it needs: both its file and symbol arms count edges whose far endpoint may sit anywhere in the graph. Reported by @vianbas.
- **Installed agent rules are refreshed from one canonical body per harness (#441).** `tokensave install` and `tokensave reinstall` now rewrite the tokensave rules block in shared global files (`CLAUDE.md`, `AGENTS.md`, `copilot-instructions.md`, `~/.agents/AGENTS.md`) when the canonical text changes, leaving owner content outside the markers untouched. Dedicated rules files (`~/.claude/rules/tokensave.md`, `~/.augment/rules/tokensave.md`, `~/.config/opencode/tokensave.md`) are overwritten only when the body actually changes. The canonical text replaces the hardcoded `.tokensave/tokensave.db` fallback with the active database path from `.tokensave/branch-meta.json`, adds branch freshness and cross-project selector guidance, scopes MCP use to code tasks, and includes a read-only guard. `tokensave doctor` reports per-harness rules version drift, missing blocks, and legacy heading-guarded blocks. Pi now has a documented rules surface at `~/.agents/AGENTS.md`.

### Fixed
- **A `Bash` grep narrowed by an include glob is no longer judged on its search root (#452, #449).** `evaluate_grep_tool_input` has honoured the native `Grep` tool's `glob` field since #279, on the principle that a glob is more specific evidence about what is being searched than the directory the walk starts from. The `Bash` path threw that information away: `extract_grep_invocation` discarded every `-`-prefixed token, so `grep -rn foo --include='*.md' .` was classified from `.`, denied as a code search, and told the caller it "targets a code file" when it targets no code at all. Hit twice in one session while working on the neighbouring half of #452, which is how it was found. `--include` in both spellings, and `rg`'s `-g`/`--glob`/`--iglob` and `-t`/`--type`, now reach the target classifier. The `rg` form was doubly broken: with `-g` unrecognised its value became the *pattern* and the real pattern became a target, so the verdict was about the wrong string entirely. `--exclude` has its value consumed rather than read as a glob, since it says what is *not* being searched. One non-code member of a mixed set passes the whole search through, on the same principle as #448 — the graph cannot answer for it, so blocking costs a round-trip through the opt-out for a query nothing else can serve. Containment is still decided before the glob is consulted, so a code glob cannot pull an out-of-tree path into scope. `Read` deliberately gets no handler and was never in the matcher tokensave installs, so routing and implementation already agreed. Reported by @xaerogonzo.
- **A hand-widened `PreToolUse` matcher survives `install` and `doctor --fix` (#452).** The installed matcher was compared for exact equality with the expected one, so a user who added a tool to `~/.claude/settings.json` silently lost the edit on the next run. A matcher that is a *superset* of what tokensave routes covers everything the handler needs and is no longer treated as drift; a narrower one is still repaired, and the repair now merges rather than replaces, so the `Glob` widening from #389 still reaches existing installs without discarding what the user put there. Reported by @xaerogonzo.
- **The `PreToolUse` grep guardrail now sees a search batched behind another command (#451).** `extract_grep_invocation` required the command to *begin* with `grep`, `rg` or `ag` once its prefix stripper had run, so anything in front of the search hid it completely: `echo "=== A ===" && grep -n SymbolOne src/one.rs`, `printf ready; rg MySymbol src/`, `ls && grep -n MySymbol src/lib.rs` all passed through untouched. That is not a crafted bypass, it is how anyone greps several symbols in one call — a label before each search is the natural way to read the combined output — so the guardrail was silently absent from a common real shape while working on the single-command spelling beside it. The Bash branch now falls back to splitting on top-level `&&`, `||` and `;` and classifying each segment on its own. The whole command is still tried first, unchanged, because that is the only path that models a leading `cd` and resolves the grep target against it; a split would read `cd src` as an ordinary command and lose that. **A denial returns the entire command to the caller unrun**, so the search is redirected only when every *other* segment is inert — `echo`, `pwd`, `ls`, `cat`, `printf`, and nothing else. `true` and `:` are inert *only ahead of* the search, where they carry no work of their own, so `true; grep -n Sym src/lib.rs` is still a batched search; after it they are consuming the search's exit status, and `grep -n Sym src/lib.rs || true` is how a failing search is stopped from failing a script, which #475/#476 pin as pass-through. Anything unrecognized counts as doing real work, which makes the failure direction allow: `git checkout -b x && rg Sym src/` still passes through, because blocking it would throw away the checkout to save a grep. The same reasoning leaves subshells, command substitution, newline-separated command lists, pipes, redirects and background jobs out of scope — the hook does not model what they run, so it does not act on them — and a segment that is a search but whose pattern is not symbol-shaped is still the pattern classifier's call, not the splitter's. Reported by @dangeReis.
- **A running MCP server now actually exits on `SIGTERM`, instead of shutting down gracefully and then sitting there alive (#450, and the reapability half of #436).** The reported symptom was that a plain `kill` did nothing — the process was still alive and still burning CPU seconds later, and `SIGKILL` was required, which loses the graceful shutdown's WAL checkpoint. Measured here with stdin held open the way a live supervisor holds it: the server ran its full shutdown, printed its `[tokensave] shutdown: … uptime 5s` summary, and was **still running 30 seconds later**. Two separate defects, both ours rather than the host's. First, `tokio::io::stdin()` performs its reads on a blocking thread, and a blocking task cannot be cancelled; the outstanding read is therefore still parked when the run loop leaves, and dropping the runtime waits for it. Under a supervisor that never closes our stdin — exactly the Codex case in #436 — that wait never ends, so the only thing that had ever *really* stopped these servers was stdin closing, and signals only appeared to work when EOF happened to follow. The serve path now flushes stdout and exits explicitly once shutdown returns, which is safe because shutdown is idempotent and has already persisted counters and checkpointed the WAL. Second, the `SIGTERM` stream was constructed *inside* the run loop, so it existed only while `select!` awaited the next line and was dropped before `handle_request` ran; tokio's registration is process-global and is not undone on drop, so after the first iteration the default disposition — terminate — was permanently replaced while, for most of a busy server's life, nothing was listening. A signal delivered during request handling was neither fatal nor observable: the next iteration built a fresh stream, which cannot see an event delivered before it existed. The stream is now registered once for the life of the loop, so it coalesces and retains a signal arriving mid-request and the server leaves at the top of the next iteration. Interrupting a long in-flight sync rather than waiting for it is a separate decision and stays open on #450. Pinned by `tests/serve_sigterm_test.rs`, which drives a real `serve` process through one request — the state in which the signal used to be lost — then `SIGTERM`s it with stdin still open; it hangs for the full 20-second budget on the previous code and exits immediately now. Reported by @not-antoni and @jasonb194.
- **`tokensave cost --export json` now publishes every priced token category, so the exported tokens can account for the exported cost (#472).** The payload carried `total_input_tokens` and `total_output_tokens` but no cache field at all, while `total_cost_usd` was computed from the full usage record — so dividing cost by tokens implied roughly $270–$360 per million, several times any published Anthropic rate, and the export disagreed with the tool's own `--by-agent` view about token volume by around 600× while agreeing with it on cost to within rounding. Two further symptoms pointed at the same cause: `sum(by_model[].tokens)` equalled `total_input + total_output` exactly, so there was nowhere in the payload for cache tokens to be hiding; and the export showed about 455 output tokens per input token, a ratio only possible if most input is uncounted, since agent workloads are strongly input-heavy — a large context is resent every turn and the completion is comparatively small. That resent context is a cache read: priced, dominant, and absent from the export. `token_breakdown_since` now returns cache creation alongside cache reads, the summary carries both, and the JSON gains `total_cache_read_tokens`, `total_cache_creation_tokens` and a `total_tokens` that sums all four. `cost_by_model_since` counts the same four categories, so the payload's own cross-check — summing `by_model[].tokens` — reconciles with the totals instead of silently agreeing with a wrong number. Reported by @xaerogonzo.
- **`tokensave cost <RANGE> --export json` now reports a `tokens_saved` that belongs to the range it was asked about, and agrees with `tokensave gain` (#473).** The field was byte-identical for every range — the savings figure for `today` equalled the one for `all` — because the caller sourced it from `global_tokens_saved()`, a lifetime, all-projects counter read from the `projects` table, and dropped it into a payload where everything around it was range-scoped. `efficiency_ratio` is derived from it and inherited the fault in a way that looked like a measurement rather than an artefact: a fixed numerator divided by a growing denominator falls monotonically as the range widens, so the ratio appeared to say something about longer horizons while describing nothing that changed. The same counter also disagreed with `gain`, which reads the `savings_ledger` and scopes correctly, by roughly 2.15× on the reporter's machine — two different "tokens saved" numbers for what a reader takes to be one quantity at one scope. `cost_summary` now derives the figure itself from the `since` the rest of the summary already uses, reading the same ledger `gain` reads, rather than accepting it as an argument: the parameter is gone, so a caller can no longer pass a value scoped differently from the summary it lands in, and the two commands cannot drift apart by source. The human table's `Savings X tokens (N% efficiency)` line is fixed by the same change. This matters beyond the JSON because `cost` is where a consumer naturally looks for a savings figure, and nothing in the payload distinguished the two scopes — a UI reading `tokens_saved` from a `cost 7d` response reported a lifetime total under a "past 7 days" heading with no way to know it had. Reported by @xaerogonzo.
- **The grep guardrail no longer denies a command because a search happens to come first (#475).** `extract_grep_invocation` only required the line to *start* with `grep`/`rg`/`ag` once the known wrappers were peeled, and then read everything after the tool name as that grep's own arguments — so a trailing `&& git status` or `&& ./deploy.sh` was invisible to it. `grep -n MySymbol src/lib.rs && ./deploy.sh` was denied as a bare code search, and because a denial returns the *whole* command unrun, the deploy was discarded along with the search. That is the expensive direction of the same blind spot #451 reports in its silent direction, and it inverts what the guardrail is for: it exists to suggest a cheaper way to answer a question, so it must never destroy work it cannot see. A search chained to anything else — `&&`, `||`, `;`, `|`, or a backgrounding `&`, tracked outside quotes so an operator inside a pattern or a quoted argument still reads as one search — now passes through untouched, and the same rule governs the `find`/`fd` discovery path, which had the identical shape. The unchained search is still redirected, and a leading `cd … &&` is still peeled as prefix noise rather than read as a chain. Reported by @dangeReis.
- **The `PreToolUse` grep guardrail no longer blocks paths the project's own config excludes from indexing (#448).** #435 fixed the out-of-tree half of this: a grep whose target resolved outside the project root is not tokensave's concern. The in-tree half stayed broken, because "inside the tree" is not "inside the index" — a project's `.tokensave/config.json` `exclude` globs keep `node_modules`, `vendor`, `build`, `target` and the rest out of the graph entirely, and the guardrail only ever asked about containment in the root. So `grep -n someExportedName node_modules/some-package/lib/index.d.ts` was blocked and redirected to `tokensave_search`, which returns nothing for it by construction: the caller was sent from a command that works to a tool that cannot answer, with `TOKENSAVE_DISABLE_GREP_HOOK=1` as the only way through. Every grep against vendored or generated code inside the tree paid that round-trip. The hook now resolves the target against the same `exclude` globs the indexer uses, read from the project's own config rather than a second list kept beside it, so the two cannot drift; both glob spellings are checked (`vendor/**` and `**/vendor`), since a grep target is as likely to name a directory as a file, and relative and absolute spellings are canonicalized alike. A config that cannot be read leaves the previous rules in charge, the same fail-open policy the containment check already applies to a path it cannot resolve — and with nothing excluded, behavior is unchanged. Reported by @not-antoni.
- **Every registered MCP tool now has an explicit tracked-branch drift classification.** A registry invariant partitions tools into selector-capable, selector-less refused, and explicitly exempt sets, so adding a tool cannot silently bypass drift handling. Runtime refusal behavior and selector schemas are unchanged.
- **Status now identifies the canonical project root selected by the CLI or MCP server (#437).** `tokensave_status` includes the absolute root as the string field `project_root`, and both normal and short `tokensave status` headers show the same `TokenSave::project_root()` value while preserving the existing branch row.
- **`tokensave_field_sites` reported a capped page as a total, so a blast radius could be silently understated (#457).** `write_count` and `read_count` were the length of the returned array *after* the `limit` cap, so they tracked the limit exactly — the reporter's back-to-back calls on the same field returned `write_count: 20` at `limit: 20` and `21` at `limit: 21` — and nothing in the response distinguished "there are 21" from "here are the first 21 of more". This is the tool whose entire purpose is "if I change this field, how many places are affected?", and the default `limit` of 200 made the number look authoritative for anything under it. The scan now counts every matching site and stops only *listing* once the cap is reached: `write_count`/`read_count` are the true totals, the new `write_returned`/`read_returned` are how many are listed, and `truncated` plus a `truncation_note` say when those differ. Also from that report, one source line holding two references to the field yields two entries with identical `file`, `line` and `snippet` — defensible as raw data, but it left the count neither a site count nor a line count, which is how a downstream consumer produced 8 and 4 for the same query, both labelled complete. Sites and distinct lines are now separate named quantities (`write_count` vs `write_lines`), so no caller has to invent its own deduplication. The cost is that a capped query now scans the remaining files instead of breaking early; that is the price of the total being real, and the uncapped default was already scanning them. Reported by @not-antoni.
- **`tokensave_field_sites` accepted a `Type::field` qualifier, ignored it, and returned the broad answer under a narrow heading (#458).** The tool's own description promised that "`Struct::field` form narrows to a specific declaration", and the handler parsed the qualifier and then assigned `qualifier_applied = false` unconditionally — the sites returned were always the bare field name's, across every struct. The `false` flag was honest and machine-readable, but it is easy to miss and the failure runs in the dangerous direction: the caller explicitly asked to narrow, so the result *looks* narrowed because that is what was requested. One downstream tool ended up refusing the query outright on seeing the flag, which made a documented feature unusable. Narrowing a text site to one struct needs the receiver's resolved type, which this source-text pass deliberately does not compute, so what changes here is the honesty rather than the scan: the tool description and the `field` parameter now state that the qualified form is accepted but **not** applied, and a qualified call comes back with a `qualifier_note` saying in words that the sites cover every same-named field and why. Whether to implement real narrowing, or to reject the qualified form outright, is left open on the issue. Reported by @not-antoni.
- **A literal search now says when it could not look inside every tracked file, instead of returning a partial answer as a complete one (#442).** `tokensave_search` with `literal: true` reads bytes rather than symbols, so it needs no parser — but it iterates the `files` table, so it reaches a file only when the index holds a row for it: one an extractor handles, or one whose extension is listed in `artifact_extensions` (#323). A tracked `.html` template or `.css` stylesheet is usually neither, and those are exactly where a flag's user-facing label, a route, or a class name lives, so `count: 8` was returned for a project that held more, with nothing to indicate a whole class of files had never been scanned. The reported consequence is the sharp one: an agent instructed to prefer tokensave over `grep` gets a confidently incomplete answer and no signal to fall back. The response now carries an `unscanned` block giving the number of tracked files the index holds no row for, a per-extension breakdown of them (capped at eight rows, the rest rolled into `more_extensions`), and the remedy — which is a real one rather than a workaround, since adding the extension to `artifact_extensions` and running `sync -f` makes those files literal-searchable, reported with `enclosing: null` where there is no symbol context. That path already worked and was simply undocumented, so `README.md` and `docs/USER-GUIDE.md` now say so, the latter documenting literal mode for the first time; the `sync` skipped-extension hint names the setting too, where it previously said only that no extractor was registered and left the user with nowhere to go. Two deliberate scoping choices. The tally is **not** filtered through `NON_SOURCE_EXTS`: that list exists to keep asset noise out of the skipped-*language* diagnostic and contains `txt`, `xml`, `ini`, `conf` and `csv`, which are precisely the text formats a literal search might be hunting in, so filtering by it would have recreated the same silent gap one level down — every unindexed extension is reported, including `.png`, and the caller decides which are worth adding. And a file the caller excluded is never reported: config `exclude` globs, project query-ignore, and the call's own `path_include`/`path_exclude` and scope prefix all apply to the report as well as to the scan, so it names only files that were asked about and could not be answered for. The enumeration is one `git ls-files` (4 ms over this repository's 564 tracked files, against a scan that reads every indexed file's bytes), shared with `detect_skipped_hidden_dirs` rather than duplicating its subprocess. `None` when git cannot answer, so "cannot tell" never renders as "nothing missing". Reported by @not-antoni.
- **Python: a function called from module scope, or a method passed by reference through `self`, was reported by `tokensave_dead_code`.** Two extractor misses. First, call sites were extracted only from function bodies, so `_KEYMAP = _build_keymap()`, a bare `_setup()`, or a call under `if __name__ == "__main__":`, `try`, `match`, `assert`, or `raise` gave the callee no incoming `calls` edge. Every module- and class-scope statement now runs the same call-site extraction, attributed to the File node or the enclosing class. Second, the value-position scan that turns `invoke(callback=_cb)` into a `uses` edge stopped at every `attribute` node, so `Thread(target=self._flush_loop)` or `schedule(self._tick, 1.0)` produced nothing for the method. A `self.name`/`cls.name` read in a value position now emits a `uses` ref when `name` is a method the enclosing class defines and never binds as an attribute — `self.status = 1` in one of its own methods, or `status = None` in the class body, makes a later `self.status` a field read, so a `status` method is still reported dead; a nested class's `self` is a different receiver and does not count, and an assignment to a `@property`/`.setter` descriptor invokes it rather than shadowing it. Also at definition time: a call in a parameter default (`def f(x=_build())`), in a class header (`class C(_make_base()):`), or in a decorator's arguments (`@_register(_build())`) now gives the callee a `calls` edge, and a bare-name decorator (`@_register`) gives `_register` one; a dotted decorator callee (`@app.route`) is left alone, since its trailing segment would bind to any same-named symbol. The ref carries the class's full qualified name (`a.py::Daemon::_flush_loop`), so it resolves to that class's method and not to a same-named class in another file. `dead_code` already counts `uses` as liveness, so no query change was needed. Pinned by `tests/python_module_scope_refs_test.rs`, which runs the real tool over a tempdir project; every case fails on the previous extractor, and the cross-file case puts the unreferenced class in the file that indexes first so a first-suffix-match resolver would bind to the wrong one.
- **A running MCP server now refuses local graph queries after a tracked-branch checkout, instead of answering from the branch it opened (#446).** A long-lived server binds to one branch's database at startup, and `git checkout` underneath it does not change which database it holds. The write side of that drift has refused since #400/#403 — automatic sync will not write branch B's files into branch A's index — but the read side answered anyway, with a warning prefix, so `search`, `context`, `callers`, `impact` and every analysis tool returned plausible results describing the wrong branch. Worse, deleting the served branch left its database answering with symbols that no longer existed anywhere. Local graph tools now fail closed with an `InvalidRequest` naming the serving branch, the working-tree branch, and the recovery action; `tokensave_status` stays callable so the situation can be diagnosed from inside the drifted session. **This changes a default**: drift was advisory unless `strict_tree` was set, and is now refused for everyone. The reasoning is that the two halves of one condition disagreed — a refusal to write and a warning-prefixed answer to read — and a warning is not a control an agent instructed to prefer tokensave over reading files can act on. `strict_tree` keeps its remaining job, the separate different-*worktree* mismatch, which is still advisory by default. Single-database projects are unaffected, since one database is shared by design, and an explicit `graph_root`/`graph_branch` selection is a read-only snapshot that never reaches the gate. Reported and fixed by @rNoz.
- **The `PreToolUse` grep guardrail no longer blocks searches that target paths outside the indexed project (#435).** The guardrail used to ask only "does the target look like code?" and redirect any symbol-shaped grep whose file extension or directory looked indexed, even when the resolved path was in `/tmp`, a sibling repo, or a vendored reference tree. It now asks "is the target inside the project *and* does it look like code?" by canonicalizing concrete paths against the project root. A leading `cd <dir> && grep ...` is re-rooted at the cd'd directory, so `cd /other/repo && grep -rn Symbol .` passes through, and `find`/`fd` commands get the same re-rooting. Backslash-escaped cd paths are resolved to the real directory, and a symlink to a code directory is classified by its canonical target. The fail-open policy for paths that cannot be resolved is shared by a single `Containment` helper, so unresolvable targets fall through to the existing extension/directory rules rather than being treated as outside. Only a single leading `cd` is modeled; if a command contains multiple leading `cd` prefixes, only the first one is used and the rest are treated as noise. Reported by @jasonb194.
- **`graph_root`'s union type is now spelled `anyOf`, because a `type` array made every Gemini-backed request fail wholesale (#440).** The parameter accepts either a single root or an array of them (#376), and that was declared as `"type": ["string", "array"]` with an `items` schema. JSON Schema permits the union; Gemini's `function_declarations` proto does not — when `items` is present it validates `type` as exactly `Type.ARRAY`, and a union cannot be expressed as a single `Type` enum. Gemini validates the whole `GenerateContentRequest` up front, so the 52 tokensave tools carrying the selector rejected all 354 tool declarations in the reporter's session with an HTTP 400 naming `properties[graph_root].items`: not a degraded tokensave, but no tool calls at all, for any server in the session. `{"anyOf": [{"type": "string"}, {"type": "array", "items": {"type": "string"}}]}` states the same constraint and passes, which @sruckh verified A/B against the same model and endpoint with one tool and everything else held identical. Nothing about what callers may pass changes, and the guard test is now two assertions rather than one — the `anyOf` shape exactly, plus the absence of any `type` key on the property, so a future edit cannot regress to the union that broke this. Reported by @sruckh, who diagnosed it to the proto predicate and supplied the fix.
- **A C++ header was parsed with the C grammar, and a macro-decorated declaration was not parsed at all, so a `.h` yielded no class, method or field node.** `.h` is the one extension naming no single language, and it was routed by extension alone; on top of that, `class MYLIB_API AFoo` reparses as a function definition, so even the C++ grammar would have seen no class. UE, Chromium, Qt and LLVM headers all spell both shapes, so the two faults compounded on exactly the trees where the headers carry the API. A `.h` is now routed by its source dialect — C++ markers, Objective-C markers, or plain C — and declaration macros are blanked space-for-byte before parsing, which keeps every offset, line and column addressing the real file, so stored coordinates, fingerprints and blame all still land where they did. A body or a brace initializer is deliberately left alone: a `MACRO(...)` there is a call, and blanking it would delete call edges. Two shapes are handled explicitly because each one otherwise loses code with no error: a caps-named *type* is not a macro, so `MYLIB_API const int x` is blanked while `HANDLE const h` is not — the test is whether a type is still named past the qualifier, and getting this wrong deletes the declaration outright; and raw string literals and digit separators are lexed, since `R"(a")"` or `1'000` would otherwise desync the scan and leave every macro after them in the file unblanked. Which braces hold declarations rather than code is decided by the keyword that opened them - `class`, `struct`, `union`, `enum`, `namespace`, `extern` - and not by the punctuation before the `{`, because a body opened past a trailing specifier (`void TearDown() override {`, `int Size() const {`, `auto Get() -> int {`) is code and the calls in it must survive; reading it as a declaration scope blanked the `EXPECT_EQ(1, n1_)` inside it. For the same reason a member initializer (`: MD(md)`) and a qualified name are not access labels, and a macro that IS the declaration keeps everything it owns - a body (`TEST(S, C) {}`), a second parameter list (COM's `STDMETHOD(Read)(args) override {}`) or a lambda argument (`INIT_FUNCTION(Name, [] { Reset(); })`). **A template layer's implementation headers are now indexed at all.** `supported_extensions()` is built from the extractors, and `CppExtractor` claimed neither `.inl`, `.ipp` nor `.tcc`, so those files were never walked - while `hooks.rs` already listed `.ipp` and `.tcc` among the extensions tokensave indexes, and `is_header_path` already treated all three as headers. They are C++ by construction, so they route by extension with no dialect sniff, and they map to `cpp` in `lang_from_path` and `ts_lang_key_from_path` too: #346 measured an unmapped extension as exempt from both cross-language guards, scoring it above the correct same-language candidate. Measured on two template-heavy trees at their current heads: asio gains 1,145 nodes over 75 files with edges 10,805 -> 13,002, and glm 2,989 nodes over 140 files with edges 1,560 -> 3,242. Routing headers to the C++ extractor also exposed five class-body members it had been dropping, each now extracted: preprocessor-guarded members (both arms, as file scope already did), a `friend` definition, a function-pointer member (previously an anonymous method), a nested type, and a `using` alias. Resolution scores C and C++ as one language family — a `.cpp` calling into its own `.h` scored 0.5, under the 0.6 floor, and lost the edge — and prefers the definition over the header declaration once both exist. Measured over a 230-file UE-style C++ codebase, 112 of them headers: header symbols 886 → 2604 (0 → 76 classes, 0 → 760 methods, 296 → 1105 fields, 0 → 33 enums), nodes 3663 → 5381, edges 1903 → 2032, and signatures carrying a newline, tab or run of spaces 356 → 0. Every name the old index carried and the new one does not is a type name the C grammar had recorded as a symbol — `UTextBlock`, `int32`, `GENERATED_BODY` — so nothing real is traded for the gain. **A `.h` carrying no C++ and no Objective-C marker still routes to C**, which is right for a C header and is the one case where the dialect is genuinely unknowable from the text. Cross-checked over five public C++ trees at their current heads (fmt, nlohmann/json, googletest, asio, wil), 2,059 C-family files: blanking preserves every file's length, line count and character boundaries, is idempotent, and every span it rewrites reads back as a caps-named macro with at most its own argument list - no span holds a `;` or a `{`. Indexing all five before and after these three rules gains 85 nodes and 67 edges and loses none; the only kind that moves down is a COM `STDMETHOD` member, now read as a method rather than a free function (442 -> 480 methods against 1067 -> 1060 functions in one tree).
- **A background config refresh no longer announces itself as a bare `✔ Wrote <path>` from under a read-only command (#419).** The trigger is now identified, and it is the one @xaerogonzo guessed. `Gitignore` is not in `should_skip_agent_install_maintenance`, so a documented query runs the startup maintenance block, where `resync_installed_agents` re-installs every tracked agent when the running version is newer than `last_installed_version` — the fallback for an external upgrade, exactly the in-place 7.9.0 → 7.10.0 he described. That resync sets `set_quiet_install(true)` to keep per-agent banners off stderr, but `write_json_file` and `write_toml_file` printed `✔ Wrote {path}` through a raw `eprintln!` rather than the quiet-gated `agent_note!`, so the one line that named a file escaped while all its context was suppressed. It fired exactly once because the resync advances `last_installed_version`, which is why it never reproduced. The two write lines now honour quiet mode like every other install message, and the resync prints one line saying what it did and why: `✔ Refreshed agent config for tokensave 7.10.0 (was 7.9.0) — 1 agent`. A user-run `tokensave install` is unaffected, since quiet mode is off there and the per-file lines still print. Also worth recording for anyone who goes looking: `last_installed_version` and `previous_version` live in `~/.tokensave/state.toml`, not `config.toml`, which is why the version stamp he searched for was not where he looked.
- **`gini --metric lines --scope file` reported a file's length as the sum of every node's span inside it, so `src/types.rs` came back as 2,104 lines against an actual 849 (#422).** The graph holds a node per struct, field, enum variant, impl block and method, and they nest, so one physical line was counted once for every node enclosing it — and the `file` node's own full span was one of the terms, so each file's length was added on top of the symbols within it. The arm now reports the `file` node's own span, which matches `wc -l`, with `max(end_line)` as a fallback for a file that has no `file` node (no language indexed today produces one). This was not a harmless constant: across the 452 files here the inflation ran from 1.00x to 3.45x, median 1.96x, so it did not cancel out of a scale-invariant coefficient, and it reordered the ranking the metric exists to produce. The top two files swapped places and only 6 of the 10 files were common to both lists; `src/mcp/tools/definitions.rs` and `src/mcp/tools/handlers/analysis.rs` are genuinely among the ten longest and were missing from the reported one. The three longest files now report 6,528, 4,988 and 3,504 lines, each equal to `wc -l`. Reported by @vianbas, who measured the distortion and deliberately left the choice of definition open; the file's own length is the reading that matches the tool's stated purpose of finding god files.
- **`tokensave_health`'s `equality` dimension counted every file's length twice over (#422).** `compute_health_snapshot` summed each node's line span into per-file complexity, the `file` node included, so a file scored its own length on top of the spans of its symbols — and a file with no extracted symbols at all scored purely its length, ranking a comment-only shell script alongside dense code. File nodes carry no branches, loops or nesting, so they contributed nothing but the duplicated span and are now skipped, while every file keeps an entry so a symbol-free one still ranks, at 0. On this repository `equality` moves from 0.4006 to 0.3621, reproduced to four places by independent arithmetic over the same index. A regression test pins the invariant that a symbol-free file's length cannot move the score: before this change, growing a comment-only file from 20 lines to 400 moved `equality` from 0.9737 to 0.5431.
- **File-scope `fan_in` and `fan_out` dropped every edge crossing a `--path` boundary (#423, #422).** The arms mapped edge endpoints to files using only the path-filtered nodes, so an edge whose other end lived outside the prefix resolved to nothing and was discarded — a scoped file's fan-in counted only its intra-prefix callers, and the whole-graph `get_all_edges()` load above it was wasted. The comment there states the intent plainly: fan-in of a scoped node counts edges from outside it. The endpoint map now spans the graph while only files inside the prefix are ranked, matching the symbol-scope arms added for #417. Under `--path src/mcp`, `src/mcp/tools/handlers/mod.rs` now reports 490 incoming edges against 271 before, and 490 is what the same count taken directly in SQL over the whole graph gives.
- **A config write that would not change a byte no longer rewrites the file, backs it up, or announces itself (#419).** `tokensave gitignore` with no ACTION is documented as a query — "omit to show current setting" — and it printed `✔ Wrote ~/.claude/settings.json`, advanced that file's mtime and left a fresh `settings.json.bak` beside it, with content byte-identical to what was already there. Nothing was lost, but `settings.json` holds the agent's permissions, hooks and MCP wiring: rewriting it races anything else with the file open, and an interruption mid-write turns a no-op into a truncated settings file. A user running a query has quiesced nothing, because they had no reason to expect a write. `write_json_file` and `backup_and_write_json` now compare the rendered bytes against what is on disk and return early when they match — no backup, no rename, no message — and `safe_write_json_file` carries the same guard for the call sites that back up themselves and call straight through. Comparison is on rendered bytes rather than parsed JSON deliberately: writing normalises key order, indentation and the trailing newline, so a formatting-only rewrite is still a real change to a file the user may track in git, and only an exact match is treated as a no-op. This covers every agent integration, since all of them write through these three functions. The condition that made a query reach a settings write at all was not reproducible from the outside and is still open on #419; what is fixed is that reaching it with unchanged content now costs nothing. Reported by @xaerogonzo, who also proposed this fix.
- **`tokensave_gini` answered three of its ten metric and scope pairs with complexity, labelled as the metric asked for (#417).** At `scope: "symbol"` only `members` had a match arm of its own, so `lines`, `fan_in` and `fan_out` fell through to the catch-all `(_, "symbol")` arm, which computes per-symbol complexity — the response carried the requested metric name and complexity's numbers, with nothing to say the metric had not been measured. Measured on this repository at `0651c0a`: all four of `complexity`, `lines`, `fan_in` and `fan_out` returned a coefficient of 0.5768 over the same 7,673 symbols; they now return 0.5768, 0.4994, 0.6718 and 0.5681. The two new arms follow the file-scope arms directly above them — `lines` is the node's line span, fan-in and fan-out count every edge kind, and an edge whose endpoints are the same symbol is not counted, so a recursive call is not a dependant of itself. The top five symbols of each metric agree exactly with the same counts taken directly in SQL against the index. `complexity` and `members` are unchanged, and the catch-all stays for a metric outside the declared enum. These arms differ from the file-scope arms under a `path` filter, and the file-scope arms are the ones that are wrong: they build their node-to-file map from the already path-filtered nodes, so an edge with an endpoint outside the prefix is dropped, which also makes the whole-graph edge load directly above them pointless when a prefix is set. The symbol arms follow the stated contract instead — a scoped symbol's fan-in counts its callers wherever they live — so under `--path src/mcp`, `handle_tool_call` has 221 incoming edges rather than the 3 the file arms would count. Fixing the file arms is left to a follow-up, alongside #422.
- **A method declared without a body in an `abstract class` never became a node, so calls into it lost their edge (#424).** `abstract render(): string;` parses as `abstract_method_signature`, and `visit_class_body` matched only `decorator`, `method_definition` and `public_field_definition`, so the declaration fell through the catch-all arm and nothing at all was recorded for it. The identical bodiless shape inside an `interface` is extracted, by `visit_interface_body`, which is why the inconsistency did not show from the outside. `AbstractMethod` was already a node kind, already accepted by the resolver's `Calls` gate, and already emitted by the Java and Kotlin extractors for the same construct; the TypeScript extractor was the one that never produced it. The consequence appears at the call site as soon as the base has more than one subclass: with no declaration to resolve to, the resolver sees only the implementations, cannot separate them, and refuses the tie, so the call produces no edge and `tokensave_callees` on the caller returns nothing. With exactly one subclass the call still lands on that implementation by name, which is why the shape hid behind the #413 fixtures, where every method has a body. Measured across three TypeScript corpora at their current heads, counting `MethodDeclaration` members with no body on classes carrying the `abstract` modifier, tests and `.d.ts` and `declare` contexts excluded: nestjs/nest has 51 across 10 abstract classes, typeorm/typeorm 9 across 6, moeru-ai/airi 2 in 1, and the index held zero `abstract_method` nodes from `.ts` in all three — the only ones present anywhere in those trees come from a Kotlin file. The declaration now becomes an `AbstractMethod` node carrying its accessibility, JSDoc and `Contains` edge, and a call through a base-typed receiver resolves to it rather than to either implementation. One consequence is a change in existing output rather than a pure addition: with a *single* subclass the call already resolved, to that implementation, and now resolves to the declaration instead — the honest answer at a base-typed call site, and the same way an interface-typed receiver already behaves, but `tokensave_callees` on such a caller returns a different symbol than it did in 7.10.0. Nothing links a declaration to its implementations, since the only edge between the classes is `extends` at class level, so a `callees` walk now stops at the declaration; adding an `overrides` edge to make that navigable in both directions is left open. This does not produce `dead_code` false positives for the implementations — verified against a fixture also holding a deliberately dead exported function, where the count stayed 0. Reported and fixed by @vianbas.

## [7.10.0] - 2026-08-19

### Changed
- **The last order-dependent pick in the resolver is gone (#412).** `resolve_from_filtered_named` — the path taken whenever the kind filter shrinks the candidate list — still chose "the first candidate in the reference's own file, else the first overall", with a comment saying so, because scoring wanted a shape it did not have. It did not: `find_best_matches` already accepts the borrowed slice, so it now scores like every other multi-candidate path, and a tie produces no edge and is recorded in `ambiguous_calls` rather than resolved by scan order. Measured by indexing a clean clone of this repository before and after: call edges 19,356 → 19,213 (-143, 0.7%), ambiguity records 10,985 → 11,148 (+163). Small because `e8a8a8a` had already removed the large tie population from the primary paths; what remains is the guarantee — no resolution in the graph now depends on file enumeration order. The guarantee is now pinned by `tests/resolution_order_independence_test.rs`, which indexes the same source twice with the candidate definitions swapped between files and compares the resulting edges. Unlike the earlier fixture in `ts_phantom_call_edges_test.rs`, which passed before the fix as well as after, the kind-filtered case there genuinely reproduces: against the previous resolver it resolves to `Handler::process` one way round and `Worker::process` the other.

- **Score ties no longer resolve to whichever candidate the file scan reached first (#412).** `find_best_match` returned one winner using `if score > best_score`, so when several candidates tied on every scoring dimension the first one reached won — and "first" is file enumeration order, which made the graph a function of the filesystem rather than of the source. It is now `find_best_matches`, returning every candidate tied for the best score, and all three call sites apply one rule: a lone winner resolves, a tie produces no edge and is recorded in `ambiguous_calls` with its candidates. The special-case `find_best_match_unambiguous` added for the bare-name fallback is gone — the honest behaviour is now the default rather than one path's opt-in. **The cost is real and language-dependent**, measured by indexing two corpora before and after: on this repository (Rust) call edges fall 22,502 → 19,309, a 14.2% loss, with ambiguity records rising 7,653 → 10,930; on a 1,332-file TypeScript corpus they fall 40,266 → 39,888, 0.9%, with records rising 2,633 → 3,011. Nothing is discarded — the edges reappear as candidate lists almost one for one. Much of the Rust figure is fabrication being withdrawn rather than fact being lost: the largest group is 1,637 `is_empty` calls, and the codebase defines exactly two local `is_empty` methods, so calls to `Vec::is_empty` were being bound to one of them arbitrarily — the phantom-edge class #378 reported, at scale. `Vec::new`, `fs::write`, `serde_json::from_str` and `TempDir::new` behave the same way. **The split has since been measured** by diffing the two indexes edge by edge: of the 3,971 `calls` edges the Rust index loses, **2,845 (71.6%) pointed from one file at a `private` item in a different file** — a call Rust cannot express, so those edges were provably fabricated. The remainder are `pub` targets, and sampling them finds the same class rather than a different one: every one of twelve inspected is a standard-library method call (`as_str` on a `String`, 680 edges) that had been bound to an unrelated private helper — in that case `as_str` in `src/accounting/discover.rs`, reached from test files that do not import it. Only 4 of the 3,971 were same-file. The 14.2% is therefore dominated by withdrawn fabrication, not by lost fact, though the `pub` remainder is characterised by sampling rather than exhaustively.

### Fixed
- **`abstract class` declarations were never extracted, taking the class, its methods, and every edge into them (#413).** An abstract class parses as `abstract_class_declaration`, a different node kind from `class_declaration`, and the extractor dispatched only on the latter — in both the top-level and `export` paths. Nothing wrong was recorded; the symbols simply never reached the graph, so every question about them answered "no" with the same confidence as a real negative. In TypeScript an abstract base is usually where the shared interface lives, so this removed exactly the methods most likely to be called, and left every concrete subclass's `extends` pointing at nothing. On the reporter's fixture the index held 2 of the expected 6 class and method nodes. Reported by @vianbas.
- **A constructor parameter property declared no field type, so calls through it did not resolve (#414).** `constructor(private readonly worker: Beta) {}` declares a field, but the declaration sits in the parameter list rather than the class body, so the field scan added in `3b294bc` never saw it — leaving the standard dependency-injection form, and the one most likely to appear in the code #378 was reported against, still falling through to the bare-name fallback. The accessibility or `readonly` modifier is what distinguishes a property from an ordinary constructor argument, so an unmodified parameter stays local to the constructor rather than becoming a field. Reported by @vianbas.

### Added
- **Ruby `attr_reader`/`attr_writer`/`attr_accessor` now emit method nodes.** A receiverless `attr_*` call in a class or module body is indexed the same way an equivalent `def` would be — same kind, visibility, parent, and `Contains` edge — including inside `class << self` and Concern `included`/`class_methods` blocks. Previously these accessors were invisible to search, callers, and dead-code analysis.

### Added
- **Ruby `alias`/`alias_method` now emit method nodes.** A receiverless `alias`/`alias_method` in a class, module, or `class << self` body is indexed the same way an equivalent `def` would be, with visibility copied from the source method. Previously these methods were invisible to search, `callers`, and dead-code analysis, and calls to them were permanently unresolvable.

### Added
- **`tokensave_ambiguous_calls`: the candidates behind a call the resolver could not pin down (#412).** #378 stopped fabricating an edge when several targets tied, which was right — an edge asserts a specific target and "one of these" is not one — but it discarded what the resolver knew. The candidates are now recorded (schema v17, `ambiguous_calls`) and readable, so the ambiguity becomes data rather than silence. A model has the source in front of it and can usually tell which `dispose` was meant from the receiver's type, which a scoring heuristic cannot see; it can only do that if it is told there was a choice. Each call site is returned with its unresolved reference and every candidate's name, kind, file, line and qualified name, scoped by `path` and capped (default 25, max 200 — a hard cap, not a page, since a large codebase can hold thousands). **`dead_code` now excludes symbols named as an ambiguity candidate**: having refused the edge, reporting the target as uncalled would trade a fabricated edge for a fabricated finding, and a finding reads as more actionable than an edge — the same mistake #346 measured at a 97% false-positive rate, in the other direction. No edges are created, so the refusal's guarantee is intact. Ambiguity is computed only for references that already failed to resolve, so the cost falls on a minority of refs rather than threading state through the parallel resolution pass.

### Fixed
- **A call through a typed field now resolves to that field's class instead of being ambiguous (#412, option C).** `collect_var_types` tracked bare `this`, typed parameters and typed bindings, but never `this.<field>` — so `this.statusBar.dispose()` reached the bare-name fallback with nothing to separate two classes that both define `dispose`, and #378 had to refuse the edge rather than guess. The field's type is in the source either way: an annotation (`private worker: Beta`, the constructor-injection shape) or an initializer (`private statusBar = new StatusBar()`), with the annotation winning when both are present. The reference becomes `StatusBar::dispose` and binds precisely, recovering an edge the refusal had to give up rather than trading recall for precision. Receivers whose type genuinely cannot be known — an untyped parameter, say — still produce no edge when several candidates tie. No measurable effect on this repository, which is Rust and never reaches the TypeScript path; validated by targeted tests on the reported shape rather than by a corpus measurement.

### Added
- **`graph_root` accepts an array, so one query can be answered across several projects (#376).** `tokensave_search` and `tokensave_files` fan out across every root named, run the ordinary selected-graph pipeline per root, and interleave the results round-robin by rank with a 25-entry per-root cap. Rank rather than score, because BM25 scores are per-database and not calibrated between them — sorting two roots' scores together compares numbers that do not share a scale. Every other tool answers about a single graph and now rejects an array with an explanation, since a union of two graphs is not a larger answer but a meaningless one. **Roots that are worktrees of a repository already named are collapsed first**, using the shared `git rev-parse --git-common-dir` that @bobbypierce42 suggested: he reported a machine with 100+ tracked projects where a dozen roots are worktrees of one repo, and round-robin across those would fill the result set with the same symbol at slightly different line numbers, with a per-root cap powerless because each worktree *is* a root and sits under it. The first checkout named wins, and the response reports which roots were collapsed rather than dropping them silently. A root that fails to open is reported inline and the other roots still answer, since one unreadable index should not cost a caller the rest. `tokensave_context` is deliberately **not** federated despite being named in the issue: it returns formatted prose rather than a ranked array, so interleaving has no meaning for it and concatenating per root is a different feature.

### Changed
- **The last handler that loaded every node to build a lookup map now projects it in SQL (#411).** `test_reached_node_ids`, behind `tokensave_complexity`'s `crap` score, used `get_all_nodes()` solely to build a `node_id → file_path` map — the same map `test_risk` needed. It reuses `get_node_paths()`. Every node really is required, since a `Calls` edge can originate anywhere and the question is whether its *source* sits in a test file, but the other twenty-six columns never were. With this, no MCP handler calls `get_all_nodes()`. Output verified byte-identical before and after against this repository's own index.

### Fixed
- **A `recv.method()` call with two equally plausible targets no longer picks one at random (#378).** `resolve_one`'s Strategy 1/1b fall back to matching the trailing segment against *any* node with that bare name when the qualified or typed match fails. Go gates this on the qualifier being a known import (`try_go_selector_match`); TS/JS never got the equivalent, so a call on a receiver whose type is external, unindexed, or a JS builtin bound to whatever local method shared the name. The reporter's follow-up found it cuts both ways: with two candidates tied on every scoring dimension, the tie-break was `if score > best_score` — first-seen wins — so `this.statusBar.dispose()` could be attributed to an unrelated `ProviderManager.dispose`, simultaneously fabricating a caller for one and hiding the real caller of the other, which is how `dead_code` reported live code as dead. The bare-name fallback now returns nothing when the top candidates are tied, rather than guessing: a missing edge degrades an answer, a fabricated one corrupts it, and #346 showed fabricated edges going on to invent cross-language cycles. Measured on this repository the refusal removes 12 call edges and adds none. **The tie-break itself is deliberately unchanged**, and this is the finding worth recording: ties decide roughly 2,834 call edges here, 12.7% of the total, so ordering them deterministically re-points that many in one release — too large to fold into a bug fix, and tracked separately. Diagnosis by @vianbas, including the correction that the ordering effect only reproduces under one file-enumeration order.

### Changed
- **`test_risk` no longer materialises every node to build a lookup map (#411).** It needs a graph-wide `node_id → file_path` map — it walks every edge, and a test in `tests/` calling a function in `src/` is precisely what the tool looks for, so the map cannot be scoped — but it never needed the other twenty-six columns of each row. A new `get_node_paths()` returns the mapping straight from SQL. Its three other passes over the node list concern executable nodes only, so those kinds are now selected in the query rather than filtered out of the whole table three times: on this repository that is 7,556 rows instead of 17,827. Output is byte-identical before and after on this repository's own index, checked rather than assumed.

### Fixed
- **The `auto_track` knob did not reach the `post-checkout` hook, so branches were tracked on every checkout regardless of the setting (#397).** `auto_track` was consulted in exactly one place, `TokenSave::open`. The hook shells out to `tokensave branch add`, and that command never looked at the config, so the knob governed one entry point and not the other — on *fresh* installs as much as on hooks written by an older version. The comment at the gate said "(git-hook path is separate)", so the split was intentional, but it makes #342 Q1's goal of a single runtime knob governing auto-track everywhere unreachable. `branch add` now takes `--if-enabled`, which exits successfully without doing anything when `auto_track` is off (honouring the `TOKENSAVE_AUTO_TRACK` override, the convention `TokenSave::open` already follows), and both the generated hook snippet and `scripts/post-checkout` pass it. The flag is opt-in rather than the default because an explicit `tokensave branch add` typed by a person is a request: silently doing nothing because of a config value they may not know about would be worse than the bug being fixed. The check runs before anything is read or written, so a declined auto-track costs nothing on a checkout. **This reaches new installs only** — the installer skips a `post-checkout` hook that already carries the tokensave marker, so migrating existing hooks remains #342 Q1.

### Changed
- **Six MCP handlers now filter nodes in SQL instead of materialising the whole node table (#410, easy half).** `redundancy`, `module_api`, `unused_imports`, `gini`, `health` and `literal_search` each called `get_all_nodes()` and filtered in Rust, so one tool call on a large project re-triggered a graph-sized allocation — the same peak the sync path has, but on a *read*, recurring per call rather than once per sync. Every one of those filters is a predicate the caller supplied: a path prefix, a set of kinds, a visibility, a minimum line span, a name substring. A new `NodeFilter` pushes them into the query. Measured on this repository (17,594 nodes): `module_api` scoped to `src/` now loads 2,206 rows instead of 17,594, `redundancy` at its default 10-line floor loads 5,018, `unused_imports` loads the 1,611 `use` nodes, a `literal_search` for `config` loads 745, and `health`/`gini` scoped to a path load 9,146. Two escaping details are covered by tests rather than left to review, because both fail silently with wrong results rather than erroring: a path prefix matches only an exact path or one followed by a separator, so `src` never sweeps in `srcfoo`, and `LIKE` metacharacters in a path are escaped, so a directory named `a_b` does not also match `axb`. Each handler's predicate is asserted to return exactly what its in-Rust filter returned. **The two remaining call sites are unchanged** and tracked separately: `test_risk` needs a graph-wide node-id-to-file map because edges point anywhere, and `test_reached_node_ids` is a reachability traversal — neither is a predicate, so neither belongs in this change.

### Added
- **`strict_tree`: refuse `tokensave_*` calls instead of answering from the wrong working tree (#372 §2).** tokensave already detects two ways its index can describe a tree you are not in — a borrowed worktree index (#312) and a branch that drifted under a running server (#400) — but both were advisory: a warning prefixed to an answer the tools produced anyway. The reporter's case for making that refusable is that every tool built on top of tokensave inherits the wrong-tree result with no signal, and an empty result reads as "no such symbol" rather than "wrong tree", so an agent rule saying "always check tokensave before reading files" is silently misled. Setting `strict_tree: true` in `.tokensave/config.json` turns both detections into errors naming the two trees (or two branches) and the remedy. Nothing new is detected; only what a detection *does* changes. It is checked before freshness work and before handler dispatch, so a refused call does no work, and explicitly selected graphs (`graph_root`) are exempt — naming another project's snapshot is a deliberate request for a tree that is not yours. **`tokensave_status` is the one exemption**, since it reports the served root and branch, which is exactly what a refused caller needs; three tools whose names suggest they belong beside it do not, and the reasoning is recorded in the code: `tokensave_config` queries arbitrary TOML/JSON files by key path and says nothing about tokensave's state, while `tokensave_diagnose` and `tokensave_diagnostics` map compiler output onto *graph nodes* and would therefore attribute a real error to a node from another tree — the exact failure strict mode exists to prevent. Off by default, deliberately: sharing one index across a family of worktrees is a legitimate setup, and hard-erroring it uninvited would be a bad surprise.

### Fixed
- **Calls inside ExUnit test bodies never reached the call graph, so `affected` and `test_risk` reported Elixir projects as untested (#387).** `test "..." do ... end`, `describe`, `setup` and `setup_all` are macro invocations rather than language constructs, so tree-sitter sees ordinary `call` nodes carrying a `do_block`. `extract_calls` is only ever reached with a `def`'s node id, so every call in every ExUnit test was attributed to nothing and became no edge. The symptom was not abstract: on an untouched `mix new` project, `tokensave tool affected` returned `affected_tests: []` for a directly tested source file, and `test_risk` reported `has_test: false` with `coverage_pct: 0.0` for a function whose passing test sat in the next file — confidently wrong in the direction that makes someone write a duplicate test, or skip running the test that covers their change. Calls in those blocks are now attributed to the enclosing `defmodule`, matching how #346 attributed calls inside a TypeScript arrow passed as an argument to its enclosing binding; a `describe` block covers the `test` blocks nested in it without double-recording. Verified on the reporter's exact reproduction: `affected_tests` now names the test file, and `test_risk` reports `has_test: true` with `coverage_pct: 100.0`. **`doctest` is deliberately not modelled** — it generates tests from `@doc` examples at compile time, so there is no call expression in the source to attribute, and linking `doctest Foo` to every function in `Foo` would fabricate coverage; a doctest-only function is still reported untested, which is wrong but honestly wrong rather than silently overstated. Reported by @graphitedesk, whose A/B isolation (adding a plain `def` to the same test module and watching `affected` go from 0 to 1) identified the cause precisely.

### Documentation
- **`docs/BRANCHING-USER-GUIDE.md` described a watcher that was removed in 6.1.1, and three of its behavioural claims were wrong (#395).** The "How the watcher handles branches" section was written around the embedded `ProjectWatcher` (`f7f7c9b`, #80), and renaming the actor would not have saved it: it promised that `last_synced_at` advances after each sync, that the sync path re-checks the current branch, and that "you don't need to restart the MCP server after adding a branch". The first two are now true, having been fixed as #399 and #400; the third never was and is now documented as the opposite, since the database a server writes to is resolved once at startup and a `git checkout` underneath it does not change that. The section is rewritten around the on-demand model that actually runs, states plainly that a restart *is* required after a checkout, and shows the drift warning a server now emits. Four further watcher references elsewhere in the file, the non-existent `tokensave mcp` command, and a fallback warning quoted with a `WARNING:` prefix it does not carry are corrected in the same pass. Reported by @vianbas, who verified every claim against both a local build and the published Linux binary.

### Fixed
- **A server kept writing to the branch it started on after a `git checkout`, indexing one branch's files into another's database (#400).** `TokenSave::open` resolves the active branch once and the MCP server holds that handle for its whole life, so a checkout underneath it was invisible: the server carried on serving *and writing to* the original branch, still reporting `active_branch: main`, with no warning on the tool response or on stderr — zero warnings over a 176-second poll across five cooldown windows in the reporter's run. The observable result was `main`'s index containing a file committed only on `feature`, so the index did not merely go stale, it described a tree that had never existed, and every downstream agent rule that treats tokensave as the source of truth inherited that silently. Automatic syncs now detect the drift and refuse rather than write across branches, and each tool response carries a warning naming the served branch and the working tree's branch. Restarting the server remains the way to serve the new branch; re-resolving the handle in place is a larger change and is not attempted here. Single-DB projects are deliberately unaffected: `init` writes branch metadata for the default branch alone, so the guard fires only when the working tree's branch has a database of its own — either tracked, or the default — mirroring the ownership rule in `branch::track_branch_copy`.
- **`hook-pre-tool-use` covered `Glob` in the handler but not in the installed matcher, so the redirect was unreachable (#389).** v7.9.0 extended the hook to path-shaped discovery (#294), and the classifier side shipped: the binary can tell a `Glob` call from a `Grep` call by the fields only `Grep` sends. What did not ship is `Glob` in the `PreToolUse` matcher, the regex of tool names Claude Code uses to decide which calls reach a hook at all — it read `Agent|Grep|Bash`, so a `Glob` call was never routed to tokensave and the branch was dead code. The `find -name` and `fd --extension` halves of #294 were unaffected, since those arrive as `Bash`. The failure was easy to miss because both commands a user would check report success: `doctor` prints `✔ PreToolUse hook installed` after verifying the entry exists, the binary resolves, and the subcommand is right — none of which is the matcher. `expected_hook_matcher` is the single source of truth for both the install path and `doctor`'s drift repair, so widening it to `Agent|Grep|Bash|Glob` also migrates existing installs on their next `install`/`reinstall`, with `doctor` flagging the stale matcher in the meantime. Reported by @xaerogonzo.
- **`last_synced_at` never advanced after a branch was created, while three surfaces rendered it as live freshness (#399).** `BranchMeta::touch_synced` had exactly one caller — the `branch add` arm — so the field recorded when a branch *entry* was created and nothing moved it afterwards, from either the CLI or the MCP server. A real `tokensave sync` that indexed a new file left the value untouched. That matters because `tokensave branch list`, the `tokensave_branch_list` MCP tool, and the `tokensave://branches` resource all present it as staleness (`synced 7d ago`), so a person or an agent asking whether a branch's index was fresh got a confidently wrong answer, and one that grew steadily more wrong the longer the branch lived. The timestamp is now advanced wherever the DB-level `last_sync_at` metadata is written — all three sync-completion points, including the incremental path the MCP server uses most — so the two cannot drift apart. Only the branch actually being served is touched, so a sync on `feature` never claims `master` was synced. The update is best-effort: a sync that indexed correctly does not fail because the metadata file could not be rewritten. Reported by @vianbas.
- **An automatic sync could index a directory from scratch, reaching ~95 GiB of RSS+swap (#396, #393).** The MCP server's startup catch-up sync and its per-`tools/call` staleness check both fed `find_stale_files` straight into a sync with no bound on the result. That is safe only while the index is populated: `find_stale_files` compares the tree against the `files` table, so an index with no files recorded classifies *every* file on disk as stale, and the fallback at `staleness.rs` returns the whole tree outright when the DB read fails. A background task therefore became a full initial index of whatever directory it happened to be pointed at — on a 2.5 TiB home directory that grew to roughly 16.7 GiB RSS plus 78 GiB swap over five hours, saturating zram and hanging `ps` on swapped-out pages; on a project that had never been `init`ed it presented as a `serve` process idling at 98-1370% CPU with no tool call in flight, which reads as a spin loop but was a real extraction running to completion. Automatic syncs now go through `find_stale_files_bounded`, which refuses in two cases: an index with no files recorded (building the first index is `tokensave init`'s job — it is explicit, reports progress, and the user chose the directory), and a stale set larger than `max_auto_sync_files` (default 2000, `0` disables). Both refusals are logged with the reason and the command to run instead, rather than silently serving a stale index. Explicit `tokensave sync` is deliberately unbounded and remains the way to index a large change on purpose; the 30 s cooldown was never a cost bound, only a frequency one, which matters while resolution still materialises the whole node graph at once (#306).

### Added
- **`tokensave_context` output includes a `### Retrieval` section, so a retrieval miss is visible instead of silent.** The section reports the hit count for each searched term, the best post-boost candidate score, and a match tier (`exact`, `strong`, `fts-only`). When query terms match nothing in the index, it lists them (capped at 6, with a `+N more` tail) and tells the agent to re-query with `keywords` synonyms or to switch to `tokensave_search`. When a task yields no searchable terms at all and no entry points, the section still appears and says so. Without this, weak entry points look the same as a sparse graph, and an agent burns follow-up calls on rephrased guesses. It stays the final section of the formatter output, and the context handler's truncation preserves everything from it to the end of the response as a suffix — which also stops a large Code section from silently truncating away `seen_node_ids`, the handle follow-up calls pass back as `exclude_node_ids`. The `exact` tier comes from exact-name and exact-source provenance rather than a score threshold, because multiplicative boosts make the score bands overlap; the score decides only `strong` versus `fts-only`, with the `strong` floor a named constant calibrated to the negated-BM25 band that #314 introduced. The `RetrievalDiagnostics` field on `TaskContext` is additive and `#[serde(default)]`, so stored contexts without it still deserialize.

### Changed
- **Resolution no longer loads the two node columns it never reads (#306, part 1).** `get_all_nodes()` selects every column, and the resulting `Vec<Node>` stays resident for the whole resolution pass, which is the graph-sized peak behind the memory reports. Two of those columns are unbounded TEXT that resolution never touches: an extractor stores a `const`'s entire initializer in `signature` — #362 found one reaching roughly 43 KB — and `docstring` carries full doc comments. `ReferenceResolver` reads only `id`, `kind`, `name`, `qualified_name`, `file_path`, `start_line`, `visibility` and `parent_id`. Both sync paths now use a `get_all_nodes_for_resolution()` that returns `NULL` for those two, keeping column positions identical so the row mapper stays shared. On this repository that omits about 1.25 MB against 2.40 MB of retained node strings across 17.5k nodes — roughly a third of the node string payload — and the saving scales with how large a project's signatures are. **This narrows the peak, it does not bound it:** the resolver borrows from the slice for its lifetime and needs a global name index to resolve cross-file references, so the pass still holds every node at once and the remaining cost stays proportional to node count. Chunking or streaming it needs a resolver redesign, and the analysis handlers that call `get_all_nodes()` independently (health, analysis, redundancy) are untouched. A test asserts the slim and full loads resolve to byte-identical edges, so a future change that starts reading either field fails loudly rather than silently seeing `None`.
- **The Related Symbols section collapses test and fixture files to one line.** A large test file could dominate the section with dozens of `test_*` entries that cost tokens without informing the agent. These files now show as a single count line, labeled `test/fixture files`, with a pointer to `tokensave_callers` for detail. The decision uses the same path classification as ranking (`is_test_path` wraps `path_boost`), so the formatter and the ranker cannot disagree about what a test or fixture file is.
### Fixed
- **The `search_terms` FTS weight is 3.0, not 6.0, now that #359 made the column live.** The column shipped in #316 with a weight described as between `qualified_name` and `name`, but 6.0 is above the 5.0 of `qualified_name`, and no real index ever exercised it: the stale triggers that #358 describes rebuilt `nodes_fts` without the column after every `index_all`. After #359 repaired the triggers, the shape of the column bit. It holds only the inner words of a camelCase name, so the field is one or two tokens, and BM25 length normalization gives a short field at weight 6.0 an outsized score. A PascalCase type that matched one query word could outrank a snake_case function that matched three, because a snake_case name has no inner words and gains nothing from the column. The new weight puts the channel below full-name and qualified-name evidence and above docstring and signature prose, which is where it was always said to sit. On the 12-query retrieval eval from #313 this restores the pre-#359 ranks exactly, and inner-word matching keeps working.
- **`file` and `doc` nodes no longer consume slots in the bounded per-term symbol fetch.** That fetch feeds the symbol candidate pool of the context builder. File and doc nodes are not symbols, and both kinds have their own surfacing channels (the related-files collection, `tokensave_doc`), but they competed for the raw-BM25 slots on common terms. Rerank-time down-weighting cannot repair a pool that was polluted at fetch time. The exclusion matters most on artifact-heavy projects, where #323 now tracks many more non-code rows.
- **A new git worktree, and a clone of a non-default branch, were indexed without their branch being tracked (#391).** The `post-checkout` hook ran `tokensave init` on git's all-zeros previous-HEAD sentinel and `tokensave branch add` in an `elif`, so the two could never both run. But `git worktree add -b feature` and `git clone -b feature` raise that sentinel *and* set git's branch-checkout flag (`$3 == 1`), and they land on a branch that is not the default one: the new index recorded only the default branch, and every MCP query from the worktree warned `branch 'feature' is not tracked — serving from 'main'`. The sentinel arm now runs `init` and then `branch add`, sequentially inside a single background job — sequentially because `branch add` copies the index that `init` creates, and as one job because two independent background jobs would race. `scripts/post-checkout`, the reference hook for manual installs, gains the same behaviour; it had never carried the `branch add` arm added in 7.3.0 at all. Ordinary branch switches, file checkouts, and clones of the default branch are unchanged. **This reaches new installs only:** the installer skips a post-checkout hook that already carries the tokensave marker and the writer never replaces existing content, so an existing hook keeps its old body — migrating those installs is the open policy question in #342 Q1. Until that is answered, an existing install can close the gap with `TOKENSAVE_AUTO_TRACK=1`, or with `"auto_track": true` in that project's `.tokensave/config.json`, on the next command that *opens* the project — a bare `tokensave branch list` does not open it and will not track. Simplest of all is a plain `tokensave branch add` in the new worktree. The section is now fenced with a closing `# tokensave: end auto-init` marker so a migration can replace it in place rather than pattern-matching the shapes that shipped in 6.4.3 and 7.3.0.
- **Stale `node_fingerprints` rows could produce phantom 1.0-similarity results in `tokensave_redundancy` (#380).** Cache validation now hashes the exact indexed node range, cache misses resolve the same line-and-column range in the tree-sitter AST, and cached rows whose stored token count differs from the live body are recomputed.
- **Cargo-driven tests no longer inherit developer-level Git hooks (#381).** A developer who had run `tokensave install` also had a global `core.hooksPath`, so real Git operations inside temporary test repositories executed TokenSave's background `post-commit` and `post-checkout` hooks. Those hooks raced the fixture itself, intermittently taking its sync lock or modifying `.tokensave/` while assertions were running. Cargo now supplies an empty command-scope `core.hooksPath` to the processes Cargo launches, while leaving the developer's global Git configuration unchanged.

### Documentation
- **The docs described an embedded file watcher that was removed in 6.1.1 (#390).** `docs/USER-GUIDE.md` carried a whole "The Embedded File Watcher" section telling readers the MCP server watches the project directory and runs incremental syncs in the background, and offered `watcher_debounce` as the knob to tune it; `UserConfig::watcher_debounce`'s own doc comment said the same. Nothing has read that field since the `notify-debouncer-full` watcher was removed (#80), so `watcher_debounce = "2s"` reads as a two-second freshness guarantee while having no effect whatsoever. The section now describes the model that actually runs: a catch-up sync when the MCP server connects, plus a staleness check at the top of every `tools/call` gated by a 30-second cooldown, with `tokensave sync` as the way to get fresher than that. The field is documented as inert rather than removed — dropping it would silently strip the key from existing `config.toml` files on their next save, and whether to deprecate it is a maintainer call. Two architecture notes in `docs/INDEX-DESIGN.md` and two comments in `src/tokensave/indexing.rs` that still named the watcher as the syncing party now name the MCP server's on-demand sync. Reported by @polarene.
- **Three documents dated the watcher's removal to 6.1.0 instead of 6.1.1, in five places.** The commit that removed it, `f7f7c9b`, is not an ancestor of `v6.1.0`; the first tag containing it is `v6.1.1`, which is also where CHANGELOG.md files the entry. `README.md`, `docs/COMPARABLE-TOOLS.md` (three places) and `docs/TOKENSAVE-VS-TOKENSAVIOR.md` are corrected. Noticed while fixing #390.
- **The bug-report template still offered `Daemon / file watcher` as an affected component.** Both are gone: `ffb5959` removed daemon mode and is first tagged `v6.0.0`, and `f7f7c9b` removed the embedded watcher and is first tagged `v6.1.1` (#80). The option is removed rather than renamed; the form keeps its `Other` option, and the field has no `default:` and no `validations: required`, so nothing else about it changes. Noticed while fixing #390.
- **SECURITY.md described the database as structural metadata only, which understated what it retains (#377).** The policy named `read_cache` as the sole place source text is kept, but `executable_body_fts` is declared without `content=''`, so FTS5 retains the original text of every column in `executable_body_fts_content` — including `body`, which holds complete function bodies. The enumeration above the sentence said "FTS5 search index", which does not tell a reader that full source is retained. Corrected in three places: the storage list, the sentence that follows it, and the Best Practices note, which likewise mentioned only symbol names and `read_cache`. No storage change is involved: the bodies are load-bearing for `tokensave_context`, and a contentless table would return NULL for `SELECT node_id, body`. The "Supported Versions" table, still listing 6.x as current, is updated to 7.x in the same pass.

## [7.9.0] - 2026-08-08

### Added
- **Verilog / SystemVerilog extraction, with an `instantiates` edge for the design hierarchy (#344).** A project containing tracked `.v` files could `init` and `sync` successfully while none of its RTL reached the graph — the files were not listed and no module was searchable. `.v`, `.vh`, `.sv`, and `.svh` are now handled by one SystemVerilog grammar (`lang-systemverilog`, in the `full` tier), extracting modules, interfaces, programs, packages, classes, functions, tasks, parameters/localparams, typedefs, package imports, and class inheritance. Module and interface instantiation becomes a new `EdgeKind::Instantiates` rather than being folded into `calls`: an instantiation is structural hierarchy, not invocation, and conflating them would put RTL hierarchy into callers/callees, impact, and dead-code results for every other language. Resolution of an `instantiates` reference is restricted to module and interface targets, so an instantiated vendor cell that is not in the index produces no edge at all instead of binding to whatever else shares its name. Internal nets and variables are deliberately not indexed — an RTL design declares them by the thousand, and they would swamp the graph without answering the questions the hierarchy is consulted for.
- **`tokensave_imports`: module-level import dependencies, cycles, and cut simulation (#334).** `tokensave_circular` answers dependency questions at the *file* level using `calls`/`uses` edges, which is the wrong unit and the wrong edge for planning a decomposition — one `from mod_b import X` produces zero `calls` edges or fifty depending on how the name is used, so counting calls says nothing about how many statements a refactor must touch. The new tool groups files into modules (configurable `depth`), reports which modules are mutually reachable, enumerates every import statement holding a given dependency together with its file and line, and — via `simulate_removal_from`/`simulate_removal_to` — recomputes the components as if one dependency were gone, which is what distinguishes a cut that breaks a cycle from one that leaves everything still mutually reachable. Imports written inside a function body are flagged `lazy`, since a lazy import usually exists *because* of a cycle and costs far less to remove than a module-level one. No new edge kind was required: extractors already emit a `Use` node per import statement carrying the imported path, line, and source text, and the resolver already binds it to its target, so the import graph is a projection of data the index already held.

- **`hook-pre-tool-use` now covers path-shaped discovery: the `Glob` tool, `find -name`/`-iname`, and `fd -e`/`--extension`/`-g` (#294).** The hook handled Agent dispatch and symbol-shaped Grep/Bash searches, but not discovery by path, so a separate Python or PS1 shim had to replicate policy for those — the two-implementation problem, where whichever layer is stricter or staler wins. Matching calls over code extensions are redirected to `tokensave_files`, which is only now a safe target: before #323 it was populated solely by symbol extraction, so redirecting `find -name "*.feature"` would have traded a working command for an empty result. The redirect is deliberately narrow, since it is a claim that the graph can answer the question: non-code extensions, mixed searches, search roots outside the index, `fd`'s default regex form, and any `find` predicate that changes what the command *does* (`-exec`, `-delete`, `-mtime`) all pass through untouched, while flags that only narrow results (`-type`, `-maxdepth`, `fd -t`) do not defeat it. `Glob` is told apart from `Grep` — both carry `pattern` — by the fields only `Grep` sends, so a content search is never misread as a path search. `TOKENSAVE_DISABLE_GREP_HOOK=1` opts out of the new paths exactly as it does the existing ones.
- **Non-code artifacts are now indexed by path, so `tokensave_files` can find them (#323).** The `files` table was populated only by the symbol-extraction pass, so `.feature` specs, fixtures, schemas, and docs were absent from it: `tokensave_files(pattern="**/*.feature")` returned nothing, the pattern was valid and simply had nothing to match, and the `find` fallback is blocked by the hook — leaving "where are the .feature files?" with no answer at all. Files with the configured `artifact_extensions` (`.feature`, `.json`, `.yaml`, `.yml`, `.sql`, `.toml`, `.proto`, `.graphql`, `.md` by default) are now tracked with a content hash and stat, so incremental sync adds, re-hashes, and removes them like any other file. They ride the existing scan, so exclude globs, `.gitignore`, the symlink-cycle prune, and the size limit all apply unchanged. Artifacts are never parsed and contribute no symbols or edges: a new `files.kind` column (schema v16) keeps them distinguishable from a source file that merely yielded no symbols, `tokensave_files` takes a `kind` filter and labels them `artifact` rather than `0 symbols`, and source-text scanners (`unsafe_patterns`, `field_sites`, exact-phrase context) skip them. An extension already owned by a language extractor is ignored in `artifact_extensions`, so configuring one cannot stop a language being parsed.
- **Sessions are now told which neighbouring projects they can reach with `graph_root` (#375).** The selector shipped in #363 could already query any sibling checkout, but nothing revealed that a sibling existed, so a session spanning two repos read an empty result as "no such symbol" — and with the shell fallback blocked by the hook, had no way left to find it (#322). Initialized projects sitting directly beside the served root are now named in the `initialize` instructions, in `tokensave_status`, and — most usefully — in empty `tokensave_search` and `tokensave_context` results, which is the moment the information is needed. Only immediate siblings qualify, so an unrelated checkout elsewhere on disk is never suggested, and at most five are listed so a crowded scratch directory cannot flood the response. Nothing is opened, indexed, or queried on their behalf; the list is a hint, and querying a sibling still requires an explicit `graph_root`.
- **Semantic MCP queries can explicitly select another initialized local project with `graph_root` and an optional tracked `graph_branch` (#363).** Selection is per call and read-only: it never initializes, syncs, migrates, auto-tracks, edits, or changes the server's default graph. Selected responses carry canonical root/branch provenance and graph-namespaced node IDs for safe follow-up traversal, while selected calls remain outside savings accounting.
- **`init` and `sync` now report tracked source files that were skipped because no extractor handles their extension (#345).** A repository whose only supported files are documentation could finish `init` with an apparently complete index, or report `sync done - 0 added, 0 modified, 0 removed`, without any hint that its actual source files were omitted, which made an unsupported language indistinguishable from an ignore rule, a size limit, a parse failure, or an empty project. Both commands now print a compact, aggregated line (`Skipped 1 tracked file (unsupported extension): .vhd (1)`), listing at most the five largest extension groups and rolling the rest into an `and N more extension(s)` tail, so a large repository never prints thousands of paths. The summary is grouped by extension and filters known binary and asset extensions, matching what `--verbose` and `sync --doctor` already showed in detail; `IndexResult` now carries `skipped_extensions` alongside `SyncResult`. Exit status and successful sync behavior are unchanged.
- **Ruby projects now have graph-parity coverage that compares complete node and edge records after incremental sync with a clean full index.** This catches count-preserving drift where one relationship disappears and another incorrect relationship takes its place.
- **`tokensave_context` takes an optional `max_code_lines` parameter to cap each code snippet by line count (#324).** Code snippets already have an approximately 1500-byte size limit and include a `tokensave_body` handle when truncated; the new parameter complements that protection by letting callers request a deliberately short, predictable prefix such as the first 10 or 20 lines of each relevant symbol. Both limits apply, with the tighter one winning, and truncation still occurs on a line boundary. Snippets below both limits are returned unchanged. The line cap defaults to unlimited and explicit values are clamped to 1–1000.
- **`tokensave cost`/`monitor` now accounts for Factory Droid local `.factory/sessions/**/*.settings.json` snapshots.** Exclusive per-session `tokenUsage` key names — `inputTokens`, `outputTokens`, `cacheCreationTokens`, `cacheReadTokens`, `thinkingTokens` (`thinkingTokens` is folded into normalised output; not exposed separately) — plus observed `factoryCredits` are imported and exposed via `cost --by-agent`. Credits are reported as a separate unit and never converted to USD. Cumulative sessions are bucketed by `providerLockTimestamp` as the copy-stable session start; `droid` `/limits` remains the authoritative monthly balance. When no Droid source exists, existing `cost` output and the original two-line monitor remain unchanged.

- **Calls made outside a function body now count as uses, cutting the `tokensave_dead_code` false-positive rate (#346, parts 2c/2d).** The reporter measured 97% false positives on a Go package tree and 3 of 3 on a Vue/Pinia frontend, noting the TypeScript cases were "the simplest possible case (same file, same closure/module scope, direct call)". They were, and the cause was that both extractors attributed call sites only to an enclosing *function* node. A call anywhere else was recorded against nothing at all. Three concrete gaps are closed. A statement-level call in TypeScript (`registerHandlers();` at module top level, a side effect that runs at import time) is now attributed to the file node, which is the enclosing scope the graph actually has. Calls inside a `const` initializer are attributed to the binding — the Pinia shape from the report, `export const useStore = defineStore("x", () => { loadFromStorage() })`, where the arrow passed as an argument gets no graph node of its own, so every helper it called looked uncalled. And in Go, a function used as a value rather than called (`var SandboxSuffixFunc = randomSandboxSuffix`) now emits a reference; the existing scan only looked for call expressions, and a bare identifier initializer contains none. That last one is deliberately scoped to a var spec's own initializer rather than every `expression_list`: emitting a reference for each identifier in every assignment and return would link unrelated same-named symbols across packages, which is the over-linking that fabricates cycles in part 1 of the same report. On the issue's reproductions the TypeScript fixture goes from 3 flagged (all false) to 0, and the Go fixture from 2 flagged (1 false) to 1 — the one genuinely dead function. The same-package test-caller case from the report (`reset()` called from `broker_test.go`) already resolved correctly and now has a regression test so it stays that way. Every fixture carries a genuinely dead function as a control, since the point is to remove false positives without blinding the tool.
- **`tokensave_search` now has an exact-phrase channel, and no longer returns invalid JSON for a symbol with a huge signature (#362 follow-up).** The reporter noted that `tokensave search "Waiting for status"` did not return the localization catalog that literally contains the phrase, and the earlier `tokensave_context` fix did not address it. The cause is separate from the BM25 effect: `search` sorts by kind tier first — deliberately, so a `use` re-export can never outrank a real definition — and a catalog is a `const`, so it could not beat a function however well it scored. A multi-word query now also runs an FTS *phrase* match, and those hits are promoted ahead of the tier sort, before truncation, so the one symbol whose text actually contains the phrase leads the results. Single-word queries are untouched: the phrase helper returns nothing for them, so ordinary name lookup ranks exactly as before. Fixing this exposed a second defect. The catalog's signature is roughly 44 KB, and the tool serialized it verbatim; that blew the 15,000-character response budget and the truncation cut mid-string, so `tokensave_search` handed its caller **malformed JSON** — reproducibly, whenever a result carried a large value initializer. Entry-point signatures were already bounded in the context formatter by the change above; that helper is now shared and applied to the search, node, and related tool payloads too. The same query goes from 15,033 bytes of unparseable output to 2,613 bytes of valid JSON.
- **`tokensave_context` no longer loses an exact quoted phrase to generic semantic matches (#362).** A context request quoting a UI string — "Diagnose why the dashboard still shows Waiting for status" with `"Waiting for status"` as a keyword — returned nothing but decoy `loadDashboardStatusNNN` functions, omitting both the localization catalog holding the phrase and the component rendering it, 5 runs out of 5 on the reporter's fixture. The catalog was indexed and FTS did match it: the extractor stores a const's whole initializer as its signature, so the 800-entry catalog is one 43 KB field, and BM25's length normalization scored that unique, exact hit at −0.07 against −5.58 for hundreds of short decoys matching only the generic words `dashboard` and `status`. The single most specific piece of evidence in the query ranked 26th. The machinery to handle this already existed — `find_exact_source_candidates` greps source, attributes each hit to its innermost enclosing node, and scores it above every FTS channel — but it was unreachable here, because `exact_source_terms` splits on whitespace and keeps only `::`-qualified tokens, so a multi-word phrase was discarded before the scan ever ran. Phrases now reach that scan: any caller keyword containing inner whitespace, plus any double-quoted span in the task text, is matched verbatim. Single words are deliberately excluded (FTS already covers them, and scanning source for one common word matches everywhere), phrases must be at least 8 characters and two words, and the list is capped at 8 so the scan stays bounded. On the reporter's fixture the catalog goes from absent to the first entry point. Two things this does *not* fix, both confirmed while testing: `tokensave_search` still ranks the catalog below short semantic matches for the same BM25 reason, and the component cannot be reached by graph expansion because the TypeScript extractor emits no edge from a function body to a module-level const it reads — `translate` → `MESSAGES` is simply missing from the graph, so the `Banner` → `translate` → `MESSAGES` path is broken at the last hop.
- **An entry point's signature is bounded to one line of at most 200 characters.** The extractor stores a value binding's entire initializer as its signature, so surfacing a localization catalog or a generated lookup table printed tens of kilobytes into the context response — on the #362 fixture, a single entry point rendered 15 KB, more than the source file it came from, which is a poor trade in a tool whose purpose is spending fewer tokens. The first line still identifies the symbol and the code block below it carries content under its own existing limits; truncation is marked with an ellipsis and happens on a character boundary. The same fixture now renders 3.2 KB.
- **`report_savings` turns off the savings narration that costs output tokens (#356).** tokensave exists to cut *input* tokens, but it asks the agent to tell the user what it saved — via the `tokensave_metrics:` line appended to every accounted tool result and a sentence in the MCP `instructions` pointing at that line. The agent obliges on nearly every turn, and those are *output* tokens, the more expensive kind, spent on a message that wouldn't otherwise exist; as the report puts it, that "largely offsets the benefits". The new per-project `report_savings` config field (default `true`, so nothing changes for anyone who doesn't ask) removes both when set to `false`: no metrics line, and the `instructions` stop asking for the report. `TOKENSAVE_REPORT_SAVINGS` overrides it per-run using the same falsey spellings as `TOKENSAVE_AUTO_TRACK` (`0`, `false`, `no`, `off`, empty), whose ad-hoc match is now the shared `config::env_bool_override` both settings call. `tokensave install` also stops writing the reporting rule into the Vibe prompt file; that runs at global scope with no project config in reach, so it reads the env var and bakes the choice into the file it regenerates. Measurement is deliberately outside the gate: the per-call ledger row, the persisted counter, and the monitor entry are all still written, so `tokensave gain`, `list`, `status` and `monitor` report exactly what they did before — the setting governs what the *model* is shown and asked to say, not what tokensave knows.
- **`tokensave init` on an already-initialized project is covered by regression tests (#372 §1).** The report described observing exit 0 with a silent-looking message in an interactive session, which cost real debugging time when a caller trusted `init`'s exit code as proof a rebuild had happened. That could not be reproduced: the already-initialized branch calls `exit(1)` with no TTY-dependent path, and the binary was confirmed to exit 1 both with stdin closed and under a pty, so the reported behaviour was a version difference rather than a live defect. No behaviour changed; `tests/init_already_initialized_test.rs` now pins it down through the real binary across the explicit-path, argument-less, and relative-`.` invocations, and asserts the refusal still names `sync --force`, so the guarantee cannot drift back.

### Changed
- **The structural call-graph tools `tokensave_impact` and `tokensave_callees` are now `anthropic/alwaysLoad` (#333).** Their schemas load into the model context up front instead of sitting behind a `ToolSearch` round-trip. Measured usage across 26 days and five repositories showed the three previously-eager tools (`context`/`search`/`status`) taking 96.7% of calls while the call-graph tools — the ones with the clearest advantage over grep-and-read for "who calls this?" / "what breaks if I change this?" — went essentially unused, because a tool the model has to already suspect exists is a tool it rarely goes looking for. The always-loaded set grows from three tools to five; the schemas are small and the prompt prefix is cached, so the up-front cost is negligible. `callers` and `affected` are deliberately left deferred for now, pending a measured usage shift.
- **Symbol FTS now stems at the index (`porter unicode61`), and camelCase inner words are indexed at insert time (schema v14).** Inflected prose in a `tokensave_context` task — "candidates", "truncation", "generates" — previously matched nothing, because the unicode61 tokenizer is exact-token and the identifiers are `candidate`/`truncate`/`generate` (#264 called out the missing `generates` → `generate` normalization). The porter tokenizer makes that class of match work for any suffix at query time instead of patching common cases query-side. A new `search_terms` column carries the words a tokenizer cannot derive on its own — the inner words of a camelCase identifier (`updateCloudClient` → `Cloud Client`), computed once at insert by `text::search_terms` and weighted between `qualified_name` and `name` in `bm25()`; it stays empty for snake_case names, which unicode61 already splits on `_`, so the column adds nothing for codebases like this one and closes the #202 class (spaced natural-language query vs. camelCase TSX symbol) for the TS/Java/Go side of the language table. Migration v14 adds the column, backfills it, and rebuilds `nodes_fts` with the new tokenizer — all derived data, so the worst case is a re-index; nothing user-authored is touched.

### Removed
- **Both query-side stemming workarounds are deleted, subsumed by the porter tokenizer.** The trailing-`s` strip from #264 and `generate_stem_variants`' hand-rolled suffix table existed only because the index didn't stem. Porter covers every conflation the suffix table targeted — verified directly against FTS5 (authentication ↔ authenticator ↔ authenticate, configuration ↔ configure, serializer ↔ serialization, truncation ↔ truncate, validator ↔ validated); the one jump neither makes is resolution → resolve, so nothing is lost. On a 12-query retrieval eval the entry points are identical with the workarounds removed.

### Fixed
- **The Python extractor now indexes imports written inside function bodies.** Only module-level `import` / `from … import` statements were visited, so a deferred import produced no `Use` node at all — invisible to `tokensave_unused_imports` and to the new import graph, and precisely the case that matters most there, since a function-body import usually exists to break a cycle. Nested definitions are still skipped by this walk, so their imports stay attributed to the function that actually contains them rather than the enclosing one.
- **Project paths are canonicalised at the global-DB boundary, so one directory can no longer occupy several rows (#372).** Every key written to or read from `~/.tokensave/global.db` — `projects.path` and `savings_ledger.project_path` alike — now goes through `global_db::normalize_project_key`, which resolves the path on disk (collapsing `.`, `./`, `../name`, symlinks and trailing separators), strips the `\\?\` and `\\?\UNC\` verbatim prefixes that `canonicalize` returns on Windows, and upper-cases a drive letter so `d:\Work\ProjectA` and `D:\Work\ProjectA` land on the same row. A path that cannot be resolved — a project directory that has since been deleted, or a synthetic path in a test — falls back to a lexical absolute form, so the key stays stable rather than collapsing onto the working directory. Two symptoms motivated this. A literal `.` was registered as a project name: `tokensave list -a` showed a row named `.` carrying a real size but no recoverable identity, and `doctor`'s stale-purge could never remove it, because `.` resolves to some real indexed directory from wherever `doctor` happens to run and so is never considered stale. And on Windows, case-variant drive letters accumulated token counts independently (14.4M on `d:\Work\ProjectA` against 12.2M on `D:\Work\ProjectA` for one confirmed-identical directory), which both under-reports `tokensave gain` per project and double-counts disk usage in `list -a`'s total. The relative-argument entry below fixed the resolver that produced the `.` row in the first place; this change makes the storage layer itself refuse the ambiguity, which is what also covers the drive-letter case, paths arriving from the MCP server, and any future caller that reaches the DB with an unnormalised path. A one-time migration, gated on a new `meta` table so it costs nothing after the first open, rewrites existing rows: duplicate spellings are merged by summing their `tokens_saved`, ledger rows are re-pointed at the canonical key so a project's recorded history follows it, and rows keyed by a relative path are dropped, since they name no recoverable directory and would otherwise stay unattributable and unpurgeable forever. The migration is best-effort in the same way as the rest of `GlobalDb`: on failure the flag stays unset and the next open retries. This addresses sections 3 and 4 of #372; the worktree-mismatch strict mode (section 2) and `init`'s already-initialized exit signal (section 1) are unchanged.
- **Schema migration v15 restores secondary indexes and FTS triggers lost by an interrupted bulk load, even when the `.tokensave/dirty` sentinel is absent (#358).** `begin_bulk_load` drops every edge, node, and unresolved-ref index plus the FTS and trait-dispatch sync triggers; `end_bulk_load` recreates them — but only if it runs. The #318 fix added sentinel-based healing in `TokenSave::open`, yet that path only fires when the dirty sentinel exists. If the sentinel was never written, was already cleaned up by a prior `open()`, or was removed manually, the DB stayed permanently unindexed: `PRAGMA quick_check` passes (the B-tree is intact), `sync` pinned a core at ~100% CPU doing full-table scans of the `edges` table (46M rows / 4.6 GB in the reported case) on every `delete_nodes_by_file` call, and the MCP server silently served an unindexed graph. Migration v15 recreates the indexes and triggers that bulk load drops with `CREATE … IF NOT EXISTS`, each gated on its base table so the repair is a safe no-op on a partial schema (#359) — instant on a healthy DB, a full repair on a damaged one.
- **`end_bulk_load` now recreates the v14-era FTS triggers that include the `search_terms` column, not the pre-v14 bodies that omitted it (#358).** After every `index_all` on a v14 DB, `end_bulk_load` dropped the FTS sync triggers and recreated the old bodies without `search_terms`, silently downgrading the FTS index so `tokensave_search` could no longer match camelCase inner words (`updateCloudClient` → `Cloud Client`) or porter-stemmed inflections on rows inserted or updated after a full reindex. The trigger bodies now match `create_schema` and `migrate_v14`.
- **Relative path arguments such as `tokensave init .` now work instead of aborting.** Every command that takes an optional project path resolved it with `PathBuf::from` and handed the value straight through, so a relative argument stayed relative even though both resolver functions document an absolute result. In a debug build `init` and `sync --force` then hit `debug_assert!(project_path.is_absolute())` and panicked with `init_and_index: project_path must be absolute`; in a release build the assertion is compiled out and the relative path is persisted instead, so `tokensave init .` registered the project as `.` in the global database while any later argument-less invocation registered the same project again under its absolute path, splitting that project's savings totals across two rows and pooling every project initialized that way into the same `.` row. Passing no argument was unaffected, because that branch already used `current_dir()`, which is why `tokensave init` worked while `tokensave init .` did not. A non-absolute argument is now joined to the working directory and normalized lexically, so `.` and `..` collapse to a root the rest of the codebase can compare and store, while absolute arguments are returned untouched and an existing project root keeps its exact spelling, symlinks included. On Windows this also covers a rooted but driveless argument such as `\tmp\project`, which `Path::is_absolute` rejects and which is now resolved against the current drive the way Windows itself resolves it. Normalization is lexical rather than `canonicalize` so that a path that does not exist yet still resolves, and so that Windows never returns a `\\?\` verbatim prefix into stored roots or user-facing messages.
- **Ruby calls with statically provable class/module receivers now resolve to singleton methods.** Constant receivers follow Ruby lexical nesting and respect shadowing assignments; `self` calls resolve from singleton methods and directly from class/module bodies, including targets defined inside `class << self`, while blocks that change `self` are excluded from the enclosing body. Ambiguous or dynamic receiver shapes remain unresolved, and the first incremental sync upgrades Ruby files from an existing index so unchanged singleton definitions participate immediately.
- **Database test suites no longer leak a temp directory per test on Windows (#367).** The setup helpers returned `(Database, TempDir)` — or `(TokenSave, TempDir)`, or `(Arc<McpServer>, TempDir)` — and every test bound the pair as `let (db, _dir) = setup().await`. Rust drops the bindings of a `let` in reverse order of declaration, so `_dir` went first and `remove_dir_all` ran on the directory while the connection still held `test.db` open. POSIX unlinks an open file happily, which is why this was invisible on Linux and macOS; Windows refuses, the removal fails, and `TempDir`'s destructor discards the error by design. Measured on Windows, `db_query_test` left 75 of its 76 directories behind on every single run, each holding its `test.db`. Repeated runs accumulate: the batches used to measure the crash in the companion change stranded 48,245 directories totalling 20.25 GB on the system drive before the leak was noticed, and filled it. A CI runner that is already tight on disk is exactly where that reads as an unrelated "no space left on device" flake. Every affected helper now returns the `TempDir` first and the call sites bind `let (_dir, db)`, which puts the database's drop ahead of the directory's; `resolution_test`, `integration_test`, and `memory_test` already ordered it that way and are untouched, and `corruption_test`'s three-element helper needed the same treatment as `migration_test`'s. Each suite re-run on Windows afterwards leaked nothing; so did a sweep of all 111 test binaries run twice, though that sweep had the companion libsql fix applied as well. This is a distinct defect from the double close above — the batches show the same leak with and without that fix — but it is the same class of bug: a resource lifetime that only POSIX forgives.
- **Every database connection was being closed twice, a use-after-free that surfaces on Windows as the intermittent `STATUS_ACCESS_VIOLATION` of #367 and #308. Fixed for CI and for builds from a checkout; binaries installed from crates.io are not covered until libsql itself ships the fix, for the reason given at the end of this entry.** libsql's `Connection::disconnect` is not idempotent: `LibsqlConnection`'s `Drop` calls it, and then the inner `Connection` field's own `Drop` calls it again. The `Arc::get_mut(drop_ref)` guard only proves unique ownership — true on both passes — and `raw` is left dangling after the first `sqlite3_close_v2`, so the second call reads an already-freed `sqlite3` object. Instrumenting the driver made the ratio exact rather than inferred: one run of `db_query_test` performs 76 `sqlite3_open_v2` calls and 152 `sqlite3_close_v2` calls, 76 returning `SQLITE_OK` and 76 returning `SQLITE_MISUSE`, and the same 1:2 ratio holds on the `GlobalDb` path. What was measured is that the second close returns `SQLITE_MISUSE` on Linux rather than faulting: the freed page is still mapped and its state check fails harmlessly. That the same read faults on Windows once the block has been recycled or decommitted is the explanation for the platform asymmetry, not a second measurement. On Windows the block can be recycled or decommitted first, and the read then either faults immediately or closes a garbage handle and corrupts the heap, which detonates later, at process exit, after every test has already reported `ok`. That is the signature #367 describes, and it explains why the same bytes pass on one run and fail on the next. This is not CI-only: any `tokensave.exe` closing a database on Windows runs the same double free. Batching the affected binary on a Windows machine puts a number on it: `db_query_test` crashed 29 times in 500 runs (5.8%) on the unpatched build, every one of them exit code `0xC0000005` after all 76 tests had reported `ok`, and 0 times in 500 runs with the fix applied — a rate that would produce a crash with probability 1 − 10⁻¹³ had it been unchanged. The other two suites named in the reports behave identically: `accounting_test`, the first one #367 cites, crashes 10 times in 500, and `resolution_test`, the suite #308 was originally filed against, 13 times in 500 — both clean afterwards, so the same cause is present in all three unrelated binaries and in both issues. That is not proof it was the only cause of every past occurrence, but nothing else has reproduced since. The batches also retire the standing theories: serialising the harness with `--test-threads=1` does not hide the crash but roughly triples it (47 in 300), so parallel test execution is not what drives it — which is as far as that experiment reaches, since it removes concurrency between tests and not every runtime teardown path; quadrupling `RUST_MIN_STACK` to 32 MB leaves the rate unchanged (12 in 300), so no amount of stack is relevant; and a suite that opens no database is clean in 500 runs. Across four binaries and 2100 runs with the fix in place there was no crash of any kind, and a sweep of every one of the workspace's 111 test binaries, run twice on Windows, came back with no access violation and no other failure. That sweep ran with this fix and the drop-order fix of the companion change applied together, so it bounds the two of them jointly rather than this one alone. The fix is one line — null `raw` after closing so the second pass is a no-op — and it is applied in `vendor/libsql`, a copy of the published 0.9.30 crate wired in through `[patch.crates-io]`, carrying upstream's MIT licence and dropping its examples, tests, benches, test assets, lockfile and contributor notes, with `[lints]` and `[workspace]` tables added to its manifest and the one-line change below, because the bug is still present upstream in both 0.9.30 and 0.10.0-pre.4 (libsql#2251, with the equivalent fix waiting in libsql#2261). `tests/vendored_libsql_test.rs` fails if the vendored copy loses the patch or drifts from the requested version, so the patch cannot be dropped silently; the vendored crate and the `[patch]` entry both go away once an upstream release carries the fix. One limit is worth stating plainly: `cargo package` strips `[patch.crates-io]` and leaves `vendor/libsql` out of the published archive, so this covers CI and anyone building from a checkout, while binaries produced by `cargo install tokensave` keep the upstream double free until libsql itself ships the fix. Neither #181's `RUST_MIN_STACK` nor #337's `/STACK:8388608` could have addressed this — both target stack exhaustion, and this is a heap lifetime bug.
- **Ruby call references now preserve explicit receivers instead of resolving by method name alone.** Calls such as `User.find`, `worker.perform`, and `user&.profile` remain receiver-qualified and no longer create edges to unrelated same-named methods.
- **Graph selector and untracked-branch errors now tell the caller how to recover.** Selecting the project the MCP server already serves said only that the root matched, and agents that pass `graph_root` explicitly were observed retrying the same failing call; the message now says to omit `graph_root` (and `graph_branch`, when present) to query the currently served graph, and states that selecting a different branch of the served project is not supported instead of leaving the branch intent unaddressed. `graph_branch` without `graph_root` carries the same guidance, and an untracked explicit branch now names the remedy (`tokensave branch add '<branch>'`, shell-quoted) in both the selected-graph and served-graph open paths, matching the fallback warnings that already did. The `graph_root`/`graph_branch` schema descriptions shown in `tools/list` now say to omit them for the served project and that `graph_root` must name a different one.
- **Tracking a branch no longer copies a truncated snapshot of the parent DB when the parent has an uncheckpointed WAL (#292).** Both `tokensave branch add` and the transparent auto-track path copied only the `.db` file, but a long-lived MCP server keeps the parent DB open in WAL mode, so committed rows — routinely the most recent sync's worth — can sit exclusively in the `-wal` sidecar at copy time. The copy then opens clean (`quick_check` passes; the B-tree is intact) while silently missing that data, which is how a branch DB ends up answering `tokensave_context`/`tokensave_search` from checkpointed tables while `tokensave_files` reports 0 files: the symptom reported in #292 and previously misattributed to branch fallback. A shared `copy_branch_db` helper now snapshots the parent with `VACUUM INTO`, which writes a transactionally consistent copy — main file plus WAL contents — under the source connection's read snapshot, correct even while a concurrent connection is mid-commit; sequencing a checkpoint and two separate file copies can never make that guarantee, since a checkpoint plus WAL reset landing between the copies would mix a pre-checkpoint `.db` with a post-reset `-wal`. Only when the snapshot itself fails (the parent is not a readable SQLite file, or an exotic locking failure) does the helper fall back to a best-effort `wal_checkpoint(TRUNCATE)` followed by a plain file copy, bringing the `-wal` sidecar along if the checkpoint could not empty it so SQLite recovers it on the new DB's first open. The already-tracked, default-branch, and no-metadata no-op paths are unchanged.
- **`branch-meta.json` is now written atomically (temp file + rename), so a concurrent or interrupted branch-track can no longer leave it torn.** `save_branch_meta` did a plain `std::fs::write`, so two `track_branch_copy` calls racing on the same repo, or a crash mid-write, could truncate or corrupt the file that maps every tracked branch to its DB; a corrupt file silently falls back to single-DB mode, so every non-default branch would then serve the ancestor's stale index. The JSON is now written to a temporary file in the same directory, flushed, and renamed over the target, which is atomic on POSIX and Windows.
- **Two git branches whose names sanitize to the same filename no longer share one branch DB and silently overwrite each other's index.** `sanitize_branch_name` maps `/`, `\\`, `:`, `.`, spaces and more to `_` and collapses runs, so `feature/foo`, `feature_foo` and `feature.foo` all became `feature_foo.db`; neither `tokensave branch add` nor the transparent auto-track path checked whether that filename was already claimed by a different branch before copying and recording it, so tracking the second branch clobbered the first branch's DB. A new `unique_branch_db_file` helper, used by both paths, keeps the readable stem when it is free and appends a short stable hash of the full branch name only on collision, so the branch-to-DB mapping is deterministic and collision-free. Existing non-colliding names are unchanged.
- **An interrupted bulk load no longer leaves the graph with duplicate edges and no indexes, and can no longer poison every later sync (#318).** `begin_bulk_load` drops `idx_edges_unique`, the index the `INSERT OR IGNORE` edge writers rely on to dedupe, and only `end_bulk_load` recreates it. If a load was interrupted in between (for example the `database table is locked` failure of #320), the graph was left un-indexed and possibly duplicated; `PRAGMA quick_check` passes on that state because the B-tree is intact, so it was served as-is and every subsequent sync piled on more duplicates — one reported database reached 914k duplicate rows over ~28k real edge tuples with no edge indexes. Worse, once duplicates existed, `end_bulk_load`'s `CREATE UNIQUE INDEX` could never succeed, so finalization failed permanently. `end_bulk_load` now collapses duplicate edge tuples (keeping the lowest `rowid` per `source, target, kind, COALESCE(line, -1)`) before recreating the unique index, and `TokenSave::open` detects a missing `idx_edges_unique` on a dirty reopen and re-runs finalization — deduping and rebuilding the dropped indexes — instead of serving the broken graph. This is a targeted repair, not a full re-index.
- **Windows test binaries no longer crash intermittently with `STATUS_ACCESS_VIOLATION` (0xc0000005), a main-thread stack overflow the existing `RUST_MIN_STACK` could not prevent (#308).** The crash appeared across unrelated test binaries with no assertion or panic, only on Windows, while Linux and macOS stayed green — the signature of running out of stack on the one thread `RUST_MIN_STACK` does not resize. That variable enlarges the threads Rust spawns, but the main thread's stack is fixed at link time and defaults to about 1 MB on MSVC against roughly 8 MB on Linux and macOS. A deep recursive AST walk (`extract_call_sites_recursive`), or resolver work that rayon's work-stealing runs on the calling thread, therefore overflowed on Windows alone and only when the deep frame happened to land on the small main-thread stack, which is why it was intermittent. The Windows MSVC target now links with `/STACK:8388608`, raising the main-thread reserve to 8 MB to match `RUST_MIN_STACK` and the other platforms; the reserve is address space committed lazily, so there is no runtime cost, and non-Windows and Windows GNU builds are untouched.
- **The PreToolUse guardrails now activate anywhere inside an indexed project, and resolve that project from the working directory the harness reports.** The hook required `.tokensave/tokensave.db` to sit in the hook process's own working directory, while `serve`, `sync`, and `status` all discover the project by walking ancestors. An agent started in a subdirectory of an indexed repository (a package inside a monorepo, for example) therefore had working tokensave MCP tools and a silently disabled guardrail, so every symbol-shaped `grep` there passed through with no savings and no explanation. Discovery now walks ancestors like the rest of the codebase, and Claude Code, Kiro, and Factory Droid events are resolved from their own top-level `cwd` with the hook process's directory as the fallback, which also makes Kiro's `preToolUse` hook agree with the `postToolUse` sync hook that already read `cwd`. A reported `cwd` is only honored when it is an absolute path to an existing directory, since the value is agent-supplied and a relative or stale one would start the ancestor walk somewhere nobody meant; every other value falls back to the hook process's directory. The `TOKENSAVE_DISABLE_GREP_HOOK` opt-out, the pattern and target classifiers, and Droid's built-in `explorer` task guard are unchanged. `HookEnv::cwd_has_tokensave_db` is renamed to `HookEnv::in_tokensave_project` now that the flag no longer describes one directory.

- **The indexing walk no longer follows a directory symlink back onto the project root or one of its ancestors, which made `init`/`sync` unbounded and pinned a core in `serve` (#327).** Every Wine prefix ships `dosdevices/z: -> /`, so a project that contains one (or whose root is `$HOME`) sent the walker across the whole filesystem: the reporter measured 683,707 crossings in 40 s and two leaked `serve` processes that burned 20.5 h and 14.4 h of CPU. Both walkers and the gitignore fallback probe now prune a directory symlink whose canonical target is the canonical project root itself or one of the root's ancestors; such a link can only re-expose paths the walk already covers, plus everything else on the disk. Symlinks into disjoint trees are unaffected and are still followed and indexed, so the external-source-tree layout stays supported; the guard is checked before the hidden-directory, `include`, and manifest rules, so nothing can re-enable an escaping link, and the walk root itself is exempt so a project opened through a symlinked path still scans. Rows indexed through such a link before this fix are removed by the next full `sync`. Deliberately unchanged: a symlink into a large *disjoint* tree is still followed (use `exclude` or `--skip-folder`), and non-symlink filesystem cycles such as bind mounts are outside the walkers' view.
- **`tokensave upgrade` no longer offers to kill its own launcher, and no longer proceeds if it cannot signal every process it was asked to kill.** Candidate selection now excludes this process and every reachable parent in the process tree (e.g. a Scoop shim or shell that launched `tokensave`), so it can't be listed as a killable "other" process; unrelated `tokensave` MCP servers are still offered. If `--kill`/an interactive `y` cannot signal every listed process, the upgrade now aborts instead of continuing, telling the user to stop them manually and retry.
- **`tokensave serve` now understands the `file://` URIs that MCP clients send in `roots`, which had made the roots-based project discovery dead on Windows (#331).** When neither the working directory nor the global database identifies a project, `serve` peeks at the `initialize` request and resolves the workspace from its `roots` array — the path that covers a VS Code multi-folder workspace, or any client launched from outside the project. It stripped a bare `file://` prefix, which is correct only on Unix: a Windows root arrives as `file:///D:/Dev/app`, leaving `/D:/Dev/app`, a path that matches no registered project and that `discover_project_root` cannot walk, so the fallback silently found nothing and the server exited telling the user to run `tokensave init` in a project that was already indexed. Percent-encoding was ignored as well, so any path containing a space (`file:///C:/My%20Project`) failed on every platform. The URI is now decoded — on bytes, so a percent-encoded multi-byte character reassembles correctly, and a decode that does not form valid UTF-8 (a lone `%C3`, a `%FF`, a truncated sequence) rejects the root instead of collapsing distinct malformed inputs into the same U+FFFD replacement path — and the leading slash is dropped only when a drive letter follows it, leaving a genuine Unix root like `/d/dev/app` alone. Registered projects are compared canonically rather than literally, so the same directory spelled `D:\Dev\app` and `D:/Dev/app` is recognized as one. Non-`file:` schemes and non-local authorities are skipped instead of being interpreted as relative paths. This is the inverse of `agents::kiro::file_resource_uri`, which already produced exactly this URI form.
- **The response to a peeked-and-replayed `initialize` request is now newline-terminated, so the roots fallback completes the MCP handshake with a live client (#331).** `serve`'s last-resort discovery consumes the first stdin line to read `initialize.roots` and replays it through `handle_and_write`, which wrote the serialized response without the trailing `\n` that the main `run()` loop appends. MCP frames stdio messages by line, so a connected client sat waiting for the response line to finish and the handshake never completed — the response was flushed but unterminated, and only an EOF (the closing pipe delivering the partial line) ever released it. Piped one-shot harnesses see exactly that EOF, and the test suite's channel transport frames by message rather than by line, so neither noticed; before the URI fix above the fallback never resolved a project on Windows at all, which is why the two bugs could only meet in a live client session. Found while verifying #331 end-to-end on a real Windows MCP client.
- **`annotates` edges no longer bind an annotation usage to another usage of the same annotation, including itself.** The resolver allowed an `Annotates` reference to target a sibling `AnnotationUsage` or `Decorator` node, and every extractor names that reference after the annotation itself, so any `@override`/`#[test]`/`@Override`-style usage could resolve straight to another usage instead of staying unresolved — a phantom edge affecting 11 language extractors. The resolver now never produces `annotates` edges at all; the attachment edge extractors already emit directly at the usage site is the only one, matching what every consumer reads. Schema migration v14 removes any phantom edges already stored in existing projects' databases automatically on next open, no resync required.
- **Dart calls made inside closures and local functions are no longer dropped.** `extract_call_sites` skipped `function_expression`/`lambda_expression` outright instead of attributing their calls to the enclosing named symbol, so calls inside Flutter callbacks (`onPressed:`, `builder:`, `setState(…)`, `.map(…)`) and local functions were invisible to `tokensave_callers`, `tokensave_impact`, and `tokensave_dead_code` — a helper reached only from a callback looked dead. Same fix as TypeScript #209 for nested arrows.
- **A project indexed by the CLI no longer force-reindexes on the first MCP tool call of every session, and a failed forced reindex no longer leaves the project with an empty index (#320).** `last_indexed_version` was written only by the MCP server's `run_version_reindex`, so every project created by `tokensave init` kept the `String::new()` default. `bump_kind("", running)` classifies an unparseable `old` as `BumpKind::Major`, so the first `tools/call` of each session logged `major upgrade or schema change detected (indexed by "", running <version>) — forcing project reindex…` and ran a full reindex — on a project that was already current. `index_all` opened that reindex with `db.clear()` *before* `db.begin_bulk_load()`, so when `begin_bulk_load` lost the table lock to a second tokensave process (a common setup: VS Code, VS Code Insiders, and one or more CLI agents all registered against the same project) the rows were already gone, the reindex returned early, and the marker was never advanced. The server treats that failure as non-fatal and keeps serving, so every subsequent tool call answered out of a gutted graph — observed as `file_count: 1` on a 259-file project — and the same destructive cycle repeated on the next start, since the marker still said `""`. A full index now records the running version, so the redundant reindex never starts; and `begin_bulk_load` runs before `clear`, so a lock failure aborts with the previous index intact instead of destroying it. Clearing after the FTS triggers are dropped also avoids firing a per-row FTS delete trigger for every node.

## [7.8.1] - 2026-07-27

### Added
- **Ruby `def`s nested inside a `do…end`/`{…}` block, in expression position, or inside a method body are now extracted** — `ActiveSupport::Concern`'s `included do ... end`, `class_eval do ... end`, `class_methods do ... end`, an arbitrary RSpec-style `describe … do`, a plain `[1].each do … end`, and even a `def install; [1].each { def m; end }; end` all previously dropped their contents entirely. This is a correction, not new syntax support: `visit_node`'s `"do_block" | "block"` dispatch arm (and the `"body_statement"` arm it recurses into) has existed since the extractor's original commit, but was unreachable — a block is always the `block` field of a `call` node, and nothing ever called `visit_node` on it, since the `call` arm dispatched only to the two directive handlers rather than recursing generically. A new `visit_block_body` supplies that entry point, dispatching on a new `BlockScope` classifier that models Ruby's actual default-definee rule rather than treating every block as a scope boundary: an ordinary block (`each`, `tap`, `describe`, `configure`, …) does not change the definee at all, so it's traversed unconditionally — an explicit receiver is irrelevant, and a `private` before or inside the block flows through it in both directions, matching Ruby 3.4.5. Only `class_eval`/`module_eval`/`class_exec`/`module_exec` (plus `Concern`'s `included`/`prepended`, which are `base.class_eval(&block)`) and `instance_eval`/`instance_exec` (plus `Concern`'s `class_methods`, approximated as `SingletonScope::Enclosing`) actually push a fresh definee, and only those get the previous treatment: skipped when the call has an explicit receiver (`Foo.class_eval do … end` defines methods on `Foo`, which can't be resolved here, so its block is left untraversed rather than mis-attached to the enclosing scope), and a visibility frame that starts public and doesn't leak in either direction, mirroring how class/module bodies already behave. No new scope is pushed in any case, so a top-level `describe … do` block's defs are extracted as `Function`s, same as a bare top-level `def`. `visit_block_body` was still only reached when the block-bearing call was itself a statement, so a block written in **expression position** — an assignment RHS (`CALLBACK = proc do … end`), a call argument (`foo(list.map { … })`), a call receiver (`list.map { … }.first`), or a bare lambda literal (`-> { … }`) — still dropped its `def`s. A new `visit_expression_blocks` (mirroring `extract_call_sites`'s descent and definition-scope skip-list) closes that gap, wired in wherever an expression can appear in statement position. `proc`/lambda bodies only run when called, so these `def`s are indexed even though they're not guaranteed to execute — same documented approximation the extractor already makes for `include Foo if enabled?` and an `if false` branch — since the captured definee (the enclosing class or module) is still known statically. The explicit-receiver skip for `class_eval`/`instance_eval`/etc. (`Other.class_eval { … }` must not attribute its methods to the enclosing scope) is unchanged and still applies through the new descent. A third reachability gap sat one level further in: **method bodies** were never traversed for definitions at all, regardless of blocks — `def install; [1].each { def generated; end }; end` never produced `C::generated`, even though `C.new.install` genuinely defines that instance method. Neither `visit_method` nor `visit_singleton_method` opens a definition scope (no `node_stack`/`class_depth`/`singleton_scope` change), so a nested `def` simply follows Ruby's cref once the body is visited: it lands on the *instance* side even when the enclosing method is itself a singleton method (`def self.install; [1].each { def m; end }; end` defines an instance method, not a class method — confirmed against Ruby 3.4.7), and on the *class* side when the enclosing method is inside `class << self`. A method body does get its own **fresh public visibility frame**, unlike a block body, which inherits one: a class-body `private` does not reach a `def` written inside a method body, and a bare `private` inside one cannot leak back out past `end` — the one place block and method bodies deliberately differ, both directions probed against Ruby 3.4.7. This also means the `include`/`prepend`/`extend`/visibility directive dispatch described below now reaches into method bodies too — correct and needed for the singleton/class-method cases (`def self.setup; class_eval { include Mixin; def m; end; private; def n; end }; end`), and only over-permissive for a receiverless directive written directly inside a plain instance-method body, where `self` is the instance rather than the class, so that particular form would raise at runtime and can't occur in code that actually runs. Three follow-on classification bugs in this same block-scope machinery, all confirmed against Ruby 3.4.7: first, `visit_block_body`'s explicit-receiver skip was too broad — right for an unresolvable receiver (`Other.class_eval`), but wrong when the receiver names the *current definee* (`self.class_eval { def x; end }` and `C.class_eval { def y; end }` inside `class C` both define `C#x`/`C#y`, and were previously dropped, since a receiverless call **is** `self.<call>` and must classify identically in every scope). A new `classify_block_receiver`/`BlockReceiver` distinguishes `Current` (no receiver, or `self`) from `EnclosingConstant` (the constant naming the *innermost* enclosing class/module — `matches_enclosing_scope_path` already had exactly the right suffix-at-innermost-only semantics, reused as-is) from `Unresolvable` (everything else, including a constant naming an outer-but-not-innermost scope, which `qualified_prefix`/`parent_node_id` cannot address). The two are not interchangeable inside `class << self`: `self.class_eval`/`class_eval` there still define a *class* method (ambient `singleton_scope` stays `Enclosing`), but `C.class_eval` names the class itself and must define an *instance* method, so `visit_block_body` now forces `singleton_scope` to `Outside` specifically for a `ReceiverBody` block with an `EnclosingConstant` receiver. Second, `classify_block_scope`'s catch-all wrongly assumed every unlisted method preserves the definee, which is false for the class-factory family — `Class.new`, `Module.new`, `Struct.new`, and `Data.define` all put the block's defs on the brand-new anonymous object they create and leak nothing to the enclosing scope (`K = Class.new do; def x; end; end` previously fabricated a top-level `Function` named `x` that Ruby never defines anywhere reachable, corrupting `callers`/`impact`/`dead_code`). A new `BlockScope::Opaque` variant, selected by receiver *and* method name together (never `"new"` alone — `class C; Foo.new { def x; end }; end` is an ordinary block and genuinely defines `C#x`), makes `visit_block_body` skip these outright instead of mis-attributing them. Synthesizing a class scope for `K = Class.new do … end` is a real feature (`visit_assignment_for_const` currently emits a `Const` node, not a class node) left as a follow-up, not attempted here. Third, `classify_block_scope` keyed the `Concern` hooks (`included`/`prepended`/`class_methods`) on method name alone, ignoring the receiver — so ordinary Ruby that happens to reuse one of those names (`registry.included { def generated; end }`) classified as `ReceiverBody`/`ReceiverSingleton`, `registry` then classified as `Unresolvable`, and the explicit-receiver skip discarded the whole block. Unlike `class_eval`/`module_eval`/`instance_eval`/their `*_exec` forms — real `Module` methods that retarget the definee for *any* receiver — `included`/`prepended`/`class_methods` carry no intrinsic scope-changing semantics in Ruby; only `Concern`'s own receiverless DSL form (`Current`/`EnclosingConstant`) does, and on any other receiver they're ordinary calls whose block inherits the enclosing cref, exactly like `each`/`tap`. The three Concern arms now additionally gate on the resolved `BlockReceiver`, falling through to `Inherit` on `Unresolvable` instead of `ReceiverBody`/`ReceiverSingleton`, so the block is traversed with the definee unchanged rather than dropped. `class_methods` on a non-DSL receiver still lands on the *instance* side, not the singleton side, matching the DSL form's own probed behavior. A fourth classification bug, adjacent but distinct: nothing verified the enclosing module was actually an `ActiveSupport::Concern` at all, so a receiverless call named `included`/`prepended`/`class_methods` was treated as scope-changing DSL even with no Concern in sight — `class C; def self.class_methods(&block); block.call; end; class_methods { def generated; end }; end` wrongly registered `generated` as a singleton method (Ruby 3.4.7: it's an instance method), and a receiverless `included` under `private` wrongly got a fresh public frame instead of inheriting the surrounding `private` (also probed against Ruby 3.4.7). A receiverless call to any of these three names raises without something making it work (`Module#included`/`#prepended` require a mandatory arg; `class_methods` is otherwise undefined), so a new `ExtractionState.in_concern_scope` flag now requires positive evidence before the DSL classification applies: either an `extend ActiveSupport::Concern` seen earlier in the same module body (`visit_mixin_directive`), or a receiverless `concern`/`concerning` block (Rails' `Module#concern`/`#concerning` build a module pre-extended by Concern with no visible `extend`, confirmed end-to-end against activesupport 8.1.3), reset on entry to each nested class/module so it can't leak from an outer scope. `class_eval`/`module_eval`/`instance_eval`/their `*_exec` forms are untouched — they're real `Module` methods and need no such evidence. `in_concern_scope` is additionally scoped by what `self` denotes in the current body, mirroring `visibility_mode`/`singleton_scope`: reset on entry to a plain `def` or `class << self` body (where a receiverless `extend ActiveSupport::Concern` would target the instance or the singleton class, not the enclosing module, and `class_methods` raises there), but inherited into a `def self.x` singleton-method body (where `self` is still the module and the DSL genuinely works) and always restored on exit, so evidence picked up while statically walking any of these three body kinds can't leak into sibling statements. `self` inside a plain instance-method body is treated as an unnamed instance rather than the enclosing class, so a nested `def self.foo`, `class << self`, or `instance_eval` there is no longer misattributed as a class method; and `concern`/`concerning` blocks are only recognized when their topic is a literal naming a valid constant, not by call name alone.

### Fixed
- **Stable preferences and machine-local state no longer regress when multiple tokensave processes save concurrently.** Routine state persistence leaves `config.toml` untouched unless stable preferences changed, while locked baseline merging preserves pending-token deltas, cache freshness, intentional version downgrades, and concurrent agent changes; a deleted `state.toml` is safely recreated without resurrecting stale values.
- **Grep guardrails now recognize the absolute (or `~`-rooted) spelling of the indexed project root, which used to slip through while `.` was redirected.** Agents commonly write the repository target in full (`rg -n handle_request /Users/me/projects/app`, or `path: "/Users/me/projects/app"`), and the classifier only matched `.`, `./`, an omitted target, a recognized code directory basename, or a supported source extension; for an absolute directory it compared just the last path component, so any repository whose folder name is not a known code directory produced no savings. The hook now also treats a target whose canonical path equals the canonical project root as the same whole-project search as `.`. Deliberately exact, and never string-prefix based: a sibling such as `<root>-old`, a parent, and every path inside the project (`<root>/docs`) keep their current behavior, an explicit `type` filter or file glob still wins over the path, and an unknown root, unknown home directory, or any path that cannot be canonicalized fails open. Only absolute (or `~`-rooted) spellings reach the new comparison, so a relative detour such as `../..` keeps its previous handling and the two extra `canonicalize` calls stay off the common path.
- **The Bash grep guardrail's shell tokenizer no longer strips backslashes on Windows, which had silently disabled the absolute-root redirect added above.** `shell_split` treated `\` as a POSIX escape character in unquoted tokens, so a Windows absolute target such as `C:\Users\me\project` was mangled to `C:Usersmeproject` before the classifier ever saw it; `canonicalize` then failed, the hook returned allow, and the test for an absolute project root panicked with an EOF-on-empty-JSON. On Windows `\` is now a literal path separator in the unquoted context (matching cmd.exe and PowerShell semantics), while the double-quote behavior — `\` escapes only before `"`, `\`, `$`, and `` ` `` — is unchanged on every platform. Unix builds are untouched.

### Removed
- **`tokensave_context` no longer advertises a call budget.** Its description carried a `CALL BUDGET: N calls maximum for this project` line, with N picked from a node-count size tier (3 for the smallest projects, 10 for the largest). The intent was to stop an agent re-querying the same question with slightly different wording, but the cap fired on the wrong cases: a genuinely multi-part question in a small repo hit "3 calls maximum" long before it was answered, and the instruction to "synthesise from what you have — do not exceed the budget" pushed the agent to guess rather than to look. It was never enforced server-side either — nothing rejected call N+1 — so its only effect was talking a well-behaved agent out of the tool that exists to be used. It also cost the very tokens it was meant to save: because the budget was derived from the node count, its value moved whenever the project grew across a tier, and tool descriptions live inside the system prompt — so a re-tiered project rewrote the cached prefix and forced the client to discard and rebuild its prompt cache. That is the same failure #260 fixed by removing the node count from this text; the budget was the remaining graph-derived value in it. The description is now a plain statement of what the tool returns, and `tools/list` serves the same static definitions as every other tool, so the whole size-tier lookup and the graph-stats query behind it are gone.

## [7.8.0] - 2026-07-26

### Added
- **`tokensave upgrade` now offers to kill the tokensave processes that are still running, and takes `--kill` to do it without asking.** The processes in question are almost always MCP servers a running agent spawned: they hold the old binary's DB handles open and keep serving code that no longer matches the on-disk index after the upgrade, which is why the guide has always told you to restart your agent afterwards. `upgrade` now lists every other tokensave process by pid and command line and asks once, right after it has confirmed a newer version exists — so an already-up-to-date run never prompts, and neither does a machine with nothing else running. `--kill` skips the question for scripted upgrades. A non-interactive run without `--kill` prints the list, warns, and continues rather than blocking on a prompt nobody can answer. On Homebrew installs the check runs before delegating to `brew upgrade`, since Homebrew decides for itself whether there is anything to do. A process that exits between being listed and being signalled counts as killed, not as a failure.
- **The `tokensave` entry plank writes now carries `primaryTools`, capping what plank inlines into its system prompt.** Plank expands the full JSON schema of every advertised MCP tool into its system prompt; with all ~80 tokensave tools that is roughly 66 KB of schema, enough prompt bloat that plank's model starts emitting malformed tool calls — which surfaces as an apparent connection failure rather than as the prompt-budget problem it is. `install --agent plank` now writes a six-tool primary set (`tokensave_context`, `tokensave_search`, `tokensave_read`, `tokensave_callers`, `tokensave_callees`, `tokensave_status`); every other tool remains reachable through plank's compact one-line-per-tool directory and its `mcp_describe`, so nothing is lost, only deferred. A user's own non-empty `primaryTools` list is preserved across reinstalls and upgrades; an absent key, a non-array value, or an empty array is replaced with the default, since an empty array is indistinguishable from a botched edit. `tokensave doctor` reports the tool count and warns when an entry written before this change has no `primaryTools`.

## [7.7.0] - 2026-07-26

### Added
- **Plank coding agent integration.** `tokensave install --agent plank` registers the MCP server in Plank's config — `~/.plank/.mcp.json` for the user scope, `./.mcp.json` with `--local` for the project scope, matching Plank's own two-layer MCP precedence. Install and uninstall read-modify-write only the `tokensave` server entry, so other configured servers and unrelated top-level keys survive both; uninstall deletes the file only when tokensave was its sole entry. Brings the supported-agent count to 20.
- **Companion Markdown documentation is now indexed and retrievable, so an agent can read a short summary instead of a 3000-line class (#154, phase 1).** Two conventions are discovered, deliberately both: **sidecar** files (`BigClass.cs` alongside `BigClass.readme.md`) need zero configuration and travel with the code in review, and a **docs directory** (`tokensave-docs/` by default, relocatable via the new `docs_dir` config field, or disabled by setting it empty) holds docs whose YAML front matter carries an `applies_to` glob list — one instructions file covering a whole family of files, which is the `**/*.es8.cs` vs `**/*.es7.cs` case from the issue. Both resolve to the same internal mapping and are stored as `Doc` nodes with `Documents` edges to the covered `File` nodes, so coverage is queryable through the existing graph rather than a parallel subsystem. A new `tokensave_doc` tool returns a file's documentation with its content, the full set of files that doc covers, and a drift signal; and `tokensave_entities` now reports `has_doc`/`doc_path` so an agent sees a summary exists *before* deciding to read the file. Drift is detected by comparing the doc's last commit against its covered files' — code committed after the doc sets `doc_stale: true`, and without git history the field is `null` (unknown) rather than a confident "not stale". Docs covering no files are dropped rather than indexed, since a glob can legitimately go empty when files are removed. Front-matter parsing is hand-rolled (one key holding a list of strings does not justify a YAML dependency) and degrades to "covers nothing" on anything it cannot read, never a hard error; it tolerates CRLF, a BOM, and both inline and block sequence forms. Section-level anchors — line ranges or `m:MethodName` symbol references — are deliberately **not** in this phase: whole-doc granularity ships first so the anchor design can be settled against real usage, per the reporter's own read that phase 1 covers most of the need.
- **`tokensave_context` entry points now show the first line of the symbol's docstring** beneath the signature. The one-line summary often answers the query outright, saving the follow-up code fetch; entries without a docstring are unchanged.
- **Ruby `include`/`prepend`/`extend` of a named module now emit a resolvable `Implements` edge from the including class or module.** These mixin keywords weren't handled at all — `include Comparable` parsed as an ordinary `call` node and was silently dropped — so a module's methods never showed up as part of the including class's API, a module included by many classes showed zero inbound relationships, and `dead_code`/`impact`/`module_api`/`type_hierarchy` saw no Ruby composition whatsoever (`ActiveSupport::Concern`, `Comparable`, `Enumerable`, every `app/models/concerns/*` file). The extractor now recognizes a receiverless `include`/`prepend`/`extend` call inside a class or module body and emits one `Implements` ref per `constant`/`scope_resolution` argument (`include A, B` emits two; `include ActiveSupport::Concern` resolves the scoped name); a dynamic argument (`extend self`, `include some_variable`), an explicit receiver (`mod.include Bar`), or a top-level `include` (which mixes into `Object`, with no class/module node to attach to) are left unhandled rather than fabricating a ref. Resolving these refs required a new arm in the resolver's `kind_compatible` gate: a Ruby `Implements` ref (`lang_from_path(&uref.file_path) == "ruby"`) now resolves *exclusively* to `NodeKind::Module` — not the shared `Trait | Interface | InterfaceType | Class | InnerClass | AbstractMethod | SealedClass | Annotation | TypeAlias` list every other `Implements`/`Extends`/`DerivesMacro` ref uses. The rule is exclusive rather than additive because Ruby itself forbids mixing in a class (`include SomeClass` raises `TypeError: wrong argument type Class (expected Module)`) — an additive rule would let a same-named `class Foo`/`module Foo` pair resolve to whichever sorts first in the resolver's internal suffix index, and would also (incorrectly) let `Extends`/`DerivesMacro` target a `Module`, neither of which Ruby ever emits. `extend`'s singleton-method semantics and `include`/`prepend`'s instance-method semantics both produce the same `Implements` edge kind for now — a distinct edge kind is a separate follow-up. `attr_*`, `alias`/`alias_method`, `define_method`, `module_function`, and `ActiveSupport::Concern`'s `included do ... end` block remain unaddressed. A mixin (or any `def`/`class`/`module`/constant) nested inside a conditional, `case`, loop, or `begin`/`rescue` in a class or module body — e.g. `include Foo if enabled?`, an `if RUBY_VERSION > "3" ... else ... end` guard, or `begin; include M; rescue LoadError; end` — is extracted exactly as if written directly in that body, since the extractor now recurses through these statement containers instead of dropping their contents; method bodies are still not traversed this way. A `private`/`protected`/`public` visibility directive nested the same way now also applies past the end of its container, matching Ruby's own runtime scoping (a `private` inside `if legacy?; end` switches the mode for everything that follows the `end`).
- **`TOKENSAVE_DISABLE_SERVER=true` is the canonical per-project MCP server opt-out.** The server still exits cleanly before project initialization, and a source-level regression test now requires future tokensave-owned environment variables to use the `TOKENSAVE_` namespace.

### Changed
- **Truncated `tokensave_context` code blocks now end with a follow-up handle instead of a dead-end ellipsis.** A snippet cut at `max_code_block_size` closes with `... [truncated — full body: tokensave_body node_id=<id>]`, so the caller can fetch the remainder in one call rather than re-deriving which symbol the fragment belonged to.

### Deprecated
- **`DISABLE_TOKENSAVE=true` remains supported as a compatibility alias with no scheduled removal.** Existing configurations continue to disable the MCP server unchanged; new configurations should use `TOKENSAVE_DISABLE_SERVER`.

### Fixed
- **Global agent setup now warns before a Cargo build-output path becomes a persistent command.** Running `install`, `reinstall`, or an automatic agent resync from `target/debug` or `target/release` still preserves the exact invoked binary, but now names the path and explains that `cargo clean` or worktree removal will break the configured hooks and MCP servers. Stable installs, project-local installs, and the Homebrew Cellar-path rewrite are unchanged.
- **The reference resolver no longer holds three copies of the node graph, cutting the sync/serve memory peak by roughly 2 KB per indexed node (#253).** `ReferenceResolver::from_nodes` built its lookup caches by cloning: every node was cloned once into `name_cache` and a second time into `qualified_name_cache`, the suffix index allocated a *full copy of the qualified name per `::` segment* (a path like `a::b::c::d` cost four), and `known_names` cloned every key of all three maps on top of that. Because the caller's `Vec<Node>` stays live alongside them, peak RSS was several multiples of the graph itself — the reported 8-10 GiB kills on a ~4.3M-node project, which the OOM killer took down along with the rest of the desktop session. This was never a leak, which is why it looked like one only under `sync`: it is a transient peak, entered by any sync that indexes at least one file, including the startup catch-up sync, and released the moment resolution finishes. The caches now borrow from the caller's slice (`HashMap<&str, Vec<&Node>>`, `HashMap<&str, Vec<&str>>`, `HashSet<&str>`), so construction allocates the index structure and nothing else. Resolution results are byte-identical — candidate ordering still follows node order, `find_best_match` scores the same set, and the whole suite passes unchanged. Measured on this repo (403 files, 15,595 nodes): peak RSS 353.5 MB → 322 MB. Remaining follow-ups, not addressed here: the caller's full `Vec<Node>` is still materialised at once, and several analysis handlers independently call `get_all_nodes()`.
- **`resolve_db_for_branch` no longer opens a hardcoded `tokensave.db` when falling back to the default branch (#300).** Both the detached-HEAD arm and the last-resort arm reported serving `meta.default_branch` but ignored that branch's actual `db_file` in `branch-meta.json`. On a project whose default branch is mapped to `branches/<name>.db` — the normal layout once the default branch has been through `branch add` — both arms opened a wrong or nonexistent DB while claiming to serve the default branch, so queries silently ran against an empty or stale index. Both now resolve through `branch::resolve_branch_db_path`, keeping `tokensave.db` only as the genuine last resort when branch metadata has no entry for the default branch. Same class of bug as ac3db6c, which fixed the equivalent hardcoding in the `pr_context` handler. The conventional layout, exact-match, and no-metadata single-DB paths are unchanged and now covered by regression tests.
- **Prose documentation files no longer outrank code in `tokensave_search` and `tokensave_context` results.** Markdown headings are indexed as `Module` nodes with `Pub` visibility, so the kind (1.2×) and visibility (1.5×) boosts handed doc headings a 1.8× multiplier over neutral code that merely shares vocabulary with the query — a search for a config symbol could put `CHANGELOG.md` above the defining module. `path_rank_multiplier` now applies a 0.3× factor to doc extensions (`md`, `markdown`, `mdx`, `rst`, `adoc`), between the existing fixture (0.1×) and test (0.4×) factors, and shared by both ranking paths. Vendor classification still wins for docs inside vendored trees. Proportional, not a filter: an exact heading match still surfaces, and when docs are the deliberate target (a `path_include: ["docs"]` filter, or a docs-only repo) every surviving candidate shares the same factor, so their relative order is unchanged.
- **Literal search (`tokensave_search` with `literal: true`) now honors `.tokensave/queryignore` like the ranked path.** The literal branch returned before the ranked path's queryignore filter ran, so paths a project had explicitly suppressed (#116) still appeared in literal matches. The indexed file list is now filtered with the same patterns before scanning — the same gap-closure shape as the `path_include`/`path_exclude` literal-mode fix in #258.

## [7.6.2] - 2026-07-25

### Added
- **CUDA/HIP `.cu`/`.cuh` files are now indexed (`lang-cuda`, `full` tier).** These extensions had no registered extractor, so every `.cu`/`.cuh` file indexed as zero symbols regardless of size. CUDA is a C++ superset, so a new extractor delegates to `CppExtractor` — the same pattern as Metal, no new grammar dependency. The one exception is `<<<grid, block>>>` kernel-launch syntax, which derails tree-sitter-cpp badly enough to drop the *enclosing function* entirely; every `<<<...>>>` span is now blanked to same-length spaces before parsing, the same technique the GLSL/Godot extractor uses for its own dialect syntax.

### Fixed
- **`tokensave_pr_context` no longer fails with `git error: cannot resolve 'main'` on repositories whose default branch isn't named `main`.** The handler hardcoded `main` as the fallback base ref instead of reading the indexed `default_branch` from `branch-meta.json`, which is what `handle_branch_diff` already does — so on a `master`-default repo (or any other name) every call that fell back to the default base errored out. The fallback now resolves from branch metadata, with `main` kept only as a last resort when no metadata is present.
- **The `tokensave init` warning for a directory outside a git repository no longer claims that `.gitignore` filtering is off, because it isn't (#283).** The scanner registers `.gitignore` as a custom ignore filename (`add_custom_ignore_filename` in `scan_files_with_gitignore`), so ignore files are read directly rather than through git repository discovery and their patterns apply with or without a `.git` directory — the reporter's TFVC-managed project was already getting the filtering the warning told them they had lost. What a non-repo tree actually loses is the exclude sources Git itself supplies: `.git/info/exclude` and the user's global excludes file. The warning is now written from the project's real state: with a root `.gitignore` it says those patterns still apply and names what doesn't, and without one it keeps the large-tree caution (#174) that motivated the warning, since nothing then constrains the walk, and points at the two remedies — add a `.gitignore`, which works without git, or set `exclude` globs in `.tokensave/config.json`. A regression test indexes a non-git fixture and asserts a `.gitignore`'d directory is skipped while its siblings are indexed, so the wording and the scanner can't drift apart again.
- **`tokensave init` no longer claims to have excluded `.tokensave` from git when it did nothing, and no longer reads stdin when it isn't a terminal (#288).** In a directory with no git repository, `init` printed `warning: could not locate .git/info/exclude` immediately followed by `Added .tokensave/ to .git/info/exclude (local, untracked)`: `add_to_git_info_exclude` and `add_to_gitignore` warn and bail on every failure path but returned `()`, so the caller announced success unconditionally — a failed write, an unlocatable git dir, or a missing parent directory all reported the same. Both helpers now return whether the entry is present (an already-present entry counts as success, since the desired end state holds) and the caller reports only what actually happened. Two adjacent problems in the same block are fixed alongside: the prompt is skipped entirely outside a git working tree, where neither answer has any effect, and it is now guarded by `stdin().is_terminal()` like the equivalent prompt in `main.rs`, so a scripted or agent-driven `init` no longer silently consumes a line of the caller's stdin and takes it for an answer.
- **Grep guardrails now honor an explicit file glob before a broad search path.** Claude and Factory Droid can send both a root such as `path: "."` and a narrower `glob` / `glob_pattern`; the shared classifier previously discarded the glob whenever `path` was present, so a legitimate Markdown search such as `**/*.md` was hard-blocked as a code search. Explicit code-only globs still redirect, mixed code/non-code globs pass through, unclassifiable globs fall back to the existing path rule, and an explicit `type` filter remains highest priority.
- **C++ declarations wrapped in `extern "C"` are indexed again, instead of silently vanishing with zero symbols and no error.** Both the single-declaration and block forms parse as a `linkage_specification` node, which had no case in the extractor's dispatch — a pattern near-universal in C/C++ interop headers. `linkage_specification` (and its block-form `declaration_list` body) now dispatch through the existing generic child-walk; namespaces already unwrap their own `declaration_list` before dispatch, so this can't double-visit them.
- **godot-cpp `_bind_methods` bindings now link the C++ method they expose to GDScript, so bound methods stop showing 0 callers / 0 impact and the binding boilerplate stops being reported dead (#269).** A Godot 4 GDExtension registers C++ methods for GDScript inside `_bind_methods` via `ClassDB::bind_method(D_METHOD("register_item", "cfg"), &MyRegistry::register_item)` (and the `bind_static_method` variant), and GDScript then calls them as ordinary methods (`MyRegistry.get_singleton().register_item({...})`). The C++ call-site walker only recorded each `call_expression`'s *callee* — `ClassDB::bind_method`, `D_METHOD` — none of which resolve to an indexed node, and it never followed the pointer-to-member *argument* `&MyRegistry::register_item`, so the bound method received no incoming edge from its own registration: `callers` came back empty, `impact` reported one node and zero edges, and `dead_code` flagged the method. `_bind_methods` itself, being invoked by the engine's `ClassDB` machinery rather than by any indexed code, was likewise reported dead. The C++ extractor now recognizes a `bind_method` / `bind_static_method` call (matched on the callee's final `::` segment, so both the bare and `ClassDB::`-qualified spellings are covered) and emits a `Calls` reference from the enclosing function to each pointer-to-member argument's qualified method name (`MyRegistry::register_item`), which the resolver links through its existing qualified-name match; only `&Class::method` pointer expressions are followed, so `DEFVAL(...)` default-argument expressions in the same call are not mistaken for bound methods. This alone clears the bound-method false positive (the method now has an incoming edge from `_bind_methods`) and gives the `_bind_methods → method` edge; no resolver change is needed for the GDScript side, because a GDScript call already resolves to the C++ method by its short name (the `D_METHOD` exposed name matches the method name by godot-cpp convention) and the singleton accessor becomes live naturally once `get_singleton()` resolves. Separately, `dead_code` now exempts any node named `_bind_methods` as an engine-contract entry point, alongside `main` and `test*`. Scope: name-based linking only, matching the reporter's proposal — no type inference, and `ADD_SIGNAL`, `bind_vararg_method`, and property (`ClassDB::add_property`) bindings are intentionally left out.
- **`.gdshader` node signatures keep the original Godot hint/qualifier text instead of the blanked copy fed to the parser (#272).** The Godot-dialect support shipped in 7.6.1 (#270, #275) rewrites `: hint_…` clauses and `global`/`instance` qualifiers into same-length spaces so the vanilla GLSL grammar can parse the file — but it then extracted every node's signature and excerpt from that rewritten copy, so `uniform sampler2D albedo : source_color;` indexed with the hint text silently dropped from its signature. Extraction now parses the normalized copy while reading all node text from the original source — the two are byte-length- and line-identical by construction, so tree-sitter's byte ranges index both interchangeably — and a hinted or `global`/`instance` uniform's indexed signature shows the real declaration text. Ported from PR #272 by @janstol, whose independently developed `.gdshader` support crossed mid-air with #275; the surviving delta (original-source signatures, its regression fixture, and the `MORE-LANGUAGES-SUPPORT` documentation note) is landed here with attribution.
- **Ruby extraction now tracks `private`/`protected`/`public` method visibility instead of marking every symbol `Pub`.** The Ruby extractor previously hardcoded `Visibility::Pub` on every extracted node, so private helper methods showed up as public API — distorting dead-code detection, API-surface reporting, and impact analysis, all of which key off `visibility`. The extractor now tracks a per-class/module visibility mode switched by bare `private`/`protected`/`public` statements, retroactively applies the symbol-list form (`private :foo, :bar`) and the singleton-method forms `private_class_method :foo` / `public_class_method :foo`, and extracts the inline form (`private def foo; end`, previously dropped entirely since `call` nodes weren't visited). `protected` maps to `Visibility::Private` (the enum has no `Protected` variant) since both mean "not public API." Visibility mode resets at each class/module body and does not leak across sibling classes.
- **Ruby `class << self` method bodies are no longer dropped from the graph, and singleton methods are now attributed to the receiver they actually belong to.** The extractor's `visit_node` dispatch had no arm for the `singleton_class` node kind produced by `class << self` (or `class << expr`), and its fallback didn't recurse — so every method defined inside one of these blocks, an extremely common Rails idiom for declaring class methods, was silently absent: no node, no `Contains` edge, no call sites, no callers, with `dead_code`, `module_api`, `impact`, and `callers` all reporting wrong results as a consequence. `class << self` is now walked as the enclosing class's singleton scope: `def foo` inside it is indexed as `NodeKind::Method` with the same qualified name `def self.foo` would produce (no separate scope is pushed, so `Contains` still comes from the enclosing class), and it's registered alongside `def self.foo` methods so `private_class_method :foo` can retroactively target it. The block gets its own visibility scope, starting public and not leaking in either direction, matching how class/module bodies already behave — a bare `private` inside the block privatizes the class methods that follow it, and `private :foo` inside the block correctly marks the singleton rather than missing (there is no instance-method node for it to fall back to). Three follow-on receiver-attribution bugs found during review of this change are fixed in the same commit: (1) `def obj.foo` was unconditionally registered as the enclosing class's singleton regardless of receiver, so `private_class_method :foo` (or, worse, an ambiguous match against a same-named `def self.foo`) could rewrite an unrelated object's method — singleton methods are now registered only when the receiver is a literal `self` or the constant naming the enclosing class/module (`def Report.generate` inside `class Report` counts as `self`); anything else is tracked separately and excluded from *both* the instance and class-method visibility lookups, so it can't be mis-targeted by either. (2) the singleton scope used to leak into `class`/`module` bodies nested inside `class << self`, so their ordinary instance methods were wrongly registered as the enclosing class's singletons and could be hit by a singleton visibility directive; `class`/`module` traversal now resets the scope for its own body and restores it afterward, matching how `visibility_mode` and `class_depth` already behave. (3) `class << other` nested inside `class << self` used to inherit the outer scope instead of being evaluated on its own receiver; the scope is now assigned unconditionally on every `class << expr`, so a foreign nested singleton class no longer inherits an outer `self`. `class << some_object` (a non-`self` receiver) still has its body visited so methods aren't dropped, but isn't registered as a singleton of the enclosing class, since the receiver can't be resolved and doing so could let an unrelated `private_class_method` match the wrong node. Two further receiver-attribution bugs found in review of this same change are fixed alongside it: (4) a literal `self` was treated as the enclosing class unconditionally, but inside a `class << …` body `self` *is* the singleton class, so `def self.foo` written there defines a method one level further out (`Report.singleton_class`, not `Report`) — `is_enclosing_receiver`'s `"self"` arm now checks `singleton_scope == Outside`, so a `private_class_method :foo` at the `Report` level no longer reaches into a nested `class << self` and privatizes a method that was never `Report`'s; the `"constant"` arm is unaffected, since `def Report.generate` genuinely is `Report.generate` regardless of singleton scope. (5) a visibility directive written *inside* a foreign singleton body (`class << config; private :process; end`) used to exclude the foreign node correctly but then fall through to the *next* same-qualified-name match — the enclosing class's own instance method — silently flipping an unrelated public method to private; the two-bucket `want_singleton: bool` selector is replaced with a three-way `VisibilityTarget` (`Instance` / `EnclosingSingleton` / `Foreign`) so a directive can only ever mark the bucket it was actually written against. (6) `is_enclosing_receiver` only recognized `self` and a bare `constant` receiver, so a qualified constant (`class << Outer::Inner`, tree-sitter-ruby's `scope_resolution` node) always fell through to `Foreign` even when it named the exact class/module it appeared inside, leaving `private_class_method :foo` unable to reach methods defined that way — an idiom Ruby genuinely privatizes. The `"constant"` arm now also accepts `"scope_resolution"`, matched against the enclosing class/module scope chain via a new `matches_enclosing_scope_path` helper: a relative path (`Outer::Inner`, or a partial suffix like `B::C`) matches if it equals any suffix of the enclosing scopes at node-stack-entry granularity (never splitting a single scope's own name on `::`), and a leading `::` anchors the match to the *whole* scope chain, since `::B` names a different, top-level object from a same-named nested `A::B`. Known remaining gaps: a `class << self` written *inside a method body* still has its defs dropped, since only call-site extraction (not the main definition walk) descends into method bodies; and node identity is unchanged — `def self.meta_only` inside `class << self` still gets the lexical qualified name `Report::meta_only` (there's no node representing "Report's singleton class" to attach it to instead), so the fix works by having the id lists decide what a directive marks, not by giving the node a more accurate name.
- **Compact Ruby class/module definitions (`class Outer::Inner`, `module A::B`) indexed as `<anonymous>`, so every method inside one was qualified `file::<anonymous>::foo` instead of `file::Outer::Inner::foo`.** `visit_class`/`visit_module` found the name by searching `node`'s *direct* children for a `constant` node, but tree-sitter-ruby types the name field as `choice($.constant, $.scope_resolution)` — a compact definition's name is a `scope_resolution` node, which has no direct `constant` child of the class/module node itself, so the search always came up empty. Both now read the unambiguous `name` field directly instead, which covers both node kinds and, per the field's text, records the class/module's `name` as the full path (`"Outer::Inner"`) rather than just its last segment — the same convention `extract_superclass` already uses for `class Foo < Bar::Baz`. `class Foo` and `class Foo < Bar` are unaffected: the field still resolves to the same `constant` node the old child search found. This is what makes `class << Outer::Inner` reach the enclosing class in a *compact* definition: no receiver text could ever equal `<anonymous>`, so this bug was blocking the qualified-receiver fix above for the compact form specifically, even once that fix landed.

## [7.6.1] - 2026-07-24

### Added
- **Minecraft datapack `.mcfunction` indexing (#262).** Java Edition datapack function files were filtered out before extraction because no extractor claimed the extension, so a datapack project indexed zero files and grew zero call edges. A new lightweight line-based extractor (no tree-sitter grammar needed — the format is one command per line with `#` comments and no sub-file structure) now treats each `data/<namespace>/function/<path>.mcfunction` file as a function-like node named by its resource location `<namespace>:<path>` (the file *is* the function; the legacy plural `functions` directory used before Minecraft 1.21 is accepted too, and nested directories become `/`-separated path segments). Call edges are created for `function <id>`, `execute … run function <id>`, `return run function <id>`, and `schedule function <id> …` commands, including on `$`-prefixed macro lines; namespace-less targets are normalized to the implicit `minecraft:` namespace so they resolve consistently. Macro-generated targets (`foo:$(name)`) and function tags (`#ns:tag`) are preserved verbatim as unresolved references — the standard representation for dynamic targets — so they never fabricate a false edge. A leading `#` comment block becomes the function's docstring. Ships in the `full` feature tier as `lang-mcfunction`.
- **Godot `.gdshader` / `.gdshaderinc` indexing (#270).** Godot 4 shader files were likewise rejected as an unrecognized extension, leaving uniforms like `wind_strength` invisible to indexed search with no client-side workaround. Godot's shading language is GLSL-family, so instead of bundling another grammar the existing tree-sitter GLSL extractor now claims both extensions and rewrites the Godot dialect to plain GLSL before parsing: `shader_type` / `render_mode` / `group_uniforms` / `stencil_mode` file directives, `global` / `instance` uniform qualifiers, and `: hint_…` clauses on uniforms (`hint_range(…)`, `source_color`, …, up to the default value or terminator) are blanked with spaces of identical byte length, so every extracted node position still matches the original file. The result: uniforms (including hinted ones), functions and their call sites, `#include` references, `varying`s, `const`s, and structs are all indexed, and the captured `shader_type` / `render_mode` directives are re-emitted as searchable const nodes carrying their full text.
- **Verbose sync and `sync --doctor` now explain files skipped for lack of an extractor (#262, #270).** Both issues hit the same diagnostic blind spot: `sync --verbose` reported only the count of accepted files, so an unsupported language was indistinguishable from a wrong project root, an ignore rule, or a stale index. The scanner now tallies every file it rejects because no registered extractor handles its extension, and `sync --verbose` / `init --verbose` print a per-extension summary (e.g. `.mcfunction: 12 file(s) skipped (no registered extractor)`, sorted by count, capped with a roll-up line to stay readable). The tally deliberately excludes known non-source extensions — images, media, archives, lockfiles, compiled artifacts, plain-data formats like `.json`/`.yaml` — and anything already covered by exclude globs or gitignore rules, so it highlights genuinely unsupported languages instead of drowning them in `.png`/`.lock` noise. The same summary is carried on `SyncResult.skipped_extensions` and printed by `sync --doctor`, where it appears even when no files changed.
- **`tokensave memory` reports per-instance RSS for every tokensave process on the machine, to diagnose the `serve` OOM kills (#253).** Long-lived `tokensave serve` daemons have been reported reaching 8–10 GiB RSS on very large repos (~4.3M graph nodes) and triggering system OOM kills, with a follow-up sighting of four *orphaned* serve processes — parent PID 1, ~9.2 GiB each, ~20% CPU, no MCP client attached. To tell the three failure modes apart — a transient peak during sync/reference-resolution, unbounded per-tool-call growth, and abandoned processes that never exited — every tokensave process now records its own resident-set size into a machine-global, fixed-size mmap table at `~/.tokensave/memory.mmap`, and the new `tokensave memory` command renders a plain-text table across all of them. The table mirrors the `tokensave monitor` architecture: a 64-slot instance table keyed by PID, written best-effort under an `fs2` exclusive lock (any failure is silently swallowed so telemetry can never break the primary workload), reusing the `sysinfo` dependency already used for self-sampling in `runtime_telemetry`. Each slot carries PID, parent PID, start/last-update timestamps, command kind (`serve`/`sync`/`index`), project folder, current and peak RSS, the current phase, the phase during which the lifetime peak was observed (`peak_phase`), a samples counter, and the last known graph node count. Samples are taken at startup (`start`), at the MCP tool-dispatch point (attributed to the tool name, so every handler that materializes the graph is covered at one site), and bracketing the suspected transient peak in indexing/sync — `sync:resolve:load_nodes`, `sync:resolve:build_caches`, `sync:resolve:done` around the `get_all_nodes()` + `ReferenceResolver::from_nodes` step that holds ~3× the graph resident at once. The report distinguishes **alive**, **dead** (the PID is gone — its recorded `peak_rss`/`peak_phase` is the forensic record an OOM-killed process leaves behind, so dead slots stay visible), and **orphan** (a still-running process reparented to init or whose recorded parent has died — the abandoned daemons from the report). Sizes render human-readably (`9.0 GiB`); `tokensave memory --clean` purges dead slots; the command always exits 0 so incident-response scripts can poll it safely. The instrumentation adds no new dependency and is not feature-gated (matching `monitor`), so it is present in every build tier.

### Fixed
- **`tokensave_context` could miss an indexed, directly relevant symbol for a natural-language task while returning only unrelated matches for the task's generic words — even though `tokensave_search` ranked that same symbol near the top (#264).** The context builder searches each conceptual term from the task prose with a bounded per-term FTS query, but that query applied its `LIMIT` **without** BM25 ordering — a deliberate guard against repository-wide sorting that overshot: for a generic term like "icon" the handful of returned rows were effectively arbitrary (ascending rowid, i.e. indexing order), so whether a strongly matching symbol ever entered the candidate pool depended on where it happened to sit in the index rather than on how well it matched. A top-level asset generator (`generateLauncherIcon`, `generateSplashScreen`, …) could thus lose every retrieval slot to incidental UI screens that merely shared words like "icon", "screen", or "logo", and no amount of downstream re-ranking could recover a candidate that was never retrieved. The bounded per-term query now orders by the same `bm25()` weights the ranked `tokensave_search` path uses before applying its small `LIMIT` — still bounded per-term work, since only rows matching that one quoted prefix term are scored, never the whole repository. Two adjacent retrieval gaps in the same pipeline are closed alongside: the candidate pool was filled by exhausting terms in prose order, so early broad terms could consume the entire pool cap before later, more discriminating terms ("favicon", "oauth") were ever searched — terms now fetch their ranked top-K up front and fill the pool round-robin, one candidate per term per round; and because the symbol FTS tokenizer (unicode61) does not stem, a prose verb like "generates" never prefix-matched indexed names such as `generateLauncherIcon` — a trailing plural/third-person `s` on a conceptual term now also folds into a second search term ("generates" → "generate") so verb-form prose reaches the camelCase identifiers it describes.
- **`cargo test --no-default-features --features lite` compiles and passes again.** Twenty-six per-language extraction test files imported feature-gated extractors (`LeanExtractor`, `RubyExtractor`, …) without a matching `#![cfg(feature = "lang-…")]` gate, and `tests/fixture_extraction_test.rs` / `tests/project_manifest_test.rs` exercised gated languages in ungated tests, so the lite tier's test build had been failing to compile at all — meaning the tier advertised as the minimal build was never actually test-verified. Every gated-language test file now carries the matching crate-level `#![cfg]`, mixed-tier files gate the affected tests individually, and the `project_manifest` unit tests use `python` (always-compiled lite tier) instead of `bash` (medium) as their override language, since the override mechanics under test are language-agnostic.
- **`tokensave_status` no longer reports an empty, whole-history-stale graph during a full reindex while search serves current data (#267).** `index_all()` (run in the background by the MCP server after a major version upgrade or schema change) commits its up-front `Database::clear()` and then spends seconds scanning, extracting, and resolving before the bulk re-insert lands. A `tokensave_status` call landing in that committed-empty window read `node_count`/`edge_count`/`file_count`/`last_updated`/`total_source_bytes` all `0` and `files_by_language {}` from the emptied `nodes`/`edges`/`files` tables — while `db_size_bytes` (SQLite doesn't shrink the file on `DELETE`) and the `last_sync_at`/`last_full_sync_at` metadata rows (untouched by `clear()`) stayed populated, and `tokensave_search` moments later returned accurate, current results once the rebuild finished. Worse, the staleness check computed `git_commits_since(last_updated)` with `last_updated == 0` (MAX of `files.indexed_at` over an empty table), which counts *every commit in the branch history* — the reporter's "149 commit(s) since last sync" warning. `handle_status` now flags the transient state with an `index_rebuild_in_progress: true` field (detected via the live sync lock: the `.tokensave/sync.lock` file exists and records a still-alive PID) instead of presenting the empty graph as the true index state, and suppresses the staleness warning while a rebuild is in flight. When the files table is empty for any reason (an interrupted rebuild whose lock is already gone), the staleness reference timestamp falls back from the zeroed `last_updated` to the `last_sync_at` metadata value, which survives `Database::clear()`, so `git_commits_since` can no longer count the whole history. Normal-path behavior is unchanged (`last_updated > 0` keeps using it). `tokensave_status` output also now carries a `version` field (`env!("CARGO_PKG_VERSION")`), a reporter side request.
- **`tokensave install --agent copilot` registered the MCP server in the wrong VS Code config file, so `doctor` permanently reported the install as missing even when Copilot had already picked the tools up (#266).** The installer wrote `mcp.servers.tokensave` into `User/settings.json`, which was VS Code's legacy/preview location for user-level MCP servers. Modern VS Code (1.102+, GA MCP) reads user-level servers from a dedicated `User/mcp.json` (top-level `servers` key) instead, and it actively migrates the old `settings.json` `mcp` key away on its own — on startup it silently moves any such entry into its internal MCP registry (which is what actually makes the server available to Copilot), and strips the key back out of `settings.json` once done. So the install could well have worked from VS Code's point of view; the real bug was that `doctor`/`has_tokensave` only ever inspected `settings.json`, and kept reporting the registration as permanently absent once VS Code had emptied out the very key they were checking — an unresolvable false-negative loop indistinguishable, from the outside, from a genuinely broken install. `install` now writes only `User/mcp.json` (and the VS Code Insiders equivalent, when present) and never touches `settings.json`, leaving any hand-maintained settings alone. `doctor`/`has_tokensave` check `mcp.json` first and fall back to a legacy `settings.json` entry for back-compat with pre-#266 installs, so either location passes; if both are registered, `doctor` now warns about the stale duplicate instead of silently ignoring it. `uninstall` cleans up both locations, so a leftover legacy registration doesn't linger after removal. `mcp.json` is now parsed as JSONC rather than strict JSON — like `settings.json`, VS Code allows comments and trailing commas in it, and treating those as a parse failure previously made `install` error out, and `doctor`/`has_tokensave`/`uninstall` silently treat the file as empty.
- **`config.toml` preferences (e.g. `wildcard_permissions`) silently reverted after being set by a concurrent process.** `UserConfig::save()` wrote its entire in-memory snapshot of `config.toml` as a whole-file overwrite, so a long-lived process (the `serve` MCP daemon, a git `post-commit`/`post-checkout` hook running `sync`) that had loaded the file before another process changed a preference — a manual edit, or `tokensave install --wildcard-permissions` — would revert that change back to the stale value on its next routine save. `save()` now re-reads the current on-disk `config.toml` and only overwrites the fields this process actually changed since its own `load()`, preserving whatever another process wrote for every field left untouched; the per-field baseline advances after every successful save instead of staying pinned to `load()` time, so a long-lived process that changes a field once doesn't keep treating it as perpetually modified and re-writing that stale value over later external changes to it. The on-disk read-modify-write span, in both `save()`'s merge and `load()`'s own config.toml read, is held under a new cross-process advisory lock (`config.toml.lock`, via the existing `fs2` dependency), since the in-process `SAVE_LOCK` alone doesn't order writes from a second *process*: without it, two concurrent processes could each merge against the same stale snapshot and lose whichever field the other had just changed, and `load()` could move aside a config.toml another process had just concurrently made valid, mistaking it for the corrupt content it had raced past. An unparseable or invalid-UTF-8 `config.toml`/`state.toml` is now also moved aside to a `config.toml.corrupt.<timestamp>.<pid>.<n>` / `state.toml.corrupt.<timestamp>.<pid>.<n>` backup on load instead of silently resetting every preference to defaults; this preservation resolves through symlinks first, so a dotfiles-managed `config.toml -> ~/dotfiles/config.toml` symlink survives (left dangling, ready for the next save to write through it again) instead of being detached from its target.

## [7.6.0] - 2026-07-22

### Changed
- **Claude Code and OpenCode rules now live in a tokensave-managed file instead of being appended to the user's own instructions (#256).** `tokensave install` previously appended a ~40-line "prefer tokensave tools" block directly into `~/.claude/CLAUDE.md` (Claude) and `~/.config/opencode/AGENTS.md` (OpenCode), guarded by a marker check that skipped the append once present — so the block never refreshed on upgrade even as the rule text improved, and uninstall had to locate and cut the block back out of a file the user otherwise hand-maintains. Rules now live in a dedicated file tokensave owns outright and rewrites unconditionally on every install: `~/.claude/rules/tokensave.md` for Claude (global) / `<project>/.claude/rules/tokensave.md` (local), auto-loaded by Claude Code's existing `rules/*.md` convention with no CLAUDE.md edit needed; and a colocated `tokensave.md` for OpenCode (global: `~/.config/opencode/tokensave.md`, local: `<project>/tokensave.md`), wired in via a deduped entry in `opencode.json`'s `"instructions"` array. CLAUDE.md/AGENTS.md are no longer touched by a fresh install. Installing over a pre-#256 setup migrates automatically: the old inline block is stripped from CLAUDE.md/AGENTS.md and the managed file takes its place, so upgraders end up with the rules in exactly one location. `tokensave doctor` now checks for the managed file (and, for OpenCode, the `instructions` entry) instead of scanning CLAUDE.md/AGENTS.md for the old marker text. Managed-file writes and the CLAUDE.md/AGENTS.md migration step are atomic (temp-file + rename, backed up first) so a failed write can't truncate existing rules, migration failures now fail the install instead of being silently swallowed, and uninstall preserves a symlinked managed file (removing its real target) instead of deleting the symlink and orphaning the target — the same symlink-safety guarantee also covers the CLAUDE.md/AGENTS.md migration step's empty-file cleanup, so a symlinked legacy instructions file survives migration too. Global OpenCode installs now wire `tokensave.md` into `opencode.json`'s `"instructions"` array by absolute path rather than a bare filename — OpenCode resolves a relative entry by globbing up from the current *project's* working directory, not from `opencode.json`'s own directory, so a bare `"tokensave.md"` there would never actually resolve to `~/.config/opencode/tokensave.md`. Local (project-scoped) installs keep the relative filename, which resolves correctly and stays portable across checkouts.

### Fixed
- **`tokensave_context`'s tool description no longer embeds the live graph node count, which was poisoning MCP client prompt caches (#260).** The description carried a `CALL BUDGET: N calls maximum for this project (NNNNNN nodes)` line, and the node count changes on nearly every re-index. Because MCP tool descriptions are part of the JSON schema that clients splice into their system prompt, any client that fingerprints or prompt-caches that prefix (Anthropic/OpenAI prompt caching, or an agent keeping a hashed system-prompt snapshot) saw the bytes change on almost every launch and was forced to discard and rebuild the cache — silently losing cache reads and inflating token cost and latency, even though nothing meaningful had changed. The volatile parenthetical is gone; the description is now stable across re-indexes and carries only the call-budget guidance, whose value moves at most when the project crosses a size tier. The node count is unchanged where it belongs — in `tokensave_status`' results — so no information is lost, only relocated off the cached surface.
- **`tokensave_search` in literal mode now honors `path_include` / `path_exclude` (#258).** Literal (exact-substring) search branched to its own scan before the path filters were ever parsed, so a caller pinning the search to a specific file with `path_include` still got matches for the same literal from unrelated files across the project — contradicting the tool schema, which advertises those filters on every search. Literal mode now applies the identical substring filtering the ranked-search path uses, with `path_exclude` taking precedence over `path_include`, and continues to respect the scope prefix.
- **Incremental sync no longer serves a stale view of a file edited within the same second at an unchanged byte size (#259).** The change detector skipped re-hashing any file whose stored `(mtime, size)` matched the filesystem, and `mtime` was recorded at whole-second resolution. An edit that landed in the same wall-clock second as the last index and preserved the file's byte count — common when a build tool or editor rewrites an HTML file's embedded script structure in place — was therefore indistinguishable from no change, and the old indexed symbols were served indefinitely (a full `sync --force` was the only recovery). File mtime is now captured at nanosecond resolution, shrinking that blind window to effectively zero. The value feeds only the sync pre-filter's `modified_at`, which is compared against itself across syncs, so the unit change is self-contained: databases written by an older build carry second-granular values that differ from the new nanosecond reading, triggering exactly one re-hash per file on the first sync after upgrade, after which the content-hash check confirms nothing changed and stores the refined mtime.

## [7.5.0] - 2026-07-21

### Changed
- **Split `~/.tokensave/config.toml` into stable config + machine-local state.** Volatile, frequently-changing fields (`pending_upload`, `cached_latest_version`, `last_*` timestamps, `cached_country_flags`, `installed_agents`, and similar) now persist to a new `~/.tokensave/state.toml` instead of `config.toml`, so `config.toml` only holds stable preferences (`upload_enabled`, `watcher_debounce`, `extraction_timeout_secs`, `wildcard_permissions`) and stays safe to check into a dotfiles repo without constant diffs. Existing installs migrate transparently on first load: legacy state values still in `config.toml` are read once and written out to `state.toml` on the next save, with no data loss. Both files are written atomically (temp file + rename, with a unique name per *call* so racing saves within one process — e.g. background MCP work and a shutdown save — can't collide on the same temp path; the remove-then-retry fallback for Windows' non-replacing `rename` is scoped to that exact `AlreadyExists` case rather than any rename failure) and resolve symlinks — including chains of them that dangle partway through (e.g. `config.toml -> managed-config -> missing-target` where `missing-target` doesn't exist yet, as with a freshly cloned but still-empty dotfiles repo), walked hop-by-hop with a bounded hop count so a cyclic chain (`config.toml -> other -> config.toml`) can't hang resolution and, since every hop in an exhausted chain is then known to still be a live symlink with no safe target to write through, fails the save outright rather than renaming over one of the links in the cycle — before writing, so a dotfile setup that symlinks `config.toml` into a repo gets the write through the link instead of the link being replaced by a plain file. On Windows, where `rename` doesn't replace an existing destination, the fallback now moves the existing file aside to a backup path and restores it if the replacement rename then fails (disk full, permissions, an antivirus lock, another process), instead of unconditionally deleting it first and risking a permanent loss of `state.toml`'s `pending_upload` and installed-agent bookkeeping. `state.toml` is written before `config.toml` so a failed state write can never strip legacy state out of `config.toml` without it landing in `state.toml`. An unparseable `state.toml` (a manual edit gone wrong, filesystem corruption, a future incompatible format) is moved aside to a `state.toml.corrupt.<timestamp>.<pid>.<n>` backup on load rather than being silently discarded and then overwritten in place by the very next routine save — the original content survives on disk for manual recovery, and the next save writes a fresh `state.toml` at the normal path. Within a process, the state-then-config write pair is now serialized under a mutex so two same-process savers (e.g. background MCP work racing a shutdown save) can't interleave their writes into a `config.toml` from one caller paired with a `state.toml` from the other; this does not extend across processes, which remains best-effort as before. `load()` now takes that same mutex while reading both files, so a same-process `load()` can no longer race a `save()` and return a `config.toml` snapshot from before the save paired with a `state.toml` snapshot from after it (or vice versa) — previously the save's own internal serialization did nothing to stop a concurrent reader from tearing across it. Atomic saves (Unix only) also now preserve the destination's existing file mode instead of resetting it to the process umask default: previously `std::fs::write` created the temp file with the umask's permissions (commonly `0o644`) and the rename onto the target adopted them wholesale, silently widening a dotfiles-managed `config.toml` deliberately locked to `0o600` (and creating `state.toml` — which holds local bookkeeping like `pending_upload` and `installed_agents` — world-readable by default) on every save; the temp file's mode is now set to match the resolved target's existing mode before the rename, falling back to a restrictive `0o600` for a file that doesn't exist yet (first save, or a still-dangling symlink chain), and the save fails outright if that mode can't be set rather than renaming a file whose permissions weren't pinned down deliberately.

### Fixed
- **Agent setup banners no longer print on every `init` and `sync` (#255).** The silent reinstall-on-upgrade path in `main` — which re-runs `install` for every tracked agent so permissions, hooks, and MCP config stay in sync with a new binary — called each integration's `install` directly, and those integrations print their full interactive setup output (`✔ Added tokensave MCP server to …`, `PreToolUse hook already present, skipping`, `Setup complete. Next steps: …`). On a machine with several agents installed that meant dozens of lines of install chatter in front of the actual output of an ordinary `tokensave init`/`tokensave sync`. Worse, the version markers (`last_installed_version`, `previous_version`) only advanced when *every* tracked agent's install succeeded, so a single config path that can never be written — an app that isn't installed, a read-only or permission-denied location such as a managed VS Code `settings.json` — pinned the markers permanently and re-triggered the whole resync, banner output included, on every subsequent command rather than once after an upgrade. That is why the report saw the install screen *every* time, not just after upgrading. Agent progress output now routes through `agent_note!`/`agent_note_inline!` macros gated on a process-global quiet flag, which the resync sets for its duration, so background maintenance is silent while an explicit `tokensave install` still prints exactly as before; and the markers advance regardless of per-agent failures, with the failures collected and surfaced once as a single `warning: could not refresh tokensave config for: <agents>` line pointing at `tokensave install` for the underlying error, instead of being retried forever in silence. The loop moved out of `main` into `agents::resync_installed_agents`, which takes the per-agent installer as a closure and returns which agents failed, so the marker-advance and no-repeat behavior are covered by tests that don't touch real agent config files.
- **Bash grep guardrail sees through a leading env assignment or `cd … &&` prefix.** `extract_grep_invocation` only recognized a code search when the command literally started with `grep`/`rg`/`ag` (after `rtk`/`sudo`/`time`/`nice`), so `FOO=bar grep -n Sym src/main.rs` and `cd src && grep -n Sym main.rs` silently bypassed the redirect. Leading `NAME=value` assignments and a leading `cd … &&`/`cd … ;` prefix are now stripped before the tool check. A pipeline (`… | grep`) still passes through, since piping another command's output through grep is not a code search. The opt-out is preserved and made more forgiving: an explicit inline `TOKENSAVE_DISABLE_GREP_HOOK=<truthy>` (for example `TOKENSAVE_DISABLE_GREP_HOOK=1 grep …`) is now honored as a deliberate bypass, exactly like the exported variable, rather than being stripped as ordinary noise and then blocked. Truthiness matches `HookEnv::from_runtime` (set, non-empty, not `0`, not `false` case-insensitive), so a falsey value such as `TOKENSAVE_DISABLE_GREP_HOOK=0` is not an opt-out and the grep is still redirected.

### Added
- **Factory Droid's built-in `explorer` subagent now redirects to `tokensave_context`.** The installed `PreToolUse` matcher includes `Task`, whose live payload identifies the built-in research agent as exact lowercase `tool_input.subagent_type: "explorer"`. Indexed projects block that exact type with Droid's exit-code-2/stderr contract and point the caller to `tokensave_context`; `worker`, `implementer`, custom explorer-named types, and every other typed subagent continue unchanged. The shared `TOKENSAVE_DISABLE_GREP_HOOK` escape hatch and non-indexed fail-open behavior are preserved. Reinstalling migrates both older `Execute`-only and `^(Execute|Grep)$` entries in place to `^(Execute|Grep|Task)$` without duplicates.
- **Factory Droid guardrail hook now also covers the native `Grep` tool (follow-up to the Droid `PreToolUse` hook).** The install matcher widens from `Execute` to `^(Execute|Grep)$` (an anchored regex, verified live to still fire on both tools, so it can never collide with a future tool name that merely contains `Execute`/`Grep`), so a symbol-shaped `pattern` on a code target routed through Droid's own `Grep` tool (previously invisible to the hook) redirects to `tokensave_search`/`tokensave_callers_for`, reaching parity with the Claude hook's Grep coverage. The shared decision core now also reads Droid's `glob_pattern` field alongside Claude's `glob`, and redirects only an explicit `output_mode: "content"`; omitted, malformed, unknown, and cheap path-only modes (including Droid's `file_paths`) pass through. Re-installing over an older `Execute`-only entry migrates it in place instead of adding a duplicate, `doctor` flags stale matchers instead of reporting them healthy, and install/uninstall preserve lookalike custom wrappers by requiring an exact tokensave command. `Read`/`LS`/`Glob` are deliberately not matched because Droid's hard-block contract has no soft-steer channel for their lossy or unclassifiable equivalents; see the PR for the full per-tool analysis.

## [7.4.2] - 2026-07-19

### Changed
- **`tokensave_health` always reports the full dimension breakdown.** The tool previously returned only `quality_signal` and `files_analyzed` by default and hid the per-dimension subscores (acyclicity, depth, equality, redundancy, modularity, coverage_discipline) behind an opt-in `details: true` argument. The subscores — each with its raw score, supporting counts, and the formula under `source` — are the actionable part of the report, so they are now always included and the `details` argument has been removed from the tool schema. Callers that were passing `details` see no error (unknown arguments are ignored); callers that were not now get the breakdown they previously had to ask for.

## [7.4.1] - 2026-07-19

### Documentation
- **Documented `TOKENSAVE_DISABLE_GREP_HOOK` as the headless / subagent (`claude -p`) bypass (#248).** Child processes dispatched by an orchestrating session inherit the parent's `PreToolUse` hook; the README now spells out that setting `TOKENSAVE_DISABLE_GREP_HOOK=1` in the child's environment is a complete, per-path opt-out honored by the native binary (no need for the blunt `--settings '{"hooks": {}}'` that strips all hooks), and clarifies that the guardrail is stateless — it never consults citation history, so it only redirects symbol-shaped searches and untyped research fan-out, in headless and interactive sessions alike. Added a regression test (`test_disable_env_bypasses_every_redirect_path`) locking the flag as a complete bypass across the Grep, Bash, and Agent paths.

### Fixed
- **The one-time schema-overhead charge counted every tool, not just the ones loaded into context.** The estimate serialized the whole `tools/list` payload (~85 tools), but only the three `anthropic/alwaysLoad` tools (`tokensave_search`, `tokensave_context`, `tokensave_status`) are resident in the model's context up front — the rest are deferred and cost nothing until fetched on demand. The charge is now computed over the alwaysLoad subset via `get_always_load_tool_definitions`, so the schema overhead folded into `after` reflects what the client actually loads instead of over-stating it by more than an order of magnitude.
- **MCP `tokensave_metrics` and `approx_tokens_saved` over-estimated token savings.** Several gaps in the accounting, now closed: (1) the ~85-tool schema payload sent to the client via `tools/list` was never debited — it's now approximated once per session and charged into the next successful call's `after` after the client actually fetches it, tracked via a dedicated flag so a dispatch error can't skip the charge, and a client that invokes a known tool directly without ever calling `tools/list` is never charged for a schema payload it didn't receive. (2) The request side of a call (tool name, serialized `arguments`, JSON-RPC framing) was invisible — it's now folded into `after` on every call. (3) `before` summed the full weight of every `touched_file` regardless of what the tool actually returned, so reference-only tools (search, graph/dead-code analysis) that merely point at files, and content tools serving something less than a full file — a `tokensave_read` line range, symbol map, signature list, or cache-hit stub; a `tokensave_body` symbol extract; `tokensave_diff_context`'s symbol references; `tokensave_blame`'s change metadata — all claimed those files' full weight as "saved". `before` is now capped at a small multiple of the response content actually returned, for every tool: a genuine uncached full-file read still gets full credit, since its JSON-wrapped response is always at least as large as the source it carries and the cap never binds in that case, while anything short of that is scaled down to match what was actually delivered. (4) `after` was measured before several warnings/banners (version update, per-file staleness, fallback branch, stale index age, worktree mismatch) were appended to the response, so their real token cost was invisible — those insertions now happen before the measurement, and the metrics line's own token cost is folded in too. (5) The persisted `tokens_saved` counter (surfaced as `approx_tokens_saved`) accumulated the gross `before` instead of `before - after`, disagreeing with the cross-project ledger, which was already net; the counter is now net and saturating (a call costing more than its baseline saves 0, never negative), and the metrics line gains an explicit `saved=` field alongside `before=`/`after=`. (6) That per-call saturation also swallowed most of the one-time schema charge: it lands on a single call, and once that call's own `after` exceeded its `before`, the excess just vanished instead of offsetting real savings from later calls — a 10,000-token schema charge landing on a call with `before=200` recovered at most 200 tokens, permanently. The persisted counter now carries any such shortfall forward as session debt and pays it down out of later calls' surplus before crediting anything further, so it converges on the session's true signed total instead of the sum of each call's individually-clamped net; the per-call displayed `before=`/`after=`/`saved=` figures and the ledger rows are unaffected. (7) The metrics line's own token cost was folded into `after` even on calls where `before` is `0` and the line is never actually appended to the response (every `tokensave_status` call, and any other tool with no recognized touched-file baseline) — recording tokens for a line the client never received, and quietly swallowing a schema charge landing on such a call. Both the append and the cost now share one condition, so they can't diverge again.
- **Scoped MCP queries returned zero results on Windows (#242).** The scope prefix (relative path from project root to the launch directory) was derived from an OS path, so on Windows it contained backslashes and never matched the forward-slash paths stored in the index — `tokensave_context`, `tokensave_search`, and every other scoped tool silently came back empty. The prefix is now normalized to forward slashes when the MCP server is constructed.
- **The Factory Droid `PreToolUse` hook ran full install maintenance on every tool call (`HookDroidPreToolUse` was missing from the hot-path skip list).** `should_skip_agent_install_maintenance` lists the per-tool-call hook subcommands that must bypass the pre-dispatch startup work (the worldwide-counter network flush, the install-stale check, and the silent-reinstall loop that rewrites every tracked agent's MCP/hook config), but the Droid hook variant was absent. So, unlike the Claude and Kiro hooks, every `tokensave hook-droid-pre-tool-use` invocation ran that maintenance: a network round-trip plus a `user_config.save()` on each call and, whenever a version transition was pending, a full rewrite of all agent configs (leaving `.bak` files) in the middle of an unrelated Grep/Execute tool call. Adding `HookDroidPreToolUse` to the skip list makes the Droid hook take the same fast, side-effect-free, JSON-only path as the other per-tool-call hooks.
- **Source files containing invalid UTF-8 bytes are indexed instead of skipped (#241).** Legacy C/C++ sources often contain isolated non-UTF-8 bytes in comments; source reads now replace malformed byte sequences with the Unicode replacement character while preserving the existing UTF-8 and BOM-marked UTF-16 handling.
- **Database connection setup retries a transient libsql open failure instead of failing the caller.** `Database::initialize` and `Database::open` opened a fresh libsql local connection, applied pragmas, and created or migrated the schema in one shot with no retry. Under heavy concurrency the libsql local driver intermittently fails while opening a connection, surfaced as `SQLITE_MISUSE` ("bad parameter or other API misuse"); this reproduced on the macOS CI runner, which opens a separate temporary database per test in parallel, and flaked one test per run (Linux never reproduced it). The setup steps are idempotent (a fresh build/connect, `PRAGMA`s, and `CREATE ... IF NOT EXISTS`), so both entry points now retry the whole sequence up to five times with a short exponential backoff (20, 40, 80, 160 ms) when the error looks transient (`misuse`, `database is locked`, `database is busy`). Genuine schema or corruption errors do not match and still surface immediately, and a normal run never retries so there is no added latency.

## [7.4.0] - 2026-07-16

### Added
- **GitHub Copilot in JetBrains IDEs (`tokensave install --agent copilot`).** The copilot agent previously configured only VS Code (stable + Insiders) `settings.json` and the Copilot CLI's `~/.copilot/mcp-config.json` — none of which the JetBrains Copilot plugin reads. Install now also registers the MCP server in the plugin's global config (`~/.config/github-copilot/intellij/mcp.json`; `%LOCALAPPDATA%\github-copilot\intellij\mcp.json` on Windows) using its top-level `servers` JSON shape, and writes tokensave's prompt rules to `global-copilot-instructions.md` in the same directory, since JetBrains does not read the VS Code `User/prompts` file. The JetBrains config is only written when `~/.config/github-copilot` already exists (the plugin creates it on sign-in), so non-JetBrains homes stay untouched — install again after signing in to Copilot in a JetBrains IDE, or let the next upgrade's silent reinstall pick it up. Uninstall, detection, and `doctor` gained matching JetBrains handling; note the JetBrains plugin reads MCP config at startup, so restart the IDE after installing.
- **Opt out of the "update available" notice with `TOKENSAVE_UPDATE_CHECK=off`.** The passive update check that runs during `init`, `sync`, `status`, and the MCP server can now be silenced by setting `TOKENSAVE_UPDATE_CHECK` to a falsey value (`off`, `false`, `0`, `no`, `disable`, `disabled`). On by default, so normal installs are unchanged. When disabled the notice paths return early without touching the network. Explicit `tokensave upgrade` and `tokensave doctor` ignore the flag and always check, so an intentional upgrade still works. This is aimed at managed installs (for example a local build symlinked onto `PATH`) where an automated `tokensave upgrade` would replace the managed binary or waste time upgrading when it was not wanted.

## [7.3.0] - 2026-07-16

### Added
- **Transparent branch tracking — auto-track the current git branch when it is untracked.** Previously an untracked branch fell back to the nearest-ancestor DB (queries still worked, but the index was the ancestor's and the branch needed a manual `tokensave branch add`). Now: the `post-checkout` git hook runs `tokensave branch add` on a branch checkout (no-op if already tracked/default), and `TokenSave::open` best-effort auto-tracks an untracked branch by copying the ancestor DB (no sync — the `post-commit` hook keeps it fresh). Off by default; opt in via the `auto_track` config field or the `TOKENSAVE_AUTO_TRACK` env var. Never fails `open()`.
- **Opt-in compact Claude Code permission grant.** `tokensave install`/`reinstall` can now write a single `mcp__tokensave__*` entry to `~/.claude/settings.json` instead of enumerating all 80+ individual tool permissions, via `--wildcard-permissions` (and `--explicit-permissions` to switch back). The choice persists to `~/.tokensave/config.toml` (`wildcard_permissions`) for global installs, so silent reinstalls on upgrade keep it. Passing one of these flags prunes the previous grant form so `permissions.allow` never accumulates both an explicit list and a wildcard/bare entry for tokensave. A flagless install/reinstall (including the automatic silent reinstall on upgrade) never rewrites an existing covering grant — a hand-authored `mcp__tokensave__*` or bare `mcp__tokensave` survives untouched even if the local config predates this feature.

### Fixed
- **`tokensave_session_recall` handles queries containing FTS5 syntax characters (#218).** The recall query was bound as a parameter (no SQL injection), but FTS5 still parses the bound string as a MATCH *expression*, so terms containing `-`, `.`, or `/` (`data-api`, `v2.1`, `src/auth.rs`) failed with an FTS5 syntax error instead of matching. The query is now escaped through the same tokenize-and-quote logic `tokensave_search` already used (each whitespace token becomes a quoted prefix term), shared via a new `to_fts_match_query` helper; a query with no tokenizable content degrades to the unfiltered recency-ordered recall instead of an invalid MATCH.
- **Python false dead-code and unused-import results from four missed reference kinds (#224).** On a ~280-file Python codebase, `tokensave_dead_code` and `tokensave_unused_imports` reported hundreds of false positives, driven by reference patterns the Python extractor and the unused-imports handler didn't record: (1) `tokensave_unused_imports` fed a Python Use node's dotted `name` (e.g. `enum.StrEnum`) through the `::`-based Rust-shaped identifier parser, so `class Colour(StrEnum)` never matched and the import looked unused — a new `python_import_identifier` derives the actually-bound name from Python import semantics (`from a.b import c` binds `c`; `import a.b.c` binds `a`; an `as` alias always wins), correctly isolating each name's own alias even when several imports share one statement (`import a, b as c`). (2) A function referenced only by name — as a dict value (`PARSERS = {"text": _parse_text}`), a keyword argument (`Spec(parse=_parse_text)`), a parameter default (`def invoke(callback=_default_callback)`), or a class-level attribute value (`class Registry: CALLBACKS = {"x": _class_callback}`) — produced no edge and looked dead; `PythonExtractor` now emits a bounded `Uses` ref for identifiers in call-argument values, assignment/`return` right-hand sides, and parameter defaults (skipping callee names, attribute sub-names, and keyword/dict keys), including when a factory returns a nested closure by name (`return add`). Class-body assignments now reach the same right-hand-side scan as module-level ones (previously gated to module scope only), without turning class attributes into module-level `Const` nodes. (3) `visit_function` never recursed into nested `def`s, so a closure's own calls (and the closure itself) were invisible to the graph — `_helper` inside `def add` nested in `make_adder` looked dead even though `add` called it; nested function/class definitions (including ones inside `if`/`try`/`with` blocks) are now visited and contained by their enclosing function, with `class_depth` reset for the walk so a closure nested inside a method is still classified as a `Function`, not a `Method`. (4) A reported cross-file `from pkg.mod import fn` miss turned out to be a symptom of (3) once same-file closures resolve correctly; re-verified with a dedicated cross-file repro and no further edge case survived. `__init__.py` re-exports (Python's intentional public-API surface) are now excluded from `tokensave_unused_imports` entirely, removing the noise the issue's larger sample was dominated by.
- **`tokensave sync`/`doctor` no longer warn about "missing" permissions when Claude Code's `permissions.allow` already covers every tool via a wildcard or bare server grant.** Both checks compared the fully expanded 80+ tool list against `permissions.allow` with literal string equality, so a user-written `mcp__tokensave__*`, bare `mcp__tokensave`, or an anchored `mcp__tokensave__prefix*` glob — all of which Claude Code fully honors as allow rules — matched nothing and was reported as "N tools not permitted" on every run. The checks now recognize any of these covering grants. An unanchored `mcp__*` is deliberately still treated as *not* covering the tools, since Claude Code itself skips unanchored globs in `allow` rules — treating it as covering would hide a real "tools not granted" state.
- **`tokensave_context` preserves exhaustive qualified-expression matches.** Source-shaped task terms and keywords such as `Type::Variant` now seed their exact literal-hit enclosing symbols ahead of generic semantic matches. These exact seeds may exceed the small semantic `search_limit` when they fit within `max_nodes`, while still respecting path filters, query-ignore rules, explicit node exclusions, and the global context budget.
- **Production paths named `test` can be classified as source code.** Add `source_path_overrides` globs to `.tokensave/config.json` when a product route or feature directory matches the default test-path heuristic. Explicit markers such as `__tests__/`, `*.test.*`, and `*.spec.*` remain tests inside overridden paths.
- **`.mjs`, `.cjs`, `.mts`, and `.cts` files are indexed instead of silently skipped (#219).** `TypeScriptExtractor::extensions()` only registered `ts`, `tsx`, `js`, and `jsx`, while the file watcher, context formatter, resolver, and DB language-display mapping already treated the ESM/CJS extensions as JavaScript/TypeScript. Since `LanguageRegistry::extractor_for_file` looks up the extractor by extension, any `.mjs`/`.cjs`/`.mts`/`.cts` file matched no extractor and was dropped from the walk with no warning — most visibly for colocated `node:test` files like `*.test.mjs`, which made `tokensave_test_coverage`, `tokensave_test_map`, and `tokensave_affected` unable to discover those tests. All four extensions are now registered; `.mjs`/`.cjs` parse with the JavaScript grammar and `.mts`/`.cts` with the TypeScript grammar, matching `.js`/`.jsx` and `.ts`/`.tsx` respectively.
- **`tokensave sync` repairs v12 databases missing the trait-dispatch caller cache.** Some databases had recorded schema v12 without the `trait_dispatch_callers` table, causing sync to fail while refreshing reverse trait-dispatch callers. Schema v13 recreates the cache table, its index, and maintenance triggers automatically before indexing continues.
- **Writing agent config no longer destroys a symlinked config file.** `safe_write_json_file` (used by every agent's `install`/`uninstall`/`doctor --fix`, e.g. `~/.claude/settings.json`) staged a `.new` sibling and atomically renamed it over the target path. `rename(2)` replaces the target's directory entry, so when the target was a symlink — a common dotfiles pattern that tracks config in a repo and symlinks it into `~/.claude/` — the rename deleted the symlink and left a plain regular file in its place, silently detaching the live config from the dotfiles source it was meant to track. The write now resolves the symlink to its real target first and stages/renames there, so the dotfiles source gets updated and the symlink itself survives untouched. Non-symlink targets are unaffected. Resolution walks multi-hop and dangling symlink chains (`config -> intermediate -> not-yet-created target`) hop by hop rather than relying on `canonicalize`, which fails outright on a dangling chain. If a chain can't be resolved safely — a cycle, an unreadable link, or a chain deeper than 40 hops — the write is aborted and the original symlinks are left untouched instead of falling back to writing over the symlink itself, which would reproduce the same destructive bug for that case. A chain of exactly 40 hops that lands on a valid target still resolves; only a chain needing a 41st hop is rejected.
- **Large-repository context and caller queries no longer perform project-scale work at request time.** Executable bodies and reverse trait-dispatch callers are materialized during indexing and kept current across full, incremental, single-file, edit, deletion, and clear paths. Context uses bounded, unranked FTS candidate retrieval before its existing structural reranking instead of globally BM25-sorting broad natural-language matches; caller traversal merges cached trait-dispatch first hops with ordinary graph edges. On the pinned polkadot-sdk benchmark, the five context tasks fell from 405–592 ms to 1.07–3.96 ms, while the affected trait-aware caller sample fell from about 68 µs to about 21 µs.
- **`tokensave_context` now discovers behavioral owner functions in large policy files.** Conceptual queries can match terms inside executable bodies and local control-flow identifiers (including conservative identifier abbreviations such as `precond`/`preconditioner`) without promoting every local binding to a graph node. Runtime-behavior requests explicitly prefer functions over imports/re-exports; exact type matches expand to their behavioral methods; and per-file diversity is applied before the final root limit while reserving a slot for an executable owner. Together these changes surface retry/cache/convergence loops and related source, boundary, and simulation APIs that were indexed but previously lost to lexical noise or large-file caps.
- **`tokensave_callers` resolves generic calls through traits back to concrete implementations.** Looking up callers of a concrete method in `impl Trait for Type` now follows the corresponding trait method and includes generic call sites, tagged with `dispatch_via_trait` and `dispatch_from`. `resolve_dispatch: false` preserves the direct-edge-only view, matching the existing forward-dispatch control on `tokensave_callees`.
- **`tokensave_affected` separates focused recommendations from broad dependency candidates.** Source files containing inline `#[test]` modules are reported as `inline_test_sources` and promoted to affected test targets, including directly changed source files that would otherwise produce a zero-test result. Inline sources receive an `inline_test_source` classification and join `recommended_tests` when they are high/medium-confidence same-crate candidates; other candidates are classified as direct, same-crate integration, cross-crate consumer, or transitive low-confidence using dependency distance and Cargo package boundaries. The backward-compatible `affected_tests` field retains all discovered test targets.

## [7.2.0] - 2026-07-11

### Added
- **`.tokensave/project.json`: explicit file/glob index entries with per-entry language overrides (#194).** Extractor dispatch was extension-only and indexing was scoped to `root_dir`, so valid Bash files like `~/.bash_profile`, `.bashrc`, or `*.shrc` could not be indexed at all — even through a symlink tree that preserves their real names. The new optional manifest lists paths or globs to index, each with an optional `language` forcing a specific extractor (`{ "path": "homedir/.bashrc.d/*.shrc", "language": "bash" }`). Project-relative entries make extensionless/wrong-extension files indexable and pass the hidden-path filter automatically; absolute and `~/…` entries (glob-capable) opt in files outside the project root, stored under their resolved absolute path. Entries are additive — the normal walk, `config.json` excludes, and size limits still apply — and an unknown `language` fails the sync loudly, listing the valid names. Documented in the User Guide's Configuration Files section.
- **AugmentCode (`auggie`) agent integration (`tokensave install --agent auggie`).** Registers the tokensave MCP server in `~/.augment/settings.json` under `mcpServers.tokensave` (stdio transport) without disturbing the file's other keys (`model`, `hooks`, `indexingAllowDirs`, `vimMode`, …), and writes tokensave's prompt rules as a dedicated `~/.augment/rules/tokensave.md` file rather than appending to a shared file — matching how Augment discovers individual `*.md` rules. `--local` writes `<project>/.augment/settings.json` and `<project>/.augment/rules/tokensave.md`. Auto-detected by `tokensave install` (and `reinstall`/`doctor`) when `~/.augment` exists. Augment's Claude-style `hooks` support is left for a follow-up PR.
- **Factory Droid `PreToolUse` guardrail hook (follow-up to the Droid MCP integration, #163).** `tokensave install --agent droid` now also registers a `PreToolUse` hook under the `hooks` object in `~/.factory/settings.json` (`<project>/.factory/settings.json` for `--local`) — the same file Factory's own hook wrappers live in, a separate file from `mcp.json`. Droid delivers the hook event as JSON on stdin with the tool payload nested under `tool_input` (the Claude/Kiro shape) and blocks a tool call via **exit code 2 + stderr**, the same mechanism as the Kiro hook. The install only registers the `Execute` matcher (Droid's confirmed shell tool) and reuses the same shared decision core as the Claude and Kiro hooks, so symbol-shaped `grep`/`rg`/`ag` calls against indexed code files redirect to tokensave MCP tools while arbitrary shell, `git grep`, non-code files, and specialized sub-agent tasks pass through untouched; `TOKENSAVE_DISABLE_GREP_HOOK=1` opts out per-call. Install/uninstall preserve every other key and hook entry already in `settings.json` (including hooks the user manages themselves); `doctor --agent droid` reports the hook's status.

### Fixed
- **All user-facing line numbers are now 1-based editor lines (#203).** Node spans are stored 0-based (raw tree-sitter rows), but only some tools converted on output: `tokensave_body`, blame, and literal-search hits reported 1-based lines while `tokensave_node`, `tokensave_entities`, `tokensave_search`, `tokensave_context`, and most analysis/health/git tools leaked the raw 0-based values. Every handler now converts at the presentation edge (`display_line`); storage and internal span comparisons stay 0-based. The pass also flushed out four more mixed-convention bugs: enclosing-symbol attribution compared 1-based lines against 0-based spans in `tokensave_diagnostics`, `tokensave_field_sites`, and `tokensave_todos` (misattributing to an adjacent symbol, the #203 pattern); and `tokensave_context` code snippets treated stored spans as 1-based — shifting every snippet up a line (leading line included, closing line dropped) and returning no code at all for symbols starting on a file's first line. The `tokensave://schema` doc wrongly described spans as 1-based and now documents the real convention.
- **`serve --path <dir>` no longer emits the borrowed-worktree warning (#201).** The heads-up exists to catch CWD discovery silently resolving *another* worktree's index — but an explicit `--path` is a deliberate choice of project root (e.g. serving a sibling repo's graph read-only from another session), and the warning's remedy ("run `tokensave init` here") is wrong for that mode. Explicitly-pathed servers now skip the CWD-vs-root mismatch check; CWD-discovered servers keep it. Staleness and sync checks were audited and already resolve everything from the served project root.
- **Windows backslash path arguments now match indexed files (#204).** `tokensave_entities({"file":"apps\\admin\\..."})`, `tokensave_files` with a backslash `path`, and `path_include`/`path_exclude` filters silently returned empty results because incoming arguments were compared verbatim against the DB's canonical forward-slash paths. The MCP dispatcher now normalizes `\` to `/` in the path-shaped arguments (`file`, `path`, `file_path`, `path_include`, `path_exclude`) before dispatch — leaving verbatim/UNC `\\`-prefixed paths untouched — and `filter_by_path_lists` normalizes its filter substrings as well, covering config-level defaults.
- **Deflaked `mcp_server_owns_watcher_and_refreshes_token_map_on_change` (#134).** The test drove the lazy staleness pipeline once and asserted immediately, but `sync_if_stale_silent` is best-effort (it swallows extraction errors and gives up on a contended sync lock), so the assertion intermittently fired before the token map reflected the new file under full-suite parallel load. The test now re-drives the pipeline until the expected state appears, bounded by a 60 s deadline — await-condition instead of await-duration.
- **The `PreToolUse` guardrail no longer hard-blocks explicitly-typed non-Explore agents, and it honors the opt-out (#195, #196).** The Agent/Task redirection blocked Claude Code's built-in `Explore` agent (correct) but *also* blocked any Agent call whose prompt text merely looked like research — regardless of `subagent_type`. That meant a deliberate delegation to a `general-purpose` worker, an implementer, a custom agent, or another harness's own task type was denied with no override, since the agent path (unlike the Grep/Bash paths) ignored `TOKENSAVE_DISABLE_GREP_HOOK`. Prompt keywords cannot distinguish research from implementation, and the guardrail's intent is to *steer* toward the MCP tools, not to wall off agent delegation. Now: the built-in `Explore` agent is still steered; an explicitly-typed non-Explore agent is never blocked on prompt text; an *untyped* Agent/Task call is still steered by prompt shape (it may be an Explore-style fan-out); and the existing opt-out env var suppresses agent redirection too, giving an explicit "continue" override.
- **The agent guardrail now gates on the `.tokensave` index, like the Grep/Bash paths (#195).** In a directory without an index, the Agent/Task branch (Claude core and the Kiro delegation path) still hard-blocked with "use tokensave MCP tools" — tools that had nothing to answer from. Both paths now no-op when `cwd` has no `.tokensave` database, and the Kiro delegation path additionally honors `TOKENSAVE_DISABLE_GREP_HOOK` like every other branch.
- **TypeScript/TSX signatures no longer truncate at a destructured parameter's brace (#205).** `extract_signature` cut the text at the first `{`, so `function StatCard({ label, value }: StatCardProps)` came back as `function StatCard(`. Signatures (and arrow-function signatures, which used a textual `=>` search with the same failure mode) are now cut at the body node's start byte, preserving the full parameter list and return type.
- **Calls inside nested arrow callbacks now produce call edges (#209).** The TS call-site walker skipped every `arrow_function`/`function` subtree, so `useMemo(() => helper(), [])` created no edge and `tokensave_callers(helper)` returned `[]`. Locally-defined callbacks have no graph node of their own, so their calls are now attributed to the enclosing named function. Arrow components with *expression* bodies (`const C = () => helper()`) — previously skipped entirely because only `statement_block` bodies were walked — are extracted too, and the receiver-typed-call pass (`this.svc.load()` inside a callback) recurses the same way.
- **JSX component renders create caller/impact edges (#210).** `<Child />` in a TSX component now emits a `Calls` ref from the rendering component to `Child` (capitalized elements only — lowercase intrinsic tags like `div` are excluded; `Ns.Component` uses the member segment), so `tokensave_callers` and `tokensave_impact` see component consumers, including colocated test renders.
- **Vitest/Jest `describe`/`it` callbacks are indexed as test functions (#211).** Top-level `describe`/`it`/`test`/`suite` calls (including dotted forms like `it.each(...)`) were ignored as bare expression statements, leaving `*.test.ts` files as just a file node plus imports — so `test_coverage`, `test_map`, and `affected` reported directly-tested functions as untested. Each wrapper call now becomes a Function node named after the suite/case title (`it formats a date`), nested suites attach beneath their parent, and calls inside the callbacks emit `Calls` refs, which the existing test-file path detection turns into coverage.
- **TypeScript decorators are extracted for classes under `export`, methods, and parameters (#206).** Decorators were only collected from direct children of `class_declaration`, but `@Controller() export class ...` parses them as siblings inside the `export_statement`, member decorators (`@Get()`) are siblings preceding the member in `class_body`, and parameter decorators (`@Body()`) live inside the parameter node. All three shapes now produce Decorator nodes with `Annotates` edges, so `tokensave_annotations` returns NestJS-style controllers instead of zero usages.
- **`tokensave_unused_imports` no longer flags every used TypeScript import (#207).** The identifier parser was `::`-path based (Rust-shaped), but a TS Use node is named after the *module path* (`@testing-library/react`), so the bound identifiers were never recovered and demonstrably-used imports were reported unused. TS/JS imports now parse identifiers from the import clause itself: default imports, `{ a, b as c }`, `* as ns`, `type`-only imports, and mixed forms; side-effect imports (`import './styles.css'`) bind nothing and are never reported.
- **`tokensave_context` code fences use the file's language instead of hardcoded `rust` (#208).** TSX/TS/Python/etc. code blocks were all emitted as ```` ```rust ````; the fence language is now derived from the block's file extension.
- **Literal-search hits are attributed to the correct enclosing symbol (#203).** The enclosing-node lookup compared the 1-based display line against 0-based stored node spans, so a hit in a one-line symbol was attributed to the *next* adjacent symbol (`RECENT_KEY`'s literal reported under `RECENT_MAX`). The same off-by-one existed in `tokensave_unsafe_patterns`; both now compare 0-based to 0-based. (The broader ask — a single documented line convention across all tools — is tracked separately in #203.)
- **`tokensave_context` matches CamelCase symbols from spaced natural-language phrases (#202).** A task saying "promo banner" never generated the `PromoBanner` candidate, so the component lost to unrelated FTS noise even inside the included paths. Adjacent plain words in the task now synthesize PascalCase and camelCase bigram symbols (`PromoBanner`, `promoBanner`) which feed both the FTS terms and the exact-name supplement.

## [7.1.0] - 2026-07-09

### Added
- **XAML / AXAML files are now indexed (#167).** WPF, Avalonia, MAUI, and WinUI markup was invisible to search and impact analysis. A lightweight hand-rolled scanner (no new tree-sitter grammar — the binary is already dominated by grammar tables) now emits: a `Class` node from the root `x:Class` (same qualified name as the code-behind partial class), `Field` nodes for `x:Name`/`Name`d elements, `Uses` refs for namespace-prefixed custom controls (`controls:TitleBar`), and `Calls` refs for event-handler attributes (`Click="OnSaveClicked"`) that resolve to the code-behind method. Binding expressions and unprefixed framework controls are excluded to keep the graph clean.
- **`TOKENSAVE_WORKERS=N` caps subprocess extraction parallelism (#183).** Extraction spawned one worker per core (`available_parallelism()`) with no ceiling, so N concurrent server instances each assumed they owned every core — amplifying pressure on an already-loaded box. Setting `TOKENSAVE_WORKERS` now clamps the worker count to `1..=available_parallelism()`; unset keeps the all-cores default, and an invalid value warns and falls back.

### Changed
- **Default excludes now match tool/output/state dirs at any depth (#173).** `target`, `.git`, `.tokensave`, `vendor`, `build`, `out`, and `.gradle` were root-anchored, so with a parent `root_dir` spanning nested projects each project's own build/state dirs (including nested `.tokensave/`) were indexed. They are now `**/`-prefixed, and the defaults gained `**/site-packages/**`, `**/.venv/**`, `**/venv/**`, and `**/__pycache__/**` to keep large Python toolchain trees out (#174).

### Fixed
- **Dart classes used as types now have dependents in `tokensave_impact` (#172).** The Dart extractor emitted no references for type annotations, so a class consumed only through parameter/return/field types or generic arguments (`List<RecordDefinition>`) had zero incoming edges and impact analysis reported no dependents. Constructor invocations fared no better: `RecordDefinition(id)` emitted a `Calls` ref, but the resolver's `Calls` kind filter only accepts callables, so the ref to the *class* silently dropped. Signatures and field declarations now emit `Uses` refs for every project-defined `type_identifier` (`dart:core` types are filtered), and `UpperCamelCase` bare callees emit a companion `Uses` ref alongside `Calls`, so constructor usage resolves to the class.
- **`tokensave init` warns and confirms before indexing outside a git repository (#174).** With `git_ignore` on but `root_dir` not inside a git working tree (e.g. a home directory), `.gitignore` filtering is inert and large non-project trees (conda `site-packages`, virtualenvs, caches) were indexed wholesale — one report saw the DB balloon to ~12 GB before it was noticed. `init` now detects the non-repo case via `gix::discover`, prints a warning, and requires confirmation on a TTY (skipped for non-interactive CI/scripts) before proceeding.
- **`install --agent claude` honors `$CLAUDE_CONFIG_DIR` (#191).** Claude Code relocates its entire user-level config when `CLAUDE_CONFIG_DIR` is set, but the installer wrote `settings.json`, `CLAUDE.md`, and `.claude.json` to hardcoded `~/.claude` / `~/.claude.json` paths, so hooks, permissions, and MCP registration landed in a directory Claude Code never read. Install, uninstall, detection, and `doctor` now resolve those paths through `CLAUDE_CONFIG_DIR` (taking the first entry of a comma-separated value), falling back to the home-dir defaults when it is unset.
- **`tokensave_diff` no longer fails with "delegated tool returned non-JSON text".** The aggregator re-parsed each delegate's already-formatted-and-`truncate_response`d text back into JSON, so any diff large enough to exceed `MAX_RESPONSE_CHARS` (and the empty "No changes detected." case) errored the whole call. Delegates now expose their structured `Value` via `*_value` helpers; `handle_diff` composes them directly and truncates once at the edge, and the `extract_value` re-parse shim is gone.
- **Cursor `preToolUse` hook emits a valid permission decision.** Cursor gates the tool on a flat `permission` field and fail-closes every Grep/Shell call when it is missing (`Hook ... returned invalid JSON`). The hook now prints `{"permission":"allow"}` on allow, and on deny adds flat `permission:"deny"` plus `user_message`/`agent_message` beside the existing nested `hookSpecificOutput`. Claude Code ignores the unknown flat field, so one payload serves both harnesses.
- **Julia extraction now records functions, structs, abstract types, macros, and call sites with the bundled grammar.** `tree-sitter-julia` 0.23 exposes Julia function/struct names and call callees positionally rather than through the `name`, `body`, or `function` fields the extractor assumed, so real `.jl` files indexed only file/module nodes and skipped nested module contents. The extractor now falls back through `signature`/`type_head`, traverses module children directly, and derives call callees from positional children.
- **Claiming a global `core.hooksPath` no longer disables a repo's *other* hook types (#164 follow-up).** The #164 fix re-chained only the two hooks tokensave installs (`post-commit`, `post-checkout`), but a global `core.hooksPath` makes git resolve *every* hook type from that one directory with no fallback to `.git/hooks/`. So a repository's own `pre-commit`, `pre-push`, `commit-msg`, `prepare-commit-msg`, … (as delivered by `init.templateDir`, husky, pre-commit, lefthook, lint-staged) still stopped firing the moment tokensave claimed `core.hooksPath` — the same silent-shadowing failure as #164, just for the hook types tokensave does not own. When tokensave claims (or already owns) the global hooks directory, it now also drops a pure forwarder for each remaining client-side hook (`git rev-parse --git-dir`-based, identical to the #164 preamble), so every repo hook keeps running. Forwarders are written only when no file of that name already exists, so user-placed hooks are never clobbered; a user-managed `core.hooksPath` is left untouched.
- **`get_stats` no longer aborts on a single NULL `kind` row.** `query_kind_counts` read the `kind` column as a non-null `String`, so one row with a NULL `kind` (e.g. an older-schema ancestor DB served via the untracked-branch fallback) failed the whole `get_stats` aggregate with `failed to read kind: Null value`. Because the monitor reads `get_stats`, this showed 0 savings even when savings were real. It now reads `kind` as `Option<String>` and coalesces NULL to `"unknown"`, matching the null-defensive scalar counts in the same path.
- **`tokensave_callers` no longer flattens transitive callers with direct ones or reports the wrong line (#171).** The handler defaulted `max_depth` to 3 while `get_callers` is a transitive BFS, so 2-/3-hop callers were merged into one flat list with nothing to distinguish them, and each row reported the caller's *declaration* line (`node.start_line`) rather than the actual call-site line — even though the `Calls` edge carried the correct `edges.line`. It now defaults to direct callers only (`max_depth` 1, still overridable up to 10), tags every result with a `depth` field (1 = direct, 2+ = transitive), and reports the call-site line from the edge (`line`) alongside the caller's declaration line (`def_line`). `tokensave_callers_for` was unaffected (single-hop bulk lookup, no line numbers).
- **`config.exclude` directories are now pruned before the gitignore-aware walker descends (#170).** The default file walker (`ignore` crate, used when `git_ignore` is on) honored `.gitignore` but not `config.exclude`, applying excludes only per-file *after* the walk. So a symlink inside an excluded directory — e.g. a Wine prefix's `dosdevices/z: -> /` under an excluded `build-output/` — was followed and the entire filesystem got walked (minutes on a FUSE mount). The walker now prunes excluded directories up front via a `filter_entry`, matching the non-gitignore walker and the `--skip-folder` flag (which feed the same exclude list).
- **Global git hooks no longer silently disable pre-existing repo hooks (#164).** Accepting the git-hooks prompt sets a global `core.hooksPath` when none is configured, which makes git ignore every repository's `.git/hooks/` — including hooks copied there by `init.templateDir` — so a dotfiles-managed `post-checkout` (or any per-repo hook) silently stopped running. tokensave-owned global `post-commit`/`post-checkout` hooks now start with a chaining preamble that forwards to the repository's own hook (via `git rev-parse --git-dir`, which is immune to the `core.hooksPath` redirection), so nothing stops running; reinstalling retrofits the preamble into hooks created by earlier versions. User-managed `core.hooksPath` setups are left untouched. When `init.templateDir` is configured, the install also says up front what setting `core.hooksPath` means.
- **Reinstall no longer clobbers a user-chosen MCP command (#161).** `tokensave install`/`reinstall` overwrote whatever command the agent config held with this install's absolute binary path, breaking configs deliberately written with a bare `tokensave` resolved via `PATH` (portable across machines with different install locations). All agent integrations now keep the existing command when it still resolves to a tokensave binary (absolute path on disk, or bare name found on `PATH`) and only replace it when it is stale or foreign. Applies to every MCP registration format: string commands, `["tokensave", "serve"]` arrays (OpenCode, Kilo), Zed's `command.path`, and the Codex/Grok TOML configs.
- **Claude Code `PreToolUse` hook never fired — the grep/Read→MCP guardrail was silently dead.** `hook_pre_tool_use` read the tool arguments from a `TOOL_INPUT` environment variable, but Claude Code delivers the hook event as JSON on **stdin** (with the tool arguments nested under `tool_input`) and sets no such variable. The decision logic was correct, but it always ran against an empty input, so every Grep/Read/Agent call was implicitly allowed and nothing redirected exploration to the tokensave MCP tools — under-reporting `tokensave monitor` savings. The handler now reads Claude's stdin contract (unwrapping the nested `tool_input`), keeps the `TOOL_INPUT` env var as a fallback for empty stdin, and preserves the existing flat-input decision logic. The sibling Kiro hooks already read stdin correctly and are unaffected.

### Added
- **Cursor agent install/uninstall manages `~/.cursor/hooks.json`.** `tokensave install --agent cursor` now upserts `preToolUse` (`Grep|Shell`), `stop`, and `beforeSubmitPrompt` hooks while preserving foreign entries (rtk, custom scripts). Uninstall removes only commands containing `tokensave`.
- **GDScript (Godot 4.x `.gd`) is now an indexed language (`lang-gdscript` feature, in `full`).** A new tree-sitter extractor covers classes, functions/methods, constructors (`func _init` parses as a dedicated `constructor_definition`), signals (new `NodeKind::Signal`, ranked like C#'s `Event`), constants, fields, enums, and inner classes, plus `extends`/call edges. The `tree-sitter-gdscript` grammar (PrestonKnopp, ABI 14) is vendored under `vendor/` and compiled via `build.rs` when the feature is enabled. Also fixes `status`'s `files_by_language`, where `.gd` files fell into the `Other` bucket even though extraction worked.
- **Factory Droid agent integration (`tokensave install --agent droid`).** Registers the tokensave MCP server in `~/.factory/mcp.json` under `mcpServers.tokensave` (stdio transport) and appends the tokensave rules to `~/.factory/AGENTS.md`. `--local` writes `<project>/.factory/mcp.json` and a repo-root `<project>/AGENTS.md`. Auto-detected by `tokensave install` (and `reinstall`/`doctor`) when `~/.factory` exists.

### Changed
- **`tokensave init` now excludes `.tokensave` via the local `.git/info/exclude` instead of the tracked `.gitignore` by default.** `.tokensave/` is a per-developer index, not a project artifact, so it should not be written into the tracked `.gitignore`, which commits a per-developer choice into shared project history. On init, if `git check-ignore` already ignores `.tokensave/` (global excludes, an existing rule, or a prior run) nothing is written; otherwise the entry goes to `.git/info/exclude` (untracked, local-only). The prompt keeps an explicit `[g]` opt-in for the old tracked-`.gitignore` behavior, so nothing regresses. The `status` warning and the new `add_to_git_info_exclude` helper (which resolves the exclude path via `git rev-parse --git-path`, so worktrees and custom `$GIT_DIR` layouts work) follow suit.
- **Extraction: 32 tree-sitter extractors now share one traversal state (`extraction::ts_state`).** Each of these extractors carried a byte-identical private `ExtractionState` struct (some with an extra `class_depth` field) plus identical `new` / `node_text` / `qualified_prefix` / `parent_node_id` / `build_result` implementations, and most also duplicated the pure node-search helpers (`find_child_by_kind` alone had 28 identical copies). They now share a single `ExtractionState` and free helper functions; extractors with genuinely language-specific state (C++, Java, Kotlin, Scala, TypeScript, Pascal, Markdown, Rust, …) keep their own structs and only adopt the shared pure helpers where their copies were identical. ~2,500 duplicated lines removed with no behavior change, and a new language extractor can now start from the shared state instead of a copy-paste.
- **Module layout: `src/tokensave.rs` and `src/db/queries.rs` split into directory modules.** The two largest files (~3.6k and ~3.4k lines) are now `src/tokensave/` (lifecycle in `mod.rs`, plus `extract` / `guard` / `indexing` / `query` / `staleness` / `memory` / `util`) and `src/db/queries/` (row mappers and shared helpers in `mod.rs`, plus `nodes` / `edges` / `files` / `unresolved` / `search` / `stats` / `clear` / `metadata` / `fingerprints`), following the section markers the files already had. Pure code motion; the public API is unchanged.
- **Zero clippy warnings across the workspace.** Cleared all outstanding pedantic warnings: merged identical match arms, case-insensitive file-extension comparisons in build-variant detection, `write!` instead of `push_str(&format!(..))` in SQL assembly, `ComplexityMetrics::default()` over ambiguous `Default::default()`, and assorted doc/raw-string nits.
- **Integration tests share a `tests/common` helper module.** `names_of`, `make_install_ctx`, `make_install_ctx_with_real_bin`, and `read_json` were copy-pasted across up to 11 test files; they now live in one place. The Kiro test previously asserted against its own byte-for-byte copy of `file_resource_uri` (so it could not catch regressions in the real one); `agents::kiro::file_resource_uri` is now public and the test exercises the production function.


## [7.0.3] - 2026-06-30

### Fixed
- **Dart: receiver method calls were never recorded, so private helpers were wrongly reported as dead introductions (#155).** tree-sitter-dart 0.2 represents a receiver call `recv.method(args)` — including the idiomatic `this._parseItem(s)` — as **two sibling `selector` nodes**: one holding the member name (`._parseItem`), a separate one holding the `argument_part`. The call-site extractor only emitted a `Calls` ref when the name and `argument_part` lived in the *same* selector (the 0.1 grammar shape), so every `this.x()` / `obj.x()` call produced no call edge. Private helpers reached only through `this.` then showed zero incoming edges and `tokensave_simplify_scan` flagged them as dead. The selector handler now also matches a name-only selector whose next sibling carries the arguments, which additionally recovers all receiver method calls (e.g. `xs.map(...)`, `.toList()`) that were silently missed before — not just private ones.
- **Monitor: the two on-screen "Saved" figures disagreed.** The cost panel's `Saved:` was sourced from `global_tokens_saved()` (the authoritative lifetime total from the global DB) while the footer summed the in-memory ring-buffer deltas (which double-count per-call file-token estimates and only cover the last `RING_CAPACITY` events). The prominent header value lagging the live footer made the monitor look like it wasn't updating. The footer now reads the same authoritative total as the header, so the two always agree; the per-tool breakdown and `TOTAL` line remain the live ring view.

## [7.0.2] - 2026-06-24

### Fixed
- **WinARM release pipeline: the signed `aarch64-windows` artifact was never published (follow-up to #151).** The Windows ARM build target (added in #151) compiled, but the signing/publishing half of `release.yml` was written for a single Windows binary: both build legs uploaded their unsigned `.exe` under the same artifact name, and the `sign-windows` job signed one binary and always uploaded it as `x86_64-windows.zip`. So no `aarch64-windows.zip` reached the release, and `update-scoop` then published a Scoop `arm64` entry pointing at a 404 with an empty hash (the failure was masked because a `VAR=$(sha256sum missing)` assignment swallows the non-zero exit under `set -e`). Fixed by giving each Windows leg a per-target unsigned-artifact name, turning `sign-windows` into a matrix over both Windows architectures (each signed and uploaded as `tokensave-<tag>-<arch>.zip`), and making `update-scoop` fail loudly when a required asset is missing. The same swallow-the-error hardening was applied to `release-beta.yml`. First surfaced on the 7.0.1 release.

## [7.0.1] - 2026-06-24

### Added
- **Qwen Code agent integration (#152).** `tokensave install --agent qwen` now registers the tokensave MCP server in `~/.qwen/settings.json` (with `trust: true` for tool auto-approval) and appends tokensave prompt rules to `~/.qwen/QWEN.md`. Qwen Code is an open-source coding CLI forked from Gemini CLI and shares its configuration shape, so the integration mirrors the Gemini one (install / uninstall / healthcheck / detection).
- **Windows ARM64 release binaries.** The release workflows now build and upload `aarch64-windows` artifacts (`tokensave-<tag>-aarch64-windows.zip`) targeting `aarch64-pc-windows-msvc` on the `windows-11-arm` runner. The Scoop manifest is updated to include an `arm64` architecture entry alongside the existing `64bit` one so Scoop installs the native binary on ARM devices automatically.

### Fixed
- **Go extractor: residual phantom call edges and `/vN` unused-import false positive after #149 (#153).** Two residual defects, each a direct continuation of a #149 fix:
  - **Phantom duplicate call edges (`callers` / `callees` / `impact` accuracy).** #149 made `pkg.Fn()` resolve to the correct same-named package via the in-scope import path, but the old bare-name resolution still *also* fired and collapsed every same-name call onto a single ranking-tie winner — so for N same-named definitions each called once, the winner accumulated N incoming edges while the rest kept 1 (N + (N−1) edges for N call sites). The resolver now drops the bare-name sibling ref whenever its selector resolved through an import path (`go-selector-import`), leaving exactly one correct edge per package-qualified call. Genuine bare calls and receiver-method fallbacks (whose qualifier is not a known import) are untouched.
  - **`unused_imports`: `/vN` import whose package name ≠ the pre-`/vN` segment.** #149 derived an un-aliased import's identifier as the segment preceding `/vN`, which is wrong when the package name differs (e.g. `github.com/resend/resend-go/v3` has package clause `resend`, but the derived `resend-go` contains a hyphen and can never appear in source), so the used import was reported unused. The derivation now rejects any candidate that is not a legal Go identifier rather than flagging on a guess it knows is invalid.

## [7.0.0] - 2026-06-23

### Added
- **Version-aware reinstall & per-project sync on upgrade.** tokensave now classifies every version transition as patch, minor, or major and runs the matching maintenance automatically. A new `cloud::bump_kind` semver classifier (returning `BumpKind::{None, Patch, Minor, Major}`, with the same beta/stable channel handling as `is_newer_version`) drives the decision. Patch bumps do nothing beyond advancing the recorded version; minor bumps trigger the existing global agent reinstall; **major bumps additionally force a per-project reindex** to backfill schema changes. The reindex runs lazily and non-blocking: on the first MCP `tools/call` in a project, the server checks whether `bump_kind(last_indexed_version, running) == Major` **or** the project DB schema is behind the latest (`read_schema_version() < latest_version()`), and if so spawns a background full re-index (`sync -f` equivalent), advancing the new `last_indexed_version` marker in `.tokensave/config.json` on success. An empty marker (pre-7.0 projects) is treated as needing the reindex so existing projects backfill the v11 schema/metrics columns on first use. The check is gated to run at most once per session. Accompanies the v11 schema/metrics work in 6.4.5.

## [6.4.5] - 2026-06-23

### Added
- **Expanded code-health metrics: cognitive complexity, Halstead, and maintainability index (#150).** Building on the existing per-function complexity counts (cyclomatic, nesting depth, unsafe/unchecked/assertion counts), `count_complexity` now also computes, in the same single AST walk:
  - **Cognitive complexity** — SonarSource-style, nesting-weighted: each control-flow structure adds `1 + (enclosing control-flow depth)`, and each extra boolean operator in a logical sequence (`&&`/`||`) adds `1`. This cannot be derived from the scalar branch/loop counts, so it is computed during extraction and stored on each node.
  - **Halstead primitives** — distinct and total operator/operand token counts, classified per-language via new `operator_types`/`operand_types` fields on `ComplexityConfig` (populated for Rust, Go, Python, TypeScript, Java, C, and C++; other languages default to empty and report 0).
  - **Derived Halstead volume/difficulty/effort and the maintainability index** — pure functions (`halstead_volume`, `halstead_difficulty`, `halstead_effort`, `maintainability_index`) over the stored token counts. The maintainability index uses the Microsoft/SEI variant scaled and clamped to 0–100.

  The new metrics are persisted on the `nodes` table (schema migration v11) and surfaced in the `tokensave_complexity` and `tokensave_node` tool outputs as `cognitive_complexity`, `halstead_volume`, `halstead_difficulty`, `halstead_effort`, and `maintainability_index`.

  - **CRAP score (change-risk / anti-pattern).** `tokensave_complexity` now also reports `crap` and `test_covered`. CRAP is `cyclomatic² · (1 − coverage)³ + cyclomatic` — it flags code that is both complex and untested (a fully covered unit scores its cyclomatic complexity; an untested unit scores `cyclomatic² + cyclomatic`). tokensave has no execution (line/branch) coverage, but it derives a per-unit coverage signal from the call graph: a unit is `test_covered` when a `Calls` edge targeting it originates in a test file or a `#[test]`-annotated function (the same definition `tokensave_test_risk` uses). This signal is binary today, so the `(1 − coverage)³` term collapses to 0 (tested) or 1 (untested); the underlying `crap_score(cyclomatic, coverage)` accepts any fractional coverage so it stays correct if execution-coverage ingestion is added later.

### Fixed
- **Go extractor: residual `dead_code` / `unused_imports` false positives after #148 (#149).** Two root causes that #148 did not cover are closed, each verified against the reporter's deterministic repros:
  - **Same-named functions in same-named packages collided (`dead_code`).** A cross-package selector call (`foojobs.NewCleanupWorker()`) resolved on the function name alone, so when two packages share a name (both `package jobs`, at `internal/foo/jobs` and `internal/bar/jobs`) and each defines a function of the same name, every call collapsed onto a single target — the others got zero incoming edges and were flagged dead. The resolver now builds a per-file map of Go import qualifier → import path and disambiguates a selector call by restricting candidates to the function defined in that import's package directory; an unknown qualifier (a receiver variable) falls back to the previous bare-name behavior, so method-call resolution is unchanged.
  - **Bare versioned (`/vN`) imports flagged unused (`unused_imports`).** For a Go semantic-import-versioned path with no explicit alias (`github.com/golang-jwt/jwt/v5`, `github.com/jackc/pgx/v5`), the in-scope identifier was derived as the trailing path segment (`v5`), which never appears in source (code writes `jwt.` / `pgx.`), so the import was reported unused. The identifier derivation now skips a trailing `vN` segment (matching `^v\d+$`) and uses the preceding segment (`jwt`, `pgx`) when no alias is present. The shared logic lives in a new `go_import` module so the resolver and the analysis tool stay consistent. Reported by @iKyi.

## [6.4.4] - 2026-06-20

### Fixed
- **Go extractor: `dead_code` and `unused_imports` no longer flood with false positives from missing call/use edges (#148).** Follow-up to #141/#137 for the Go extractor, which only emitted edges for direct `call_expression` callees and stored import paths verbatim. Three gaps are closed:
  - **Value references (`dead_code` classes C/D).** A function named without being called — as a registry/slice entry (`var reg = []func() error{applyA, applyB}`), a call argument (`mux.HandleFunc("GET /x", HandleX)`, `withMiddleware(tagRoute)`), or a struct-field value (`{wrap: Recover}`) — produced no edge, so live wiring functions were flagged dead. The extractor now emits a `uses` reference for bare `identifier`/`selector_expression` values in `argument_list` and `literal_element` positions, and scans package-level `var`/`const` initializers (previously never traversed). It also recurses into `func_literal` bodies, so a function called only inside a closure/goroutine stays alive.
  - **Generic calls (`dead_code` class B).** A generic call such as `slices2.Distinct[int](xs)` parses as a `type_conversion_expression` wrapping a `generic_type`, so no `call_expression` was ever seen and the target had zero incoming edges. The extractor now emits a `uses` reference to the base name of any `generic_type` instantiation, keeping generic functions (and types) alive.
  - **Import usage (`unused_imports`).** Go imports are slash-separated paths, but the package identifier was derived with the `::`-based Rust parser, so `"net/url"` yielded the token `net/url` — which never matches the `url.Parse` call site, flagging ~97% of imports as unused. The analysis now derives the Go in-scope identifier as the alias (`u "net/url"` → `u`) or the last path segment (`net/url` → `url`). Import parsing was also rewritten to extract a clean path plus alias (fixing a pre-existing case where `u "net/url"` was stored mangled as `u "net/url`), and blank (`_`) and dot (`.`) imports are now never reported, since they are deliberate. Reported by @iKyMTV.

## [6.4.3] - 2026-06-19

### Added
- **`post-checkout` hook for automatic initialization on clone.** `tokensave install` now offers a global git `post-checkout` hook alongside the `post-commit` hook, gated by the same opt-in (`--git-hook` flag / interactive prompt). When accepted, it writes a `post-checkout` hook that runs `tokensave init` in the background — but only on the initial checkout of a fresh `git clone` (the all-zeros previous HEAD), so ordinary branch switches don't trigger indexing. A reference `scripts/post-checkout` is included for manual installs, and the hook is also installed on the next run for users who already have the `post-commit` hook. The README documents both hooks.
- **`path`, `path_include`, `path_exclude` params on `dead_code`, `hotspots`, and `unused_imports`.** These three analysis tools were the only ones missing path-scoping support. Same semantics as `god_class`, `coupling`, `complexity`, and `largest`. `path` filters to a directory prefix; `path_include`/`path_exclude` filter by substring (exclude takes precedence).
- **`default_path_include` / `default_path_exclude` config fields.** Query-level defaults in `.tokensave/config.json` applied to analysis tools when no explicit `path_include`/`path_exclude` is passed. Useful for permanently excluding test/mock directories from analysis results without repeating args on every call.

### Changed
- **`git_ignore` now defaults to `true`.** New projects indexed with `tokensave init` will respect `.gitignore` rules out of the box. Existing projects are unaffected (their config is already persisted). Use `tokensave gitignore off` or set `"git_ignore": false` in config to opt out.

### Fixed
- **`tokensave install` writes a version-stable binary path for Homebrew hooks (#146).** `which_tokensave` used `std::env::current_exe()`, which resolves symlinks — so under Homebrew it embedded the version-pinned Cellar path (e.g. `.../Cellar/tokensave/6.4.2/bin/tokensave`) into agent hook commands. The next `brew upgrade` or `brew cleanup` removes that path, leaving the agent unable to find tokensave and silently breaking every hook. Install now detects a Homebrew Cellar executable and prefers the stable `<prefix>/bin/tokensave` symlink (the same path `which tokensave` reports), which always tracks the current version. Non-Homebrew installs (cargo, manual) are unaffected. Reported by @cavanaug.
- **Dead-code analysis resolves calls through receiver bindings, `Self::`, and dotted method calls (#141).** After the trait-impl fix (#137), `tokensave_dead_code` still reported functions/methods as dead when their only call sites used call forms the resolver didn't trace. Three gaps are closed:
  - **`Self::`/qualified call pre-filter (resolver, all languages).** `resolve_all`'s pre-filter dropped any `Self::method` / `Type::method` ref whose *literal* string wasn't a known name — so it never reached `resolve_one`, which strips the prefix and matches the simple name. It now admits a ref when its trailing simple name is known, and `resolve_one` gained a `.`-separator fallback so Python/TS/Java dotted calls (`obj.method`, emitted as the full callee text with no bare-name ref) resolve via the method name. This alone recovers `Self::` calls in Rust and *all* receiver-method calls in the dotted-callee languages.
  - **Receiver-type inference (per-extractor: Rust, TypeScript, Java, Python).** A method call through a binding (`r.render_to_png()`) was emitted only as the bare method name, which mis-resolves when several types define a method of that name (e.g. two `render_to_png`s — all call edges piled onto the wrong one). Each extractor now builds a per-function variable→type table (from typed parameters, constructor/`new` initializers or type annotations, and `self`/`this`) and emits a precise `Type::method` ref that the resolver binds by qualified/suffix match to the correct impl/class.
  - **Build-variant call-edge propagation (#141 Option 2: Rust `#[cfg]`, Go platform files).** Conditionally-compiled definitions (`#[cfg(target_os="macos")] fn f()` vs its `not(...)` twin; `foo_linux.go` vs `foo_windows.go`) are the same logical symbol compiled per-config, but the name-based resolver binds a call to one variant, leaving the others with zero incoming edges and flagged dead. A new post-resolution pass groups build-variant siblings (Rust: same `qualified_name` + `#[cfg]`-gated; Go: same package directory + function name across files) and replicates the call edge to every sibling. Verified against the mex crate: `dead_code_count` dropped from 7 to 0, every removal being a genuine reachable symbol.
- **`tokensave install` / `reinstall` no longer reorders keys in agent config files (#140).** The installers parse each agent's JSON config (`mcp.json`, `settings.json`, …) into a `serde_json::Value`, edit the tokensave entry, and write it back. Because the `serde_json` dependency didn't enable `preserve_order`, its object maps were backed by `BTreeMap` and re-serialized **every** key alphabetically — turning a one-line edit into a whole-file diff that clobbered the user's preferred ordering. Enabling the `preserve_order` feature makes `Value` retain the original key order (and append newly-inserted keys like the `tokensave` MCP server at the end), so install now touches only the related lines. Reported by @cometkim.

## [6.4.2] - 2026-06-17

### Fixed
- **Dead-code trait-impl exclusion now actually fires for stdlib traits (#137, completes 6.4.1).** The 6.4.1 fix keyed off the impl block's outgoing `implements` edge, but that edge only materializes when the trait resolves to an *indexed* node. Impls of stdlib/external traits — `Display`, `Drop`, `Deref`, `From`, `AsRef`, the exact traits in the bug report — never get the edge, so the fix was inert on real codebases (the mex codebase has zero `implements` edges; all 13 trait-impl methods were still flagged). Detection now keys off the impl node's signature (`impl <Trait> for <Type>` contains ` for `), with the `implements` edge kept only as a fallback for multi-line headers that `extract_first_line` truncates before the ` for `. Verified against mex: false positives dropped from 20 to 7, the 7 remaining being genuine inherent/free functions. Inherent impls (`impl Type`, no ` for `) and ObjC `@implementation` blocks are still untouched.

## [6.4.1] - 2026-06-17

### Fixed
- **Dead-code analysis no longer reports Rust trait-impl methods as false positives (#137).** Methods inside `impl Trait for Type` blocks — `Display::fmt`, `Deref::deref`, `Drop::drop`, `From::from`, `AsRef::as_ref`, etc. — are dispatched implicitly by the compiler (format macros, auto-deref, drop glue, `?`/`.into()`), so they carry no incoming `calls` edge, and because they can't be written `pub` in Rust they are stored as `private` and slipped past the visibility filter. `tokensave_dead_code` flagged them as dead (inflating `dead_code_count`) and `tokensave_health` counted them against the `redundancy` dimension. `find_dead_code` now excludes methods whose parent is an `Impl`-kind node bearing an outgoing `implements` edge — precisely Rust's trait-impl blocks, which contain only the trait's methods. Inherent-impl and free functions are still reported. The exclusion is language-aware: class-based languages (C#/Java/TS/PHP/VB) attach `implements` to the `Class` node and store interface methods `public` (already filtered), and ObjC `@implementation` blocks carry no `implements` edge, so neither is affected. New `include_trait_impls: true` arg on `tokensave_dead_code` opts back into reporting them.

## [6.4.0] - 2026-06-16

### Added
- **`tokensave discover` — missed-opportunity analyzer (#123).** A deterministic command over the already-ingested Claude Code transcript data (`turns` table) that surfaces navigation turns (`Read`/`Grep`/`Glob`) a tokensave graph query could have answered more cheaply, bucketed by the tokensave tool that would have applied (Read→`outline`/`read`/`node`, Grep→`search`/`callers`/`callees`/`impact`, Glob→`files`/`search`). Reports the input tokens spent on those turns ("addressable") and a conservative recoverable-token lower bound. Detection is by tool name only — a turn counts only if *every* tool it used was a navigation tool, and Bash `grep`/`find`/`cat` are deliberately not counted (their command text isn't persisted), so the figure never over-claims. `--since <range>` and `--json` supported.
- **Pi coding agent integration (#128).** `tokensave install --agent pi` registers the MCP server in Pi's config at `~/.pi/agent/mcp.json` (honoring `$PI_CODING_AGENT_DIR`). install/uninstall read-modify-write only the `tokensave` server entry, preserving any other configured servers. Brings the supported-agent count to 16.
- **Compact memory delta at `session_start` (#122).** `tokensave_session_start` now returns a bounded `memory_delta` (up to 5 recent decisions + 5 recent code areas, each headline truncated) so a session resumes with a cheap "where we left off" summary instead of a full recall dump. The existing baseline-save contract is unchanged; a memory-read failure degrades to `null` rather than blocking the baseline.

### Changed
- **Recency-decay recall ranking (#122).** `tokensave_session_recall` (no-query path) now ranks decisions by an exponential recency-decay weight (14-day half-life) instead of strict newest-first. Older decisions sink but are **never** dropped or expired — they remain permanently recallable. The FTS/query path is unchanged.
- **Calls-first BFS traversal (#118).** Graph traversal now visits `calls` edges ahead of reference edges, so a budget-limited subgraph fills with call structure before fanning out to references. Improves `callers`/`callees`/`context`/`impact` results when `max_nodes`/depth cut traversal short.
- **Entry points capped before expansion (#120).** `tokensave_context` now caps the number of BFS roots to `search_limit` before expanding, so each top-ranked entry point keeps a meaningful share of the node budget instead of spreading it thin across many roots.
- **Non-production directories down-weighted in ranking (#119).** `examples`/`samples`/`benchmarks`/`demos` (singular and plural) now receive a soft ranking penalty in `tokensave_search`/`tokensave_context`, below first-party `src`/`app`/`lib` but above vendored/generated trees. A re-rank only — results are never dropped, and vendor classification still takes precedence.

### Fixed
- **Exact-name matches no longer buried by FTS noise (#117).** `tokensave_context` merged search channels with first-seen deduplication, so a symbol returned first by full-text search kept its low BM25 score even when it was also an exact name match — burying perfect matches the exact-name supplement was meant to surface. Channels are now merged by **max** score across FTS and exact-name lookups; exclusion (`exclude_node_ids`), `min_score`, and the candidate cap are all preserved.

## [6.3.3] - 2026-06-15

### Added
- **Query filtering & ranking for noisy repos (#112).** Four improvements that make `tokensave_search` / `tokensave_context` usable in repos that mix application code with vendored/generated trees:
  - **`path_include` / `path_exclude`** params on `tokensave_search` and `tokensave_context` — restrict results to / away from file-path substrings (e.g. `path_exclude: ["node_modules", "dist", "venv"]`). Exclude takes precedence; absent params are a no-op (#113).
  - **`literal: true`** mode on `tokensave_search` — exact-substring search over source text (for runtime error strings like `provider destroyed`), returning `{file, line, text, enclosing}` locations instead of ranked symbols (#114).
  - **App-dir ranking** — results under `src`/`app`/`lib` are boosted and vendor/generated trees (`node_modules`, `dist`, `build`, `target`, `venv`, `.venv`, `site-packages`, `vendor`, `__pycache__`, `.next`, `out`) penalized by default. Re-orders only; never drops results (#115).
  - **`.tokensave/queryignore`** — a project-level, gitignore-like file applied at query time to suppress known-noise paths from `tokensave_search` / `tokensave_context` results (paths stay indexed and reachable via direct lookups) (#116).

## [6.3.2] - 2026-06-15

### Added
- **`tokensave install --local` / `uninstall --local`** (#110) — register tokensave in the current project's config instead of the user's global config. Writes project-scoped config you can commit and share with your team: for Claude, `./.mcp.json`, `./.claude/settings.json`, and `./CLAUDE.md`. Supported agents: claude, cursor, gemini, zed, opencode, roo-code, kiro — each writing its own project-scoped file (`.cursor/mcp.json`, `.gemini/settings.json`, `.zed/settings.json`, `opencode.json`, `.roo/mcp.json`, `.kiro/settings/mcp.json`). Agents without a project-scoped config (codex, cline, copilot, antigravity, kimi, vibe, grok, kilo) report a clear error. `tokensave doctor` now treats a project-local install as a valid state and no longer deletes it.

## [6.3.1] - 2026-06-10

### Fixed
- **Incremental sync now prunes deleted files** (#108). The single-file sync path behind `sync_if_stale[_silent]` — used by the MCP startup catch-up and the 30 s lazy watcher — only re-extracted the given paths, so a file deleted from disk kept its file row, symbols, and edges as orphans: phantom results in `hotspots`/`rank`/`search` and an inflated `file_count`, while `last_sync_at` kept advancing. Deleted paths are now cascade-deleted, mirroring the full-sync removal branch.
- **Go extractor resolves cross-package calls** (#109). Selector callees (`b.Helper`, `s.Get`) were emitted verbatim and never matched a node name, so imported functions and methods on imported types produced no `calls` edge — leaving `callers`/`callees`/`impact` blind across package boundaries and flooding `dead_code` with false positives. The extractor now also emits the bare last segment (same approach as the Rust dot-call fix, #74).
- **`callers`/`callees` error on unknown `node_id`** (#109). A nonexistent node ID (e.g. a symbol name passed positionally: `tokensave tool callers Helper`) silently returned `[]`, indistinguishable from a valid "no callers found". Both handlers now fail with a `node not found` error pointing at `tokensave_callers_for`/`tokensave_search` for name-based lookups.

## [6.3.0] - 2026-06-09

### Added
- **`tokensave_dependencies`** — package-manifest introspection across **17 ecosystems** (#105, #106, #107). Auto-detects which manifest(s) live at the project root and serves a uniform shape; polyglot repos return one block per ecosystem. Supported: Rust (`Cargo.toml` + workspace globs + `[patch.*]`), Node (`package.json` + npm/yarn/pnpm workspaces + `overrides`/`resolutions`), Python (`pyproject.toml` PEP 621 + Poetry, `requirements*.txt`), Go (`go.mod` + `replace`), Java (`pom.xml` + `<modules>` + `<dependencyManagement>` BOMs), .NET (`*.csproj`/`*.fsproj`/`*.vbproj` + `Directory.Packages.props`), PHP (`composer.json`), Ruby (`Gemfile`), Swift (`Package.swift`), Elixir (`mix.exs`), Erlang (`rebar.config`), R (`DESCRIPTION`), Haskell (`*.cabal`), OCaml (`*.opam` + `dune-project`), Dart/Flutter (`pubspec.yaml`), Crystal (`shard.yml`), and Gradle (`build.gradle` via tree-sitter-groovy, `build.gradle.kts` via tree-sitter-kotlin, `gradle/libs.versions.toml` Version Catalogs with `version.ref` resolution, and `settings.gradle{,.kts}` multi-module discovery). Three modes: zero-input workspace summary (with `licenses` aggregate, `version_drift` array, per-member `members_detail`), `crate`/`package` lookup (with `resolved` versions when `include_lockfile: true`), and per-`member` listings. Opt-in lockfile resolution covers `Cargo.lock`, `package-lock.json`/`yarn.lock`/`pnpm-lock.yaml`, `poetry.lock`/`uv.lock`/`Pipfile.lock`, `go.sum`, `packages.lock.json`, `composer.lock`, `Gemfile.lock`, `pubspec.lock`, `shard.lock`. Workspace globs accept trailing `*`, intermediate `*`, recursive `**` (depth-bounded), and `!negation`.
- **`tokensave_test_coverage`** — per-file/symbol/test-fn coverage rollup with transitive call expansion (#103). Three input modes: `file` returns per-symbol classification + tested/untested rollup, `symbol` returns the test functions that transitively reach it, `test_fn` returns the non-test symbols a test transitively exercises. Tests are identified by file-name convention (`tests/`, `*_test.*`, etc.) or `#[test]`-style annotations. Replaces the multi-step `test_risk` + raw-SQL workflow surfaced in the conversation audit.
- **`tokensave_annotations`** — generic attribute/annotation/decorator introspection across all 31 languages (#104). Two modes: histogram (no `name`) returns top-K annotation names by usage count across the project or a path prefix; site mode (with `name`/`file`/`target_kind` filters) returns `(annotation, target)` rows joined via the existing `annotates` edge. Works wherever the extractor records annotation edges — Rust attributes (`#[derive]`, `#[cfg]`, `#[tokio::test]`), Python decorators, Java annotations, TS decorators, Dart annotations, etc.
- **`PreToolUse` hook now redirects symbol-shaped `Grep`/`Bash` calls to tokensave MCP tools** (#102). Extended the existing `Agent`-only matcher to `Agent|Grep|Bash` so the hook intercepts grep/rg/ag invocations whose pattern matches `[A-Za-z_]\w*(::\w+)*` optionally joined by `|` / `\|` or wrapped in `\b…\b`, when the target is a code file/dir/`type` in a tokensave-indexed project. Block message names the right tokensave tool per pattern shape (`tokensave_search`, `tokensave_signature_search`, `tokensave_callers`). Auto-migrates older installs via `tokensave doctor --fix`. Safety guards: regex metachars pass through, `output_mode=files_with_matches`/`count` pass through, `git grep` passes through (history search), piped commands pass through, and `TOKENSAVE_DISABLE_GREP_HOOK=1` provides per-shell opt-out.

### Changed
- **New dependencies (both pure-rust, no transitive bloat):** `yaml-rust2 = "0.10"` for YAML-format manifests/lockfiles (pubspec.yaml, shard.yml, pnpm-lock.yaml); `dekobon-tree-sitter-groovy = "0.2"` for `build.gradle` parsing (tree-sitter-language 0.1 / tree-sitter 0.26 — matches existing deps).

## [6.2.0] - 2026-06-09

### Added
- **`tokensave_blame` and `tokensave_log`** — per-symbol git blame and history. Walks history via `gix`, parses each historical blob with tree-sitter, and matches the target symbol across commits via structural fingerprints. Tracks cross-file renames via a probe at the boundary. (#100)
- **`tokensave_diff`** — sem-style unified diff that orchestrates `changelog`, `commit_context`, and `diff_context` behind a single `{ from, to, changes }` envelope. (#100)

### Changed
- **Renamed `tokensave_outline` → `tokensave_entities`** to match sem terminology. The handler is unchanged; only the public MCP tool name (and CLI subcommand) is renamed. (#100)

## [6.1.4] - 2026-06-07

### Added
- **ActionScript (AS2/AS3) extractor (`.as`).** Tree-sitter grammar (vendored from jcs090218/tree-sitter-actionscript, compiled by `build.rs`) behind the `lang-actionscript` feature (Full tier). Extracts classes, interfaces, methods, constructors, fields/consts, and imports with `Contains`/`Extends`/`Implements`/`TypeOf`/`Calls` edges; targets AS2 (AVM1) as emitted by the JPEXS/FFDec decompiler and `package`-wrapped AS3 (thanks @tanguc, #97).
- **Grok Build (xAI) agent integration.** `tokensave install --agent grok` registers the MCP server in `~/.grok/config.toml` (under `[mcp_servers.tokensave]`) and appends prompt rules preferring the tokensave MCP tools (with guidance on graph queries, DB fallback, and issue reporting) to `~/.grok/AGENTS.md`. Full `uninstall` + `doctor` support. Updates docs and agent registry tests (thanks @davidullo, #99).
- **F\* proof-oriented language support (`.fst` / `.fsti`).** Extracts top-level definitions along with their pre/post-conditions (thanks @hacklex, #98).
- **`tokensave install --git-hook <default|yes|no>` flag.** Lets scripted/CI installs pre-decide the global `post-commit` hook prompt instead of blocking on a TTY `read` (or silently skipping on a non-TTY). `default` preserves today's interactive behavior (thanks @Puppo, #96).

### Fixed
- **`test_tool_definitions_have_annotations` write/exec allowlist.** The regression test added in 6.1.3 asserted `readOnlyHint: true` for `tokensave_replace_symbol`, `tokensave_insert_at_symbol`, and `tokensave_run_affected_tests` — the very tools 6.1.3 switched to `readOnlyHint: false` — leaving the suite red on `master`. Added them to the allowlist.

## [6.1.3] - 2026-06-04

### Fixed
- **Write/exec MCP tools no longer advertise `readOnlyHint: true` (#94).** `tokensave_replace_symbol`, `tokensave_insert_at_symbol`, and `tokensave_run_affected_tests` mutate source files or run a `cargo test` subprocess, but were annotated read-only via the shared `def()` helper — so harnesses that auto-approve read-only tools could edit files or compile and execute project code without prompting. They now use a new `def_rw()` helper that stamps `readOnlyHint: false`, matching the other edit tools. A regression test asserts every write/exec tool is non-read-only.


## [6.1.2] - 2026-05-30

### Added
- **Svelte extractor (`.svelte`).** Locates all `<script>` blocks (instance and `<script module>` / `<script context="module">`), blanks out template markup, and delegates to the TypeScript extractor. Symbols, interfaces, exported functions, and `$props`/`$derived` rune declarations are indexed with correct line numbers in the original file. Closes #93.
- **Astro extractor (`.astro`).** Parses the `---` TypeScript frontmatter block at the top of the file. Imports, interfaces, exported functions, and top-level `const`/`let` declarations are indexed; the HTML template body below the closing `---` is ignored. Closes #93.

### Fixed
- use OpenCode correct global rules file path (`AGENTS.md` instead of `OPENCODE.md`) (thanks @davidefossacecchi, #92)

## [6.1.1] - 2026-05-26

### Added
- **Borrowed-worktree detection on every MCP read tool.** When a git worktree is nested inside the main checkout (e.g. agent tooling that puts worktrees under `.claude/worktrees/<name>/` or `.worktrees/<name>/`), tokensave's `discover_project_root` walks up and silently resolves the MAIN checkout's `.tokensave/` — returning results for the wrong branch with no warning, while symbols changed only in the worktree are invisible. New `src/worktree.rs` runs `git rev-parse --show-toplevel` on the caller's CWD and on the resolved index root; when they belong to different working trees, the verbose warning is included in `tokensave_status` and a one-line notice is prefixed to every read tool response. Detection runs once at server startup (≤2 `git rev-parse` spawns total per session). Ported from codegraph #312.
- **Catch-up sync on MCP connect.** `McpServer::new` now spawns a non-blocking task that runs `find_stale_files` + `sync_if_stale_silent` + `refresh_file_token_map` once at startup, bypassing the 30 s cooldown. Picks up changes made while the server was down — terminal `git pull`, IDE edits before the agent launched, files touched by another tool — so the first tool call sees a fresh index instead of waiting through the cooldown. Ported from codegraph #414.
- **`scripts/prepare-release.py` to auto-promote `[Unreleased]` → `[<version>]` in CHANGELOG.md.** Idempotently renames the `[Unreleased]` block to a dated `[<version>]` block at release time (Case A), or merges into a pre-existing `[<version>]` block by sub-section (Case B). Avoids the codegraph v0.9.5 failure mode where a sparse hand-staged `[<version>]` block silently shadowed the much-larger `[Unreleased]` section above it during release-notes extraction. Ported from codegraph #436. Wire into the release workflow when a version bump lands.

### Changed
- **Embedded MCP watcher replaced with on-demand staleness check (#80).** The `notify-debouncer-full` watcher was the source of severe CPU and memory pressure on large monorepos: top-level-only filtering of `IGNORED_DIRS` meant nested `apps/*/node_modules`, `packages/*/target`, `**/dist` were watched at the OS level, producing event storms and unbounded `RecommendedCache` growth (one user reported the process climbing to 19 GB before being killed). The watcher is now gone — along with the `notify-debouncer-full` dependency. Index freshness is maintained by a lazy `find_stale_files` walk (same gitignore-aware logic `sync()` uses) gated by a 30-second cooldown and invoked at the top of every MCP `tools/call`. Cost: walks on the cold tool call after a quiet window add tens of ms to milliseconds depending on repo size; in exchange, the unbounded-memory class of bug is structurally gone. Reported by @AGiorgetti and @ottob.
- **Per-file staleness banner replaces the binary "STALE INDEX" warning.** Tool responses that referenced files whose in-line sync couldn't refresh now get a focused banner naming exactly those files with their edit ages (e.g. `src/foo.rs (edited 3m ago)`) and an explicit instruction to `Read` those files directly — while telling the agent the rest of the response is authoritative. Replaces the previous all-or-nothing wording that made agents distrust the entire response. The machine-readable `tokensave_graph_stale` marker is preserved. Ported from codegraph #428.
- **Kiro steering is loaded as a resource.** The Kiro installer now writes `~/.kiro/steering/tokensave.md`, loads it from the managed agent's `resources` list with an absolute `file://` URI, leaves the custom-agent `prompt` unset so Kiro's default prompt is preserved, keeps MCP approval policy out of `mcp.json`, and installs permissive `tools: ["*"]` plus `allowedTools: ["@builtin", "@tokensave"]` defaults for the managed agent.

### Fixed
- **Windows: same physical file no longer indexed twice under both path-separator variants (#87).** On Windows, sync entry points that accepted caller-supplied relative paths (`sync_if_stale`, `sync_if_stale_silent`, `sync_single_files`, `check_file_staleness`) did not normalize backslashes before hitting the DB. When an MCP tool response carried OS-native paths (`src\foo.py`) into the staleness check, the lookup missed the canonical row stored under `src/foo.py`, the file got re-indexed under the backslash variant, and the `files` table grew a duplicate row for every physical source file. Downstream tools (`god_class`, `complexity`, `dead_code`, `redundancy`, `unused_imports`) returned doubled results; the `health` `redundancy` dimension reported ~0.39 instead of ~0.50+. Fix: defensive `normalize_rel_path` (single `replace('\\', "/")`) applied at all four sync entry points, with regression tests. Reported by @xaerogonzo.

## [6.1.0] - 2026-05-25

### Added
- **`tokensave tool <name>` — schema-driven CLI dispatcher.** Every MCP tool is now reachable from the command line through a single dynamic subcommand that introspects each tool's JSON schema and coerces `--key value` flags accordingly. `tokensave tool` (no args) lists tools grouped by category; `tokensave tool <name> --help` prints schema-derived parameters. Reserved flags: `--json` (raw response), `--project <path>`, `--args <json>`, `-h`/`--help`. Positional args bind to required string properties (e.g. `tokensave tool search foo`). `@file` values are read from disk for multi-line strings. Replaces seven hand-rolled subcommands (`query`, `context`, `body`, `impact`, `callers`, `files`, `affected`); `query` is kept as an alias for the renamed `search`. New file: `src/tool_command.rs` (~729 LoC).
- **`tokensave_find_exact_symbol` MCP tool.** Bare-name lookup against `idx_nodes_name` — a single O(log n) index probe with no BM25 ranking, no fuzzy match, no qualified-name suffix walk. Use this when the symbol name is already known; use `tokensave_search` for relevance-ranked discovery. ~30–200 µs per lookup vs. ~700 µs for the BM25 path.
- **`tokensave_call_chain` MCP tool.** Finds the shortest *directed* call chain between two node IDs along outgoing `calls` edges only. New `find_path_directed` BFS in `graph/traversal.rs` (the existing `find_path` is bidirectional and wrong for "how does A reach B" questions). Bounded by `max_depth` (default 8, max 20).
- **`tokensave_file_dependents` MCP tool.** Lists every indexed file that imports or otherwise depends on the given file. Thin wrapper around the existing `TokenSave::get_file_dependents` that was previously only reachable through `tokensave_affected`'s test-rollup path.
- **`tokensave_replace_symbol` MCP tool.** Symbol-aware body replacement: resolves a name via exact qualified-name match, narrows to callable kinds on ambiguity, and refuses the edit rather than picking the wrong site if more than one callable matches. Reads the file, splices the symbol's `start_line..=end_line` range with `new_source`, writes back, and reindexes the touched file. Plays the role of token-savior's `replace_symbol_source`.
- **`tokensave_insert_at_symbol` MCP tool.** Inserts content immediately before or after a named symbol's source range — same resolution semantics as `tokensave_replace_symbol`. `position` is `"before"` or `"after"` (default after). Plays the role of token-savior's `insert_near_symbol`.
- **`tokensave serve --timings` flag.** When set, every `tools/call` response gains a `_meta.duration_us` field reporting the handler's pure execution time in microseconds. Lets clients (and benchmarks) attribute latency to actual query work vs. JSON-RPC / stdio / Python-parse overhead. Toggleable at runtime through the new `McpServer::set_timings_enabled` setter so embedders can flip it per-session.
- **Indexer benchmark harness** at `benchmarks/run_benchmarks.py` — adapts `Mibayy/token-savior`'s `run_benchmarks.py` to drive both tools side-by-side on the same clone of FastAPI, sharing a random symbol sample (seed=42) so per-query rows are directly comparable. tokensave is driven through a long-lived `tokensave serve --timings` MCP session for the query column. Latest report (`benchmarks/comparison-report.md`): cold index 2.9× faster, impact analysis 43× faster than token-savior.
- **tsbench fork** at `benchmarks/tsbench/` — patch + reproduction README + per-run summary for running `Mibayy/tsbench` (token-savior's own 96-task agent benchmark) against tokensave. First-attempt untuned result: 184/192 = 95.8% vs. token-savior's audited 97.9%. The harness rewrites `SYSTEM_PROMPT_TS` to map each token-savior tool to its tokensave equivalent and relaxes the `--disallowedTools` list to allow `Read`/`Edit` fallback on the four task categories tokensave has no direct tool for.
- **`docs/TOKENSAVE-VS-TOKENSAVIOR.md`** — full capability + performance comparison document covering parsing strategy (regex annotators vs. tree-sitter grammars), the 11 health-analytics tools that have no token-savior equivalent, query-latency numbers (apples-to-apples find / body / impact), the tsbench 184/192 result with per-task failure analysis, and an honest "when to use which" guide.

### Fixed
- **`tokensave serve` no longer blocks MCP `initialize` on the watcher's filesystem walk (#84).** Constructing the embedded `notify_debouncer_full` watcher does a synchronous `walkdir` over every registered subtree to seed its file-id map. On a large JS/TS monorepo with multi-gigabyte `node_modules` / `.next` / `dist` trees this can take 30+ seconds — long enough to blow the client's `initialize` timeout. Fix: `ProjectWatcher::new` now runs inside `tokio::task::spawn_blocking` from a detached `tokio::spawn`, so `McpServer::new` returns immediately and the MCP stdio loop can answer `initialize` / `tools/list` in milliseconds. The `CancellationToken` is stored on the server up front so `shutdown` can cancel mid-walk if the agent disconnects before the watcher finishes initialising. Reported by @ottob with a sample-trace and an FSEvents-sandbox repro that left zero ambiguity about root cause.
- **`tokensave serve` no longer runs pre-serve maintenance work (#84).** `Commands::Serve` was running `try_flush` (synchronous HTTP round-trip to the worldwide counter), `check_install_stale`, and the silent-reinstall loop over every tracked agent before the MCP stdio loop even started. All three are now gated behind `should_skip_agent_install_maintenance`, alongside `Install` / `Reinstall` / `Uninstall` / `Doctor`. Same maintenance still runs on the user's next interactive `tokensave …` invocation.
- **`tokensave install --agent antigravity` now registers in both the IDE config and the CLI plugin directory (#85).** Previously only `~/.gemini/antigravity/mcp_config.json` was written, leaving the Antigravity CLI (`agy`) unable to see tokensave in `/mcp`. New: also writes `~/.gemini/antigravity-cli/plugins/tokensave.json` with the same `{"mcpServers": {"tokensave": {...}}}` shape. `uninstall` removes both, `doctor` reports both, `is_detected` triggers on either path. Reported by @ottob.
- **MCP `last synced N ago` warning no longer fires after a no-change sync (#86).** The warning was reading `MAX(files.indexed_at)`, which only advances when a file is actually reindexed. On quiet repos a successful `tokensave sync` (0 added / 0 modified / 0 removed) left `indexed_at` stuck and the warning fired forever. New: the warning is computed from the `last_sync_at` metadata key, which `sync()` writes unconditionally on every successful invocation. Falls back to `MAX(indexed_at)` only when the metadata key is missing (e.g. a freshly-initialised project that has never been synced). New `TokenSave::last_sync_timestamp()` helper exposes this for embedders. Reported by @uwe-sure.
- **`files_by_language` status output now uses real language names instead of bucketing everything as `Other`.** The SQL `CASE` in `Database::get_stats` only recognised four languages (Rust / Go / Java / Scala) and dumped everything else — Python, TypeScript, C, Swift, Kotlin, etc. — into `"Other"`. Replaced with a Rust-side bucketing helper covering 46 extractor languages; Python files in the FastAPI benchmark now correctly report as `Python` instead of `Other`. Includes special-case basename matching for extensionless `Dockerfile` / `Makefile`.
- **Pre-existing breakage in `tests/mcp_server_test.rs` repaired.** The whole `test-transport`-gated integration suite (31 tests) had been silently failing to compile since `McpServer::new` switched its return type to `Arc<McpServer>` — `setup_server` and `run_server_with_messages` still expected bare `McpServer`. Switched both helpers to `Arc<McpServer>` and bumped the resource-count assertion (`tokensave://status/files/overview/branches`) from 4 to 5 to include the newer `tokensave://schema` resource. All 31 tests now pass.

### Changed
- **Seven hand-rolled CLI subcommands replaced by the unified `tokensave tool <name>` dispatcher.** `query`, `context`, `body`, `impact`, `callers`, `files`, `affected` were each ~50–150 LoC of clap glue duplicating what the MCP tool already declares in its schema. All seven are gone; the same operations are reached as `tokensave tool query …`, `tokensave tool body …`, etc. Drops ~600 LoC of dispatch boilerplate from `src/main.rs`. `query` is kept as an alias for the renamed `search` so muscle memory still works.

### Internal
- New methods on `TokenSave`: `get_call_chain`, `get_nodes_by_name`, `replace_symbol`, `insert_at_symbol`, plus a private `resolve_symbol_for_edit` helper that does qname-first resolution and refuses ambiguous matches rather than silently picking the wrong site.
- New method on `GraphTraverser`: `find_path_directed` (BFS that follows outgoing edges only, with `max_depth` bound).
- `McpServer` now has a `timings_enabled: AtomicBool` field with `set_timings_enabled` / `timings_enabled` accessors, toggleable per-session.
- The dispatcher in `mcp/tools/handlers/mod.rs` wraps each `handle_tool_call` invocation with `Instant::now()` when `timings_enabled` is set and injects `_meta.duration_us` into the JSON-RPC result.

## [6.0.0] - 2026-05-25

### Breaking
- **Daemon mode removed.** The `tokensave daemon` subcommand, autostart flags (`--enable-autostart` / `--disable-autostart`), foreground mode, and all `daemon-kit`-backed service registration are gone. ~1,100 lines of platform glue (launchd plists, systemd user units, Windows SCM, PID files, UAC elevation) deleted. File-watching now lives inside the MCP server itself — it spawns a `notify`-backed watcher that runs `sync_if_stale_silent` for the duration of the agent session, and exits when the agent disconnects. Multiple MCP peers on the same project converge through the existing per-project sync lock plus `sync_if_stale_silent`'s peer-coordination check; no new primitive was needed. Users with a registered autostart service should unload it manually — see "Migration" in `docs/TOKEN-SAVE-WHATSNEW.md` §6.0.0.
- **`UserConfig::daemon_debounce` renamed to `watcher_debounce`.** TOML load is backwards-compatible via `#[serde(alias = "daemon_debounce")]` and any config-mutating command rewrites the file with the new name; Rust struct literals referencing the old name are a compile-time break.
- **`McpServer::new` now returns `Arc<Self>`.** The embedded watcher task captures a `Weak<Self>` so it cannot extend the server's lifetime. Embedders that bound the return value continue to compile; destructuring by value or storing into a non-`Arc` field needs to adapt.
- **`tokensave install --agent claude` writes the modern hook shape `{type, command, args}`.** Legacy single-string `"command": "<bin> <subcmd>"` entries are detected by `tokensave doctor` and auto-rewritten using `current_exe()` as the binary path (issue #81). This is a breaking change for any external tooling that introspects `~/.claude/settings.json` and assumed the legacy concatenated form.
- **Beta release channel disabled.** `.github/workflows/release-beta.yml` is gated behind `BETA_CHANNEL_ENABLED=false` and a `workflow_dispatch`-only trigger. The code is preserved for future revival; no `*-beta.*` versions will ship from this commit forward.

### Added
- **`tokensave_redundancy` MCP tool (#83).** AST-level functional-duplicate detector. Computes four signals per function/method body via tree-sitter — AST shape hash, control-flow-graph hash, ordered call-sequence hash, and a 5-gram token-shingle set — then blends them into a `[0, 1]` composite similarity score (weights 0.40 / 0.25 / 0.20 / 0.15). Pairs are bucketed `definite` / `likely` / `naming_only` and ranked by score. Language-agnostic by design: kind walks use raw tree-sitter strings, so the same code path works for every supported grammar. Computation is lazy — fingerprints land in a new `node_fingerprints` table (schema v10) keyed by `(node_id, body source hash)` and persist across MCP sessions. Pairwise comparison is bucketed by body-token count (±25 % window) so it stays sub-quadratic on large repos.
- **`tokensave_runtime` MCP tool + `tokensave status --runtime` flag (#80).** Captures a process + database telemetry snapshot: PID, RSS, virtual size, sustained CPU% sampled over 200 ms, uptime, host CPU count, total system memory, DB / WAL / SHM file sizes, `journal_mode` PRAGMA, total indexed source bytes, node and edge counts, and a derived `db / source` bloat ratio. Lets users hitting unexpected resource pressure attach a structured snapshot to a bug report. Text report mirrors the `tokensave status` layout; JSON output via `--json` for machine consumption.
- **`tokensave_health` `details=true` sub-score breakdown (#82).** Returns per-dimension `{ score, interpretation, raw_count, source }` objects covering acyclicity (with `edges_in_cycles`), depth (`max_chain` / `ideal_chain`), equality (gini + textual interpretation), redundancy (`dead_count` / `total_fns`), modularity (textual label + components-after-hub-removal), and coverage discipline (`skip_test_coverage_count`). The composite `quality_signal` (geometric mean × 10 000) is preserved as the headline figure.

### Changed
- **File-watcher rewritten around `notify-debouncer-full` 0.8.0-rc.2.** Replaces the DIY tokio debounce timer with the maintained library, which coalesces rename pairs, suppresses redundant modify-after-create, and batches event bursts cross-platform. Drop-in transparent to callers — `ProjectWatcher::new(root, debounce)` signature unchanged.
- **Watcher no longer recursive-watches the project root.** Top-level entries are enumerated at startup; `IGNORED_DIRS` (`target/`, `node_modules/`, `.git/`, …) and dotdirs (`.vscode`, `.idea`, …) are *never registered as watches*, so the kernel never reports events for them. The root itself is watched non-recursively to surface new top-level directories appearing after startup. This is the primary mitigation for the high CPU/RAM symptoms reported in #80 on Windows, where `ReadDirectoryChangesW`'s per-watch buffer could be overwhelmed by churn inside a large `node_modules`.
- **Direct `notify` dependency dropped.** Pulled transitively through `notify-debouncer-full`; project_watcher imports types via `notify_debouncer_full::notify` to keep a single crate instance in the resolver graph (debouncer 0.8.0-rc.2 depends on `notify` 9.0.0-rc.4).
- **`doctor` auto-repair logic for hook entries.** Modern-shape hooks with the wrong subcommand are fixed in place (preserving the user's bin path); legacy single-string hooks are rewritten using `current_exe()` since the embedded path cannot be parsed unambiguously when it contains spaces. Breaks the doctor → install loop that issue #81 reported on Windows path-with-spaces installs.

### Fixed
- **Hook install on paths with spaces (#81).** Previously, `install --agent claude` wrote `"command": "C:/Path With Spaces/tokensave.exe hook-pre-tool-use"` as a single string. Claude Code whitespace-splits the field, so the kernel executed `C:/Path` with arg `With` and the hook silently never fired. `doctor` correctly diagnosed the mismatch but then re-installed the same broken shape, looping forever. Switching to `{"command": "...", "args": ["hook-..."]}` sidesteps the splitter entirely. Doctor now also recognises the legacy shape as needing repair, not removal.

### Schema
- **Migration v10.** Adds the `node_fingerprints` table backing `tokensave_redundancy`: `node_id PRIMARY KEY`, `ast_hash`, `cfg_hash`, `call_seq_hash`, `shingles` (comma-separated lowercase hex), `body_tokens`, `source_hash`. Indexed on `ast_hash` and `body_tokens` for the redundancy query's bucketed scan. As with prior migrations, opening a v9 database triggers a one-time full re-index on first open.

### Internal
- **New `src/redundancy.rs` module.** Tree-sitter walk utilities (kind-only AST hash, control-flow filtered hash, ordered call-sequence hash), token shingle generator, Jaccard similarity over sorted shingle sets, composite similarity blender, and the severity bucketing rules. Eight unit tests cover identical-body / different-structure / call-sequence-order / Jaccard self-similarity behaviour.
- **New `src/runtime_telemetry.rs` module.** `RuntimeSnapshot { process, database }` collected via `sysinfo` 0.32 + libsql PRAGMA reads. Process side requires a refresh + 200 ms sleep + refresh sequence because sysinfo reports CPU% as a delta between successive readings.
- **`TokenSave::db_path()` accessor.** Recomputes the on-disk DB path via `resolve_db_for_branch` for diagnostics. Stable across the lifetime of an open `TokenSave`.
- **Tool registry grew from 68 → 71 (or 67 → 70 when `ast-grep` is unavailable).** Two new entries: `tokensave_redundancy`, `tokensave_runtime`. The previously shipped `tokensave_health` `details=true` schema knob is documented here for visibility.

## [5.1.2] - 2026-05-20

### Added
- **Kiro agent integration.** `tokensave install --agent kiro` now installs the MCP server, global steering, managed Kiro agent config, default CLI agent selection, and Kiro hook mappings for prompt context, delegated tool context, and post-write re-indexing. Doctor and uninstall support are included with coverage for workspace overrides and idempotent cleanup.

### Fixed
- **Edit tool UTF-8 failure handling (#78).** `tokensave_multi_str_replace` and `tokensave_insert_at` no longer panic when failure previews or long anchors contain multi-byte UTF-8 characters.
- **GW-BASIC / MSBASIC2 REM stripping.** Comment extraction no longer relies on a byte-length guard; the `REM` keyword is now content-checked (case-insensitive) before slicing, eliminating a latent panic on multi-byte comment text.

### Changed
- **Shared `text::utf8_prefix_at_or_before` helper.** The UTF-8-safe byte-budgeted prefix logic introduced for the edit-tool fix is now a shared `crate::text` utility, and `context::builder` reuses it in place of its hand-rolled `is_char_boundary` walk-back.

## [5.1.1] - 2026-05-16

### Performance
- **`tokensave_dead_code` no longer times out on chromium-scale repos.** The pre-4.14.8 form ran the leading-wildcard `LIKE '%::test'` chain inside a correlated `NOT EXISTS` on every dead-code candidate row — fast on scirs (0.097 s, 76 K `annotation_usage`) but timed out at the 25 s probe ceiling on chromium, cascade-poisoning every subsequent MCP tool call via JSON-RPC id reuse. 4.14.8's `WITH test_marker_ids AS (...)` CTE attempt regressed scirs from 0.1 s to >60 s because SQLite inlined the single-reference CTE, so the wildcard scan ran per candidate row instead of once; that attempt was reverted in 4.14.9. A first attempt that put marker ids into a single TEMP table and probed via `e2.source IN (SELECT id FROM temp.test_markers)` ALSO failed on chromium: SQLite picked `idx_edges_unique (source, target, kind)` for the correlated subquery and iterated every marker as the outer driver for every candidate (~13 K markers × ~134 K candidates ≈ 1.7 B probes), >60 s. New shape — **three-step resolve + pre-join + probe via TWO TEMP tables**:
  - `Database::collect_test_marker_ids` runs the marker `SELECT` exactly once over the `kind = 'annotation_usage'` partition (indexed via `idx_nodes_kind`).
  - `Database::populate_test_marker_temp_table` drops + recreates `temp.test_markers` (with `PRIMARY KEY` on `id` so SQLite builds a real B-tree) and bulk-inserts in 500-id chunks.
  - `Database::populate_test_annotated_targets_temp_table` joins `edges WHERE kind = 'annotates' AND source IN temp.test_markers` once, materialising "which node ids are annotated by any test marker" into `temp.test_annotated_targets` (PK on `target`). ~15 K rows on chromium.
  - `find_dead_code`'s outer SELECT then uses `nodes.id NOT IN (SELECT target FROM temp.test_annotated_targets)` — a single PK probe per candidate against a small indexed lookup table, the optimiser cannot re-shape this into a per-marker iteration.
  - Both temp tables are unconditionally dropped on the wrap path so a failed query does not leak rows to the next caller on the same connection.

  Inline comment block on `find_dead_code` documents all three prior pathologies (pre-4.14.8, 4.14.8 CTE, single-temp-table attempt) and a `DO NOT regress this` warning to forestall the next attempt.

  Verified end-to-end via the MCP probe (`scripts/mcp_probe/probe.py`) against the real chromium DB (7.5 GB, 4.4 M nodes, 206 K `annotation_usage`, 411 K annotates edges):

  | call | duration | notes |
  |---|---|---|
  | `dead_code {}` | 2.45 s | cold cache (first call after server start) |
  | `dead_code {limit: 10}` | 1.10 s | warm |
  | `dead_code {include_public: false}` | 1.10 s | warm |
  | `dead_code {path: "src"}` | 1.10 s | warm |
  | `dead_code {path: "lib"}` | 1.09 s | warm |

  Was 5/5 TIMEOUT @ 25 s pre-fix, cascade-poisoning every subsequent tool in the probe matrix. Direct `sqlite3` runtime measurement (no MCP layer): 0.75 s end-to-end (markers 42 ms → targets 102 ms → main 600 ms). On scirs (76 K `annotation_usage`): 0.6 s end-to-end — a regression-acceptable trade-off vs. the 0.097 s pre-4.14.8 baseline given that chromium went from >25 s timeout to <1.1 s steady-state. The synthetic regression test `tests/graph_test.rs::dead_code_marker_resolve_is_single_pass` (50 K `annotation_usage` / 5 K functions) runs in 1.3 s release / 3.6 s debug with a 5 s assertion ceiling.

## [5.0.0] - 2026-05-16

The largest functional jump since 4.0: nine new MCP tools, a cross-session response cache, and a schema-level rework of containment.

### Added

- **`tokensave_read`** — mode-aware file read (`full`, `lines`, `map`, `signatures`) with cross-session cache. `map` and `signatures` are graph-only — no source bytes are touched. A re-call on an unchanged file returns a ~30-token `{"unchanged": true, …}` stub. The cache key folds `last_sync_at` for graph-backed modes so a force-reindex correctly invalidates derived rows.
- **`tokensave_outline`** — flat list of every top-level symbol in a file, with optional kind filter. The cheapest way to orient before zooming into a large file.
- **`tokensave_implementations`** — find every type implementing a given trait, or every body of a given method name. Returns method bodies with signatures.
- **`tokensave_unsafe_patterns`** — surface `.unwrap()` / `.expect()` / `panic!` / `todo!` / `unimplemented!` / `unsafe { }` sites with an `in_test` flag. Word-boundary matching avoids `.unwrap_or` false positives; an `exclude_tests` option skips test-shaped paths.
- **`tokensave_diagnostics`** — runs the project's compile / type checker (cargo / tsc / pyright) and returns structured errors mapped to graph nodes. Replaces the recurring "shell out → parse text → read file" loop with one structured response. Cargo target dir is forced to `.tokensave/target/` so it can't race with the user's interactive cargo runs.
- **`tokensave_config`** — query TOML / JSON config files by dotted key path. Single file (`path`) or glob (`glob`); returns parsed value plus a heuristic line number. DB-free — works on uninitialized projects.
- **`tokensave_signature_search`** — find functions / methods by signature shape: return type, parameter substring, async flag, path filter. All filters AND-compose.
- **`tokensave_constructors`** — locate every literal-instantiation site of a struct (`Foo { … }`) and report which fields each site sets — plus `missing_fields` relative to the struct's current definition. The classic "I added a required field, what breaks?" question. String- / char-literal awareness and `match` / `if let` / `while let` pattern filtering keep the result list clean.
- **`tokensave_field_sites`** — partition every `.<field>` reference into reads and writes. Writes include `=`, compound assignments, and `&mut x.field` borrows; `==` and `=>` correctly count as reads.
- **`tokensave bench` colored console output** — default `tokensave bench` is now a fixed-width colored table instead of a markdown dump. Compact `k` / `M` numeric units; savings percentages colored by tier (green ≥80 %, yellow ≥50 %, red <50 %); aggregate footer in the same tier color. `--json` is unchanged.

### Changed

- **Schema v9: cross-session response cache.** New `read_cache` table keyed by `(project_id, session_id, file_path, mode, args_hash)` with `mtime_ns` for freshness. Backs `tokensave_read`.
- **Schema v9: `Contains` edges denormalized into `nodes.parent_id`.** The same migration folds containment off the edges table and onto a new column. Cleaner queries — `get_children_of(parent_id)` is one indexed lookup — and the read-only SQL layer no longer has to filter by edge kind for every "find members of this container" question. Extractors keep emitting `Contains` edges as before; the storage layer hoists them into `parent_id` at insert time and skips persisting the row.

### Migration notes

- **v9 is forward-only.** First sync after upgrade auto-applies the migration, populates `parent_id` from existing `Contains` rows, and deletes those rows.
- **Recovery path: `tokensave sync -f`.** If a downstream consumer still queries `Contains` edges directly (none of the in-repo tools do), force-sync rebuilds the graph from source under the new schema.
- External SQLite consumers reading the `edges` table should switch from `kind='contains'` filters to `nodes.parent_id` joins.

## [4.14.11] - 2026-05-16

### Performance
- **Same `node.child(i)` O(N²) trap fixed in `batch_extractor.rs`.** `visit_top_level`, `visit_label`, `extract_docstring`, and `extract_label_call_sites` all walked top-level children of the Batch program via `root.child(i)` in an index loop. Refactored: `visit_top_level` materialises children once into a `Vec<TsNode>` via cursor (`collect_children` helper) and downstream helpers take `&[TsNode]` + index instead of `(root, index)`. One O(N) allocation up front, O(1) lookups thereafter, no behavior change.
- **Same trap fixed in `powershell_extractor.rs::find_descendant_by_kind`.** The iterative DFS pushed children with `current.child(i)` in a `for i in (0..N).rev()` loop. Replaced with a `TreeCursor` walk + `stack[start..].reverse()` to preserve first-child-pops-first order — matches the `complexity.rs::push_children` pattern from 4.14.10.
- **Same trap fixed in `clojure_extractor.rs::extract_calls`.** Top-level form iteration over `list_lit` children used `node.child(i)` indexed loop. Replaced with cursor stepping (with `goto_next_sibling` `skip` times for the `skip` parameter). Particularly relevant on Clojure files with hundreds of top-level forms.
- **Same trap fixed in `cobol_extractor.rs::visit_procedure_division`.** The seed pass that collects PROCEDURE DIVISION children into a `Vec` for multi-pass paragraph grouping used `node.child(i)` in a loop — bites on monolithic COBOL files with many paragraphs. Switched to cursor walk; same O(N) materialisation, O(1) downstream indexing.

## [4.14.10] - 2026-05-16

### Performance
- **`count_complexity` (called from every extractor on every function) no longer hits an O(N²) trap on high-fanout AST nodes.** The body-walk in `src/extraction/complexity.rs` seeded its stack and pushed children with `node.child(i)` inside a `for i in 0..N` loop. Tree-sitter's `node.child(i)` is **O(i)** — it walks the linked sibling chain from the first child — so the seed + per-pop push pair was O(N²) for every node along the way. On `kernel/bpf/verifier.c` (20 K lines, monster switch statements with thousands of cases) a single `tokensave init` showed the progress bar wedged on that one file long enough that users reported it as "stuck"; chromium had files taking ~3 min individually. New `push_children` helper uses a `TreeCursor` (O(1) per sibling step) and reverses the appended slice so LIFO pop order still produces left-to-right traversal. Same fix applied to `extract_call_name`, `extract_macro_name`, and `rightmost_identifier` — all three did the same O(N²) `child(i)` scan over identifier candidates. Measured on `verifier.c` after the fix: 78 ms end-to-end (file read + parse + extract). Includes `examples/bench_extract.rs` so you can re-measure with `cargo run --release --example bench_extract <path-to-c-file>`.

## [4.14.9] - 2026-05-16

### Fixed
- **Revert the 4.14.8 `find_dead_code` CTE refactor — it was a massive regression on real repos.** 4.14.8 moved the test-marker name match into `WITH test_marker_ids AS (...)` thinking that would amortise the leading-wildcard `LIKE`. In practice SQLite does not always materialise a single-reference CTE, and `e2.source IN (SELECT id FROM test_marker_ids)` inside a correlated `NOT EXISTS` degenerated into a per-row scan of the full `annotation_usage` table. On scirs (76 K annotation_usage rows, 153 K annotates edges) `tokensave_dead_code` went from **0.097 s** (pre-4.14.8) to **>60 s timeout**, which hung the MCP probe matrix — every subsequent tool then appeared to time out because the late response poisoned the JSON-RPC id matching (the cascade caveat in `scripts/mcp_probe/README.md`). The original `JOIN nodes a ON a.id = e2.source` form works because `idx_edges_target_kind` narrows to the (typically 0-3) annotates edges per candidate first, then joins via the nodes PK, so the LIKE only runs on that small per-candidate slice. A `Do NOT lift this into a CTE` comment is left at the call site so future refactors don't repeat the mistake. Other 4.14.8 perf changes (SCC frame clone, multi-source BFS, lines cache, dedup'd FTS terms, file-content cache, cycle-path borrowing, inheritance-depth CTE shape, has_bare_call fast path, placeholder builder) are kept as-is.

## [4.14.8] - 2026-05-16

### Performance
- **Tarjan SCC no longer clones the entire neighbor list per edge visited.** The iterative DFS in `graph/scc.rs` used `work.last_mut().cloned()` on each loop iteration — that deep-copies the top tuple `(node, neighbors, idx)` including the whole `Vec<N>` of neighbors, once per neighbor visited (so ~`out-degree × visits` full Vec clones during a node's life). Rewrote the loop to peek the top frame with `work.last_mut()` and clone only the two values actually needed (`node` and `next`) before any `work.push(...)`. Every SCC consumer benefits: `tokensave_circular`, `tokensave_port_order`, and (since 4.14.7) `tokensave_recursion`.
- **`tokensave_diff_context` walks the impact radius once for the union of modified symbols, not once per symbol.** The old loop called `get_impact_radius(node.id, depth)` for every modified node — each call ran an independent BFS from scratch, so any downstream node reachable from K modified ancestors got re-traversed K times. New `GraphTraverser::get_impact_radius_multi(seed_ids, max_depth)` does one BFS seeded with all modified node IDs and a single shared `visited` set; the result has every reachable node visited at most once. Surfaces as `TokenSave::get_impact_radius_multi`. Particularly impactful on diamond-dependency hotspots (shared utility files reachable from every changed module).
- **`tokensave_recursion` caches source-file lines instead of re-splitting on every self-edge check.** `is_direct_self_call` was caching the raw `String` source but then doing `let lines: Vec<&str> = source.lines().collect();` on each call — for a 10 k-line file with N self-edges, that's N × 10 k allocations purely to throw away. Cache changed to `HashMap<String, Option<Vec<String>>>` so the line vector is built once per file.
- **`tokensave_recursion` cycle-path DFS uses borrowed `&str` and stops once the limit is hit.** `cycle_path_for_scc` / `dfs_cycle_path` previously used `Vec<String>` / `HashSet<String>` on hot paths, allocating a `String` per neighbor visit even though every id already lived in `scc_set`. Switched to `&str` borrows over the SCC's existing storage. The outer loop also sorts SCCs by length first and short-circuits as soon as `cycles.len() == limit`, so we no longer enumerate every cycle in a giant mutually-recursive graph before truncating.
- **`tokensave_inheritance_depth` CTE collapses the hierarchy before joining `nodes`.** The recursive CTE produced one row per (leaf, depth) pair across the full hierarchy; the outer SELECT then ran the `file_path LIKE ?` filter over all of them. Wrapped the hierarchy in a `leaf_depths` CTE that `GROUP BY leaf_id` first, so the path filter and node join only see distinct leaves.
- **`tokensave_dead_code` resolves the test-marker annotation set in a single CTE pass.** Each candidate dead-code row previously re-evaluated `a.name LIKE '%::test'` (and three more leading-wildcard `LIKE`s) — none of those can use an index, so the cost scaled with `dead-candidates × annotation_usage`. New `WITH test_marker_ids AS (…)` resolves the marker ids once; the dead-code subquery then checks `e2.source IN (SELECT id FROM test_marker_ids)`.
- **`ContextBuilder::find_entry_points` deduplicates FTS terms across the five search rounds.** Full query, extracted symbols, stem variants, and agent-provided extra keywords overlap heavily (e.g. `symbol "foo"` and `keyword "foo"` produce identical FTS results); each duplicate term cost a full DB roundtrip on the single-connection libsql. Terms are now collected into one ordered, deduplicated list before any `search_nodes` calls — original priority preserved (full query → symbols → stems → keywords) so the `cap`-based early exit still favours higher-signal terms first.
- **`ContextBuilder` reads each source file at most once per `build_context`.** Both `extract_code_blocks` and `merge_adjacent_blocks` previously called `get_code(node)` which did its own `fs::read_to_string` per call; merging K adjacent blocks meant K disk reads of the same file. Introduced `get_code_cached(node, file_cache)` that consults a shared `HashMap<String, Option<String>>`; `build_context` allocates one cache for the request and threads it through both phases.
- **`has_bare_call` short-circuits lines with no `(` and rejects substring matches on both identifier boundaries.** Common short names like `new` / `get` / `len` triggered `line.match_indices(name)` over the full line and then filtered post-hoc — pathological on comment/docstring lines that mention the name without calling it. Added a `line.contains('(')` fast path plus an after-byte identifier-boundary check (so `new` no longer pre-matches inside `newer`).
- **`get_nodes_by_ids` and friends build their `IN (?, ?, …)` placeholder string in one allocation.** Previous `(1..=ids.len()).map(|i| format!("?{i}")).collect()` allocated one `String` per id and an intermediate `Vec<String>` per call — visible on profiles because `traverse_bfs` invokes `get_nodes_by_ids` once per BFS level. New `build_qmark_placeholders(n)` writes `?, ?, ?, …` into a single pre-sized `String`; libsql binds anonymous markers positionally so behaviour is unchanged. Applied at `get_nodes_by_ids`, `batch_incoming_call_counts`, and `search_nodes_by_exact_name`.

## [4.14.5] - 2026-05-16

### Fixed
- **`tokensave_inheritance_depth` no longer explodes on cyclic / near-cyclic trait-bound graphs.** The recursive CTE in `get_inheritance_depth` had a depth bound of 50 but no cycle detection, so any cycle in the `extends` graph (common in Rust workspaces where generic trait bounds form indirect cycles) made the CTE traverse the cycle up to the depth limit from every entry point. On polkadot-sdk (959 `extends` edges) the query took >60 s and timed out; smaller workspaces (sotf 89, scirs 90, sonium 5) were fine. Fix tracks visited node IDs in a path column and skips recursion when the next target is already in the path — query completes in 0.55 s on polkadot end-to-end through MCP. Regression test `test_get_inheritance_depth_terminates_on_cycle` constructs a 3-node graph with an A↔B cycle and a C→A edge, then asserts the query returns in <2 s with all three nodes reported at finite, bounded depth.

### Added
- **`scripts/mcp_probe/` — MCP test-matrix harness.** Drives a fresh `tokensave serve` MCP server over stdio against a configurable set of real repos and exercises every read-only tool with 5 query variants per language, producing a per-tool / per-repo status table that flags tools needing investigation (errors, timeouts, empty results, perf regressions). Same harness doubles as a benchmark — per-call timings are logged, repos serve as a fixed corpus for cross-version perf comparison. Pluggable per-language probe modules under `tools/`; Rust ships included (`tools/rust.py` exercises all 50 MCP tools). `repos.toml` (overridable via `$TOKENSAVE_PROBE_REPOS`) holds the repo set. JSON-RPC ids are strictly matched in `probe.py::McpClient` so a slow call cannot poison subsequent ones; `isolated.py` adds a fresh-server-per-tool retry loop for tools that already showed a real timeout. `build_matrix.py` renders the log to markdown. Used to find and prove this release's `inheritance_depth` cycle bug; same harness verifies the 4.14.4 fixes stay green across the four real repos (sotf, sonium, scirs, polkadot-sdk).

## [4.14.4] - 2026-05-16

### Fixed
- **`tokensave_doc_coverage` reports public fields, enum variants, constants, statics, type aliases, properties, …** The query previously filtered to `kind IN ('function', 'method', 'class', 'interface', 'trait', 'struct', 'enum', 'module')` — so a Rust file full of `pub` undocumented struct fields reported `total_undocumented: 0` even though `tokensave_module_api` listed dozens of public symbols on the same file. Kind allow-list expanded to include `field`, `enum_variant`, `const`, `static`, `type_alias`, `property`, `csharp_property`, `record`, `data_class`, `sealed_class`, `object`, `case_class`, `kotlin_object`, `inner_class`, `abstract_method`, `constructor`, `struct_method`, `val`, `var`, `mixin`, `extension`, `union`, `typedef`. Excludes `namespace` and `package` — those are aggregators that almost never carry their own doc and would just drown out actionable items. Single `const` deduplicates the prefix and no-prefix branches. Verified end-to-end on real DBs: `biquad.rs` in sotf went from 0 → 23 undocumented public symbols; polkadot-sdk reports ~51 K with a sensible per-kind breakdown. Regression test `test_get_undocumented_public_symbols_includes_fields_and_variants`.
- **`tokensave_dead_code` excludes `#[test]`-annotated functions whose name does NOT start with `test`.** The previous filter was name-prefix-only (`name NOT LIKE 'test%'`), so `#[test] fn from_measurement_slope_excludes_lfe()` and similar leaked through. The libtest harness is an implicit caller that never appears as a graph edge, so without this filter most Rust tests with non-`test*` names got misreported as dead. Detection now walks the `annotates` edges and excludes any node whose annotation_usage name is `'test'`, `'…::test'` (covers `tokio::test`, `async_std::test`), `'wasm_bindgen_test'`, or `'…::wasm_bindgen_test'`. The JOIN is constrained to `a.kind = 'annotation_usage'` to avoid accidental matches. Real-DB impact: sotf 1794 → 540 dead functions (-70 %), sonium 778 → 209 (-73 %), scirs 4 839 → 2 469 (-49 %), polkadot-sdk **12 136 → 2 295 (-81 %)**. Manual spot-check on polkadot confirmed every dropped name is unambiguously a `#[test]` function. Regression test `test_find_dead_code_excludes_test_annotated`.
- **`tokensave_ast_grep_rewrite` surfaces a useful message when `ast-grep` exits non-zero with empty stderr.** ast-grep returns exit 1 with completely silent stdout/stderr when its pattern matches 0 nodes or when the file's language can't be inferred from the extension (`.txt`). The previous error string was `"ast-grep failed: "` — empty trailer, no actionable info. New handler falls back through stderr → stdout → an explicit explanation listing likely causes (pattern matched 0 nodes, language not inferred, invalid pattern), plus the exit code and the file + pattern that failed. Regression test `ast_grep_rewrite_surfaces_useful_error_on_empty_stderr`.
- **`tokensave_port_status` no longer cross-matches methods that share a name but belong to different parent types.** The match key was `(name.to_lowercase(), kind_compat_group)`, so `Biquad::new` matched `Adaa::new`, `Biquad::process` matched any other `process`, and so on — useless on Rust workspaces where every type has a `new`, `process`, `fmt`, `reset`, etc. Match key now also includes the parent qualifier (stripped of generics — `Biquad<T>` and `Biquad` resolve identically) for kinds that have one (`method`, `field`, `enum_variant`, `struct_method`, `abstract_method`, `constructor`, `csharp_property`, `property`, `val`, `var`); top-level kinds (struct, function, enum, trait) keep name-only matching since their containing context in `qualified_name` is just a file path. Regression tests `port_status_does_not_match_methods_of_different_parents` (Biquad in dir A, Adaa in dir B — must NOT match) and `port_status_matches_methods_with_same_parent_type` (Biquad in both dirs — must match).
- **`tokensave_branch_diff` returns an empty diff when base == head instead of erroring.** Previous behaviour was `MCP error -32603: base and head are the same branch`, inconsistent with `tokensave_pr_context` which already handled the same case by returning empty arrays. Same-ref now returns the normal JSON shape with `summary: {added:0, removed:0, changed:0}`, empty `added`/`removed`/`changed` arrays, and a `note` field explaining the equality — so callers can rely on a single response shape. Regression test `branch_diff_returns_empty_when_base_equals_head`.

## [4.14.1] - 2026-05-15

### Fixed
- **`tokensave_search` always ranks definitions above `use` re-exports.** BM25 was scoring short `pub use crate::operator::LinearOperator;` rows highly enough that five re-exports outranked the actual `pub trait LinearOperator { … }` definition. Sort now uses a coarse `kind_tier` as the primary key (defs tier 0, impl tier 1, values/members tier 2, modules tier 3, `use`/`include`/annotation usage tier 4); BM25 score is secondary within a tier. Added a per-row exact-name match boost (+10) so a trait named exactly `Foo` beats a `Method` whose qualified name happens to contain `Foo`. Regression test `search_ranks_trait_definition_above_use_reexports` constructs a trait plus five `pub use` re-exports across sibling modules and asserts the trait is hit #1.
- **`tokensave_dead_code` no longer treats `annotates` / `derives_macro` / `contains` edges as "this function is alive" evidence.** Real-world Rust pervasively uses `#[inline]`, `#[derive(Debug)]`, and similar attributes — every annotation_usage node creates an `annotates` edge pointing at the function, which the previous `kind != 'contains'` filter accepted as a live reference. Result on the sonium codebase: 0 dead functions across 5,715. Narrowed the SQL filter to an explicit allowlist of real-use edges: `calls`, `implements`, `extends`, `type_of`, `returns`, `receives`, `uses`. Regression test `dead_code_flags_unreferenced_fn_with_attribute` exercises the `#[inline]` case.
- **`tokensave_unused_imports` handles grouped imports (`use std::collections::{HashMap, HashSet};`).** The previous parser treated the literal string `{HashMap, HashSet}` as one identifier and never matched it against the file body, so every grouped import was effectively ignored — explaining the user's "0 unused across 3,404 use nodes" report. A new `identifiers_from_use_path` helper splits grouped/aliased/nested forms (`foo::bar`, `foo::bar as baz`, `foo::{a, b as c}`, `foo::{a, nested::b}`, `foo::{self, bar}`), and the handler now reports one entry per truly-unused identifier with an `unused: <identifier>` field. Regression test `unused_imports_handles_grouped_use` verifies the unused half of a grouped use is flagged while the used half is not.
- **`tokensave_changelog` filters deleted-subtree directory entries.** When an entire subtree was removed in a diff, gix yielded a directory-mode deletion entry whose path was gone from disk by the time the post-hoc `is_dir()` check ran — so directories like `crates/sonium-bem` slipped through as `removed_or_not_indexed`. `git_diff_files` now inspects `entry_mode.is_tree()` on each gix `Change` record (addition/modification/deletion/rewrite) and never pushes a tree entry into the changed-files list. The disk-based `is_dir()` filter is kept as belt-and-suspenders for additions/modifications. Regression test `changelog_filters_deleted_directory_entries` synthesises a `git rm -r crates/` commit and asserts no non-`.rs` paths appear in `changed_files`.
- **`tokensave_diff_context.modified_symbols` dedupes by node id and dedupes the input `files` array.** Callers that synthesised the file list from upstream tooling (directory walks, multi-source mergers) sometimes passed the same path multiple times — `hmatrix.rs` was reported up to 7× in a row. Added a `modified_seen: HashSet<String>` to guard pushes and an early `files` dedup pass. Regression test `diff_context_dedupes_modified_symbols_on_duplicate_input` passes the same path three times and asserts unique node ids in the output.
- **`tokensave_pr_context` collapses Cargo.toml into a single `config_summary` entry.** Behaviour was already present; added the regression test `pr_context_collapses_cargo_toml_keys` which synthesises a real git history with a 50-dependency Cargo.toml bump and asserts at most one Cargo.toml entry surfaces (kind = `config_summary`).
- **`tokensave_circular` SCC disjointness stress test.** Added `circular_emits_disjoint_sccs_under_load` — five 3-file cycles connected by non-cyclic DAG-style tails — to guard against any future SCC implementation drift that might let a file leak into more than one cycle entry.

### Added
- **`tokensave_port_order` surfaces intra-cycle ordering signals.** Each cycle entry now reports per-symbol `in_cycle_in_degree` and `in_cycle_out_degree`, a file-level `members_in_cycle` breakdown ranked by member count, an explicit `entry_point` (the SCC member with the smallest in-cycle out-degree — leaf-most, the natural starting point), and a `break_point_candidate` (the highest in-cycle in-degree node, the hub whose call sites are the most-effective refactor target). Replaces the previous flat blob of 200+ symbols with no guidance on where to start. Regression test `port_order_provides_intra_cycle_ordering` wires a 4-node SCC with one obvious hub and asserts `break_point_candidate.name == "h"`.

### Changed
- **`tokensave_ast_grep_rewrite` is conditionally registered.** The tool is only advertised via `tools/list` when the external `ast-grep` binary is on PATH at server-startup time (cached via `OnceLock` so we don't fork on every `tools/list` request). When the binary is missing, models never see a tool that would immediately return "ast-grep is not installed" on first call. `tokensave::mcp::tools::ast_grep_available()` is now public; tests in `mcp_handler_test::test_tool_definitions_complete` and `mcp_test::test_tool_definitions_count` branch on it so they pass on hosts with or without the binary installed.

## [4.14.0] - 2026-05-15

### Fixed
- **`tokensave_run_affected_tests` dispatches directly-changed test files.** Previously the handler only walked callers of every node in `changed_paths` — `#[test]` functions are leaves with no callers, so a PR that only touched `tests/foo.rs` returned "no tests cover the changed paths" and skipped running anything. The handler now also dispatches test functions whose file is itself in `changed_paths` (either via `is_test_file` path heuristic or `#[test]` annotation), with the test recorded as covering itself in `covers_source_ids`.
- **`parse_derives_in_attr_block` handles rustfmt's multi-line derive blocks.** The previous line-bounded scanner only matched `#[derive(...)]` when the closing `)` was on the same line, so rustfmt's split form (`#[derive(\n    Debug,\n    Clone,\n)]`) dropped every derive. The parser now joins the attribute-block lines and scans for `#[derive(` ... `)` across the whole region. Two new unit tests (`parses_multiline_derive_attribute`, `parses_multiline_derive_mixed_with_single_line`) cover the split form.
- **`tokensave_diagnose` normalises absolute and backslash paths.** Cargo emits absolute spans when `--manifest-path` points outside cwd, and Windows cargo emits backslash-separated paths; neither matches the indexed forward-slash, project-relative form. `node_at_location` now calls a new `normalize_lookup_path` helper that (1) replaces `\` with `/`, (2) strips the canonicalised project-root prefix for absolutes, and (3) falls back to a raw prefix strip when canonicalisation fails. A diagnostic spanning either form now maps to the correct node.
- **Resolver kind-compatibility filter now applies to the same-file blocklist branches (bug #11 follow-up).** PR8's filter was wired into the main `try_exact_name_match` / `try_qualified_match` paths but not the two `CROSS_FILE_BLOCKLIST` branches in `try_exact_name_match` and `try_exact_name_match_simple`. Common blocklisted names (`new`, `default`, `clone`, …) could still bind a `Calls` reference to a same-file non-callable — a struct or const sharing the name. Both branches now filter candidates through `kind_compatible` before declaring a same-file match. Regression test `resolver_blocklist_branch_respects_kind_filter` reproduces the case (`struct new` + `caller() { let _ = new(); }`) and asserts callees only include callable kinds.

## [4.13.0] - 2026-05-15

### Fixed
- **Resolver kind-compatibility filter (bug #11)** — `tokensave_rank --edge-kind implements` (and every downstream tool: `tokensave_impls`, `tokensave_type_hierarchy`, `tokensave_callees`'s trait dispatch, …) was poisoned by the resolver fuzzy-binding `impl Default for X` to whatever local node happened to share the name `Default`. The sonium codebase had a parser `Token` enum with a `Default` variant; 150 manual `impl Default for X` blocks all bound to that one `enum_variant`, swamping the rank tool with junk.
- New `kind_compatible(ref_kind, target_kind)` helper in `src/resolution/resolver.rs` enforces a structural matrix:
  - `Implements` / `Extends` / `DerivesMacro` → must target trait/interface/class/abstract-method/sealed-class/annotation/type-alias kinds
  - `Calls` → must target a callable (function/method/struct-method/constructor/abstract-method/arrow-function/procedure/macro)
  - `Annotates` → must target annotation/decorator kinds
  - `Uses` / `TypeOf` / `Returns` / `Contains` / `Receives` → permissive (any kind)
- Both `try_qualified_match` and `try_exact_name_match` now apply the filter; when filtering shrinks the candidate list, a `resolve_from_filtered` helper picks the same-file candidate first then falls back to the first overall, with confidence reduced to reflect the partial match. This prevents the previous "any-name-wins" behaviour without dropping legitimate resolutions.
- Regression test `implements_refs_dont_resolve_to_enum_variants` constructs the exact sonium-style scenario (`enum Token { Default, Plus }` plus two manual `impl Default for X` blocks) and asserts that `tokensave_rank --edge-kind implements` does NOT list an `enum_variant` or `field` target. Existing DBs need `tokensave sync --force` to re-resolve refs under the new constraints.

## [4.12.0] - 2026-05-15

### Added
- **`src/graph/scc.rs` — Tarjan's strongly-connected-components algorithm.** Iterative (no recursion, no stack-blow risk on deep graphs), generic over node-id type, returns components in reverse-topological order matching what port ranking needs. Used by both `tokensave_circular` and `tokensave_port_order`. Five unit tests cover DAGs, two-node cycles, three-cycle-plus-tail, self-loops, and reverse-topo emission order.

### Fixed
- **`tokensave_circular` reports one entry per SCC, not per DFS walk (bug #10)** — the previous implementation emitted every distinct DFS path through a cycle, producing 73 overlapping cycle entries on the sonium codebase that all shared a long common tail. `find_circular_dependencies` now computes SCCs via Tarjan and emits one entry per genuine mutually-recursive group, filtering out trivial single-node components that don't have self-loops. The legacy `dfs_cycle_detect` helper and `_legacy_walk_cycles` shim were removed.
- **`tokensave_port_order` exposes per-SCC cycle groups (bug #12)** — previously, every unsorted node after Kahn's topological sort was lumped into a single "Mutual dependency — port together" entry, so two disjoint mutually-recursive pairs `(a,b)` and `(c,d)` would render as one mega-cycle and lose all signal. The handler now runs Tarjan on the subgraph of unsorted nodes and emits one cycle entry per non-trivial SCC, with the `files` set of each cycle surfaced so the user has a concrete "break this edge" target. Each entry carries `symbols`, `files`, `size`, and a refined `note`.

## [4.11.0] - 2026-05-15

### Fixed
- **`tokensave_dependency_depth` no longer follows `implements`/`extends` edges (bug #7)** — the resolver fuzzy-binds `impl Debug for T` and similar across unrelated files, producing chains of spurious file-to-file deps (the report observed a 19-level chain spanning 17 unrelated files terminating in a foreign crate). `build_file_adjacency` now follows only `calls` and `uses` edges. Existing `tokensave_health` and `tokensave_circular` callers benefit too — they share the same adjacency builder.
- **`tokensave_dead_code` no longer reports 0 on `pub`-heavy codebases (bug #8a)** — two fixes: (1) the `NOT EXISTS` subquery now excludes `Contains` edges, which previously masked every node behind its parent's bookkeeping edge; (2) new `include_public: true` argument opts into auditing pub items with no indexed callers, useful for workspace-internal cleanup. Default behaviour (no flag) still excludes pub items as before.
- **`tokensave_unused_imports` no longer returns 0 on real codebases (bug #8b)** — the previous graph-only check tested `incoming.is_empty()`, but every Use node has at least one Contains edge from its parent, so the predicate never fired. New heuristic reads the source file once (cached per file) and checks whether the imported identifier appears as a whole-word token outside the use statement itself; matches what `cargo`'s own unused-import lint does. `pub use` re-exports, glob imports, and `use self::...` are skipped (intentional aliases / out-of-scope for textual heuristics). Three regression tests cover unused-detection, the dead-code Contains-edge bug, and the new `include_public` opt-in.

### Changed
- **`TokenSave::find_dead_code` signature** — gained an `include_public: bool` parameter. Existing callers (`tokensave_health`, internal tests) updated to pass `false` to preserve previous semantics.

## [4.10.0] - 2026-05-15

### Fixed
- **`tokensave_body` prefers callable kinds over same-named fields (bug #1)** — sonium hit a case where querying `gmres` returned only a struct field literally named `gmres` and missed the obvious `pub fn gmres(...)`. The handler now does an exact-name DB lookup first (via the PR1 suffix-fallback path) so the function isn't buried under BM25 noise, then sorts matches by `body_kind_preference()`: callable (0) > type def (1) > impl (2) > value (3) > field/variant (4) > use (5).
- **`tokensave_changelog` / `commit_context` / `pr_context` no longer list directories (bug #4)** — gix's `for_each_to_obtain_tree` yields directory-level entries when an entire subtree changes. `git_diff_files` now filters out any path that resolves to a directory on disk, so callers see only file paths.
- **`tokensave_diff_context.impacted_symbols` dedupes by node id (bug #5)** — diamond dependencies caused the same downstream node to appear 6+ times consecutively. `impacted_seen: HashSet<String>` now guards inserts.
- **`tokensave_recursion` drops length-1 self-cycles (bug #6)** — single-node cycles are almost always either resolver fuzzy-binding (`self.push()` cross-bound across distinct impls of the same name) or trivial self-recursion. Cycles with `< 2` distinct nodes are now filtered out before being added to the result set.
- **`tokensave_commit_context` / `tokensave_pr_context` collapse config-file symbols (bug #3)** — Cargo.toml's 50+ dependency keys used to each enumerate as a separate "modified symbol", blowing past 50K tokens on a real diff. Both handlers now emit a single `{kind: "config_summary", file, config_keys: N}` entry per file with role `config` (`*.toml` / `*.yaml` / `*.json` / `*.ini` / `*.cfg` / `*.lock`).
- **`classify_file_role` no longer flags source files with inline tests as "test" (bug #3 follow-up)** — a `src/foo.rs` with `#[cfg(test)] mod tests` at the bottom keeps role `source`. The "test" bucket is reserved for files that exist purely to host tests (path-based check via `is_test_file`). Three unit tests in `mcp::tools::handlers::git::tests` cover the classification matrix.
- **Rust extractor emits `Extends` edges for supertrait bounds (bug #9)** — `trait Leaf: Middle + Base` now produces unresolved refs with `EdgeKind::Extends` for each bound, so `tokensave_inheritance_depth`'s recursive CTE walks Rust supertrait chains correctly. Bound extraction handles `type_identifier`, `scoped_type_identifier`, `generic_type`, and `higher_ranked_trait_bound`. Existing DBs need a re-index (`tokensave sync --force`) to pick up the new edges.

## [4.9.0] - 2026-05-15

### Added
- **`tokensave_derives` tool** — surfaces the `#[derive(...)]` macros attached to a type plus the trait + method names each one synthesizes. Closes the dead-end-search gap where calls like `.clone()`, `format!("{:?}", x)`, or `serde_json::to_string(&x)` resolve to methods that never appear in the graph (the impl is generated by the proc-macro at compile time). Accepts either `qualified_name` or `node_id`. Well-known derives carry full info (trait path, method list, source crate); unknown / proc-macro derives surface with `well_known: false` and just the derive name.
- **`derive_table` module** (`src/derive_table.rs`) — static knowledge of well-known derives (`Debug`, `Clone`, `Copy`, `Default`, `PartialEq`, `Eq`, `PartialOrd`, `Ord`, `Hash`, `Serialize`, `Deserialize`, `Display`, `Error`), each mapped to its canonical trait path and method names. Five unit tests cover known + unknown derives and the `enrich` wrapper.
- **`derives` field on `tokensave_node` output for type nodes** — when the queried node is a `Struct` / `Enum` / `Union` / `Record` / `CaseClass` / `DataClass` / `PascalRecord`, the response now includes a `derives` array so callers don't need a second roundtrip just to learn what derives are present.
- **`TokenSave::get_derives_for_node(node_id)`** — public helper that re-reads the node's source-file attribute block and parses `#[derive(...)]` directly. The graph's `DerivesMacro` edges are unreliable: the resolver fuzzy-binds std-trait names like `Debug` to nonsense targets (e.g. a `Debug` enum variant in an unrelated test fixture), and the unique constraint on `(source, target, kind, line)` then collapses multiple derives on the same type onto a single edge. Re-parsing from source costs one `fs::read` per node lookup (cheap at typical Rust source sizes) and recovers the full derive list. Five unit tests in `derive_parse_tests` cover single/multi-block derives, qualified paths, mixed attribute kinds, and dedup.

### Changed
- **Total MCP tools: 59 → 60** — `tokensave_derives` added.

## [4.8.0] - 2026-05-15

### Added
- **`tokensave_diagnose` tool** — parses raw `cargo check` / `cargo clippy` / `rustc` stderr into structured diagnostics, then maps each one to the smallest containing graph node and (by default) pre-attaches up to 5 callers. Closes the today-an-agent-hand-parses gap: the response includes severity, optional error code (`E0308`, clippy lint name), message, file/line/column, the owning node (id, kind, qualified_name, span), and the call sites the broken code is reachable from. Diagnostics without a `--> file:line:col` span are dropped — they cannot be located. Accepts a `severity` filter (`error` / `warning` / `all`) and a `max_diagnostics` cap (default 50, hard cap 500).
- **`tokensave_run_affected_tests` tool** — closes the loop opened by `tokensave_test_map` / `tokensave_test_risk`. Given `changed_paths` (or, by default, `git diff --name-only HEAD`), the handler walks the graph to find every test that covers a function/method in those files, then runs `cargo test --no-fail-fast -- <names>` with `kill_on_drop` and a configurable `timeout_secs` (default 300). Parses libtest stdout into JSON `{ test, passed, covers_source_ids[] }` entries plus pass/fail counts and the cargo exit code; trailing stdout/stderr are tailed at 2 KB each so the response stays in budget. `max_tests` defaults to 100 (hard cap 500) so a refactor touching everything doesn't dispatch an unbounded list.
- **`src/diagnose.rs`** — standalone parser module. Five unit tests cover typed errors (`error[E0308]`), clippy-style headers without codes, summary lines without spans (correctly dropped), multi-diagnostic blocks, and ANSI-prefixed lines.
- **`TokenSave::node_at_location(file, line_1based)`** — public helper that returns the smallest-span node containing a 1-based source location. Used by `tokensave_diagnose`; converts to the internal 0-based representation transparently.

### Changed
- **Total MCP tools: 57 → 59** — `tokensave_diagnose` and `tokensave_run_affected_tests` added.
- **New handler module `src/mcp/tools/handlers/workflow.rs`** — keeps cargo/libtest plumbing out of `graph.rs`, which is for code-graph queries.

## [4.7.0] - 2026-05-15

### Added
- **`tokensave_impls` tool** — index of `impl Trait for Type` blocks. Accepts optional `trait` and `type` filters (both short and qualified names). With neither, returns every impl in the graph. Surfaces information that was previously buried behind the second-class `Implements` edge: which types satisfy a given trait, which traits a type implements, and the impl blocks themselves with their files and signatures.
- **Trait dispatch resolution on `tokensave_callees`** — when a callee resolves to a method whose enclosing scope is a trait, the handler walks back via `Implements` edges to surface the concrete impl methods reachable through that trait. New entries are tagged `dispatch_via_trait: true` and carry a `dispatch_from` pointer to the trait method. Pass `resolve_dispatch: false` to opt out and get only direct call edges.
- **`TokenSave::get_impls(trait, type)`** — public helper backing the new tool.
- **`TokenSave::get_trait_dispatch_targets(method)`** — public helper that returns every impl-method satisfying a given trait method, used by `handle_callees` to surface dispatch targets.

### Changed
- **Total MCP tools: 56 → 57** — `tokensave_impls` added.
- **`tokensave_callees` description and schema** updated to advertise dispatch resolution and the new `resolve_dispatch` argument.

### Fixed
- **`tokensave_search` ranks definitions above references (PR1 follow-up)** — BM25 alone was placing `use foo` statements ahead of the actual `pub fn foo()` definition because both score similarly when the symbol name matches. `TokenSave::search` now over-fetches and re-ranks: every `NodeKind` carries an explicit bonus (callable defs +3.0, type defs / proto defs +2.5, impl blocks +2.0, values / macros / enum variants +1.0, members +0.5, neutral 0.0, container modules -1.5, annotation usages -2.0, `use` / `include` -3.0). The match is exhaustive so adding a new `NodeKind` forces a re-tune here. Result: searching for `gmres` returns the function before its imports.
- **`get_nodes_by_qualified_name` falls back to suffix or bare-name match (PR1 follow-up + user feedback)** — strict equality match remains primary. On empty results: queries with `::` retry as `qualified_name LIKE '%::<query>'` (full scan, `LIMIT 50`); queries without `::` retry as `name = ?` using `idx_nodes_name`. Both forms now resolve, e.g. `get_impls`, `TokenSave::get_impls`, and the full doubled path all return the same row. `tokensave_signature` and `tokensave_by_qualified_name` share the lookup so they agree.
- **Rust extractor no longer doubles the file path in `qualified_name`** — `qualified_prefix()` prepended `self.file_path` even though the file root was already pushed onto `node_stack` at extraction start, producing qnames like `src/foo.rs::src/foo.rs::Type::method`. Now iterates the stack only, yielding `src/foo.rs::Type::method`. Existing DBs will keep the old form until re-indexed (`tokensave sync --force`).
- **`get_impls` batches the trait lookup (PR2 review follow-up)** — previously one `get_node_by_id` per impl block (N+1). Now collects every Implements-edge target then issues a single `get_nodes_by_ids` to populate the trait map.
- **`graph_stale` insertion asserts on non-object results (PR1 review follow-up)** — `handle_tools_call` now `debug_assert!`s that the wrapped tool result is a JSON object before attaching the `graph_stale` field, matching the "crash hard on unknown value" convention so a future handler returning a non-object is caught immediately instead of silently dropping the structured staleness signal.
- **`cost_to_expand` body heuristic documented as Rust-tuned (PR1 review follow-up)** — the `20 tokens/line` rate over-estimates Haskell/Python by ~2-3x; the doc comment now explicitly says so and notes the single-line floor of 20 tokens, since this number is part of the public tool contract.

## [4.6.0] - 2026-05-15

### Added
- **`tokensave_signature` tool** — signature-only lookup by `qualified_name` or `node_id`. Returns visibility, signature string (generics, params, return type, where clauses), docstring, kind, and async flag for matching nodes. No body content. Replaces most agent `Read` calls when only the public-API surface of a symbol is needed.
- **`graph_stale` field on tool results** — when files referenced by a tool result remain stale after the post-call sync attempt, the JSON-RPC response now carries a top-level `graph_stale: ["path", …]` array plus a machine-parseable `tokensave_graph_stale: [...]` text marker. The existing human-readable WARNING is preserved. Closes the silent-drift gap where renamed/deleted symbols could return phantom callers/callees without a programmatic signal.
- **`cost_to_expand` annotation on node results** — `tokensave_node` and `tokensave_signature` responses now include `cost_to_expand: { body, full_file }` (approximate tokens) so callers can decide whether to set `include_code=true` before re-querying. Body estimate uses ~20 tokens/line; `full_file` uses indexed `files.size / 4`.
- **`tokensave://schema` MCP resource** — markdown resource documenting the on-disk `.tokensave/tokensave.db` schema: tables, columns, indexes, FKs, common query recipes (impl-of-trait, top callers, largest functions), and gotchas (content-hashed IDs, trait dispatch, derive macros). Makes the SQLite escape hatch usable without trial-and-error.
- **`TokenSave::get_file_size_bytes(path)`** — public helper that returns the indexed byte size of a file (0 when unknown). Backs the `cost_to_expand` full-file estimate.

### Changed
- **Total MCP tools: 55 → 56** — `tokensave_signature` added; all existing tools unchanged.

### Fixed
- **Clippy: project-wide cleanup to restore `-D warnings`** — 43 pre-existing lib errors and 3 bin errors resolved without behavioral change: module doc comments wrap snake_case tool names in backticks; `bench.rs` uses `write!` instead of `format!(..).push_str`; `extraction_worker.rs` converted to `let…else`; redundant closures in `agents/copilot.rs`, `extraction/haskell_extractor.rs`, `mcp/tools/handlers/memory.rs` replaced with method references; `resolution/resolver.rs` merges identical match arms; `serve.rs` uses `sort_by_key`; `upgrade.rs` uses `is_ok_and`; `main.rs` drops a useless `.into()`.

## [4.5.1] - 2026-05-15

### Added
- **`tokensave monitor` highlights the last 3 updates** — the most recently active (project, tool) pair renders green, second-to-last orange, third-to-last yellow. Re-firing the same tool moves it to the front rather than duplicating. Cleared on Ctrl+R.
- **Welcome banner on fresh installs** — when `tokensave` is invoked with no subcommand and the global DB has zero registered projects, print a cyan welcome that explicitly suggests `tokensave init` before the existing "Create one now?" prompt. Returning users see no change.

### Fixed
- **CI: `clippy::items_after_test_module` denied under Rust 1.95.0** — two test modules from the v4.5.0 work (`gain_tests` in `src/commands.rs`, `gain_format_tests` in `src/display.rs`) were inserted mid-file. Rust 1.95.0 promoted this lint into `clippy::all`, which the project denies project-wide. Both moved to file end.
- **CI: `cargo fmt` drift across 11 files** — accumulated unwrapped one-line `println!` / `match` / struct literals from the v4.5.0 features; re-formatted to match `rustfmt` expectations.

## [4.5.0] - 2026-05-15

### Added
- **Cross-session memory primitives (3 new MCP tools)** — `tokensave_record_decision`, `tokensave_record_code_area`, and `tokensave_session_recall` persist agent decisions and worked-on paths in the per-project DB so they survive across sessions. `session_recall` uses FTS5 for fuzzy retrieval. Backed by two new tables and an FTS mirror added in schema migration v8.
- **`tokensave gain` CLI for the savings ledger** — every MCP tool call now writes an append-only row to a new `savings_ledger` table in the global DB. `tokensave gain [--all] [--history] [--range 7d] [--json]` reports tokens saved + dollar estimates (Sonnet input pricing, refreshed daily via LiteLLM).
- **`tokensave bench` reproducible retrieval benchmark** — runs a fixed query set through `tokensave_context` and reports retrieval savings vs a full-file baseline (CCE-style methodology). Ships with a 10-query generic default set embedded into the binary (no external file dependency); `--queries <file>` accepts a custom set. Measured **93% mean retrieval savings on tokensave's own repo** (180K → 3.4K tokens across 10 generic queries).

### Changed
- **Schema bumped from v7 to v8** — adds `memory_decisions`, `memory_code_areas`, and the `memory_decisions_fts` virtual table. Existing user DBs upgrade idempotently via `migrate_v8`; fresh installs use the mirrored DDL in `create_schema`. No breaking changes; existing tools and queries continue to work.
- **`GlobalDb::open()` refactored to delegate to `GlobalDb::open_at(path)`** — enables test isolation via `tempfile::TempDir` without process-wide `HOME` mutation. The public `open()` API is unchanged.
- **Total MCP tools: 52 → 55** — three new memory tools added; all existing tools unchanged.

### Fixed
- **`coverage_discipline` health penalty reduced from 10% to 2% (issue #76)** — annotating genuinely untestable functions with `/// skip-test-coverage` was dropping `quality_signal` despite improving `coverage_pct`, because the penalty had no positive counterbalance (coverage doesn't feed into the composite health score). Max penalty reduced so honest annotation is not punished.

## [4.4.0] - 2026-05-14

### Fixed
- **Rust extractor now detects function calls inside macro invocations (issue #72)** — `assert!(check_count(5))` previously only created a Calls edge to `assert`, missing `check_count`. The extractor now walks into `token_tree` nodes inside macros to find nested call patterns.
- **`test_risk` denominator no longer includes test functions (issue #73)** — functions with `#[test]` annotations and functions inside `::tests::` modules are now excluded from `total_functions` and the coverage percentage denominator.
- **Rust extractor resolves instance method calls (issue #74)** — `instance.method()` now emits an additional unresolved ref with just the method name, allowing the resolver to match it against `impl` method definitions. Previously only associated function calls (`Type::new()`) were resolved.

### Added
- **`/// skip-test-coverage` doc comment convention (issue #75)** — mark genuinely untestable functions to exclude them from `test_risk` coverage calculations. The `skipped` count appears in the summary. A `coverage_discipline` health dimension penalises overuse (up to 10% quality signal reduction).
- **VS Code Insiders support for the Copilot installer (issue #69)** — `tokensave install --agent copilot` now also configures `Code - Insiders/User/settings.json` alongside the regular VS Code path.
- **Copilot prompt instructions (issue #70)** — the Copilot installer now writes `copilot-instructions.md` with tokensave MCP tool guidance to VS Code (`User/prompts/`), VS Code Insiders, and Copilot CLI (`~/.copilot/`).

## [4.3.18] - 2026-05-14

### Fixed
- **Inline `#[cfg(test)]` test modules are now recognized as test coverage** — `test_map`, `health`, `test_risk`, `affected`, `impact`, and `commit_context` previously only detected tests by file path patterns (`tests/`, `_test.`, etc.). Functions annotated with `#[test]` inside inline `#[cfg(test)] mod tests { ... }` blocks in source files (226 such functions in tokensave's own codebase) were invisible to coverage analysis. The Rust extractor now emits `Annotates` edges from `#[cfg(test)]` to modules, and all test-detection handlers query `#[test]` annotations via the graph in addition to checking file paths.
- **`tokensave serve` resolves the correct project in multi-folder workspaces (issue #66 reopened)** — when multiple projects are registered in the global DB, the `serve` fallback now picks the project closest to cwd (ancestor match first, then descendant match) instead of failing with an ambiguity error. As a last resort, the server peeks at the MCP `initialize` request's `roots` array to discover the workspace folder the client is working in.

## [4.3.17] - 2026-05-14

### Fixed
- **`tokensave upgrade` no longer breaks Homebrew installs (issue #67)** — previously, self-upgrading a Homebrew-managed install mutated the Cellar directly, leaving Homebrew's recorded keg state inconsistent and causing later `brew upgrade` to fail. `tokensave upgrade` now detects Homebrew installs and delegates to `brew update && brew upgrade tokensave`. (PR #68, thanks @lesbass)
- **Exclude globs now match nested directories (issue #64)** — the default `node_modules/**` pattern only excluded top-level `node_modules/`, not nested ones like `projectA/node_modules/`. Changed default to `**/node_modules/**`. Also added `is_excluded_dir()` so bare patterns like `**/dist` correctly prune directories during scanning without requiring a trailing `/**`.
- **VS Code multi-folder workspaces can now start the Copilot MCP server (issue #66)** — the Copilot config used `${workspaceFolder}` which VS Code cannot resolve in multi-folder workspaces. Dropped in favour of the serve command's built-in project discovery, matching every other agent integration.

## [4.3.16] - 2026-05-11

### Fixed
- **Windows CI failure introduced by v4.3.15's zed regression test** — `test_zed_install_preserves_existing_config` seeded `AppData/Roaming/Zed/settings.json` on Windows, but `zed_config_dir` actually uses `.config/zed/settings.json` on every non-macOS platform (Linux *and* Windows). The hand-written `#[cfg(target_os = "windows")]` branch in the test silently diverged from the production helper, so the test wrote the seed to one path and the install wrote to another — backup never appeared at the seeded location and the test failed. The Windows job (Linux passed, since its branch happened to be correct) was the only one to catch the drift.

### Changed
- **`AgentIntegration::primary_config_path(home) -> Option<PathBuf>`** — new trait method that returns the single config file the integration rewrites on install/uninstall. Every agent that goes through `safe_write_json_file` or `write_toml_file` implements it (claude, gemini, cursor, opencode, zed, cline, roo-code, copilot, kilo, antigravity, codex); vibe leaves the default `None` because its TOML config is append-only and has no rewrite path. Regression tests in `tests/agent_test.rs` now call `agent.primary_config_path(home)` instead of duplicating platform-conditional path logic — the production helper is the single source of truth, so a future `zed_config_dir`-style change can't drift between tests and reality. A meta-test (`test_every_tested_agent_advertises_primary_config_path`) walks every integration covered by the install regression suite and asserts the method returns `Some(path)` under the test home, so a new integration added without wiring it up fails fast with a clear message instead of producing a confusing missing-backup panic later.

## [4.3.15] - 2026-05-11

### Fixed
- **Installing the Codex integration no longer wipes `~/.codex/config.toml` (issue #63)** — `load_toml_file` used `contents.parse::<toml::Value>()`, which in the `toml = "1"` crate parses a single TOML *value* rather than a *document*. Any well-formed `config.toml` therefore parsed as an error and silently fell back to an empty table; `install_mcp_server` then serialized that empty-plus-tokensave table back over the file, erasing every other key the user had set (model, approval_policy, other `[mcp_servers.*]` entries, comments). `load_toml_file` now uses `toml::from_str::<toml::Table>` so real documents round-trip, returns `Result` instead of swallowing errors, and refuses to overwrite when an existing file cannot be parsed (so a typo or partial edit leaves the original intact for the user to fix). `doctor_check_config`, `install_mcp_server`, `uninstall_mcp_server`, and `CodexIntegration::has_tokensave` were updated to handle the `Result` shape — the doctor now reports parse errors as a failed check, and `has_tokensave` returns `false` on parse error rather than panicking.

### Changed
- **Every config-file write across all agent integrations now leaves a `.bak` copy first.** Previously only install paths went through `backup_config_file`; uninstall paths and `doctor` auto-repair paths called `std::fs::write` directly, so a corrupted serialization or a bug in the rewrite logic could destroy the user's settings with no recovery. A new shared `backup_and_write_json` helper (in `src/agents/mod.rs`) wraps `backup_config_file` + `safe_write_json_file` with best-effort error handling suited to uninstall flows. Every agent's uninstall path (claude, cursor, copilot, cline, zed, kilo, roo-code, opencode, gemini) now goes through this helper, as do the claude `doctor` auto-repair and local-settings-cleanup paths. The Codex TOML write path (`write_toml_file`) also creates a `.bak` before writing for the same reason. Eight per-agent install-side regression tests plus a cursor uninstall-side regression test were added to `tests/agent_test.rs` to guard the new invariant.

## [4.3.14] - 2026-05-11

### Fixed
- **`tokensave_body` no longer drops the function's outer closing brace (issue #62)** — `handle_body` returned the source spanning `start_line..end_line`, but stored line fields are tree-sitter rows (0-based) while `extract_lines` was written assuming 1-based inclusive inputs. The mismatch meant `lines[start..end_line]` exclusive — one short, lopping off the trailing `}` (or any language's outer block closer sitting on its own line). Inner braces were unaffected because they were never on the boundary. `extract_lines` now treats inputs as 0-based row indices and slices inclusively, so the returned body is byte-exact usable as an `Edit` tool `old_string`. Regression added in `test_body_returns_full_function_source` (`tests/mcp_handler_test.rs`) — verified failing pre-fix with `body: "\nfn format_greeting(name: &str) -> String {\n    format!(\"Hello, {}!\", name)"` (closing `}` missing).

### Changed
- **`tokensave_body` now exposes `start_line` / `end_line` as 1-based file line numbers** — they were previously the raw 0-based tree-sitter row indices, which read as "off by one" against the line numbers any editor or `Edit`-style tool displays. The values now match what users see when they open the file, so the reported `end_line` is the line containing the function's closing brace. The shift is local to `handle_body`; other handlers still expose `node.start_line` as-is.

## [4.3.13] - 2026-05-10

### Changed
- **Switched to `tree-sitter-grammars/tree-sitter-markdown` (block + inline split parsers)** — the previously-vendored `ikatyang/tree-sitter-markdown` (last updated 2023, GLR-heavy without native frontmatter handling) hung the indexer on otherwise-fine markdown files containing YAML frontmatter. Specifically, the old grammar parsed `---\n…\n---` content as ordinary markdown, where 6/8/10-space-indented YAML lines were simultaneously valid as both deeply-nested list-item continuations and as indented code blocks; tree-sitter's GLR explored all alternatives in parallel, with the surviving-versions count growing exponentially per line. A real-world 18 KB resume.md hung the worker indefinitely; a 4.4 KB minimal reproducer was bisected and is now a regression fixture (`tests/fixtures/markdown_yaml_frontmatter_hang.md`). The new grammar emits an opaque `(minus_metadata)` / `(plus_metadata)` node for frontmatter, so the markdown rules never see the YAML — the same 4.4 KB reproducer parses in ~7 ms, the full 18 KB file in ~16 ms. The markdown extractor was rewritten for the new AST (block parser produces `(atx_heading … heading_content: (inline …))`, headings still become `Module` nodes; the inline parser is run over each `(inline)` byte range via `set_included_ranges` to extract `(inline_link)` for `Uses` edges). All 16 existing markdown extraction tests still pass; 3 new regression tests guard the migration.

### Added
- **Per-file extraction timeout** — every extractor round trip is now wrapped in a watchdog (configurable via `extraction_timeout_secs` in `~/.tokensave/config.toml`, default 60 s). A file whose extractor doesn't respond in time has its worker subprocess killed via `Child::kill()` and is recorded in `SyncResult.skipped_paths` with reason `"extractor timed out (>Ns)"`. Worker crashes (the existing failure path) are now also recorded with reason `"extractor crashed (...)"` instead of disappearing silently. This bounds the worst case for any future grammar pathology — `tokensave sync` can no longer hang forever on a single malformed file.

## [4.3.12] - 2026-05-09

### Changed
- **The beta channel is open again** — `tokensave channel beta` was hard-gated to `"the beta channel is not available at this time"` while the prior 4.5.x beta line was being merged into stable. With v5.0.0-beta.1 published on the prerelease channel, the gate is removed: `switch_channel` now resolves `"beta"` through the same path as `"stable"` and downloads the latest GitHub prerelease. The `unknown channel` error message also lists `beta` as a valid target again.
- **Retired the "beta channel has been merged into stable" nudge** in `main.rs`. Beta users (anyone whose binary version contains `-`) used to see the nudge on every invocation; with the channel reopened the nudge is no longer correct. Beta users now stay on beta until they explicitly run `tokensave channel stable`.

### Fixed
- **`tokensave wipe` no longer leaks the global DB into the wipe set when `$HOME` is symlinked** — the home `.tokensave` skip relied on lexical path equality, so a user whose `$HOME` resolves through a symlink (e.g. macOS `/Users/x` vs the canonical `/private/var/...`) could see `~/.tokensave` show up as a wipe target if the descendant walk reached it via the canonical chain. The skip now canonicalizes both the home path and every candidate before comparing.

### Changed (carried forward from the prior unreleased section)
- **Descendant walk for `tokensave wipe` / `tokensave list` is now iterative with cycle protection** — `find_descendant_tokensave` used to recurse, which made deep trees a stack-overflow risk and relied entirely on `file_type()` skipping symlinks for cycle safety. It now uses an explicit worklist plus a canonical-path `visited` set, so the walk is bounded even if a directory cycle slips past the symlink filter (e.g. Windows junctions).
- **`tokensave doctor` purges stale global-DB entries in batched statements** — purging used to issue one `DELETE` per stale row, which meant N serial round-trips against libsql for a stale-store cleanup (the case that prompted this: 216 deletes). A new `GlobalDb::delete_projects(&[String])` issues one `DELETE … WHERE path IN (…)` per chunk of 256, so the same 216-row purge is now one round-trip.
- **`gather_local_projects_from` is now a separately-exported helper** — extracts the pure discovery logic from the cwd-driven `gather_local_projects` wrapper so the ancestor + descendant walk can be unit-tested without mutating the process's working directory. Backed by 7 new tests covering cwd / ancestor-only / descendant-only / ancestor+descendant dedup / `node_modules` skip / canonical home-skip / empty-dir.
- **Cleared `clippy::map_unwrap_or` warning in `display::shuffle_flags`** — the xorshift seed now uses `map_or` instead of `map(...).unwrap_or(...)`. Behavior unchanged.

## [4.3.11] - 2026-05-09

### Added
- **`tokensave doctor` now reports stale entries in the global DB and offers to purge them** — projects registered in `~/.tokensave/global.db` whose `.tokensave/` directory is gone (deleted, moved, or scratch dirs cleaned up by the OS) are listed under the "Global database" section. Up to 10 paths are shown with an "… and N more" tail. When run interactively, the doctor prompts `Purge N stale row(s) from the global DB? [Y/n]`; on confirmation each stale row is deleted via `GlobalDb::delete_project`. When stdin is not a terminal (CI, piped invocation), the stale list is shown as a warning with a hint to re-run interactively.

### Fixed
- **`tokensave reinstall` now refreshes every detected agent, not just the first one ever installed** — `migrate_installed_agents` previously returned early as soon as `installed_agents` was non-empty. A user who installed agent A and later configured agent B (e.g. installed Copilot first, then Claude) would have only A in the list, so `reinstall` silently skipped B and its tool permissions never got refreshed when new tools shipped. The migration now scans every agent on each call and additively appends any whose tokensave config exists on disk but is missing from the tracked list. Side effect: a stale `tokensave install` warning ("N new tokensave tool(s) not yet permitted") could persist across reinstalls — that no longer happens. The detection logic is also extracted into a pure `detect_missing_installed_agents` helper covered by a regression test that reproduces the original "claude missing when copilot is tracked" scenario.
- **`tokensave wipe` warning banner now reaches full width** — the colored title row was 49 visual columns while the `═` rules above and below were 64, producing a short red strip floating between long horizontal lines. The title is now centered and padded with red-background spaces, sandwiched between two blank red rows so the warning reads as a single fixed-width block.

## [4.3.10] - 2026-05-09

### Added
- **`tokensave list` command for inspecting tracked projects** — `list` shows the same projects `wipe` would target (current folder, ancestors, and descendants), with on-disk `.tokensave/` size and tokens-saved per row, sorted by tokens-saved descending. `tokensave list --all` (or `-a`) lists every project tracked in `~/.tokensave/global.db`, marking entries whose `.tokensave/` directory has been removed as `(stale)`.

### Changed
- **Country flags in `tokensave status` are now shuffled on every render** — when more flags are tracked than fit on the line, the row used to always show the same prefix and `…` truncate the rest. Each `status` invocation now applies a Fisher-Yates shuffle (xorshift64 seeded from time + PID) before truncation, so a different sample of contributing countries is shown each time.

### Fixed
- **Tool-permission warning now points at `tokensave reinstall`** — when new tokensave tools are detected that aren't yet permitted in the agent config, the warning previously said "Run `tokensave install` to update", which would re-do the full install. The warning now reads "Run `tokensave reinstall` to update permissions", which is the right command for refreshing permissions on already-installed agents.

## [4.3.9] - 2026-05-09

### Added
- **`tokensave wipe` command for clearing local DBs** — `wipe` finds every `.tokensave/tokensave.db` project in the current folder, all its ancestors, and all its descendants (skipping `node_modules`, `target`, `.git`, `vendor`, `dist`, `build`, `.next`, `.venv`, `__pycache__`, and the user-level `~/.tokensave/`), then prompts for a `go!` confirmation before removing each `.tokensave/` directory and its row in the global DB. `tokensave wipe --all` (or `-a`) instead wipes every project tracked in `~/.tokensave/global.db` and then deletes the global DB itself, leaving it empty. Both flows display a bordered, blinking warning that lists every target before asking for confirmation.

## [4.3.8] - 2026-05-06

### Added
- **`DISABLE_TOKENSAVE=true` environment variable to opt out per-project (#19)** — when set in the MCP server configuration, the `serve` command exits cleanly without initializing. This lets users selectively disable tokensave for large projects that consume too much RAM, without removing it from their global agent config.

## [4.3.7] - 2026-05-06

### Fixed
- **Incremental sync no longer aborts on cross-file edge references (#58)** — `insert_edges` now uses a conditional INSERT that silently skips edges whose source or target node does not yet exist in the database. Additionally, both incremental sync loops now insert all nodes first and queue edges for a second pass, so cross-file edges within the same sync batch always find their targets. Previously, `INSERT OR IGNORE` did not suppress FK violations, causing the sync to abort with `FOREIGN KEY constraint failed`.

## [4.3.6] - 2026-05-06

### Fixed
- **`upgrade` no longer stops the daemon when release assets aren't ready yet** — the preflight asset check now runs before stopping the daemon, so if CI hasn't finished building the release binaries, the command exits cleanly without disrupting the running MCP server.

## [4.3.5] - 2026-05-06

### Changed
- **Copilot MCP server now passes the workspace folder to `serve`** — both the VS Code (`mcp.servers.tokensave`) and the Copilot CLI (`mcpServers.tokensave`) registrations now launch the daemon as `tokensave serve -p ${workspaceFolder}` instead of plain `tokensave serve`. This lets the MCP server scope its index to the active workspace automatically without requiring a manual `-p` flag.
- **Copilot agent args validation tightened** — tests for `CopilotIntegration` now verify that `"serve"` is strictly the first argument and that all remaining args are limited to `-p` / `${workspaceFolder}`. This prevents silent regressions where extra or reordered flags could be injected into the MCP server launch command.

### Fixed
- **`serve` now falls back to the global project database when CWD discovery fails (#55)** — when VS Code Copilot (or another host) launches `tokensave serve` with the working directory set to the user's home folder and `${workspaceFolder}` fails to resolve, the server now checks `~/.tokensave/global.db` for registered projects. If exactly one project is found, it is used automatically; if multiple are found, they are listed on stderr with guidance to pass `-p <path>`.
- **`insert_at` no longer strips the trailing newline from edited files (#57)** — `str::lines()` discards the final `\n`, so the file was silently rewritten without its POSIX-required trailing newline. The join result now re-appends `\n` when the original file ended with one.
- **Clippy CI failures resolved** — fixed 6 `deny`-level clippy errors across extractors (identical `if`/`else` blocks in clojure, redundant `trim()` before `split_whitespace` in haskell, `map_or` → `is_some_and`, `Iterator::last` → `next_back` in SQL, `too_many_arguments` allow in haskell `emit`).
- **Foreign-key violations during incremental sync now point at the recovery path** — when an extractor produces an edge whose source or target is not in the same file's node set, `tokensave sync` would die with `failed to insert edge: SQLite failure: FOREIGN KEY constraint failed` and no guidance. Full re-index masks this because bulk load disables FK enforcement, so the top-level error handler now detects this specific failure and suggests `tokensave sync -f`.
- **Spinner no longer leaks on early exit** — added `Drop` for `Spinner` so when `?` propagates an error mid-sync the worker thread is joined, the line is cleared, and the cursor is restored. Previously the cursor stayed hidden after a failed sync.

## [4.3.4] - 2026-05-02

### Fixed
- **`tokensave sync` no longer hangs on large monorepos with `node_modules` symlinks** — the directory walker now prunes excluded directories (e.g. `node_modules`, `vendor`, `build`) at the `filter_entry` level before descending into them. Previously, exclusions were only checked per-file after the walker had already entered the directory, so monorepo setups where a package manager creates symlinks inside `node_modules` pointing back into source directories (e.g. `../../api`) could cause the scanner to spin indefinitely. Closes #36.

## [4.3.3] - 2026-05-02

### Added
- **`tokensave_body`** — new MCP tool that returns the full source body of a symbol by name (function, struct, const, etc.). Collapses search + node lookup + file read into a single call; returns multiple ranked matches when the name is ambiguous.
- **`tokensave_todos`** — new MCP tool that finds TODO, FIXME, XXX, HACK, WIP, NOTE, and UNIMPLEMENTED markers across the project. Each result includes the marker kind, file, line, the comment text, and the enclosing symbol name. Filterable by marker kind and path prefix.

### Fixed
- **SQL (and 8 other new-language) files no longer panic during sync** — `tokensave-large-treesitters 0.4.0` is now published to crates.io and `Cargo.toml` references the registry version instead of a local path. Users who built 4.3.2 via `cargo install` received the old 0.3.2 grammar bundle (no SQL), causing a panic per `.sql` file. Closes #53.

### Changed
- **`tokensave-large-treesitters` dependency pinned to published 0.4.0** — switched from a local path dependency to `"0.4.0"` so `cargo install tokensave` picks up the full grammar set including SQL, R, Julia, Haskell, OCaml, Clojure, Erlang, Elixir, and F#.

### Internal
- **Grammar completeness test** — `ts_provider::tests::all_extractor_keys_are_registered` verifies every language key an extractor passes to `ts_provider::language()` is present in the bundled grammar table. CI will catch mismatches before a release ships.

## [4.3.2] - 2026-05-01

### Added
- **9 new language extractors — R, SQL, Julia, Haskell, OCaml, Clojure, Erlang, Elixir, F#** — closes the gap between tokensave and sentrux for functional and data-science languages. Each extractor handles the language's primary top-level constructs and is gated behind its own `lang-*` feature flag, all included in `full`:
  - **R** (`.r`, `.R`) — function assignments (`foo <- function(...)`), call sites, roxygen2 docstrings. Requires `tokensave-large-treesitters` ≥ 0.4.0.
  - **SQL** (`.sql`) — `CREATE TABLE`, `CREATE VIEW`, `CREATE FUNCTION`, `CREATE PROCEDURE` via `tree-sitter-sequel`.
  - **Julia** (`.jl`) — `function`, `macro`, `struct`, `abstract_definition`, `module` definitions; import/using nodes.
  - **Haskell** (`.hs`, `.lhs`) — `function`/`bind` declarations, `data_type`/`newtype`, `class`, `instance`, `import` nodes.
  - **OCaml** (`.ml`, `.mli`) — top-level `let_binding` (function vs const), `type_definition`, `module_definition`, `class_definition`, `open` nodes.
  - **Clojure** (`.clj`, `.cljs`, `.cljc`) — `defn`/`defmacro`, `ns`, `def`/`defonce`, `defprotocol`/`defrecord`/`deftype` via `list_lit` dispatch on the first symbol.
  - **Erlang** (`.erl`, `.hrl`) — `fun_decl` with arity-qualified names (`foo/2`), `-module` attribute, `-type`/`-opaque` declarations.
  - **Elixir** (`.ex`, `.exs`) — `def`/`defp`, `defmodule`, `defmacro`/`defmacrop`, `defstruct` via `call`-node dispatch on the function head.
  - **F#** (`.fs`, `.fsi`, `.fsx`) — `function_or_value_defn`, `type_definition`, `module_defn`, `namespace`, `open_decl` nodes.
- **Complexity configs for all 9 new languages** — `R_COMPLEXITY`, `SQL_COMPLEXITY`, `JULIA_COMPLEXITY`, `HASKELL_COMPLEXITY`, `OCAML_COMPLEXITY`, `CLOJURE_COMPLEXITY`, `ERLANG_COMPLEXITY`, `ELIXIR_COMPLEXITY`, `FSHARP_COMPLEXITY` added to `src/extraction/complexity.rs`.
- **`tokensave-large-treesitters` 0.4.0** — bundles the 9 new tree-sitter grammars: `tree-sitter-r`, `tree-sitter-sequel`, `tree-sitter-julia`, `tree-sitter-haskell`, `tree-sitter-ocaml`, `tree-sitter-clojure-orchard`, `tree-sitter-erlang`, `tree-sitter-elixir`, `tree-sitter-fsharp`.

### Fixed
- **`tokensave monitor` displayed temp directories as projects** — MCP clients that create per-request temp directories (names matching `.tmp…`) were appearing as project entries in the monitor. These are now filtered out at render time; the TOTAL line reflects only real projects.

### Changed
- **`tokensave monitor` now supports scrolling** — Up/Down arrows scroll one line at a time; PageUp/PageDown scroll one screen. Scroll offset is clamped to the available content and resets to zero on Ctrl+R. Footer hint updated accordingly.

## [4.3.1] - 2026-05-01

### Fixed
- **`tokensave_str_replace`, `tokensave_multi_str_replace`, and `tokensave_insert_at` silently mutated files for unsupported types (issue #51)** — all three tools write the file to disk and then call `reindex_file` to update the graph. For file types without a registered extractor (e.g. `.css`, `.html`), `reindex_file` returned `Err("unsupported file type: …")`; the `?` propagated that error to the caller, which reported tool failure — but the write had already been committed. The fix changes `reindex_file` to return `Ok(())` early when no extractor is found, so edits to unsupported file types succeed and the graph simply skips reindexing for those files.

### Changed
- **Sync duration is now tracked and displayed** — `GraphStats` gains a `last_sync_duration_ms` field persisted to the metadata store. All three sync paths (full index, `sync_single_files`, `sync_with_progress_verbose`) write this value. The status table's sync row now shows the duration inline: `Last sync 2m ago (1.2s)  Full sync 1d ago`. Duration is omitted when the value is unknown (existing databases before this change).

## [4.3.0] - 2026-04-30

### Added
- **Subprocess-isolated extraction** — every file is now parsed inside a short-lived worker process rather than in the sync process itself. If a tree-sitter grammar segfaults, calls `abort()`, or otherwise terminates by a path Rust cannot intercept, only the worker dies; the pool respawns it, the offending file is logged and skipped, and sync continues. This is a stronger guarantee than the v4.2.1 `catch_unwind` defense, which could only catch Rust panics.
  - The worker is exposed via a hidden subcommand (`tokensave extract-worker`) that authenticates against the parent through a 256-bit per-spawn token: required as both an env var and as the first 32 bytes on stdin. A user invoking the binary directly hits the missing-env check and exits non-zero. The subcommand is also hidden from `--help`.
  - When `current_exe()` does not point at a real `tokensave` binary (e.g. under `cargo test`, where the test harness is the running binary), extraction transparently falls back to the in-process path. Tests therefore continue to exercise extractors directly without needing to spawn subprocesses.
  - Defaults to `available_parallelism()` workers; opt out via `TOKENSAVE_DISABLE_SUBPROCESS=1` if needed.

### Changed
- Single-file extraction (used by the `tokensave_str_replace`, `tokensave_insert_at`, etc. edit tools) still runs in-process — the subprocess overhead is unjustified for one-shot operations and these tools are interactive enough that an extractor crash is immediately visible.

## [4.2.1] - 2026-04-30

### Fixed
- **Sync no longer aborts when a tree-sitter grammar hits an internal assertion (issue #49)** — the vendored `tree-sitter-markdown` C++ scanner contains `assert()` calls that, on certain autolink constructs, called `abort()` and killed the entire `tokensave sync` process (core-dumped on Linux). Two layers of defense:
  - Added `.cargo/config.toml` with `CFLAGS=-DNDEBUG` and `CXXFLAGS=-DNDEBUG`. `cc-rs` reads these env vars when compiling vendored grammars in `tokensave-large-treesitters`'s build script, disabling C/C++ assertions in release builds. A failed assertion now degrades to a malformed parse tree (which the extractor handles gracefully) instead of `SIGABRT`.
  - Added a `safe_extract` helper that wraps every `extractor.extract()` call site with `std::panic::catch_unwind`. A Rust panic from any extractor (malformed input, future bugs) now logs the file path and skips it instead of bringing down the whole sync.
- See issue #50 for the broader follow-up: migrating to pure-Rust generated parsers via the `--rust` fork of tree-sitter to eliminate this class of failure entirely.

## [4.2.0] - 2026-04-30

### Added
- **Health & structural analysis tools** — seven new MCP tools that expose quality insights from the existing code graph:
  - `tokensave_health` — composite quality signal (0–10000) from five independent dimensions: acyclicity, depth, equality, redundancy, and modularity. Uses geometric mean so no single dimension can be gamed. Supports `details: true` for per-dimension breakdown.
  - `tokensave_gini` — Gini inequality coefficient for any metric (complexity, lines, fan_in, fan_out, members) across files or symbols. Identifies god files and uneven complexity distribution with interpretive labels and ranked outliers.
  - `tokensave_dependency_depth` — longest file-level dependency chains (Lakos levelization). Shows transitive fragility that direct coupling metrics miss, with full chain reconstruction after cycle-breaking via Tarjan's SCC.
  - `tokensave_dsm` — Design Structure Matrix in three output formats: `stats` (density, cluster count), `clusters` (per-directory edge analysis), and `matrix` (NxN grid with short filenames). Reveals hidden coupling patterns and layering violations.
  - `tokensave_test_risk` — risk-weighted test gap analysis combining complexity, fan-in, test coverage, and git churn (90-day window) into a single score. Answers "where should the next test go?" with `include_tested` option for finding weak-test candidates.
  - `tokensave_session_start` — saves current health metrics as a JSON baseline for later comparison. Call before starting an AI coding session.
  - `tokensave_session_end` — re-computes health and diffs against the session baseline. Reports per-dimension deltas with improved/degraded/unchanged labels, overall pass/fail, and cleans up the baseline file.
- **Git churn integration** — new `src/graph/git.rs` module shells out to `git log` at runtime to compute per-file commit frequency. Used by `tokensave_test_risk` as a risk multiplier (log2-scaled) without persisting any data to the tokensave DB.
- **File-level DAG builder** — new `build_file_adjacency` method on `GraphQueryManager` constructs a directed file dependency graph from the existing edge data in a single SQL query. Shared foundation for health, depth, DSM, and modularity computations.

## [4.1.8] - 2026-04-30

### Added
- **`include` config glob** — new `include` field in `.tokensave/config.json` lets users whitelist hidden (dot-prefixed) paths for indexing. By default, all dot-directories are skipped during sync; paths matching an `include` glob (e.g. `[".github/**"]`) are now walked and indexed. The `exclude` list still applies after inclusion, so `.git/**` and `.tokensave/**` remain filtered even with broad include patterns.
- **Markdown extraction** — tree-sitter based markdown parser that extracts headers as `Module` nodes with hierarchical `Contains` edges, and code links as `Uses` edges for cross-reference tracking (PR #47)

## [4.1.7] - 2026-04-29

### Fixed
- **Nested `.gitignore` files were silently ignored** — `git_ignore(true)` in the `ignore` crate relies on git repository detection (walking up to find `.git`) to build the gitignore rule stack. When the walk root was outside a git repo — or in a subdirectory that the crate couldn't trace back to a `.git` — rules in nested `.gitignore` files were never applied. Added `add_custom_ignore_filename(".gitignore")` to the `WalkBuilder`, which makes the crate read every `.gitignore` it encounters as a standalone ignore source regardless of git repo presence. Five regression tests cover: subdirectory exclusion, scope isolation, negation overrides, deep descendant exclusion, and a direct `ignore`-crate sanity check.

## [4.1.6] - 2026-04-29

### Fixed
- **`logging/setLevel` returned MethodNotFound on every session start** — the server correctly advertised the `logging` capability in its `initialize` response (required for the `notifications/message` version-warning feature), but had no handler for the `logging/setLevel` request that MCP clients send immediately after. Every session produced a `-32601` error in the client log. The handler now returns an empty success as required by the MCP spec (RFC 5424 log-level filtering is advisory; the server continues to emit notifications at its own discretion).
- **`java_extraction` panic on empty Javadoc** — parsing a Java file containing a docstring with no content caused a panic (fixes #44).

## [4.1.5] - 2026-04-29

### Added
- **Edit primitives for code modification** — four new MCP tools enable Claude and friends to edit files without regex or shell quoting hazards (PR #43 by @pierreaubert):
  - `tokensave_str_replace` — replaces a unique `old_str` with `new_str`; fails if 0 or >1 matches, protecting against multi-edit bugs
  - `tokensave_multi_str_replace` — applies N `(old, new)` replacements atomically; all-or-nothing transaction
  - `tokensave_insert_at` — inserts content before or after a unique anchor string or line number
  - `tokensave_ast_grep_rewrite` — structural code rewrite via ast-grep CLI (`--rewrite` mode)
- **Auto re-indexing** — all four edit tools automatically re-index the modified file in the code graph after writing, keeping the graph in sync without manual steps (PR #43 by @pierreaubert)

### Performance
- **Fixed N+1 query patterns in graph traversal** — `traverse_bfs`, `traverse_dfs`, `get_callers`, `get_callees`, `get_file_dependencies`, `get_file_dependents`, and `find_dead_code` were each making a separate database query per node, causing excessive CPU usage on large codebases. All methods now batch-fetch nodes using a single `WHERE id IN (...)` query, reducing database roundtrips from O(N) to O(1). (PR #40 by @pierreaubert)

### Fixed
- **`find_dead_code` hit SQLite variable limit on large codebases** — the query used `IN (?, ?, …)` binds which SQLite caps at 999 variables; replaced with `NOT EXISTS (SELECT 1 FROM edges WHERE …)` to avoid the limit entirely. (PR #43 by @pierreaubert)
- **`tokensave_test_map` failed to resolve cross-crate qualified calls** — when a reference contained `::` (e.g. `crate_name::func`), a failed qualified-name match returned `None` without falling back to a simple-name lookup, breaking test coverage queries for integration tests that call across crate boundaries. Fixed by removing the early return and adding a simple-name fallback that strips the qualifier before matching. (PR #43 by @pierreaubert)
- **Sync frequency reduced and stale-warning auto-sync added** — sync interval dropped from its previous default to 2 s (configurable); the MCP server now automatically triggers a live sync when an agent receives a stale-graph warning, avoiding a manual `tokensave sync` round-trip. (PR #43 by @pierreaubert)
- **`TOOL_NAMES` and `EXPECTED_TOOL_PERMS` were static** — `doctor` and `install` would not detect or register newly-introduced MCP tools. Both lists are now built dynamically so adding a tool automatically propagates to health checks and permission installation. (PR #43 by @pierreaubert)
- **`tokensave monitor` now groups output per project then per tool** — previously all tool calls were listed in a flat stream; entries are now grouped by project path first, then by tool name, making it easier to see which project is driving activity. (PR #43 by @pierreaubert)

## [4.1.4] - 2026-04-25

### Fixed
- **`tokensave monitor` panicked on macOS/Linux with "Cannot start a runtime from within a runtime" (issue #39)** — the previous fix for the Windows panic kept a Unix-only branch that built a new `tokio::runtime` and called `block_on` from inside `#[tokio::main]`, which panics on every platform, not just Windows. `refresh_cost_cache` now uses `block_in_place + Handle::current().block_on` unconditionally, since `monitor::run()` is always invoked from the existing multi-threaded runtime.

## [4.1.3] - 2026-04-24

### Fixed
- **Backslashed Windows hook paths never self-healed (issue #38)** — the v4.0.2 fix for #20 normalized `which_tokensave()` output but could not rewrite existing settings. `install_single_hook` is idempotent by presence, so when a tokensave hook already existed with a backslashed path, the silent backfill in `check_install_stale` left it untouched. Additionally, the backfill only scanned `~/.claude/settings.json` — project-level `.claude/settings.json` and `.claude/settings.local.json` were never touched, so opening a previously-configured project could still trigger `bash: C:Usersalkamscoopappstokensavecurrenttokensave.exe: command not found`. Fixed with a new `normalize_hook_command_paths` pass that rewrites any backslash-containing tokensave hook command to forward slashes, and by extending the backfill to the current project's `.claude` directory.

## [4.1.2] - 2026-04-22

### Added
- **Mistral Vibe agent integration** — `tokensave install --agent vibe` registers the tokensave MCP server in Vibe's `~/.vibe/config.toml` as a `[[mcp_servers]]` stdio entry, and appends prompt rules to `~/.vibe/prompts/cli.md`. Supports install, uninstall, and healthcheck. Respects the `VIBE_HOME` environment variable. Closes #37.

## [4.1.1] - 2026-04-22

### Added
- **`tokensave sync --verbose` (`-v`)** — prints per-phase diagnostic lines during sync to help diagnose slow or stuck syncs on large repos. Shows file counts, change breakdowns, and timings for each phase (scan, stat-check, hash, content check, index, resolve, DB write). Also works with `--force` full re-index. Addresses #36.

## [4.1.0] - 2026-04-20

### Added
- **Walk-up project discovery** — `tokensave serve`, `tokensave sync`, and `tokensave status` now walk up the directory tree to find the nearest `.tokensave/` database when no `--path` is given. This means you can launch an AI agent from a subdirectory of your project and tokensave will find the index automatically — similar to how git finds `.git/`. `tokensave init` is unchanged and always creates a new project at the target directory.
- **Subdirectory scope filtering** — when the MCP server is started from a subdirectory, listing and discovery tools (`tokensave_files`, `tokensave_search`, `tokensave_context`, `tokensave_dead_code`, `tokensave_rank`, `tokensave_largest`, `tokensave_coupling`, `tokensave_complexity`, `tokensave_doc_coverage`, `tokensave_god_class`, `tokensave_unused_imports`, `tokensave_hotspots`, and others) automatically scope results to that subdirectory. Graph traversal tools (`tokensave_callers`, `tokensave_callees`, `tokensave_impact`, `tokensave_affected`, `tokensave_type_hierarchy`) remain unscoped so cross-directory relationships are preserved. The user can always override the scope by providing an explicit `path` parameter. `tokensave_status` reports the active scope prefix when one is in effect.

## [4.0.7] - 2026-04-18

### Fixed
- **Symlinked source directories were not indexed** — both the plain `walkdir` and `.gitignore`-aware `ignore::WalkBuilder` file discovery paths now follow symlinks (`follow_links(true)`), so projects that expose source code through symlinked directories are fully indexed. (PR #34 by @lesbass)

## [4.0.6] - 2026-04-18

### Added
- **GLSL language support** — new tree-sitter-based extractor for OpenGL shading language files (`.glsl`, `.vert`, `.frag`, `.geom`, `.comp`, `.tesc`, `.tese`). Extracts functions, structs with fields, uniform/in/out/varying declarations, preprocessor defines, call sites, and complexity metrics. Requires `tokensave-large-treesitters` 0.3.0. Feature-gated as `lang-glsl` in the Full tier. Closes #35.

### Fixed
- **`tokensave upgrade` fails on Homebrew installs** — `self_replace` failed with `ENOENT` on Homebrew symlinks because it resolved relative symlink targets from CWD instead of the symlink's parent. Now dispatches to install-method-aware replacement: Homebrew bypasses `self_replace` and atomically replaces the binary at the canonical Cellar path, renames the version directory, and updates the symlink + `INSTALL_RECEIPT.json` so `brew` reports the correct version. Scoop updates the version directory, junction, and `manifest.json`. Other symlinked installs get a canonicalization fallback. Supersedes PR #33.

## [4.0.5] - 2026-04-17

### Changed
- **Separate `tokensave init` from `tokensave sync`** — previously, `tokensave sync` silently created a new database if none existed. This was a problem because the global git post-commit hook runs `tokensave sync` in every repo after each commit, causing phantom `.tokensave/` databases to appear in projects that never opted in. Now `tokensave init` handles first-time project setup (creates DB + full index) and errors if already initialized, while `tokensave sync` only performs incremental updates and errors if the project was never initialized. The git hook (`tokensave sync >/dev/null 2>&1 &`) now safely exits with an error in non-enrolled repos — no database created. All agent setup messages and documentation updated to reference `tokensave init` for first-time use.

## [4.0.4] - 2026-04-17

### Added
- **Google Antigravity support** — new `tokensave install --agent antigravity` registers the MCP server in `~/.gemini/antigravity/mcp_config.json`. Includes install, uninstall, healthcheck, and auto-detection. Closes #24.
- **Kilo CLI support** — new `tokensave install --agent kilo` registers the MCP server in `~/.config/kilo/kilo.jsonc` using Kilo's `mcp` key with `type: "local"` format. Includes install, uninstall, healthcheck, and auto-detection. Closes #31.

### Changed
- **Simpler install prompts** — `tokensave install` now asks a Y/n question per detected agent instead of showing a multi-select dialog box. Prints a +/- summary of changes at the end. Removed `dialoguer` dependency.
- **No-op upgrade is no longer an error** — `tokensave upgrade` when already on the latest version now exits successfully instead of printing a misleading error. Same for `tokensave channel` when already on the requested channel. (PR #30 by @lesbass)

### Fixed
- **Default branch detection wrote `"HEAD"` instead of actual branch name** — `detect_default_branch()` used `reference.name()` on the `refs/remotes/origin/HEAD` symbolic ref, which returns the ref's own name. Now resolves through `reference.follow()` to get the target (e.g. `refs/remotes/origin/master`), then strips the prefix correctly. (PR #26 by @LucioPg)
- **Branch detection in git worktrees** — `current_branch()` read `.git/HEAD` directly as a plain file, which fails in git worktrees where `.git` is a pointer file (not a directory). Fixed with a two-tier approach: `gix::open()` first, then `git symbolic-ref -q HEAD` subprocess fallback. (PR #28 by @LucioPg)
- **Windows monitor nested runtime panic** — `tokensave monitor` cost cache refresh panicked on Windows due to nested tokio runtimes. Now uses `block_in_place` + `Handle::current()` on Windows. (PR #29 by @LucioPg)
- **Clippy clean** — resolved all clippy errors across the codebase; CI clippy step now passes.

## [4.0.3] - 2026-04-16

### Fixed
- **Windows daemon nested runtime panic** — `tokensave daemon` panicked on Windows because `daemon-kit` runs the closure inline (no fork), creating a nested tokio runtime. Now uses `block_in_place` + `Handle::current()` on Windows while keeping `Runtime::new()` on Unix where the forked child genuinely has no runtime.

## [4.0.2] - 2026-04-14

### Added
- **Token cost observability** — new `tokensave cost` command parses Claude Code session transcripts (`~/.claude/projects/**/*.jsonl`), classifies each API turn into 13 task categories (coding, debugging, exploration, ...), and computes dollar cost per model. Supports `--by-model`, `--by-task`, `--export json|csv`, and time ranges (`today`, `7d`, `30d`, `all`). Model pricing is refreshed from LiteLLM every 24 hours and cached at `~/.tokensave/pricing.json`. Cost data is stored in the existing `~/.tokensave/global.db`. The `tokensave status` header now shows today's cost, 7-day cost, and efficiency ratio. The `tokensave monitor` TUI includes a cost panel. The `hook_stop` handler prints a session cost receipt. Task classification adapted from [AgentSeal/codeburn](https://github.com/AgentSeal/codeburn).
- **`tokensave status --details`** — the node-kind breakdown table is now opt-in via the `--details` flag. Default status output is more compact.
- **Per-file diversity caps** — `tokensave_context` now limits how many symbols from a single file appear in results (default: `max_nodes/3`, minimum 3), preventing one large file from dominating context output. Configurable via the new `max_per_file` parameter.
- **Exact name match supplementing** — context search now supplements FTS5 results with exact case-insensitive name lookups, so perfect symbol name matches are never buried by BM25 noise.
- **Stem variant search expansion** — search terms are expanded with suffix-based stem variants (e.g. "authenticate" also finds "authentication", "authenticator") via 13 derivational suffix rules, improving recall for conceptual queries.
- **Co-occurrence boosting** — when a query has multiple terms, symbols where 2+ terms co-locate in name, qualified name, or file path get a multiplicative score boost, improving precision on multi-word searches.
- **Edge recovery after node trimming** — when BFS subgraph expansion trims nodes to fit `max_nodes`, edges are now filtered to retain only those connecting surviving nodes, keeping the returned subgraph consistent.
- **Adaptive SQLite pragmas** — `cache_size` and `mmap_size` now scale to the DB file size instead of using fixed 64 MB / 256 MB values. Small projects (5 MB DB) drop from ~320 MB baseline to ~12 MB; large projects keep the same performance.
- **`tokensave reinstall` command** — re-runs install for all already-configured agents, refreshing MCP server registration, hooks, permissions, and prompt rules without the interactive picker.

### Removed
- **Graph visualizer** — `tokensave visualize` command, `src/visualizer.rs`, and the embedded HTML file have been removed. The upstream CodeGraph project also removed its visualizer in the same period.

### Fixed
- **Windows path separators in hooks and MCP config** — `which_tokensave()` now normalizes backslash paths to forward slashes, fixing broken hook command execution on Windows (e.g. Scoop installs). Existing settings with backslash paths are also normalized when read back.

## [4.0.0] - 2026-04-13

### Added
- **Multi-branch indexing** — opt-in per-branch databases so switching branches never gives stale results. `tokensave branch add` tracks a branch by copying the nearest ancestor DB and syncing only changed files. `tokensave branch list`, `tokensave branch remove`, `tokensave branch removeall`, and `tokensave branch gc` manage tracked branches.
- **`tokensave branch removeall`** — remove all tracked branches except the default in one command, deleting their DB files.
- **`tokensave_branch_search`** MCP tool — search symbols in another branch's code graph without switching your checkout.
- **`tokensave_branch_diff`** MCP tool — compare code graphs between two branches: shows symbols added, removed, and changed (signature differs). Supports file and kind filters.
- **`tokensave_branch_list`** MCP tool and **`tokensave://branches`** MCP resource — list tracked branches with DB sizes, parent branch, sync times.
- **Branch fallback warnings** — when the MCP server serves from an ancestor branch DB (current branch not tracked), every tool response warns to `tokensave branch add`.
- **`keywords` parameter for `tokensave_context`** — agent-driven synonym expansion. Pass extra search terms (e.g. `["login", "session", "token"]` for "authentication") and the context builder searches each keyword independently, bridging conceptual queries to lexically-unrelated symbol names without embedding models.
- **`tokensave monitor` CLI command** — global live TUI showing MCP tool calls from all projects in real time via a shared memory-mapped ring buffer at `~/.tokensave/monitor.mmap`. Entries show `prefix - project - tool_name` so multiple tool suites and projects are distinguishable. Uses `memmap2` with file locking for concurrent writer safety.
- **`path` filter on 7 analytics MCP tools** — `tokensave_god_class`, `tokensave_largest`, `tokensave_complexity`, `tokensave_rank`, `tokensave_coupling`, `tokensave_inheritance_depth`, and `tokensave_recursion` now accept an optional `path` parameter to scope results to a directory (e.g. `"path": "src/main/java"`), preventing large languages from dominating global rankings.
- **Right-click context menu in graph visualizer** — callers, callees, call graph, and impact actions on node right-click.
- **Type annotation references** — TypeScript, Java, and Kotlin type annotation references now tracked as edges in the graph.
- **Graph visualizer** — interactive Cytoscape.js-based code graph visualization served via `tokensave visualize`.
- **Daemon version mismatch detection** — `tokensave daemon --status` warns when the daemon version differs from the CLI with a corrective restart command.
- **Parent branch in status output** — `tokensave status` and `tokensave_status` now show which branch a tracked branch was seeded from.

### Removed
- **Vector/embedding module** — removed `src/vectors/`, `enable_embeddings` config field, and `Vector` error variant. The `keywords` parameter on `tokensave_context` replaces the need for local embedding models. The `vectors` DB table is retained (empty, harmless) to avoid migration issues.

### Changed
- **Monitor is now global** — moved from per-project (`<project>/.tokensave/monitor.mmap`) to machine-level (`~/.tokensave/monitor.mmap`). `tokensave monitor` no longer takes a `--path` flag.
- Quality improvements to resolution, search, and traversal.
- Tool count increased from 34 to 37.

### Dependencies
- Added `memmap2`, `crossterm`, `fs2` for the monitor feature.

## [3.5.1] - 2026-04-13

### Fixed
- **Doctor validates hook subcommands** — `tokensave doctor` now checks that each hook event uses the correct tokensave subcommand (e.g. `hook-prompt-submit` for `UserPromptSubmit`, not an invalid or mismatched command).
- **Doctor auto-repairs broken hooks** — when a hook has a wrong subcommand or is missing entirely, `tokensave doctor` replaces it with the correct command automatically.

### Added
- **18 unit tests for Claude hook lifecycle** — install, uninstall, doctor detection, and doctor auto-repair for all three hook events.

## [3.5.0] - 2026-04-13

### Added
- **Per-call token savings reported inline** — every MCP tool response now appends a `tokensave_metrics: before=N after=M` line showing how many raw-file tokens were avoided.
- **`UserPromptSubmit` and `Stop` hooks** — `tokensave install` now registers three hooks (PreToolUse, UserPromptSubmit, Stop) instead of just PreToolUse. Existing installs are silently backfilled on startup.
- **`tokensave current-counter` / `reset-counter` commands** — expose and reset a per-project local token counter, separate from the lifetime total.
- **Respect global gitignore** for `.tokensave` warning.

### Changed
- **Hook install/uninstall generalized** — `install_hook` and `uninstall_hook` now iterate over all three hook events.
- **Sync uses mtime/size pre-filter** — skips hashing unchanged files, only reads files whose mtime or size changed since last sync.
- **Dependency upgrades** — dialoguer 0.11→0.12, notify 7→8, sha2 0.10→0.11, zip 6→8, windows-sys 0.59→0.61.

## [3.4.6] - 2026-04-07

### Fixed
- **SQLite FTS corruption from interrupted sync** — handle UTF-16 encoded files, report unreadable files during sync.

## [3.4.5] - 2026-04-07

### Added
- **`--version` / `-V` flag** to CLI.

### Fixed
- Replace `self_update` crate with direct `ureq`+`tar`+`self_replace` implementation for more reliable upgrades.

## [3.4.4] - 2026-04-07

### Fixed
- Fix `tokensave upgrade` ENOENT error on Homebrew symlink installs.

## [3.4.3] - 2026-04-07

### Fixed
- Handle UTF-16 encoded files and report unreadable files during sync.

## [3.4.2] - 2026-04-07

### Added
- **`tokensave channel` command** — show or switch the update channel (stable/beta).

### Fixed
- Cross-workflow Homebrew/Scoop failures on wrong release type.
- Better upgrade error messages when CI is still building.

## [3.4.1] - 2026-04-07

### Fixed
- Beta Homebrew bottle 404 — fix bottle archive naming.
- Update notices now suggest `tokensave upgrade` instead of platform-specific commands.

## [3.4.0] - 2026-04-07

### Added
- **`tokensave upgrade` command** — self-update the binary directly from GitHub releases. Detects the current channel, downloads the correct platform-specific archive, and replaces the running binary.
- **Annotation/attribute extraction for 7 languages** — Rust, Swift, Dart, Scala, PHP, C++, and VB.NET. All create `AnnotationUsage` nodes with `Annotates` edges. Brings annotation support to 12 of 31 languages.
- **McpTransport trait** — zero-cost abstraction for MCP server I/O, enabling in-memory test transports.
- **370+ new tests** — line coverage 71% → 84%.

## [3.3.3] - 2026-04-05

### Added
- `tokensave sync --doctor` lists added/modified/removed files.

## [3.3.2] - 2026-04-05

### Fixed
- **Windows build failure blocking Homebrew/Scoop updates** — `SHELLEXECUTEINFOW` in `windows-sys` 0.59 requires the `Win32_System_Registry` feature flag, which was missing. This caused Windows CI builds to fail since v3.2.0, and because the release workflow used `fail-fast: true`, the failure cascaded to skip the Homebrew tap and Scoop bucket update jobs entirely. Users on Homebrew were stuck on v3.1.0. ([#12](https://github.com/aovestdipaperino/tokensave/issues/12))
- **`HANDLE` type mismatch on Windows** — `windows-sys` 0.59 changed `HANDLE` from `usize` to `*mut c_void`. The UAC elevation code now uses `std::ptr::null_mut()` and `.is_null()` instead of literal `0`.
- **Release workflow resilience** — changed build matrix to `fail-fast: false` and downstream jobs (`update-homebrew`, `update-scoop`) to `if: !cancelled()`, so a single platform build failure no longer blocks formula/manifest updates for platforms that succeeded.

## [3.3.1] - 2026-04-05

### Fixed
- **Windows `is_installed()` always returned `false`** — the daemon autostart check via `daemon-kit` used a file-path probe that returns `None` on Windows, so `is_service_installed()` never detected an existing service. This caused `tokensave install` to re-offer autostart every time. Now dispatches to the Windows SCM query that was already implemented but never wired up. (daemon-kit 0.1.4)
- **Windows `--enable-autostart` failed on reinstall** — running `tokensave daemon --enable-autostart` twice would error with "service already exists". The installer now stops and removes the old service before re-creating, making the operation idempotent. (daemon-kit 0.1.4)

### Added
- **Upgrade-aware daemon restart** — the background daemon now snapshots its own binary's mtime and size at startup and checks every 60 seconds. When an upgrade is detected (via `brew upgrade`, `cargo install`, `scoop update`, or any package manager), the daemon flushes pending syncs, logs the event, and exits. The service manager (launchd `KeepAlive`, systemd `Restart=on-failure`, Windows SCM failure actions) automatically relaunches with the new binary. Previously the old version ran until the next reboot or manual restart.
- **Windows SCM failure recovery** — the Windows service is now configured with `ServiceFailureActions` (restart after 5s, then 10s) so the SCM relaunches the daemon after upgrade-triggered exits.
- **Daemon version logging** — the daemon startup log now includes the version (`v3.3.1 started, watching N projects`) so log readers can confirm which version is running after an upgrade restart.

### Changed
- Bumped `daemon-kit` dependency from 0.1.3 to 0.1.4.

## [3.3.0] - 2026-04-05

### Changed
- **Sync progress now matches full-index display** — `tokensave sync` now shows `[current/total] syncing file (ETA: Ns)` with the braille spinner and path truncation, matching the progress display used during initial indexing. Previously sync only showed phase names without file counters or ETA.

### Added
- **MCP tool annotations** — all 34 tools now include `readOnlyHint: true` and a human-friendly `title` in their MCP annotations. Clients that support annotations can run all tokensave tools concurrently without permission prompts and display cleaner tool names.
- **`_meta["anthropic/alwaysLoad"]`** on core tools — `tokensave_context`, `tokensave_search`, and `tokensave_status` are marked for immediate loading, bypassing the client's tool-search round-trip on first use.
- **Server instructions** — the MCP `initialize` response now includes an `instructions` field guiding the model to start with `tokensave_context` and noting all tools are read-only and safe to call in parallel.
- **MCP resources** — three resources exposed via `resources/list` and `resources/read`:
  - `tokensave://status` — graph statistics as JSON
  - `tokensave://files` — indexed file tree grouped by directory
  - `tokensave://overview` — project summary with language distribution and symbol kinds
- **`tokensave_commit_context`** — semantic summary of uncommitted changes for commit message drafting. Returns changed symbols grouped by file role (source/test/config/docs), a suggested commit category, and recent commit subjects for style matching.
- **`tokensave_pr_context`** — semantic diff between two git refs for pull request descriptions. Returns commit log, symbols added/modified, affected tests, and impacted modules.
- **`tokensave_simplify_scan`** — quality analysis of changed files: detects symbol duplications, dead code introductions, complexity hotspots, and high-coupling files.
- **`tokensave_test_map`** — source-to-test mapping at the symbol level. Shows which test functions call which source functions and identifies uncovered symbols.
- **`tokensave_type_hierarchy`** — recursive type hierarchy tree for traits, interfaces, and classes showing all implementors and extenders with file locations.
- **`tokensave_context` extended** — new `include_code` parameter includes source code snippets for key symbols (wires through to the existing context builder). New `mode: "plan"` parameter appends extension points (public traits/interfaces with implementor counts) and test coverage for related modules.

### Changed
- Tool count increased from 29 to 34.
- Trimmed verbose tool descriptions for lower token overhead in deferred tool lists (`tokensave_rank`, `tokensave_coupling`, `tokensave_port_status`, `tokensave_port_order`, `tokensave_affected`, `tokensave_complexity`, `tokensave_doc_coverage`, `tokensave_god_class`, `tokensave_recursion`, `tokensave_inheritance_depth`, `tokensave_distribution`).

## [3.2.2] - 2026-04-05

### Fixed
- **MCP tools no longer warn on patch-only updates** — the `tokensave_status` MCP tool now uses `is_newer_minor_version` instead of `is_newer_version`, so patch-level releases (e.g. 3.2.0 → 3.2.1) no longer trigger update warnings in MCP tool output. The CLI status command continues to show all available updates.
- **Separate beta/stable update channels** — `is_newer_version` now returns `false` for cross-channel comparisons (beta vs stable). Previously a beta user could be told to upgrade to a stable release, or vice versa. Each channel now only sees updates from its own channel.

## [3.1.1] - 2026-04-02

### Fixed
- **Windows daemon service installation** — `tokensave install` and `tokensave daemon --enable-autostart` no longer fail on non-elevated Windows terminals. When administrator privileges are required to register the Windows Service, the process now automatically requests UAC elevation for just the service installation step; everything else continues non-elevated. ([#7](https://github.com/aovestdipaperino/tokensave/issues/7))
- **Quieter version update warnings** — the CLI no longer warns about patch-only releases (e.g. 3.2.0 → 3.2.1); warnings now appear only for minor or major version bumps. The status page (`tokensave_status` MCP tool) continues to show all available updates.

## [3.1.0] - 2026-04-01

### Fixed
- **Edge duplication during incremental sync** — reference resolution was re-resolving ALL unresolved refs on every sync (not just from changed files) and inserting duplicate edges with no deduplication. Over many syncs this caused unbounded DB growth (e.g. 5.1 GB for a 108 MB codebase). A unique index on edges and `INSERT OR IGNORE` now prevent duplicates entirely. A V5 migration automatically deduplicates existing databases on upgrade. ([#5](https://github.com/aovestdipaperino/tokensave/issues/5))

### Added
- **Concurrent sync prevention** — a PID-based lockfile (`.tokensave/sync.lock`) prevents the CLI and the background daemon from running sync simultaneously. If a sync is already in progress, the second attempt fails immediately with a clear error message. Stale locks from crashed processes are reclaimed automatically.
- **`doctor` database compaction** — `tokensave doctor` now opens the project database, reports its size, and runs `VACUUM + ANALYZE` to reclaim space. Particularly useful after upgrading from versions affected by edge duplication.
- **Index design documentation** — new `docs/INDEX-DESIGN.md` describes the full indexing pipeline, database schema, extraction process, reference resolution, incremental sync, and how `diff_context` uses the graph.

## [3.0.1] - 2026-04-01

### Fixed
- **Safe JSON config editing** — `tokensave install` no longer silently destroys agent config files (e.g. `opencode.json`, `settings.json`) when they contain invalid or unparseable JSON. Previously, a parse failure caused the file to be silently replaced with an empty object plus the tokensave entry, wiping all existing configuration.

### Added
- **Atomic backup before config writes** — a `.bak` copy of the original file is created (via atomic staging) before any modification. If the install fails at any point, the original file is untouched and the backup is preserved.
- **Strict JSON/JSONC loading for edits** — new `load_json_file_strict` and `load_jsonc_file_strict` functions return an error (with a helpful hint) when an existing file cannot be parsed, instead of silently returning `{}`.
- **Atomic config writes** — new content is written to a `.new` sibling file first, then atomically renamed into place via `rename(2)`. The original file is never opened for writing, so a crash or interruption cannot leave it half-written.
- **20 regression tests** covering backup creation, strict loading, atomic writes, round-trip validation, and the end-to-end install cycle for both valid and corrupt config files.

## [3.0.0] - 2026-03-28

### Changed
- **Bundled tree-sitter grammars** — all 31 language grammars now come from the `tokensave-large-treesitters` crate (which includes `tokensave-medium-treesitters` and `tokensave-lite-treesitters`). Zero individual `tree-sitter-*` crate dependencies remain in tokensave itself. The grammar provider (`ts_provider`) is a single `LazyLock<HashMap>` lookup, replacing 100+ lines of per-crate match arms.
- **Removed vendored C grammars** — the Protobuf and COBOL grammars previously compiled from C source via `build.rs` are now vendored inside the bundled crate. tokensave no longer needs `cc` as a build dependency.
- **Simplified feature flags** — the `lang-*` feature flags still control which extractors are compiled, but no longer pull in individual grammar crate dependencies (all grammars are always present via the bundle). The `ts-ffi`/`ts-rust`/`ts-both` grammar source selection flags have been removed.

### Added
- **Daemon install prompt** — `tokensave install` now offers to install the background daemon as an autostart service (launchd on macOS, systemd on Linux) after agent configuration. Skips silently in non-interactive mode or when the service is already installed.
- **Last sync / Full sync in status** — the status table header now shows a third row with relative timestamps for the most recent incremental sync and the most recent full reindex, stored in the metadata table.

## [2.4.0] - 2026-03-27

### Added
- **Daemon mode** — `tokensave daemon` watches all tracked projects for file changes and runs incremental syncs automatically; debounce configurable via `daemon_debounce` in `~/.tokensave/config.toml` (default `"15s"`)
- **Daemon management** — `--stop`, `--status`, `--foreground` flags for process control; PID file at `~/.tokensave/daemon.pid`
- **Autostart service** — `--enable-autostart` / `--disable-autostart` generates and manages a launchd plist (macOS) or systemd user unit (Linux); cross-platform via `daemon-kit` crate
- **Doctor daemon checks** — `tokensave doctor` now reports daemon running status and autostart configuration
- **`daemon-kit` crate** — new standalone cross-platform daemon/service toolkit published to crates.io, using `daemonize2` on Unix and `windows-service` on Windows

## [2.3.2] - 2026-03-27

### Added
- **5 new agent integrations** — Copilot (VS Code), Cursor, Zed, Cline, and Roo Code now supported via `tokensave install --agent <id>`; each registers the MCP server in the agent's native config format (VS Code `settings.json`, `~/.cursor/mcp.json`, Zed `settings.json`, Cline/Roo Code `cline_mcp_settings.json`)
- **Auto-detect agents** — running `tokensave install` without `--agent` detects which agents are installed by checking their config directories; if one is found it installs directly, if multiple are found an interactive checkbox selector is shown
- **Installed-agent tracking** — `installed_agents` list in `~/.tokensave/config.toml` tracks which integrations are active; on upgrade from older versions the list is backfilled by scanning existing configs
- **Uninstall-all** — `tokensave uninstall` without `--agent` silently removes all tracked integrations
- **JSONC parser** — VS Code and Zed settings files (JSON with comments and trailing commas) are now parsed correctly

### Changed
- **Renamed `Agent` trait to `AgentIntegration`** and all struct names from `XxxAgent` to `XxxIntegration` for consistency; functions renamed accordingly (`get_integration`, `all_integrations`, etc.)

## [2.3.1] - 2026-03-27

### Changed
- **Version-update warning suppressed for 15 minutes** — the "Update available" notice shown after `sync` and in MCP tool responses is now suppressed for 15 minutes after it was last displayed, reducing noise for frequent users; `tokensave status` always shows the warning regardless of suppression

## [2.3.0] - 2026-03-27

### Added
- **`--skip-folder` flag for sync** — accepts one or more folder names to exclude during indexing (e.g. `tokensave sync --skip-folder tests benches`); each folder is converted to a `folder/**` glob pattern at runtime
- **ETA during full index** — the progress spinner now shows `[current/total]` file counts and an estimated time remaining (e.g. `[12/150] indexing src/main.rs (ETA: 8s)`)

### Changed
- `index_all_with_progress` callback signature now provides `(current, total, path)` for richer progress reporting
- Schema migration re-index also shows `[current/total]` progress

## [2.2.0] - 2026-03-27

### Changed
- **Status table title split into two rows** — top row shows version (left) and country flags (right); bottom row shows token counts right-aligned in green
- **Country flags always shown** — removed `--show-flags` option; flags are now fetched automatically and cached for 30 minutes
- **Fixed table width** — cell width capped at 32 columns (max table width 100), with a derived maximum of 25 display flags
- **Upgraded gix to v0.81.0** — from v0.72.1; added explicit `sha1` feature flag and adapted to new `ControlFlow`-based tree diff API

## [2.1.0] - 2026-03-26

### Added
- **QuickBASIC 4.5 language support** — new `QuickBasicExtractor` handles `.bi` (include) and `.bm` (module) files, sharing the QBasic grammar under the existing `lang-qbasic` feature flag (31 languages total)
- **`gix` for native git operations** — replaced `Command::new("git")` shell-outs with the `gix` crate (minimal features: `revision` + `blob-diff`), removing the runtime dependency on a `git` binary for commit counting and tree diffing
- **Test coverage improvements** — 77 new tests across 6 files:
  - `complexity_test.rs` (18 tests) — direct tests for the complexity counting algorithm: branches, loops, nesting, unsafe blocks, unwrap/expect detection, assertion counting
  - `rust_extraction_test.rs` (17 tests) — Rust extractor: functions, structs, enums, traits, impls, modules, async, visibility, derive macros, call sites
  - `display_test.rs` (10 tests) — formatting functions with boundary values
  - `php_extraction_test.rs` (11 tests) — classes, interfaces, traits, namespaces, enums, visibility, inheritance
  - `ruby_extraction_test.rs` (9 tests) — classes, modules, methods, inheritance, constants, nested classes
  - `quickbasic_extraction_test.rs` (12 tests) — QB4.5-specific parsing (REDIM, SLEEP, ERASE), SUBs, FUNCTIONs, TYPEs, call sites

### Changed
- **Legacy BASIC grammars updated to 0.2.0** — `tree-sitter-qbasic`, `tree-sitter-msbasic2`, and `tree-sitter-gwbasic` bumped from 0.1 to 0.2, adding 27 new AST node types for QuickBasic 4.5 constructs (REDIM, SLEEP, ERASE, SHELL, metacommands, and more)
- `git_commits_since` now uses `gix` revision walk with `ByCommitTimeCutoff` sorting, which is more efficient than the previous `git log` approach as gix stops walking once all queued commits are older than the cutoff
- `handle_changelog` tree diff now uses `gix` tree-to-tree comparison with rename tracking, replacing `git diff --name-only`

## [2.0.3] - 2026-03-26

### Fixed
- **Windows: sync re-adding files** — normalize all relative file paths to forward slashes in the scanner, preventing path mismatch between index and sync on Windows
- **Windows: wrong upgrade command** — detect Scoop installations (`\scoop\` in binary path) and suggest `scoop update tokensave` instead of `cargo install tokensave`
- **Windows: git hook backslashes** — write forward slashes in `core.hooksPath` and the post-commit hook snippet, since Git's shell expects `/` separators
- **Scoop bucket structure** — moved manifest to `bucket/` subdirectory for better compatibility with `scoop update`
- **Double-counted token savings** — "Global" total no longer includes the current project's count; display now shows "Project" and "All projects" labels

## [2.0.2] - 2026-03-26

### Fixed
- COBOL tree-sitter scanner uses fixed-size arrays instead of C99 variable-length arrays, fixing MSVC compilation failure on Windows that blocked the v2.0.0 Scoop manifest update

## [2.0.0] - 2026-03-26

### Added

#### 16 new language extractors (15 → 30 languages)
- **Swift** — classes, structs, protocols, enums, extensions, init constructors, async methods, visibility modifiers, inheritance
- **Bash** — functions, `readonly` constants, `source` imports, command call sites, comment docstrings
- **Lua** — functions, colon-methods (OOP via metatables), `require()` imports, LDoc comments, `local` constants
- **Zig** — structs, enums, unions, pub/private visibility, `@import` resolution, `test` blocks as functions, doc comments
- **Protobuf** — `message` → `ProtoMessage`, `service` → `ProtoService`, `rpc` → `ProtoRpc` (new node kinds), enums, fields with type signatures, nested messages, `oneof`, package, imports
- **Nix** — functions, modules (attrsets), constants, `inherit` as imports, `apply_expression` call sites, `#` comments
- **VB.NET** — classes, structures, interfaces, modules, enums, `Sub`/`Function`, `Sub New` constructors, properties, `Inherits`/`Implements`, XML doc comments
- **PowerShell** — functions, typed constants, `Import-Module` / dot-source imports, command call sites, `<# ... #>` block comments
- **Batch/CMD** — labels as functions, `SET` as constants, `CALL :label` as call sites, `REM` docstrings (no complexity counting — too flat)
- **Perl** — `sub` functions/methods, `package` as modules, `use`/`require` imports, `our` constants, method invocations (`->`), `#` comments
- **Objective-C** — `@interface`/`@implementation`/`@protocol`, instance (`-`) and class (`+`) methods, `@property`, `NS_ENUM`, `#import`, message expression call sites, inheritance and protocol conformance
- **Fortran** — `module`, `program`, `subroutine`, `function`, derived `type` with fields, `type extends()` inheritance, `interface`, `parameter` constants, `use` imports, `!` comments
- **COBOL** — `PROGRAM-ID` as module, paragraph labels as functions, `WORKING-STORAGE` data items as fields/constants, `PERFORM` as call sites, `REM` comments (vendored grammar)
- **MS BASIC 2.0** — subroutine synthesis from `REM...RETURN` blocks, `LET` constants, `GOSUB`/`GOTO` call sites
- **GW-BASIC** — `DEF FN` functions, `WHILE/WEND` loops, subroutine synthesis, typed constants
- **QBasic** — `SUB`/`FUNCTION` blocks, `TYPE...END TYPE` as structs with fields, `CONST`, `DIM SHARED`, `CALL` sites, `SELECT CASE`

#### Enhanced Nix extraction
- **Derivation field extraction** — `mkDerivation`, `mkShell`, `buildPythonPackage`, `buildGoModule`, `buildRustPackage`, `buildNpmPackage` calls have their attrset arguments extracted as `Field` nodes (`pname`, `version`, `buildInputs`, `nativeBuildInputs`, `src`, `meta`, etc.)
- **Import path resolution** — `import ./path.nix` creates a `Use` node with a `Uses` unresolved ref, enabling cross-file dependency tracking via `tokensave_callers` and `tokensave_impact`
- **Flake output schema awareness** — in `flake.nix` files, standard output attributes (`packages`, `devShells`, `apps`, `nixosModules`, `nixosConfigurations`, `overlays`, `lib`, `checks`, `formatter`) are force-classified as `Module` nodes with recursive child extraction

#### Feature flag tiers
- Three compilation tiers via Cargo feature flags to control binary size:
  - **`lite`** (11 languages, always compiled): Rust, Go, Java, Scala, TypeScript/JS, Python, C, C++, Kotlin, C#, Swift
  - **`medium`** (20 languages): lite + Dart, Pascal, PHP, Ruby, Bash, Protobuf, PowerShell, Nix, VB.NET
  - **`full`** (30 languages, default): medium + Lua, Zig, Objective-C, Perl, Batch/CMD, Fortran, COBOL, MS BASIC 2.0, GW-BASIC, QBasic
- Individual `lang-*` feature flags for cherry-picking languages (e.g., `--no-default-features --features lang-nix,lang-bash`)
- `default = ["full"]` — existing users get all 30 languages with no config changes

#### New node kinds
- `ProtoMessage` — Protobuf message definitions
- `ProtoService` — Protobuf service definitions
- `ProtoRpc` — Protobuf RPC method definitions

#### Porting assessment tools
- **`tokensave_port_status`** — compare symbols between source and target directories within the same project to track porting progress; matches by name with cross-language kind compatibility (`class` ↔ `struct`, `interface` ↔ `trait`); reports matched/unmatched/target-only counts and coverage percentage
- **`tokensave_port_order`** — topological sort of source symbols for porting; uses Kahn's algorithm on the internal dependency graph to produce levels (port leaves first, then dependents); detects and reports dependency cycles

#### Agent prompt improvements
- **SQLite fallback instruction** — agents are told to query `.tokensave/tokensave.db` directly via SQL when MCP tools can't answer a code analysis question
- **Improvement feedback loop** — agents propose opening a GitHub issue when they discover an extractor/schema/tool gap, reminding the user to strip sensitive data

### Changed
- Cargo.toml `description` now lists lite-tier languages with "and many more" instead of all 30
- Vendored tree-sitter grammars for Protobuf and COBOL (no compatible crates for tree-sitter 0.26)

### Breaking
- Tree-sitter grammar dependencies for medium/full tier languages are now **optional** behind feature flags. Downstream crates depending on specific extractors must enable the corresponding `lang-*` feature.
- `cargo install tokensave --no-default-features` now builds a **lite** binary (11 languages) instead of the previous 15. To get the old behavior, use `cargo install tokensave` (default = full, 30 languages).
- Three new `NodeKind` variants (`ProtoMessage`, `ProtoService`, `ProtoRpc`) added — code matching exhaustively on `NodeKind` will need updating.

### Upgrade guide
```bash
cargo install tokensave          # or: brew upgrade tokensave
tokensave install                # re-run to get updated prompt rules
tokensave sync --force           # re-index to pick up new language extractors
```

## [1.10.0] - 2026-03-26

### Added
- **Version update notifications** — the MCP server checks GitHub releases (with a 5-minute cache) and warns users when a newer version is available, via both a `notifications/message` logging notification and a text block prepended to tool responses
- **Global git post-commit hook** — `tokensave install` now offers to install a global `post-commit` hook that auto-runs `tokensave sync` after each commit, keeping the index up to date without manual intervention
- MCP `logging` capability advertised in `initialize` response
- Minimal gitconfig parser for reading `core.hooksPath` from `~/.gitconfig` and `~/.config/git/config` without shelling out to `git`
- 12 unit tests for gitconfig parsing, insertion, and tilde expansion

## [1.8.3] - 2026-03-26

### Fixed
- OpenCode MCP config uses `mcp` key (not `mcpServers`) with `"type": "local"` and `"command": [bin, "serve"]` array format, matching the current OpenCode schema
- Removed legacy `~/.opencode.json` fallback — config always writes to `~/.config/opencode/opencode.json` (or `$XDG_CONFIG_HOME`)
- Healthcheck validates the `command` array contains `"serve"` instead of checking `args`

## [1.8.2] - 2026-03-26

### Fixed
- OpenCode config path resolution now checks `~/.config/opencode/opencode.json` (modern location) before `$XDG_CONFIG_HOME` and `~/.opencode.json` (legacy)
- OpenCode prompt path prefers `~/.config/opencode/OPENCODE.md` when the modern config directory exists

## [1.8.1] - 2026-03-26

### Added
- **OpenCode agent** (`tokensave install --agent opencode`) — registers MCP server in `.opencode.json`, appends prompt rules to `OPENCODE.md`; healthcheck validates config and prompt file
- **Codex CLI agent** (`tokensave install --agent codex`) — registers MCP server in `~/.codex/config.toml` with auto-approval for all 27 tools, appends prompt rules to `~/.codex/AGENTS.md`; healthcheck validates config, tool approval counts, and prompt file
- TOML helpers (`load_toml_file`, `write_toml_file`) in agents module for Codex config support
- `TOOL_NAMES` constant with bare tool names (without agent-specific prefix) for cross-agent use

### New files
- `src/agents/opencode.rs` — `OpenCodeAgent` implementing `Agent`
- `src/agents/codex.rs` — `CodexAgent` implementing `Agent`

## [1.8.0] - 2026-03-26

### Added
- **Multi-agent architecture** with a trait-based `Agent` abstraction (`install`, `uninstall`, `healthcheck`) to support CLI agents beyond Claude Code
- `tokensave install [--agent NAME]` replaces `claude-install` — defaults to `claude` when no agent is specified
- `tokensave uninstall [--agent NAME]` replaces `claude-uninstall` — defaults to `claude`
- `tokensave doctor [--agent NAME]` now checks all registered agents by default; use `--agent` to narrow to one
- Agent registry with `get_agent()`, `all_agents()`, and `available_agents()` for programmatic access
- `tokensave install --agent unknown` returns a clear error listing available agents

### Changed
- Extracted ~600 lines of Claude-specific install/uninstall/doctor logic from `main.rs` into `src/agents/claude.rs`
- Shared helpers (`load_json_file`, `write_json_file`, `which_tokensave`, `home_dir`, `DoctorCounters`, `EXPECTED_TOOL_PERMS`) moved to `src/agents/mod.rs`
- Error messages updated from `tokensave claude-install` to `tokensave install`
- Backward compatibility preserved: `tokensave claude-install` and `tokensave claude-uninstall` still work as aliases

### New files
- `src/agents/mod.rs` — `Agent` trait, `InstallContext`, `HealthcheckContext`, `DoctorCounters`, agent registry, shared helpers
- `src/agents/claude.rs` — `ClaudeAgent` implementing `Agent`

## [1.7.1] - 2026-03-25

### Fixed
- Database schema migrations now trigger an automatic full re-index instead of printing a warning asking users to run `tokensave sync --full` manually

### Changed
- Decomposed 6 oversized functions into small orchestrators + helpers for NASA Power of 10 Rule 4 compliance (no function exceeds 47 lines):
  - `run_doctor` (389 → 31 lines + 14 helpers)
  - `claude_install` (265 → 35 lines + 8 helpers)
  - `claude_uninstall` (160 → 16 lines + 6 helpers)
  - `print_status_table` (179 → 22 lines + 6 helpers)
  - `extract_symbols_from_query` (147 → 13 lines + helper)
  - `get_tool_definitions` (445 → 30 lines + 27 per-tool `def_*()` helpers)
- Added 84 `debug_assert!` preconditions and postconditions across 10 source files for NASA Power of 10 Rule 5 compliance (zero overhead in release builds)

## [1.7.0] - 2026-03-25

### Added
- **3 new safety metrics on every function/method node** extracted from the AST during indexing, enabling NASA Power of 10 compliance audits without grep:
  - `unsafe_blocks` — counts unsafe blocks/statements (Rust `unsafe {}`, C# `unsafe {}`)
  - `unchecked_calls` — counts force-unwrap and unchecked operations (Rust `.unwrap()`/`.expect()`, TypeScript `!`, Kotlin `!!`, Java `.get()` on Optional, Scala `.get()`, Ruby `.fetch()`)
  - `assertions` — counts assertion calls per function (Rust `assert!`/`debug_assert!`, Java `assertEquals`, Python `assertEqual`, Go `require`, C++ `EXPECT_EQ`/`ASSERT_TRUE`, and framework-specific variants for all 15 languages)
- Extended `ComplexityConfig` with 6 new fields (`unsafe_types`, `unchecked_types`, `unchecked_methods`, `call_expression_types`, `call_method_field`, `assertion_names`, `macro_invocation_types`) to support cross-language detection
- `count_complexity` now accepts source bytes for method-name and macro-name matching in call expressions
- DB migration V4 adds `unsafe_blocks`, `unchecked_calls`, and `assertions` columns to the nodes table
- `tokensave_node` and `tokensave_complexity` MCP tools now include the 3 new fields in their responses
- Migration log message advises users to run `tokensave sync --full` to populate new columns for existing data

## [1.6.2] - 2026-03-25

### Fixed
- Suppressed the "new tokensave tool(s) not yet permitted" warning when running `tokensave claude-install`, since that command is about to fix the permissions anyway

## [1.6.1] - 2026-03-25

### Fixed
- `claude-install` now registers all 27 tool permissions — 9 tools added in v1.6.0 (`complexity`, `coupling`, `distribution`, `doc_coverage`, `god_class`, `inheritance_depth`, `largest`, `rank`, `recursion`) were missing from `EXPECTED_TOOL_PERMS`, so `claude-install` didn't grant them and `doctor` didn't flag them
- README permissions example updated to show all 27 tools (was showing only 9)
- README: fixed MCP server location reference (`~/.claude.json`, not `~/.claude/settings.json`)

## [1.6.0] - 2026-03-25

### Added
- 9 new MCP tools (27 total) for codebase analytics, code quality, and guideline compliance:
  - `tokensave_rank` — rank nodes by relationship count with direction support (incoming/outgoing); answers "most implemented interface", "class that implements the most interfaces", etc.
  - `tokensave_largest` — rank nodes by line count; find largest classes, longest methods
  - `tokensave_coupling` — rank files by fan-in (most depended-on) or fan-out (most dependencies)
  - `tokensave_inheritance_depth` — find deepest class hierarchies via recursive CTE on extends chains
  - `tokensave_distribution` — node kind breakdown per file/directory with summary mode
  - `tokensave_recursion` — detect recursive/mutually-recursive call cycles (NASA Power of 10, Rule 1)
  - `tokensave_complexity` — rank functions by composite complexity score with real cyclomatic complexity from AST
  - `tokensave_doc_coverage` — find public symbols missing documentation (Rust guidelines M-CANONICAL-DOCS)
  - `tokensave_god_class` — find classes with the most members (methods + fields)
- **Complexity metrics on every function/method node** — 4 new columns extracted from the AST during indexing:
  - `branches` — branching statements (if, match/switch arms, ternary, catch). CC = branches + 1.
  - `loops` — loop constructs (for, while, loop, do). Enables NASA Rule 2 audits.
  - `returns` — early exits (return, break, continue, throw).
  - `max_nesting` — deepest brace nesting level. Enables NASA Rule 1 (≤4 levels) audits.
- Generic `count_complexity()` helper with per-language configs for all 15 supported languages
- DB migration V3 adds the 4 complexity columns to the nodes table
- All new tools use efficient SQL queries (JOINs, GROUP BY, recursive CTEs) instead of loading all edges into memory

## [1.5.4] - 2026-03-25

### Fixed
- Token counter inflation: `tokensave_files` no longer accumulates tokens saved (listing file names is metadata, not a file-read substitute)
- Worldwide counter staleness: periodic flush every 30 seconds during MCP sessions instead of only on shutdown
- Shutdown flush was effectively a no-op (delta always 0 because `accumulate_tokens_saved` already upserted the current value to global DB); now uses `last_flushed_tokens` to correctly track remaining delta

## [1.5.1] - 2026-03-25

### Added
- `tokensave doctor` command — comprehensive health check of binary, project index, global DB, user config, Claude Code integration (MCP server, hook, permissions, CLAUDE.md), and network connectivity
- Stale install warning: automatically detects when `claude-install` needs re-running due to new tool permissions and warns on every CLI command

### Added
- 9 new MCP tools (18 total):
  - `tokensave_dead_code` — find unreachable symbols with no incoming edges
  - `tokensave_diff_context` — semantic context for changed files (modified symbols, dependencies, affected tests)
  - `tokensave_module_api` — public API surface of a file or directory
  - `tokensave_circular` — detect circular file dependencies
  - `tokensave_hotspots` — most connected symbols by edge count
  - `tokensave_similar` — find symbols with similar names
  - `tokensave_rename_preview` — all references to a symbol
  - `tokensave_unused_imports` — import statements never referenced
  - `tokensave_changelog` — semantic diff between two git refs
- `get_all_edges()`, `get_nodes_by_file()`, `get_all_nodes()`, `get_incoming_edges()`, `get_outgoing_edges()` delegation methods on `TokenSave`
- `find_circular_dependencies()` graph query for file-level cycle detection
- `tokensave status` prompts to create index if none exists (Y/n)
- Country flags in status output via `--show-flags`

## [1.4.3] - 2026-03-25

### Added
- Country flags row in `tokensave status` — shows emoji flags of countries where tokensave is used, centered below the token counters
- `fetch_country_flags()` in cloud module (500ms timeout, best-effort)
- Flags truncated with ellipsis if they exceed the available table width

## [1.4.2] - 2026-03-25

### Added
- PHP language support (`.php`) — functions, classes, methods, traits, interfaces, enums, constants, properties, namespaces, imports, and call sites
- Ruby language support (`.rb`) — methods, classes, modules, constants, inheritance, and call sites

## [1.4.1] - 2026-03-25

### Added
- Cross-platform release workflow — GitHub Actions builds prebuilt binaries for macOS (ARM), Linux (x86_64, ARM64), and Windows (x86_64) on every release
- Scoop package manager support for Windows (`scoop install tokensave`)
- Automated Scoop bucket updates on release
- Automated Homebrew formula + bottle updates on release

### Changed
- README updated with all install methods (brew, scoop, cargo, prebuilt binaries)

## [1.4.0] - 2026-03-25

### Added
- Worldwide token-saved counter — aggregates anonymous token counts across all tokensave users via Cloudflare Worker + Upstash Redis
- `tokensave status` shows three tiers: Local, Global, and Worldwide token counts
- `tokensave disable-upload-counter` / `tokensave enable-upload-counter` commands to opt out of uploading
- All upload state stored transparently in `~/.tokensave/config.toml`
- Version check on `status` (5-min cache) and `sync` (parallel, no added latency) with auto-detected upgrade command (cargo/brew)
- First-run notice informing users about the worldwide counter and how to opt out
- Flush cooldown (60s) after failed uploads to prevent sluggish CLI during outages
- Network Calls & Privacy section in README documenting all outbound requests

### Changed
- `update_global_db()` now computes token-saved deltas for accurate pending upload accumulation
- Moved Cloudflare Worker source to separate `tokensave-cloud` repository

## [1.3.0] - 2026-03-24

### Added
- User-level global database (`~/.tokensave/global.db`) that tracks all TokenSave projects and their cumulative saved tokens
- `tokensave_status` and CLI `tokensave status` now report both local (project) and global (all projects) tokens saved when the global DB is available
- All CLI entry points (`sync`, `status`, `claude-install` init) register the project in the global DB on every run
- MCP server updates the global DB on every token accumulation and on shutdown (best-effort, no locking)

### Changed
- `print_status_table` title row shows `Local ~X  Global ~Y` when global data is available, falls back to `Tokens saved ~X` otherwise

## [1.2.1] - 2026-03-24

### Fixed
- Renamed all remaining `codegraph` references in release workflow, Homebrew formula, setup script, and hook to `tokensave`
- Release workflow now produces `tokensave` binary, bottles, and source tarballs (was still using `codegraph` names)
- Homebrew formula class renamed from `Codegraph` to `Tokensave` with updated URLs
- Setup script variable `CODEGRAPH_BIN` renamed to `TOKENSAVE_BIN`
- CLAUDE.md marker in setup script updated to use `Tokensave` name

## [1.2.0] - 2026-03-24

### Added
- `claude-install` CLI command — configures Claude Code integration (MCP server, permissions, hook, CLAUDE.md rules) in a single step, replacing the bash `setup.sh` script
- `hook-pre-tool-use` hidden CLI command — cross-platform PreToolUse hook handler written in pure Rust (no bash/jq dependency), blocks Explore agents and exploration-style prompts

### Removed
- Embedded bash hook script — the hook is now a native Rust subcommand

## [1.1.0] - 2026-03-24

### Added
- `tokensave files` CLI command — list indexed files with `--filter` (directory prefix), `--pattern` (glob), and `--json` output
- `tokensave affected` CLI command — BFS through file dependency graph to find test files impacted by source changes; supports `--stdin` (pipe from `git diff --name-only`), `--depth`, `--filter`, `--json`, `--quiet`
- `tokensave_files` MCP tool — file listing with path/pattern filtering, flat or grouped-by-directory output
- `tokensave_affected` MCP tool — find affected test files via file-level dependency traversal
- Graceful shutdown handler for MCP server — persists tokens-saved counter, checkpoints SQLite WAL, and logs session summary on SIGINT/SIGTERM
- `Database::checkpoint()` method for WAL cleanup on shutdown

## [1.0.1] - 2026-03-24

### Changed
- Increased ANSI logo size by 25%

## [1.0.0] - 2026-03-24

### Changed
- **Renamed project from `token-codegraph` to `tokensave`**
- Crate name: `tokensave` (was `token-codegraph`)
- Binary name: `tokensave` (was `codegraph`)
- Data directory: `.tokensave/` (was `.codegraph/`)
- MCP tool prefix: `tokensave_*` (was `codegraph_*`)
- Version bump to 1.0.0

### Added
- TypeScript/JavaScript language support (.ts, .tsx, .js, .jsx)
- Python language support (.py)
- C language support (.c, .h)
- C++ language support (.cpp, .hpp, .cc, .cxx, .hh)
- Kotlin language support (.kt, .kts)
- Dart language support (.dart)
- C# language support (.cs)
- Pascal language support (.pas, .pp, .dpr)
- Legacy `.codegraph/` directory detection with migration warning
- CHANGELOG.md for tracking version history

## [0.6.0]

### Added
- Scala language support (.scala, .sc)

### Fixed
- Self-animating spinner with cursor hiding and path truncation
- Show each language as its own cell in status table

### Changed
- Show indexed languages in status, fix multi-language file discovery

## [0.5.2]

### Changed
- Update repo URLs after GitHub rename to tokensave
- Rename crate to tokensave for crates.io

## [0.5.1]

### Added
- Compact bordered table for status output

## [0.5.0]

### Added
- Java language support (.java)
- Go language support (.go)
- ANSI logo and crates.io readiness

### Changed
- NASA rules compliance improvements

## [0.4.2]

### Added
- Versioned DB migration system with exclusive locking

### Fixed
- Create metadata table on open for existing databases

## [0.4.1]

### Added
- Show version number in tokensave status
- Persist tokens-saved counter to database
- Show indexed token count in tokensave status

### Changed
- Update dependencies

## [0.4.0]

### Added
- Initial Rust language support (.rs)
- Replace rusqlite with native libsql (Turso) crate
- Sync progress spinner and post-commit hook
- Prompt to create index when invoked with no command
- Install section with setup script and hooks

### Changed
- Replace `index` command with `sync --force`

## [0.3.0]

### Added
- MCP tool call logging to stderr
- Merge init and index into a single command

### Fixed
- Harden MCP inputs and prevent path traversal

## [0.2.0]

### Added
- Go extractor with deep extraction support
- Java extractor with deep extraction support
- LanguageExtractor trait and LanguageRegistry for multi-language dispatch
- Runtime stats tracking to MCP server
- Homebrew release workflow

### Fixed
- Sanitize FTS5 search queries to handle special characters
- Address code review findings (UTF-8 safety, FK violations, stats accuracy)

## [0.1.0]

### Added
- MCP server (JSON-RPC 2.0 over stdio)
- CLI interface and TokenSave orchestrator
- Vector embeddings for semantic search
- Context builder for AI-ready code graph context
- Incremental sync for detecting file changes
- Graph traversal and query operations
- Reference resolution module
- Tree-sitter Rust extraction module
- libsql database layer with full CRUD operations
- Configuration module with glob-based file filtering
- Core types and error handling scaffold
[6.1.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.1.1
[6.2.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.2.0
[6.3.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.3.0
[6.3.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.3.1
[6.4.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.0
[6.4.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.1
[6.4.2]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.2
[6.4.3]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.3
[6.4.4]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.4
[6.4.5]: https://github.com/aovestdipaperino/tokensave/releases/tag/v6.4.5
[7.0.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.0.0
[7.0.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.0.1
[7.0.2]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.0.2
[7.4.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.4.0
[7.4.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.4.1
[7.4.2]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.4.2
[7.5.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.5.0
[7.6.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.6.0
[7.7.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.7.0
[7.8.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.8.0
[7.8.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.8.1
[7.11.0]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.11.0
[7.11.1]: https://github.com/aovestdipaperino/tokensave/releases/tag/v7.11.1
